# Hypothesis Backlog

Families group questions for multiple-testing correction. A hypothesis becomes a question
only after the Registrar drafts a PREREG and Haci commits it.

## F1 — Selection edge
- [x] H-001 → Q001 (REJECTED 2026-09-11: drafted under the old rule 5) — **re-registered as Q006** on the path objective (`research/questions/Q006_control_cohort_path/PREREG.md`)
- [x] H-002 (F1) — **merged into Q027** (`research/questions/Q027_score_ranking_validity/PREREG.md`, PREREG_LOCKED 2026-09-14, commit `db1cf12`; registrar, autonomous run 2026-09-14, DP-29). **Not drafted: no PREREG, no directory, no question number used (Q038 is still free).** **Why this is a merge and not a new question.** A near-miss design only tells you something when the line actually *does* something: stocks just above it get treated differently from stocks just below. **The 70 line has done nothing since the 2026-07-08 run.** `_qualify_lane` applies `threshold = max(lane_threshold, publication_floor)` (volatilx `services/super_agent_select_scoring.py:1518`; `publication_floor = 80.0`, `services/super_agent_select_models.py:86-91`, commit `1765a6f`, 2026-07-07, DATA_NOTES; `sas_runs.config_json` null→80.0 from the 2026-07-08 run, `STEWARD_Q027_exposure.md` (g)). So a 69.9 and a 70.1 now get the same `threshold_pass = false`, the same `qualification_reason = below_threshold_80.0`, and neither is published. The other 70.0 cuts in the code (`position_size_weight` and `bot_eligible`, `services/super_agent_select_public.py:72`, `:250`; `ai_agents/super_agent_select_agent.py:347`, `:647`) only run on published payloads, and every one of those is ≥ 80. Bears have been judged at 80 since 2026-06-29 (`bear_publish_threshold`), and their dark sub-threshold grading band starts at 60, not 70 (`:1610`). **So in any window the desk can still use, 65–70 vs 70–75 tests no treatment. It only asks whether the score orders outcomes between 65 and 75, and that is Q027's question.** Q027 tests it on the same population (every 16:05 candidate, published or not), with the same outcome (a target at the night's common ATR distance `d*_t`, 20 sessions from the t+1 open, rule 5 / DP-01), on prospective nights only (≥ 2026-09-15): E1 (the per-night score-vs-path IC, MPE 0.05) and E2 (top minus bottom tercile, MPE ±5.0 pp, DP-20). Its band secondary also registers the **adjacent-band contrast at exactly the 70 line**, `70–80` minus `< 70`, night-demeaned (H-002's own adjacent-band baseline). Both cells are on the affirmative-reporting list, each projecting 80.4 contributing nights, and a material inversion blocks Q027's CONFIRMED (§4.3, §8 clause 9). **The version of H-002 where the line *was* a treatment can never reach the floor, however long the desk waits.** 70 gated bull publication only on nights ≤ 2026-07-07. After the DP-06 split (2026-06-01) and `exclusions_v003`, that leaves **22 non-excluded nights** (25 elapsed sessions 2026-06-01..07-07, less 2026-06-26, 07-02 and 07-06), a closed set below DP-21's 80. Everything earlier is contaminated: the in-sample nights are pre-DP-06, and EXPLORE_001 ("Near-miss 65–70 (H-002)") already printed this exact contrast on them. So that data is post-hoc for H-002 (DP-55), and a historical-only run (DP-31) is unavailable on both counts. **Rejected before merging:** (i) registering the narrow 5-point cut as a separate primary on Q027's own population, nights and machinery. That is a sub-cell of a locked question picked outside its closed list, which Q027 §10 threat 12 and §4.3's closed suppression list exist to stop, and it adds a low-power primary to a family correction for a line that controls nothing. It would also fail DP-57. With about 5–6 rows per arm per night (planning figure from `STEWARD_Q027_exposure.md` (d): 575 eligible `70–80` rows over 48 nights), the 80-night floor gives a date-clustered 95% half-width of about 6–7 pp before episode clustering (DP-51). The NULL branch the mechanism predicts ("the threshold is arbitrary": CI inside ±5.0 pp) is out of reach at the floor, so the verdict would be INCONCLUSIVE by construction (DP-45, the H-080 precedent). (ii) Re-cutting to the line that *is* live today, 75–80 vs 80–85 at the publication floor. That is a new hypothesis (DP-25) and belongs in INBOX as its own H-entry, not here. (iii) Q005, which reads no outcome column and has no testable primary. **What Q027 gives H-002, and what it does not:** a descriptive `70–80` vs `< 70` contrast with raw p, plus the inversion blocker. It never confirms or rules out a step at 70 and licenses **no change to `qualification_threshold`**. That costs nothing while the line gates nothing. **Re-entry trigger (measured, not a date):** a shipped change (dated DATA_NOTES entry, DP-50(a)) that makes 70 a live selection or publication boundary again, e.g. `publication_floor` removed or set ≤ 70, or `timeframe_thresholds` / `bear_publish_threshold` at 70. H-002 then re-enters at the front of the queue as its own question, on nights after that ship only, and the prior exposure above is disclosed. **Counted once:** H-002 adds no primary to F1 or F2 and no correction set changes. No outcome was read in this triage; the decision rests on platform code, `config_json` counts and the Steward's counts-only report. Original entry: Near-miss cohort: candidates scoring 65–70 vs 70–75 — baseline: adjacent band — n: TBD — mechanism: threshold is arbitrary — noise risk: small band
- [x] H-003 (F1) — **merged into Q029** (`research/questions/Q029_layer_value_ablation/PREREG.md`; registrar, autonomous run 2026-09-14; DP-29). It is Q029's **E3ⱼ** endpoint, registered on the **path** objective rather than on the "out-of-sample return" the entry names (rule 5, DP-01): the ablated `overall_score` is recomputed from the frozen `score_details_json.weighted_dimensions` with layer `j`'s effective weight set to 0, the platform's own `_qualify_lane` bull-lane selection is replayed with **the night's own `config_json`** (volatilx `services/super_agent_select_scoring.py:1489-1550`, `:1642-1649`), and the endpoint is the change in the slate's **within-night control-adjusted L3-touch rate** (Q006 §3 `E_t`, at Q027's common ATR distance `d*_t`), ablated minus production, placebo-adjusted against a same-size random swap from the night's near-miss pool (Q029 §3 B3 — the baseline H-003 as filed did not have). MPE ±5.0 pp (DP-20; the doubling clause considered and declined because the contrast is paired within night over largely the same picks — Q029 §4.2). Baseline B5 **is** H-003's own: the full v1.6 slate on the same night. **Not counted twice** — H-003 contributes no separate primary to the F1 correction set, which is Q006 (2) + Q024 (6) + Q025 (2) + Q029 (16) = 26. The ablation limb survived its pre-lock feasibility gate: Gate 0 reconstructs the platform's own `overall_score` on **100.00% of the 2026-06-01..2026-09-10 segment nights (68/68)** and the bull-lane replay reproduces the published set on **100.00%** of them, so `E3ⱼ` is registered for six of the seven layers (smart_money_confirmation is DEAD — 0.00% non-null, E3 ≡ 0 by mechanical identity). Original entry: Layer ablation: drop each weighted layer, re-rank, out-of-sample return — baseline: full v1.6 — n: all published nights — mechanism: dead layers add noise
- [x] H-062 (F1) — **DEFERRED** 2026-09-13 (`research/questions/DEFERRED.md`, entry H-062; registrar, autonomous run). Blocker is **exposure, not measurability**: bear-contributing nights run at **8 / 51 sessions = 0.157 per session** inside the DP-06/DP-43 window (weekly 2026-09-12 §6; `STEWARD_Q011_exposure.md` §3 for the session count), so DP-21's 80-contributing-night floor projects to a decision date of ≈ **2028-07-17**, about 22 months past DP-43's 12-month ceiling — the question goes to DEFERRED.md with that date named instead of being locked. Robust to the rate assumption: even at the April-heavy whole-sample rate (0.253/session) the floor is ≈ 15 months out. Not merged into Q006 — Q006 carries bull/bear as a stratification cell (§5), not a primary endpoint, and its bear cell is expected to be SUPPRESSED. Re-check trigger: the first freeze on or after **2027-09-01** (accrual alone), or any freeze where the Steward measures ≥ 0.37 bear-contributing nights per session over a trailing quarter. No F1 slot is consumed and the family correction is unchanged. Original entry: Bear-direction picks touch their ladder levels less often than same-night candidates' downside at the same ATR distance (skip bear picks, or only in weak tapes) — baseline: same-night unpublished candidates, synthetic downside target at the same ATR distance, matched on beta60 / ATR% / 20-session run-up — in-sample n: 25 bear picks / 14 nights (144 level rows); direction: bear picks below control — why it might be real: bear setups fight the prevailing tape and the bear projection library is new (projection_bear_v2) — why it might be noise: tiny n, entirely an up-tape; the sealed Jun–Aug tape is the fair test of tape dependence. Source: EXPLORE_001 §A.
  - Weekly snapshot 2026-09-12 (descriptive; manifest_v001 + prices_001, next-open entry, manual-run nights excluded): bearish SAS picks, direction-adjusted T+20 return −8.21% [−15.87, −1.50] over 22 nights / 36 picks (W20, NON_QUOTABLE); next-open L2 touch within 20 sessions 54% vs 73% for bullish picks. Jun–Aug alone is 8 nights (SUPPRESSED), so the figure is still mostly April. The snapshot reads sealed data, so a PREREG built on this is post-hoc for manifest_v001; register on the path objective (as H-062 already does), not on the return.

- [x] H-067 (F1) — **registered as Q024** (`research/questions/Q024_sas_vs_simple_benchmarks/PREREG.md`; registrar, autonomous run 2026-09-13, DP-40..48). Registered on the **path** objective (rule 5, DP-01): **six primary endpoints, one per benchmark arm, all two-sided** — `E_a` = the night-level mean of [the SAS slate's L3-touch rate within 20 sessions from the session t+1 open (DP-03(b), DP-09, DP-42) **minus** arm `a`'s touch rate of a target at the **identical ATR distance in the arm's own ATR units**], for `a ∈ {SPY, equal-weight S&P 500 (RSP), sector-adjusted random from the same-night candidate pool, 20-session momentum rank, 60-session momentum rank, a simple technical rank}`. **That ATR-distance construction is the "risk adjustment" Haci asked for**, expressed on the path: SPY is asked to travel the same distance in SPY's units of risk that the pick is asked to travel in its own. **MPE ±5.0 pp per arm** (DP-20 unchanged — `E_a` is a single difference of two matched touch rates, the Q006 E1 shape, **not** a difference of differences, so the doubling clause does not apply). Haci's 1/3/7/14/30-session risk-adjusted returns are printed in full and **decide nothing**. **The question-level verdict turns on `E_min` = the excess over the *best* arm** (step-down max-T across arms, FWER-controlled), because H-067 asks about benchmarks plural: beating five alternatives and losing to the sixth is a description of what SAS is, not a confirmation (DP-45). A **clause-7 money gate** binds every confirmed arm — the platform's own committed SELECT scale-out `[0,15,30,15,25,15]` truncated to a 20-session budget, in ATR per trade (DP-10) — so a pick that reaches its target more often but pays less per unit of risk than the benchmark cannot confirm. **The analyst-revisions arm is DEFERRED as an arm** (`research/questions/DEFERRED.md`, entry "H-067 (arm only)"; DP-41/DP-47): no analyst, estimate-revision, rating-change or price-target field exists in `models.py`, `services/symbol_context_builder.py`, any SAS scoring layer or any frozen table — the only "analyst" string in `volatilx/services` is a prompt word at `services/market_intelligence_summary.py:38` — and the question proceeds with six arms rather than deferring whole. **Not merged into Q006** (whose baseline is SAS's *own* unpublished candidates, distance-matched on beta/ATR%/run-up; Q024's sector-adjusted-random arm is the nearest neighbour and §7 forbids reading either as a second confirmation of the other — they can disagree, and "beats a sector-matched random draw, loses to 20-day momentum" is the most informative outcome available), **nor into Q022** (same sector-matched control construction, different question), **nor into Q002** (speed vs the matched control), **nor into H-003** (layer ablation — the platform's own `technical_structure_score` rank is carried only as a descriptive comparator and confirms nothing about it). **No rule-14 exception is requested** (DP-41): every arm is a function of 16:05 ET candidate rows, bars dated ≤ t, the trading calendar, and the sha256-pinned `data/sp500_sectors.json` blob (Q022's pin reused, single commit 2026-05-17, never re-synced). **Window is prospective-only — pick nights 2026-09-14..2027-04-07 (142 elapsed sessions)** — the weeklies have already printed SAS's absolute sealed-period returns, so the desk cannot claim the SPY arm is blind on those nights without hedging (DP-45; the Q018/Q023 precedent); the sealed stretch **2026-07-08..2026-08-12** (R1's 26 matured nights) is printed once as a labelled post-hoc panel entering no verdict, with 2026-08-13..2026-09-11 printed as counts only, and PROSPECTIVELY_CONFIRMED is reachable by construction (every night post-dates the lock; DP-24 satisfied, DP-31 does not apply). Decision **Monday 2027-05-17**, one automatic DP-13 extension to pick nights ..2027-05-19 decided **Monday 2027-06-28** (hard stop), then DEFERRED — 8.1 and 9.5 months from lock, inside DP-43's 12-month ceiling. **Sized on the measured rate: R1 (`research/reports/STEWARD_Q024_sas_vs_simple_benchmarks_exposure.md`, delivered 2026-09-13) measured 0.5652 contributing nights per elapsed session (26/46), tied across SPY, SECRAND, MOM20, MOM60 and TECH — 1.57× the 0.36 DEFERRED-gate line, so Q024 locks and no arm defers**; every date moved **out** from the drafted 2027-02-16 / 2027-03-29 / 2027-03-31 / 2027-05-10 by about ten weeks, because 0.5652 is slower than the draft's 0.75 planning haircut (dates move out only, DP-43/DP-45). The projected margin is thin by construction — 80.3 nights against the 80 floor, because 0.5652 is bound to `manifest_prices_v001`'s own forward-bar horizon (only 26 of 46 nights can mature; the pre-maturity rate is 45/46 = 0.9783 with 26/26 conversion) — and the registered remedy is DP-13's single extension, never a shorter window or a lowered floor. **R1(d), the RSP availability check, could not be run** (no Alpaca credentials in the Steward's environment), so the **EW arm is registered conditionally**: the Steward's freeze-time pinnability check is R2's first action, before `manifest_prices_v002` is pinned and before `eval.py` exists — pinnable → `m = 6`; not pinnable → EW deferred **as an arm**, `m = 5`, no proxy ever substituted. **F1's correction set at this lock is Q006 (2) + Q024 (6) + Q025 (2) = 10 primaries, and never falls below 10** (Q025's later deferral does not lower it — the never-shrink rule holds the stricter denominator). Population is **bullish published picks only** (`qualified IS TRUE AND selected_rank IS NOT NULL`, DP-28) — every named benchmark is long-only — with bearish picks excluded and counted; the **90+ band, `k_t` = 4 and `k_t` = 3 are SUPPRESSED at lock** (counts only, permanently), with `k_t` = 7 and regime `bullish` left marginal to the standing ≥ 20-measured-night rule. **Every decision is settled** in `research/questions/Q024_sas_vs_simple_benchmarks/DECISIONS.md` (2026-09-13) and folded into the PREREG by `@registrar apply Q024`; the only routed item still open is **R2**, the successor freezes (delivery by Monday 2027-05-10, RSP check first), which is **not a blocker for lock** (DP-23). Original entry: **Haci's null: SAS ranking carries no predictive information beyond simple benchmarks.** Pattern: the published SAS slate (Top 8 / Top 10 by `overall_score`) does not beat embarrassingly simple alternatives on the pick's price path after selection (rule 5: first touch of L1–L6 / synthetic targets at the same ATR distance is primary; fixed-horizon 1/3/7/14/30-session returns, risk-adjusted, are secondary and descriptive) — baselines: SPY; equal-weight S&P 500; sector-adjusted random draw from the same-night candidate pool; 20-session momentum rank; 60-session momentum rank; a simple technical rank; analyst revisions (only if the desk holds a point-in-time source — otherwise DEFERRED for that arm) — n: TBD — why it might be real: seven weighted layers (projection 29 / technical 27 / flow 24 …) aggregate information a single momentum rank does not, and Q006's distance-matched control only tests SAS against its own candidate pool — why it might be noise: directional_pct ≈ base rate and champion symbols grade below base rate (CLAUDE.md known findings); SAS picks are momentum names, so a momentum rank may absorb the whole effect; benchmark returns are not on the ladder objective and need a synthetic-target mapping at the pick's own ATR distance. Related: Q006 (control cohort, same-night unpublished candidates), Q001 (REJECTED, old rule 5). Source: INBOX 2026-09-13 (Haci).
- [x] H-068 — **DEFERRED** 2026-09-13 (`research/questions/DEFERRED.md`, entry "Q025 / H-068"; registrar, autonomous run, on that file's **second** admission ground — DP-43's 12-month ceiling). **Not registered; the id Q025 is consumed by the DEFERRED entry and is not reused.** Blocker is **the control construction, not measurability**: the Steward measured the binding (E2) contributing-night rate at **0 / 25 matured elapsed sessions = 0.000 per session** (0 of 46 on the full-window proxy; July 0/18, August 0/7 — `research/reports/STEWARD_Q025_exposure.md` §(h)), below the PREREG §5 gate of 0.35, so DP-21's 80-contributing-night floor is unreachable inside DP-43's ceiling and **no floor date is defined** (the one-sided 90% lower bound of 0.000 is 0.000; even the 90% Clopper–Pearson *upper* bound of ≈ 0.088/session implies ≈ 909 sessions ≈ 3.6 years for one endpoint alone). Mechanism: the hard sector block leaves a median pool of ~5–7 same-night same-sector names and the five-feature 1.5-MAD caliper then passes only **15 of 197 matured picks (7.6%)** — **no night in 46 ever carried 3 B1-valid picks** (max 2) — while **B4, the Q006 three-feature construction, is valid for 100% of the same picks**; the caliper inside the block is the whole constraint, not sector thinness (XLK drops 94.4% on 71 picks), not maturity, not feature coverage (100% / 99.4%), not the pinned sector artifact, not a mid-window ship. **Waiting does not fix it** — this is a per-night structural property of the match, not an arrival rate, so unlike H-062 there is **no date-based re-check trigger**; the trigger is the Steward measuring **≥ 0.35 nights with ≥ 3 B1-valid picks per elapsed session** over a trailing quarter (today 0.000; pick-level B1 validity today 7.6%), re-checked at the first freeze after a type-(b) platform change ships. A wider caliper or a soft sector block is a **different, weaker question and must be a successor PREREG with its own id** (DP-45), never a relaxation of this one; the platform-side fix is filed as **ENHANCEMENTS EN-016** (a larger per-night candidate universe). No verdict of any kind, no `eval.py`, no `results/`. **F1's correction set is unchanged at Q006 (2) + Q024 (6) = 8.** The design as drafted is kept as the historical record at `research/questions/Q025_exposure_attribution/PREREG.md` (stays PREREG_DRAFT, no `schedule.json`) with `DECISIONS.md` beside it. Not merged into Q006: Q006 matches on beta60 / ATR% / 20-session run-up over 2026-04-01..2026-09-10 and names the unmatched sector/size gap as a *threat* (§10.1) it does not test; Q025 adds a hard sector block (the pinned `sp500_sectors.json` blob Q022 established), size (`log10 adv20`), `mom60` and a caliper, and its second primary **is** the difference between the two matches. The within-exposure-cell rank permutation (H-068 baseline (b)) is the null that decides Q025's E1. The sector arm is **not** deferred — `industry` (12%, PI-007) is never used; sector comes from the pinned artifact at 99.5% coverage. Size has no true market-cap source in any freeze and is proxied by trailing dollar volume, stated as a proxy. Original entry: **the SAS path edge is inherited market/sector/factor exposure, not ranking information.** Pattern: after conditioning on known exposures — market beta (beta60), sector, size, 20/60-session momentum, volatility (ATR%) — the published SAS slate's excess first-touch rate at L1–L6 (and, descriptively, its fixed-horizon return) over exposure-matched controls collapses toward zero; the residual is what SAS ranking adds — baselines: (a) same-night unpublished candidates matched on the full exposure vector (beta60 / sector / size / momentum20 / momentum60 / ATR%), synthetic targets at the pick's own ATR distance; (b) a permutation control that shuffles SAS rank *within* exposure cells on the same night, so the exposure profile is held fixed and only the ranking is destroyed; (c) sector-and-beta-matched random draws from the screened universe — n: TBD — why it might be real: SAS picks are momentum names in a concentrated tape (Q022 sector-cluster nights; champion symbols grade below base rate), so a strong-sector tilt could explain the whole hit rate; Q006 matches on beta60 / ATR% / run-up only, leaving sector and size unmatched — why it might be noise: with ~8 picks a night, exposure cells are thin and within-cell permutation has few degrees of freedom; sector labels are sparse (`industry` 12% populated, PI-007), so sector must come from a point-in-time external map or be DEFERRED; factor-model residuals on 20-session horizons are noisy and the MPE must be stated on the path objective, not in alpha units. Related: H-067 (same null from the benchmark side — the two may register as one question with attribution as a stratum, or H-068 as the successor that decides *why*), Q006 (partial exposure match), Q022 (sector clustering). Source: INBOX 2026-09-13 (Haci).
- [x] H-071 (F1) — **merged into Q024** (H-067; DP-29). Haci's H1 "Project NULL": does full VolatilX beat a dumb model and matched random portfolios — PASS ≥ +0.10R/trade incremental expectancy vs NULL, positive out-of-sample in ≥ 2 independent windows, CI preferably > 0; FAIL no economically meaningful OOS advantage → current full SAS has no demonstrated incremental edge. Every element is already registered: the dumb models are Q024's SPY / equal-weight / 20- and 60-session momentum / simple-technical arms; the matched random portfolio is Q024's sector-adjusted random draw from the same-night pool (and Q006's distance-matched control at the same ATR distance); "incremental expectancy per trade" is Q024's clause-7 money gate — the platform's own committed scale-out in **ATR per trade (DP-10, 0.25 ATR), not R**, because the desk assumes no stop (DP-02) and R needs one; and "≥ 2 independent windows" is rule 7's calendar split plus DP-24's post-lock nights, which is what separates HISTORICALLY_ from PROSPECTIVELY_CONFIRMED. Q024 decides 2027-05-17 (extension 2027-06-28); Q006 gives the first read on 2026-10-05 against SAS's own pool. Residue: a cost haircut on the money gate — H-078's subject. Source: Haci's Master Hypothesis Program (`research/reports/INBOX_2026-09-14_master_hypothesis_program.md`, H1).
- [x] H-072 (F1) — **merged into H-068 / Q025 (DEFERRED)** (DP-29). Haci's H2 "Attribution": is performance alpha rather than market / sector / size / momentum / beta exposure — PASS positive residual alpha after market, sector, size, momentum and vol controls, stable OOS; FAIL residual ≈ 0 → SAS is repackaging known exposures. Identical to H-068, whose exposure-matched control the Steward measured as **non-existent on any night** (0 of 46 nights with three matched picks; `research/questions/DEFERRED.md`, "Q025 / H-068"); the re-check trigger stands (≥ 0.35 nights/session with ≥ 3 B1-valid picks, or a type-(b) platform change — EN-016, a larger candidate universe, is the platform-side fix). What is testable now already runs: Q024's sector-matched random arm and Q006's beta / ATR / run-up match are partial attributions, and their disagreement is informative (Q024 §7). The regression form Haci names — return = β·market + sector + size + momentum + vol + residual, on fixed-horizon returns — is **descriptive under rule 5** (fixed-horizon return decides nothing) and is registrable as a descriptive-only successor once Q024 decides; it is not a substitute for the path-matched control, because a 20-session factor residual on ~8 picks a night cannot clear an MPE stated in touch-rate units. Source: Master Hypothesis Program, H2.
- [x] H-075 (F1) — **registered as Q029** (`research/questions/Q029_layer_value_ablation/PREREG.md`; registrar, autonomous run 2026-09-14, DP-40..48), **with H-003 (layer ablation) and H-020 (GEX, the weight-0 layer) folded in** (both marked above; DP-29, counted once, here). Registered on the **path** objective (rule 5, DP-01), never on return: **three primaries per evaluable layer**, all night-level, all two-sided, over **all 16:05 candidate rows** for the two IC endpoints and over the replayed bull-lane slate for the third. **E1ⱼ** = the mean per-night **marginal** Spearman IC of layer `j`'s subscore with the path-outcome rank (sessions to first touch, non-touchers tied last) at the night's common ATR distance `d*_t` within 20 sessions from the t+1 open, **MPE `|ρ̄| ≥ 0.05`**; **E2ⱼ** = the same **given the other retained layers** (partial rank correlation, both variables residualized within night), **MPE 0.05** — this is **H-076's contribution limb**, which Q028 explicitly defers here; **E3ⱼ** = the **leave-one-layer-out re-ranking**, the change in the slate's within-night control-adjusted L3-touch rate (Q006 §3 `E_t`), **ablated minus production**, **MPE ±5.0 pp** (DP-20; the doubling clause is considered and declined with a one-line reason — the two `E_t`'s are paired within night over largely the same picks and the same control pool, so its premise of independently-estimated components fails, and at 10.0 pp the endpoint would return INCONCLUSIVE by construction). **The whole question runs on Q027's machinery by reference** (§2.0): Q027 §2.1–§2.5's population screens, the common ATR distance `d*_t`, the entry, the clock, the grading rules, the exclusions and the contributing-night and episode definitions are adopted verbatim as Q027 registers them after its own DECISIONS.md is applied — one machinery, two questions, so a difference between "does the composite order outcomes" and "does this ingredient" can only come from the ranking variable. **The ablation is exactly computable from frozen columns, and that is the draft's load-bearing finding:** `score_details_json.weighted_dimensions[j].{available, weight, base_weight, score}` stores each row's own **point-in-time effective weight** (volatilx `services/super_agent_select_service.py:306-336` ← `services/super_agent_select_scoring.py:1329-1340`), so `base_score = Σ score·w / Σ w` (`:1348`), `completeness` (`:1347`), the weight-dependent missing-data penalty (`:725-748`, `:745`) and the platform's own `_qualify_lane` selection (`:1489-1550`, bull lane `:1642-1649`, with the night's own `config_json` thresholds, `min_completeness` and the 8-cap) all replay without reconstructing the run's context packet — guarded by a **Gate 0** that re-derives the production `overall_score` to within 0.05 on ≥ 99% of a night's rows (whole-window ≥ 95% or `eval.py` fails loudly) and by a **1% loud-failure gate** on reproducing the frozen published set (DP-28's predicate). **Three re-specifications of this entry, recorded rather than silently applied:** (i) `sas_candidates` carries **`score_details_json`, not `component_scores_json`** — the latter is a night-level column of `market_regime_daily` (Q028 §2.5's correction, inherited), so the agent-level cut reads `score_details_json` at Q028's 90% parse-coverage floor and stays descriptive; (ii) **fundamental_quality is a third weight-0 layer**, not just GEX and smart-money — `enable_fundamental_enrichment` also defaults `False` (`services/super_agent_select_models.py:110-111`), so the REMOVAL-vs-ADDITION direction is decided **per layer per night** from the row's own stored effective weight, and a layer whose nights split is two endpoints that are never pooled (the DP-06 pattern on a weight vector); (iii) the GEX addition weight does not exist in v1.6 (base 0.0, `:9-17`) and is **measured** by R1b from the last pre-2026-05-14 `config_json`, with **12.0** as the registered fallback from the code comment's own reallocation arithmetic. **Four baselines, one per endpoint plus the control:** the within-night subscore permutation (E1), the **residual** permutation that destroys layer `j`'s contribution while preserving both its relation to the other layers and theirs to the outcome (E2), a **placebo swap** of the same size drawn from the night's near-miss pool (E3 — the baseline H-003 as filed did not have, and the placebo-**adjusted** value is the one that must clear the MPE), and rule 5's distance-matched control inside `E_t` by construction (Q006 §3's 10 nearest on beta60 / atr_pct / runup20 at the identical ATR distance). **Window is prospective-only — pick nights 2026-09-15..2027-03-05 (119 elapsed sessions), Q027's window adopted whole** so one successor freeze, one maturity horizon and **one borrowed exposure count** (`research/reports/STEWARD_Q027_exposure.md` R1(a), named as borrowed; no second count is routed) serve both, and **Q029 locks or defers with Q027 on the same ≥ 0.36 contributing-nights-per-elapsed-session gate — measured 0.6761 (48/71) at `record`, CLEARED by 88%, so both lock**. Decision **Monday 2027-04-12**, one automatic DP-13 extension to window end 2027-04-19 decided **Monday 2027-05-24** (hard stop), then DEFERRED — 6.9 and 8.3 months from lock, inside DP-43's ceiling; PROSPECTIVELY_CONFIRMED reachable by construction (DP-24 satisfied, DP-31 does not apply). **Every date moved out from the drafted 2027-01-26 / 2027-03-08 / 2027-04-19 because the measured rate is slower than the borrowed 0.9538 (DP-43, DP-45).** **The prospective-only window is not a formality here: the platform's own August layer audit already printed this hypothesis's statistic on the sealed cohort** — `docs/SAS_SCORING_RESEARCH_PLAN.md:53-67`, seven rows of "corr vs hit" / "corr vs favorable" on n = 635 decidable published picks with a verdict per layer ("Largest weight, no signal", "Only weighted layer that works", "Mildly anti-predictive", "Dead — NULL on all 5,868 rows") — read by the registrar while drafting, printed once as a labelled prior-art panel entering no verdict, with §6 listing the four binding consequences (no threshold, cut, MPE, layer list, direction or suppression rule derived from it; every endpoint two-sided; the audit's own range-restriction caveat is one reason the population is all candidates rather than published picks). **DP-51 applies in full and is a decision clause**: two CIs per primary, the episode being one symbol's run of candidate appearances (E1/E2) or slate appearances (E3) at gaps ≤ 10 sessions, and clearing MPE on the date-clustered CI but not the episode-clustered one is INCONCLUSIVE, never CONFIRMED. **`m` is fixed at `record` from R1b's counts** — a counts-only, outcome-free **feasibility** request (layer coverage under Q028 §4.0's retained-layer rule, the effective-weight census and REMOVAL/ADDITION split, the GEX pre-v1.5 weight, the Gate-0 rehearsal, **the per-layer slate-change rate**, the near-miss pool, and the DP-50(a)/(b) commit sweep including whether the **v1.7** scoring workstream ships inside the window) — **delivered 2026-09-14 (`research/reports/STEWARD_Q029_feasibility.md`) and `m` = 16, fixed and closed**: three endpoints each for flow_strength, technical_structure, gex_alignment (an ADDITION test at the **measured** weight **12.0** — the drafted fallback did not fire), fundamental_quality and catalyst_event, plus **E3_projection alone** (its subscore is non-null on 62.74% of segment rows and clears the 70% floor in no month, so both its IC endpoints are UNEVALUABLE); **smart_money_confirmation is DEAD ×3** as expected, but **fundamental_quality is a live REMOVAL layer** (`enable_fundamental_enrichment` True on 67 of 68 segment nights), contradicting the draft's expectation. Gate 0 reconstructs at **100.00% on the 2026-06-01..2026-09-10 segment (68/68 nights)** with the 84.77% whole-freeze figure a dated pre-v1.5 artefact printed beside it, and the slate replay disagrees on **0.00%** of segment nights, so **the ablation limb exists and no PI is filed**. **F1's correction set at this lock is Q006 (2) + Q024 (6) + Q025 (2, which never shrinks) + Q029 (16) = 26**, and **E1ⱼ / E2ⱼ additionally carry a required companion BH correction across F2** (Q023 (2) + Q027 (2) + the `2L` = 10 IC endpoints, L = 5, so 14), q ≤ 0.10 in both families with the larger q quoted — the Q015 / Q020 pattern, because a layer IC and Q027's composite IC are not independent tests. **Not merged into Q027** (which owns the composite IC and computes no layer-level IC precisely so H-075's primaries stay its own), **nor Q028** (the structure limb, which reads no outcome; neither question alone answers H6 and the joint answer is assembled by the Reporter from ledgered verdicts — the H-077 pattern), **nor Q006** (whose `E_t` is Q029's ingredient, not its claim: E3 can be large while `E_t` is zero and vice versa), **nor Q024** (slate-level benchmarks; the `mom20` comparator is descriptive), **nor Q021** (pin risk, a different GEX quantity). **A layer register, not a single verdict:** each of the seven rows prints in every branch with a label fixed at lock — CARRIES / HARMFUL / MARGINAL_ONLY / DECORATIVE / DEAD / UNDECIDED — and the question-level verdict maps to Haci's own wording (CONFIRMED iff ≥ 1 layer CARRIES or HARMFUL; NULL iff every evaluable layer is DECORATIVE). **§9 carries a DP-50(b) constraint the desk must hold to:** any change to the weights, the timeframe multipliers, the enrichment flags, `qualification_threshold`, `publication_floor`, `max_output_cap`, `min_completeness`, the ATR-elite caps or the GEX offset — **including any promotion of the v1.7 score the platform's own research plan schedules** — is flag-off until 2027-04-12 (2027-05-24 if the extension fires), the same clause and the same dates Q027 carries, checked before any fix brief is written; if it ships anyway the window is cut at the ship date, out and never in — and as of the lock the DP-50 sweep since `fa70688` returns **NONE**, with v1.7 unshipped and unscheduled. **All decisions are settled and applied: `DECISIONS.md` (2026-09-14, decide + record — 27 items, 15 corrections, 2 DEFAULTED under DP-43), folded into the PREREG by `@registrar apply Q029`; no item is pending and the draft is ready to lock.** Original entry: **drafts as one PREREG with H-003 (layer ablation) and H-020 (GEX, the weight-0 layer)** — Haci's H5 "Ingredient value": which layers carry independent predictive information — PASS a layer has stable OOS IC and/or its removal materially hurts the slate's net expectancy; FAIL a layer adds no incremental OOS value → remove or demote it. Two co-primaries per layer: (a) the layer subscore's per-night Spearman IC with the path outcome (H-073's statistic, computed per layer on all 16:05 candidates), and (b) leave-one-layer-out re-ranking — recompute `overall_score` from the frozen subscores with the layer's weight set to 0 (`_effective_dimension_weights`, volatilx `services/super_agent_select_scoring.py:682`; timeframe multipliers applied per lane), re-select the top 8, and measure the change in the slate's within-night control-adjusted L3-touch rate (Q006 §3 `E_t` construction), MPE ±5.0 pp (DP-20). The ingredient list is the seven scored layers — flow `:751`, technical `:831`, GEX `:882`, projection `:925`, fundamentals `:979`, catalyst `:1014`, smart-money `:1051` — plus the two modifiers, cross-layer bonus `:1078` and conflict penalty `:1116`; all are 16:05 writes (`sas_candidates` carries every subscore and `component_scores_json`), so no rule-14 exception (DP-41). Haci counts "10 agents": the `ai_agents/` modules (principal, projection_expert, expert, omega, earnings, option_flow_monitor, price_action, …) feed the layers rather than scoring separately, so the agent-level cut is a secondary on `component_scores_json` where a field resolves to one agent, else descriptive. Two layers are already at weight 0 — GEX (computed, unused) and smart-money (forced 0, PI-008) — so "removal" is the production state and the test for them is **addition** at the v1.6 nominal weight. Baseline: the full v1.6 slate on the same nights. n: TBD (every published night; the ablated slate needs only frozen columns and pinned bars). Why real: seven weighted layers with a 29 / 27 / 24 top three should not all carry independent information, and Q005 found the score distribution shifting mid-window. Why noise: the layers are correlated (H-076), so single-layer removal understates a shared factor; re-ranked slates overlap the published one on most nights, which shrinks the contrast — report the overlap. Related: H-076 (redundancy), H-073 (the IC), Q005 (score decomposition, reads no outcome), H-014. Source: Master Hypothesis Program, H5.
- [x] H-085 (F1) — **merged into Q027** (`research/questions/Q027_score_ranking_validity/PREREG.md`, PREREG_LOCKED 2026-09-14; registrar, autonomous run 2026-09-14, DP-29 / DP-47). **Not drafted: no PREREG, no directory, no question number used (Q037 stays free).** No outcome was read; the decision rests on platform code and three counts-only Steward reports. **Why this is a merge, the H-002 pattern again: the 80 floor is almost never the line that decides publication.** `_qualify_lane` sorts the main lane (bullish + mixed) by score, applies `max(lane_threshold, publication_floor)` and then **stops at `max_output_cap = 8`**. Every further row that clears the floor is marked `capped_by_max_output` (volatilx `services/super_agent_select_scoring.py:1509-1537`; `services/super_agent_select_models.py:91-92`). So 80 is the marginal publication line only on a night with **8 or fewer** main-lane rows at or above 80. The counts say that night is close to non-existent. (i) Before 2026-07-08 the bull threshold was 70, so a sub-80 row was published whenever fewer than 8 rows scored 80 or more. **1 of 383 matured published picks** (48 nights, 2026-06-01..08-12; `STEWARD_Q015_exposure.md`, pre-funnel band U) scored below 80. It is dated **2026-06-02**, a `payload_disabled_runs` night that Q027 excludes whole. The floor would have bound on at most **1 of 25 elapsed sessions (0.04)** in the only fully observable cohort where a binding floor is visible (DP-53, exclusions retained), and on **0 of the 21 nights that could contribute**. Nothing has moved a score since then (`STEWARD_Q027_exposure.md` (g): every scoring field is constant across all 71 runs), so 2026-07-08 made publication_floor a nearly inert line. (ii) At or above 80 the cap does the cutting. The main lane holds **692 `capped_by_max_output` rows**, and the median night's publishable set before screening is **16 rows for 8 slots** (`STEWARD_Q035_exposure.md` (a), (c)). The `80–85` band holds 627 eligible candidates on 48 of 48 nights (`STEWARD_Q027_exposure.md` (d)) but only 311 published picks on the same 48 nights (Q015 R1). The funnels differ, so this is approximate: **about half of `80–85` goes unpublished on a typical night.** So 75–80 against 80–85 is **not** published against just-unpublished. Both 75–80 and the bottom of 80–85 are unpublished. **(iii) Even where the floor did bind, publication cannot move a stock's price path to a distance-matched synthetic target.** What the contrast measures is whether the score orders outcomes between 75 and 85. That subject is calibration (F2 by DP-29), and it is **Q027's**. Q027 runs on the same population (every 16:05 candidate, published or not), the same outcome (a target at the night's common ATR distance `d*_t`, 20 sessions from the t+1 open) and prospective nights only (≥ 2026-09-15). Its E1 is the per-night score–path IC (MPE 0.05) and its E2 the tercile contrast (±5.0 pp, DP-20). **Its band secondary registers the adjacent contrast at exactly the 80 line, `80–85` minus `70–80`, night-demeaned.** It also reports published-versus-unpublished sub-cells (DP-28) and a material inversion at 80 **blocks Q027's CONFIRMED** (§4.3, §8 clause 9). **Rejected before merging:** (a) **Registering the 75-cut as its own primary on Q027's population and nights.** It would be a sub-cell of a locked question picked from outside its closed list, which Q027 §10 threat 12 exists to stop. It would also fail DP-57. Planning figures: about 6 rows a night in 75–80 (half of 575 `70–80` rows over 48 nights; the 75–80 count itself is unmeasured) and about 13 in 80–85. At worst-case binary variance, 80 nights give a date-clustered 95% half-width of about **5.4 pp**, before block and episode inflation (DP-51; 70% of symbols recur). Against the +5.0 pp MPE, CONFIRMED has under 50% power even at a true +5 pp. The "floor is arbitrary" null (CI inside ±5 pp) needs roughly 370 nights, about 550 sessions, well past DP-43's ceiling, so the verdict would be INCONCLUSIVE by construction (DP-45). (b) **Restricting to nights where the floor actually binds.** Its rate is 0.04 a session, about 0.15 at the one-sided 90% upper bound. 80 contributing nights would take roughly 550 to 2,000 sessions, years past DP-43's 12-month ceiling on either figure. (c) **Re-cutting to the line that does decide publication, the rank-8 cap** (last published against first capped, same night). Not filed. For the reason in (iii) it reduces to score ordering inside 80–85, which is Q027's E1 again. The only part of "did publishing add anything" that is not score ordering is price impact from subscriber flow, which no one has hypothesised and which needs order-flow data the desk does not hold. **What Q027 gives H-085, and what it does not.** It gives a descriptive `80–85` vs `70–80` contrast with raw p, the published/unpublished split and the inversion blocker. It never confirms or rules out a step at 80, and it licenses **no change to `publication_floor` or `max_output_cap`**. **Re-entry trigger (a measured count, not a date).** The Steward, on a then-current freeze over a trailing quarter, measures **≥ 0.36 floor-binding nights per elapsed session**. A floor-binding night is a non-excluded night with **no** main-lane `capped_by_max_output` row and at least one `below_threshold_80.0` row scoring in [75, 80). The check runs at the first freeze after any dated DATA_NOTES ship (DP-50(a)) that could make the floor bind: `publication_floor` raised, `max_output_cap` raised, a smaller candidate universe, or a scoring change that lowers the 8th-ranked score. H-085 then re-enters at the front of the queue on post-ship nights only, with the prior exposure above disclosed. **Counted once:** H-085 adds no primary to F1 or F2 and changes no correction set. Original entry: Near-miss at the live publication line: candidates scoring 75–80 vs 80–85 around the 80.0 publication floor (volatilx services/super_agent_select_models.py:91) — baseline: the adjacent band below the floor, same night, distance-matched targets — n: TBD — why real: 80 is the line that actually decides publication since 2026-07-08, so a score just under it is the cleanest counterfactual for 'did publishing add anything' — why noise: the score may be flat across 75–85 and the floor arbitrary, and Q027's band secondary (70–80 / 80–85) may already answer it. Successor to H-002 (merged into Q027). Source: INBOX 2026-09-14

## F2 — Calibration
- [x] H-010 (F2) — **merged into Q027** (H-073; DP-29) as its **band-level secondary**, not as a separate question: per-band **night-demeaned** touch rates over all 16:05 candidates at the night's common ATR distance, bands `< 70` / `70–80` / `80–85` / `85–90` / `≥ 90` (the platform's own qualification threshold, publication floor and elite line — `services/super_agent_select_models.py:85`, `:91`), adjacent-band contrasts (H-010's own baseline), and a monotonicity statistic `M` against the within-night score permutation null (`research/questions/Q027_score_ranking_validity/PREREG.md` §4.3). It is a secondary with teeth: a **material band inversion** — any adjacent-band contrast with ≥ 20 contributing nights below −5.0 pp with its own CI excluding 0 — **blocks Q027's question-level CONFIRMED** (§8 clause 9), because "materially non-monotonic" is half of H-073's own FAIL condition. Night-demeaning is the answer to the snapshot problem below (a raw 70–80 vs 80–90 contrast is a calendar contrast once the 70–80 band vanishes after May); the sealed nights that produced the snapshot are **not used for any verdict** — Q027's window is prospective-only and the sealed stretch is a labelled post-hoc panel. **The `≥ 90` cell is NOT suppressed** — the draft expected it to be, on published-elite monthly counts; R1(d) measured elite **candidates** present on 17 of 48 nights (35%), projecting 28.5 contributing nights at session 119, and the registered rule is a measured count on this question's own population, not an expectation (Q027 DECISIONS item 10 / Correction 8, the list fixed at lock and closed). Q027 §9 still says a suppressed band licenses nothing, **and suppression never removes the inversion blocker** — it is computed for every adjacent pair with ≥ 20 measured contributing nights whether or not the band is on the reporting list (Correction 2). An inversion at the 90 line routes to H-080 as evidence rather than answering it. Original entry: Score-band monotonicity under fixed exit rule — baseline: adjacent band — mechanism: score should order expected return
  - Weekly snapshots 2026-09-10/09-12 (descriptive, NON_QUOTABLE, sealed data seen): (a) the 70–80 band has essentially stopped appearing after May (1 pick Jun–Aug), so any 70–80 vs 80–90 comparison is a calendar comparison; (b) the 90+ lead looks like an April–May effect: pooled 12-week W20 90+ return (next-open entry) +20.3% [+3.2, +38.9] for the 12 weeks to 06-26 (24 nights), +1.7% [−8.4, +12.5] to 07-24 (26 nights), −3.9% [−11.7, +3.9] to 08-12 (20 nights); per-month 90+ cells are below the 20-night floor. Any test must stratify by calendar period and sector (memory/semis) or it is testing April.
- [x] H-011 — DEFERRED (Q036 drafted; the arm E2 compares against does not exist on this scoring configuration — `completeness_score` never falls below 65.3686, so `medium ∧ overall_score ≥ 82` is an empty cell by arithmetic) — **not registered**, entry in `research/questions/DEFERRED.md` (registrar, autonomous run 2026-09-14, DP-40..48), **second admission ground (DP-43's 12-month ceiling) in its strongest form, the Q025 shape**: the binding rate is not slow, it is **exactly zero**, so the projected floor date is **undefined** rather than distant. The lock-or-DEFER gate (`DECISIONS.md` item 7, fixed at `decide` before any count existed) failed on **limb (c)**, E2's `r_join`: **0.0000 on every form** (0/47 matured, 0/71 elapsed, 0/67 non-excluded) against **≥ 0.43**, while **E1 cleared at 0.5532** (26/47; 0.3662 on the full denominator, `ρ_co` 0.6269) — and item 7 answers exactly that pattern with *"DEFERRED, not lock E1 alone"*, because E1 without E2 cannot be distinguished from Q027's ranking result (§1.2, §8 clause 4, DP-29). **The cause is arithmetic, not sampling:** `_confidence_label` (`services/super_agent_select_scoring.py:1166-1171`, called at `:1425` after the ATR-elite cap at `:1407-1423`) makes `high ⇔ score ≥ 82 ∧ completeness ≥ 65` and `medium ⇔ ¬high ∧ completeness ≥ 45`, so E2's arm needs `completeness ∈ [45,65)` above 82 — and **the minimum `completeness_score` anywhere in the window is 65.3686 across all 4,195 scored rows**, published and unpublished alike, with **every cell of both §(e) grids in the `[65,100]` column** (Q014 §2 independently records the same 65.4 floor). All **26 of E1's contributing nights** fall out of E2 solely because every MED row sits in **`[80.05, 81.99]`** — §2.5's anticipated zero-mass gap at its maximum possible value. Measured on `manifest_v001` + `manifest_prices_v001` against `exclusions_v003.json`, pick nights 2026-06-01..2026-09-10, counts only, no outcome of any kind read, no live query (`research/reports/STEWARD_Q036_exposure.md`, DP-50(c)): **71 elapsed sessions · 68 non-excluded · 67 after Q036's own `payload_disabled_runs` criterion (2026-06-02) · 47 matured to t+20** (last 2026-08-12); `d*_t` defined on all 67 nights, median **1.858 ATR** (reproducing `STEWARD_Q027_exposure.md`'s 1.849 on an independently built ATR series); published composition **401 high / 141 medium / 0 low on 542 rows**, 25 of 67 nights single-label and every one all-HIGH; **(d) LOW arm 0 nights — structurally absent, not thin**; **(g) label-integrity mismatch 0.000% (0 of 542, 0 boundary cases)** against a 2% blocker, so **this is not a data-quality defect** and item 10's PI branch does not fire. **Waiting does not fix it:** DP-13 extends a *marginal* floor once by +30 sessions on measured counts; it cannot create a row in a cell whose membership condition is unsatisfiable under the running configuration, at a zero rate the floor date is undefined (contrast Q034's finite "≈ session 880"), and the extension is registered only at a lock that never happens — nights arrive at ~21 a month and add E2 contributing nights at **0.000**, so **no calendar re-check date is named and none is invented**. **Rejected before deferring:** E1 alone (it restates Q027 and drops the one endpoint that made this a different question); a lowered or widened completeness cut (DP-25 — the 82 and 65 are the platform's constants at `:1167-1170`, not desk choices, and there is no "completeness ≥ 55" version); the unpublished `threshold_pass` dark cohort (item 1 / Correction 5 — `threshold_pass` already requires `overall_score ≥ 80 ∧ completeness_score ≥ 35` at `:1518-1521`, so it carries the **identical** floors and the identical ceiling, measured **564 medium / 82 high / 0 low on 646 rows**, and it is Q027's population exactly); and DP-13. **Re-check trigger is a measurement, not a date:** the Steward measuring, on a then-current freeze over a trailing quarter, **E2's `r_join` ≥ 0.43 on the `overall_score ≥ 82` series with E1's ≥ 0.43 at the same time** — whose never-once-observed precondition is a published row carrying `completeness ∈ [45,65)` together with `score ≥ 82`, i.e. the scoring stack leaving a *partial* layer gap on a high-scoring name; re-check **at the first freeze after any change to the enrichment flags, the weights/timeframe multipliers, `_confidence_label`'s constants or the candidate universe ships**, with the desk taking **no position on whether any of them should ship** (the Q025(b) precedent). On re-entry (**`@registrar apply Q036`**) the **whole** R1 limb set (a)–(h) is re-run on the then-current freeze with the repo re-verified read-only, the current exclusions file replacing `exclusions_v003`, the window cut at any §2.6 ship date, and **E1's 0.5532 not carried forward as a planning rate**; the whole §5 schedule is recomputed from the new lock date, **out only**. **PREREG.md and DECISIONS.md are preserved exactly as drafted** — `apply` was **not** run, so Corrections 1–6 stand in `DECISIONS.md`, and **where the draft's body and `DECISIONS.md` "## Record — 2026-09-14" disagree, the record governs** and every provisional date (window 2026-09-15..2027-06-11, decision 2027-07-26, extension 2027-07-27 / 2027-09-13) is **struck in full and never inherited** (the Q025 / Q030 / Q035 precedent); `state.json` `PREREG_DRAFT` → `DEFERRED`, **no `PREREG_LOCKED`, no `schedule.json`, no pin, no `eval.py`, no `results/`, no verdict of any kind** — a gate shortfall is not a verdict, and E1's 0.5532 is an exposure rate, not a result about the label. The question number **Q036 is consumed and not reused**. **F2 returns to 16** (Q023 2 + Q027 2 + Q029's 10 + Q031 2 = `Q031 §7`'s own figure) — Q036's two primaries never join it, so **no locked file is edited**; **R2 is withdrawn** (no successor freeze is built or requested, and the shared Q027 / Q029 / Q031 / Q033 / Q034 build is not extended or re-scoped) and **R3 is not issued** (the 2027-04-05 script deadline is void, and the §6 sealed post-hoc panel is therefore **not computed** — the 2026-09-12 weekly's uncontrolled "78% vs 65%" line stays uncorrected and unquotable). **Not merged into Q027** (DP-29: the label is a joint step function of `overall_score` **and** `completeness_score`, not a band of either, so Q027 cannot return a verdict on it), and **no successor may re-specify toward the arm that has mass** — `[80,82)`-vs-`[82,+)` is Q027's band contrast, the dark cohort is Q027's population, and `completeness_score` as a continuous predictor is a different hypothesis with its own PREREG (DP-25). **The related platform fact is filed as `PI-016`** (`research/PLATFORM_ISSUES.md`, med, OPEN): the conviction label is degenerate on the published slate — a **recommendation only** under DP-07 / DP-48, `HACI_DECIDED` unset until `/desk-run prompt PI-016`, and explicitly **not** a finding about whether the label predicts the price path. **Correction 6, recorded because three locked or drafted files reason from the wrong value:** the **runtime** `sas_runs.config_json` shows `enable_fundamental_enrichment = True` on **all 67** non-excluded nights (catalyst True, smart-money at its coded False), contradicting the ORM default at `services/super_agent_select_models.py:109-111` that several PREREGs cite — a `DATA_NOTES.md` note is routed to the Data Steward (N1), `Q028 §9/§10` is **flagged to the Red Team and not edited**, and `Q035` is moot. **No rule-14 exception was requested and none was needed** (DP-05 untouched, DP-41 not engaged). Original entry: Confidence label (high/medium/low) vs realized return — baseline: label shuffled. **The drafted record follows, as written on 2026-09-14 and not a registration:** drafted as Q036 (`research/questions/Q036_confidence_label_validity/PREREG.md`). **Not merged into Q027**, and the reason is read from the platform rather than assumed: `_confidence_label` (`services/super_agent_select_scoring.py:1166-1171`, called at `:1425`) is a joint step function of `overall_score` **and** `completeness_score`, not a band of the score, so it is not Q027's band-level secondary. On the published slate (`publication_floor = 80`, `min_completeness = 35` — `services/super_agent_select_models.py:91,98`) high-versus-medium above score 82 reduces exactly to `completeness ≥ 65` versus `45–65`. Registered with **two primaries**: **E1** the unconditional within-night high−medium touch rate at Q027's common ATR distance `d*_t`, t+1 open entry, 20 sessions (DP-03(b), DP-09, DP-42), against H-011's own label-shuffle baseline; **E2** the same on adjacent-score-rank discordant pairs above 82 — the part that is *not* a restatement of Q027, with §8 clause 4 making "E1 confirms, E2 nulls" a **NULL for Q036**. Both two-sided, MPE ±5.0 pp (DP-20, DP-44), episode-clustered CI as a blocker (DP-51). **Window prospective-only, pick nights ≥ 2026-09-15**: the 2026-09-12 weekly already printed the medium-versus-high next-open L1 touch rate on sealed nights, so the sealed stretch carries no verdict and is printed once as a labelled post-hoc panel (the Q027/Q023 pattern). Rule 14 is clean — the label is written by the same 16:05 ET scoring pass as `overall_score`, so **no rule-14 exception is needed** (DP-41 not triggered). Decision date **2027-07-19**, single DP-13 extension to **2027-08-30**, then DEFERRED. **Lock is gated on Steward R1** (§5.1, counts only, no outcome): if the share of nights carrying both a `high` and a `medium` published row falls below 0.36, the question goes to DEFERRED instead of locking (DP-43's 12-month ceiling). The projection-target confidence label (`services/projection_picks_scoring.py:218-264`) is a different field and stays out of scope — the 2026-09-10 weekly's proposal to extend H-011 to it remains a separate, unregistered hypothesis (DP-25).
  - Weekly snapshot 2026-09-12 (descriptive, NON_QUOTABLE, sealed data seen): on next-open entry, "medium" picks touched L1 within 20 sessions more often than "high" picks (78% vs 65%, W20 population), and "high" picks had L1 already passed at the open more often (24% vs 16%); Jun–Aug T+20 return medium +1.12% [−0.66, +2.89] (28 nights) vs high −1.81% [−4.09, +0.63] (49 nights). Possible mechanism: "high" goes with names already gapping, so the label may mark extension rather than quality. Also extend to AI Picks projection labels: low-confidence projection-bullish picks beat high-confidence ones (T+60 +60.8% [+9.3, +115.0] vs +16.0% [+9.3, +21.9]; outlier-prone, 72 low-confidence picks).
- [x] H-013 (F2) — **registered as Q023** (`research/questions/Q023_band_80_90_strongly_bullish/PREREG.md`; registrar, autonomous run 2026-09-13). Registered on the **path** objective, not on the T+20 return the weekly snapshots used (rule 5, DP-01): **two primaries**, both night-level arm contrasts over published 80–90 picks, both two-sided — **E1** the *within-night control-adjusted* L3-touch rate within 20 sessions from the session t+1 open (DP-03(b), DP-09, DP-42; Q006 §3 control construction verbatim), averaged per night, `strongly_bullish` nights minus other nights, **MPE ±10.0 pp** (a difference of differences — DP-20's larger-MPE clause, the Q012 P2 / Q018 P4 / Q022 E1 precedent); and **E2** the realized **ATR per published pick of the platform's own committed SELECT scale-out** `[0,15,30,15,25,15]` on the flattened L1–L6 ladder, each level on its own lane window over a 60-session budget (volatilx `services/sas_conviction_card.py:203-218`, `:182-195`), same arm contrast, **MPE ±0.25 ATR** (DP-10, DP-44). **Both are needed and neither alone licenses a rule** (§8, §9): E1's within-night control strips the tape out by construction, so it can only say the *selection* is worse calibrated, never that the band is unprofitable; E2 keeps the tape in, which is what a trader feels, but a raw arm contrast on a night-type *defined by* the tape is partly the tape. **Window is prospective-only — pick nights ≥ 2026-09-14** — because the sealed period has already been read for this hypothesis (the weekly 2026-09-10 / 09-12 regime × band return cells below): the sealed stretch is printed once as a labelled post-hoc panel that enters no verdict, and PROSPECTIVELY_CONFIRMED is reachable by construction (DP-24 satisfied; DP-31 does not apply). **No rule-14 exception is requested** (DP-41): the arm is `market_regime_daily.market_regime`, v1.2 only, same-evening writes only, and the window starts long after the 2026-06-09 point-in-time boundary — the row with `trading_date = t` is the one the platform's own consumer uses to gate a pick scored on t's close (`services/market_regime/consumer.py:52-60`), with the strictly-prior `effective_for_trading_date` label carried as a binding sensitivity (§8 clause 7). **The dominant threat is that the arm is calendar:** the label is smoothed by hysteresis (promote 3-of-5 — `services/market_regime/scorer.py:60`, `:386-420`), so contributing nights arrive in runs, and the draft answers it with an **episode-block bootstrap, episode-label permutation, and a ≥ 5-episodes-per-arm gate** on top of DP-21's floors (§8 clause 2). Second threat, disclosed not gated: `strongly_bullish` is nearly a function of trailing SPY price, so the trailing-tape-matched sensitivity is mandatory (§10 threat 2). **Not merged into Q015** (85–90 path shape, same level and clock, no regime arm anywhere), **Q005** (a score decomposition that reads no outcome column at all, so its regime strata cannot contaminate an outcome question), **Q006** (whose picks-vs-control excess *is* `E_t`; §7 forbids reading E1 as a second confirmation of a selection edge), **Q022** (identical two-stage shape, different night-level arm) or **Q012 / Q018** (plan contrasts; Q023 runs one plan). Filed in **F2** with **m = 2** and the F2 correction set computed at the decision pass — today Q023 alone, Q005 excluded as a no-primary diagnostic. **Lock was conditional on R1** (§5.3, blocking, since no desk report had ever counted a regime-arm split or an episode) and **R1 cleared it** (`research/reports/STEWARD_Q023_exposure.md`, 2026-09-13, counts only, 2026-06-09..2026-09-10, 65 elapsed sessions): all three gate limbs clear — contributing nights **0.9538**/session vs ≥ 0.43, rarer-arm (NOTSTRONG) nights **0.2000** vs ≥ 0.11, rarer-arm **episodes 0.0615** vs ≥ 0.027 — so **Q023 locks, not DEFERRED**. But the measured rarer-arm rate came in *below* the drafted 0.25 planning rate, so every date moved **out** (DP-43, DP-45 — a faster rate would have changed nothing): **registered schedule — pick nights 2026-09-14..2027-02-25 (114 elapsed sessions), decision Monday 2027-06-07** (8.8 months, inside the 12-month ceiling; 2027-05-31 is Memorial Day, hence the extra week), one automatic DP-13 extension to ..**2027-04-09** decided **Monday 2027-07-19** (hard stop), then DEFERRED. The window is sized **in whole rarer-arm episodes** (3.25 contributing nights per episode → 7 episodes → 114 sessions), not at the flat night rate that would have ended 2027-02-04 and expected the extension to fire. All five §11 decisions are settled in DECISIONS.md (2026-09-13): the `trading_date = t` row keys the night with the legality test tightened to a **timestamp** comparison against that night's own `sas_runs.finished_at`; pooled NOTSTRONG with the 60% composition guard fixed at lock (sealed panel: 100% `bullish`); E2 on the committed plan's own 60-session budget; the **≥ 5-episodes-per-arm gate as a gate** (extension → DEFERRED, never INCONCLUSIVE); and B3 kept with an 80% coverage gate (measured 98.17% already). Sub-cell **suppression list fixed at lock** (90+; `neutral`/`bearish`/`risk_off`; `pullback_in_bull`; confidence terciles; all six tape cells; the pick-level splits) — the NOTSTRONG arm projects 22.8 nights against a 20-night floor, so no split of it can clear. **Live risk, unchanged:** the NOTSTRONG arm has produced nothing since 2026-07-29 (one uninterrupted 30-session `strongly_bullish` stretch), so DEFERRED after the single extension is a real outcome. Original entry: The 80–90 band underperforms on nights the point-in-time regime label is `strongly_bullish` — baseline: the same band on `bullish` / other point-in-time nights; and the projection-bullish list on the same nights, to separate a SAS effect from a tape effect — n seen: 30 strongly_bullish nights / 226 picks vs 14 bullish nights / 113 picks (W20, Jun 9 – Aug 12) — direction seen: T+20 −2.61% [−5.61, +0.46] next-open basis (−3.04% [−5.80, −0.37] close basis) vs +2.17% on bullish nights (SUPPRESSED, 14 nights); projection-bullish on the same strongly_bullish nights −0.75% — why it might be real: late-trend exhaustion; scores are high because trend and flow agree after the move already happened — why it might be noise: regime and calendar are fully confounded (every point-in-time night is Jun–Aug), the bullish comparison cell is below the floor, the CI already crosses zero on the actionable basis, and the projection list was also weak on those nights. Probably needs prospective nights (manifest_v002+) before a PREREG is viable. Source: weekly snapshots 2026-09-10 and 2026-09-12 (sealed data seen — post-hoc for manifest_v001).
- [ ] H-012 (F2) — **partially merged into Q027** (H-073; DP-29) as a **stratum only; the line stays open and stays in the queue**, because the primary form of H-012 is not registered anywhere. The merge is partial by design: Q027 recomputes both primaries (the per-night IC, the tercile touch contrast) separately on `conflict_penalty > 0` and `conflict_penalty = 0` rows, and carries H-012's own comparison — penalized minus unpenalized touch rate, matched within night on the **pre-penalty base score** (`base = overall_score + conflict_penalty`, re-derived from `score_details_json`, ±2.0-point caliper) — as baseline **B5** (`research/questions/Q027_score_ranking_validity/PREREG.md` §3 B5, §4.3). **Every part of it is descriptive: no verdict rests on it, it prints raw p only, and Q027 §9 says explicitly that a striking split here licenses no change to the penalty — it is a reason to register H-012 as its own primary question, not a finding.** The line stays open for that successor. Original entry: Conflict penalty validity: penalized candidates vs unpenalized at same base score
- [x] H-014 — DEFERRED 2026-09-14 (`research/questions/DEFERRED.md`, entry H-014; registrar, autonomous run, DP-43 second ground). Blocker: the test is event-driven and has no qualifying events. Per the Steward censuses (`STEWARD_Q029_feasibility.md` §(e), `STEWARD_Q027_exposure.md` R1(g)), the only score-moving `config_json` changes in manifest_v001 are 2026-05-01 (fundamental enrichment on) and 2026-05-18 (v1.5 weights + ATR-elite block, inseparable), about 6 clean nights apart. The historical limb is therefore below the 80-night floor and post-hoc. No score-moving field changed 2026-05-19..2026-09-10, so the re-specified Channel 3a cannot fire on Q005's P1 fall (config fact, NON_QUOTABLE). Locked Q031 §9 holds every qualifying field flag-off until 2027-06-07 (2027-07-19 if extended), and no ship is scheduled, so no in-ceiling date can be projected. Triggers: a qualifying change is scheduled (lock before the ship) or ships and holds ≥ 20 nights. The firing set, the local-step requirement and the GEX-offset definition (`score_details_json.gex_missing_offset_applied`) carry forward as method. Not merged (DP-29); F2 set unchanged at 16. Original entry: (F2, successor to Q005 §8 Channel 3a) — Re-specify the "configuration change brackets the fall" rule so it can only fire on changes that can mechanically move `overall_score` (weights, timeframe multipliers, ATR block/cap, projection cap, scoring enable-flags held ≥ 20 nights) and requires a step at the change date (local before/after), not a global before/after split that any mid-sample date passes when the series declines. Publication-only gates (`publication_floor`, `bear_publish_threshold`, `bear_max_output_cap`) and single-night flag toggles that revert would be excluded. Also fix, in advance, the +5 GEX-offset firing definition (recorded offset vs "null gex_alignment_score"; they diverge 0.5% vs 31% in Apr–Jun because the offset code postdates April). Source: Q005 run 2026-09-11 (results/NOTES.md Part B, flags F1/F2). Raised by the Researcher AFTER seeing Q005's output, so any successor is post-hoc with respect to manifest_v001 and must say so (or wait for a later freeze); Q005 stands as run.
- [x] H-073 (F2) — **registered as Q027** (`research/questions/Q027_score_ranking_validity/PREREG.md`; registrar, autonomous run 2026-09-14, DP-40..48), **with H-010 folded in as the band-level secondary and H-012 as a descriptive stratum** (both marked above; DP-29, counted once, here). Registered on the **path** objective (rule 5, DP-01), never on the return: **two primaries**, both night-level over **all 16:05 candidate rows** (published and unpublished, `qualified` and not — Q005 §2's population), both two-sided. **E1** = the mean per-night **Spearman ρ between `overall_score` and the path-outcome rank** — sessions to first touch, non-touchers tied last, i.e. *whether and when* (DP-08) — within 20 sessions from the session t+1 open (DP-03(b), DP-42), **MPE `|ρ̄| ≥ 0.05`**; **E2** = the mean per-night **top-tercile minus bottom-tercile touch rate**, **MPE ±5.0 pp** (DP-20 unmodified — a single difference of two touch rates measured at the identical ATR distance inside one night, the Q006 E1 shape, not a difference of differences). **The distance-matched control of rule 5 is built into the construction, not bolted on:** every candidate on a night is asked to travel the **same number of its own ATRs**, `d*_t` = the median of that night's *published* L3 distances in ATR units (split-scale screened first — DATA_NOTES / APH-KLAC-CRWD-MNST), so the engine's own choice of how far to aim a target cannot masquerade as ranking information. Outcomes are graded **from pinned bars only** — unpublished rows are ungraded in the platform's own outcome columns (FREEZE_v001 §4: 0 of 5,572 non-qualified rows sealed), and `outcome_*` / `sas_selection_excursion.*` / `uoa_symbol_daily.fwd_return_*` are never joined. **The IC MPE is Haci's number, not a DP entry:** ρ ≥ 0.05 comes from the Master Hypothesis Program H3 PASS line and is applied as registered, with its source stated in §4.2; DP-44 has no rank-correlation unit and an autonomous run writes no DP from a DEFAULTED item, so **"IC MPE unit" is §11 item 3, a standing rule proposed to the Decision-maker** for H-075's per-layer ICs and H-082's decay series. **Haci's two other PASS limbs are blocking clauses, not reported statistics:** "positive in ≥ 60% of out-of-sample windows" is §8 clause 10 on calendar-month blocks, and "materially non-monotonic → FAIL" is §8 clause 9 — a **band inversion** (any adjacent-band contrast, ≥ 20 nights, below −5.0 pp with its own CI excluding 0) blocks the question-level CONFIRMED whatever E1 and E2 do. **DP-51 applies in full**: each primary carries **two** CIs — date-clustered and **episode-clustered** (episode = one symbol's run of eligible candidate appearances with gaps ≤ 10 sessions, bootstrap resampling episodes and nights jointly) — and clearing MPE on the first but not the second is **INCONCLUSIVE, never CONFIRMED**; the threat it answers is acute here, because the same ~60 symbols recur nightly with almost fully overlapping 20-session windows. **Window is prospective-only — pick nights 2026-09-15..2027-03-05 (119 elapsed sessions, sized at `record` on the measured rate)** — because the sealed period has been read for this hypothesis (the 2026-09-10 / 09-12 weeklies printed band-level returns; CLAUDE.md carries "88–90 underperforms 90+"), so the sealed stretch **2026-06-01..2026-08-12** prints once as a labelled post-hoc panel entering no verdict, **split at 2026-07-06** (the `_enforce_ladder_monotonic` ship, which changes the ladders `d*_t` is computed from). PROSPECTIVELY_CONFIRMED is reachable by construction (every night post-dates the lock; DP-24 satisfied, DP-31 does not apply). Decision **Monday 2027-04-12**, one automatic DP-13 extension to pick nights ..2027-04-19 decided **Monday 2027-05-24** (hard stop), then DEFERRED — 6.9 and 8.3 months from lock, inside DP-43's ceiling (`schedule.json`, `pin_at_decision: true`). **Sizing was the one thing the draft did not have, and R1 supplied it:** no exposure report had ever been run on an all-candidates funnel, so R1 was made **blocking for the lock** and measured §2.5's complete contributing-night definition — **0.6761 per elapsed session (48/71, `research/reports/STEWARD_Q027_exposure.md`)**, clearing the lock-or-DEFER inequality of **≥ 0.36** (the DP-43 ceiling solved at this lock date) by 88%, so **Q027 locks**. Every date moved **out** from the drafted schedule (2027-03-08 → 2027-04-12), because the measured rate is *slower* than the borrowed 0.9538; the 0.9577 eligible-night proxy, with 100% of matured nights converting, was **refused** — it extrapolates past the price freeze's own forward-bar horizon and would have bought seven weeks with a number the desk has not measured (DP-45). **No rule-14 exception is requested** (DP-41): every input is a 16:05 ET candidate field, a pinned bar dated ≤ t, or the t+1 open used **only** as the entry price. **Not merged into Q006** (published picks vs unpublished matched controls — a selection edge and a ranking edge are independent claims and §7 forbids reading either as confirming the other), **nor Q024** (slate-level benchmarks; Q027's `mom20` rank-IC comparator is descriptive and confirms nothing about it), **nor Q023** (whose population Q027 contains but whose arm is a regime label), **nor H-075** (per-layer ICs are **not computed here**, so its primaries stay its own). **F2's correction set at this lock is Q023 (2) + Q027 (2) = 4 and never falls below 4.** **All thirteen decisions are settled** (`DECISIONS.md`, decision-maker, decide + record passes 2026-09-14; applied to the PREREG by the registrar the same day, `schedule.json` written): the six drafted §11 items all went the Registrar's way, items 10–13 were decided on existing entries, **two were DEFAULTED** (#7 the prospective-only window start, #8 the window end and decision date — both the same cost, waiting), and **R2 (successor freezes) is the only routed item still open**, due before the decision date and blocking nothing (DP-23). Nine corrections applied, each tightening a clause: E2's blocking companion restated as the **touch-within-5-sessions** contrast in DP-20 units, since DP-44 gives sessions-to-touch no MPE; **suppression stripped of any power to remove the inversion blocker**, which runs on measured counts for suppressed and unsuppressed bands alike; every sub-cell's status **fixed at lock and closed** from R1's measured candidate-level composition; "published" named as **DP-28's predicate** wherever it is used, dark-lane rows out of the `d*_t` median and in the population; and a **fourth exclusion criterion, `payload_disabled_runs`** — a night whose published slate carries `analysis_status = "disabled"` / no `lane_plans` at all (2026-06-02, and 2026-04-01 / 05-01 / 07-02, every one the first published night of a month) is excluded **whole** rather than fallback-filled, because R1(c) measures `d*_t` doubling (1.06 → 2.10 ATR) across one config ship, so an imported distance is a different experiment. **Two cells came off the expected-suppression list on the measurement:** `≥ 90` (elite *candidates* on 17 of 48 nights, projecting 28.5) and **bear-only** (candidate-level bearish rows on 48 of 48, projecting 80.4) — the published-elite counts and H-062's published-bear rate are measured on a narrower population and do not transfer. Suppressed instead: five of six `tape_t` cells and the three `market_regime` labels absent from every measured night (`bearish` / `neutral` / `risk_off`). **No mid-window scoring split exists at the lock** — R1(g)'s code sweep returned NONE, every scoring field is constant across all 71 in-window runs, and both `config_json` changes are **publication gates** (`bear_publish_threshold` 2026-06-29, `publication_floor` 2026-07-08) that predate the window by more than two months. Original entry: **drafts as one PREREG with H-010 (band monotonicity), which becomes its band-level secondary; H-012's conflict-penalty split rides as a stratum** — Haci's H3 "Ranking validity": does a higher SAS score mean a better expected outcome — PASS mean per-night Spearman IC ≥ 0.05, positive in ≥ 60% of out-of-sample windows, and the top tier materially beats the bottom tier; FAIL IC ≈ 0, unstable, or materially non-monotonic → stop presenting the score as a precise conviction ranking. Population: **all 16:05 candidates** (`sas_candidates`, ≈ 57 rows per night, every one carrying `overall_score` and the seven subscores), published or not — the ranking claim is about the score, not the top 8, and the unpublished rows are the counterfactual (graded from the pinned bars, not from the platform's outcome columns — FREEZE_v001 §4). Primary endpoints: (a) per-night Spearman ρ between `overall_score` and the path outcome — first touch of a target at the pick's own printed L3 ATR distance within 20 sessions from the t+1 open (DP-03(b), DP-09, DP-42), synthetic at the same distance for candidates without a ladder — averaged over nights, CI by night bootstrap (rule 6), MPE ρ ≥ 0.05 (Haci's number; DP-44 has no IC unit, so it is recorded as a DP entry at draft); (b) the top-tercile minus bottom-tercile touch-rate contrast within night, MPE +5.0 pp (DP-20). "≥ 60% of windows" is the rule-7 calendar / regime split plus monthly blocks, reported as a stability secondary, not a third primary. The 20-session return IC prints and decides nothing (rule 5). Baseline: the same ρ with scores shuffled within night (permutation null). Why real: the score is the product's central claim, and the platform's own `directional_pct` grade sits at base rate (CLAUDE.md known findings), so the path version is the fair test. Why noise: the weeklies have already printed band-level *returns* on sealed nights (H-010's snapshot: the 90+ lead looks like April–May), so the band secondary is post-hoc for manifest_v001 and the registrar sizes a prospective window (the Q023 / Q018 pattern) or discloses; score compression — the 70–80 band vanished after May and the publication floor is 80 (volatilx `services/super_agent_select_models.py:91`) — leaves the published-only variant a 20-point range, which is why the all-candidates population is primary. Related: H-010, H-012, Q005, Q015, Q023, H-080. Source: Master Hypothesis Program, H3.
- [x] H-079 — registered as Q032 (2026-09-14, `@registrar apply Q032`; `research/questions/Q032_regime_conditionality/PREREG.md`, ready to lock). Window pick nights 2026-09-15..2027-03-09 (121 sessions, floor A at the item-3-capped 0.6620; matured `r₄₀` 62/72), decision Monday 2027-05-17, one DP-13 extension to session 151 (2027-04-21) decided Monday 2027-06-28 = hard stop; `e_min` = 3 (BENIGN 3, HOSTILE 3, `research/reports/STEWARD_Q032_limbs_true_partition.md`, corrected); limb (f) CLEAR at `d19c9a9`; F2 = 17, F1 companion = 29 as `G` rejoins. History: DEFERRAL LIFTED 2026-09-14 (was: Q032 drafted; the SPY-from-2025-01-02 history the arm needs requires Alpaca credentials absent from the desk session). Limb (g) was attempted for the first time and **passes** — SPY returns **424** split-adjusted daily bars for 2025-01-02..2026-09-11 against the **≥ 250** prior sessions `vol_t` needs (`research/reports/STEWARD_Q032_spy_history_probe.md`); the credentials were in `.env.research` all along and the measuring session had not loaded them (`research/lib/desk_env.py`). Q032 re-enters the queue at the front and resumes at `@registrar apply Q032`, schedule re-derived from the actual lock date, out only (DP-43, DP-45). Original entry follows — **not registered**, entry in `research/questions/DEFERRED.md` (registrar, autonomous run 2026-09-14, DP-40..48), **first admission ground** — the objective cannot be measured with the artefacts the desk holds. The lock-or-DEFER gate failed on **limb (g)**, the SPY-history feasibility probe: `ALPACA_API_KEY` / `ALPACA_SECRET_KEY` **ABSENT** from the desk session (`research/reports/STEWARD_Q032_exposure.md` §(g), direct presence test, probe not attempted), so the **SPY split-adjusted daily bars from 2025-01-02** that §2.2's `SMA200` and its **≥ 250-session** realised-volatility tercile require cannot be fetched — **the same blocker, the same two variables and the same session as H-074 / Q030**, whose trigger this entry **cites by id rather than restating**. **The counts confirm it from the other side:** under the literal §2.2 rule the contributing-night rate is **0/71 in all three forms** — `manifest_prices_v001` holds 153 SPY bars (2026-02-02..2026-09-10) reaching only **133** computable rvol20 observations, so **no night in this or any pinned freeze ever reaches 250 prior SPY sessions**, and `SMA200` exists nowhere in it (a **six-cell** proxy composition is observable where §2.2 registers nine). **The three counted limbs were measured on the SMA50-only one-sided bound, which is explicitly *not* §2.2-legal, and are recorded for the re-check only:** contributing nights **0.6620**/session (item 3's cap binding on 0.8710) vs ≥ 0.40; rarer-arm (HOSTILE) nights **0.3099** vs ≥ 0.10 (22 of 68 — far richer than §5.1's 0.20 placeholder, so floor **A** would bind, not floor B); rarer-arm gate-counting tape-episodes **0.0563** unmatured but **0.0141** on the **t+40-matured** basis DECISIONS item 2 requires, vs ≥ 0.025. Pool depth **confirmed** (min 37 / median 53 / max 60, **0** nights below 3) and the CTRA truncated-history signature confirmed — no DP-50(a) finding; commit sweep **NONE** at HEAD `d19c9a9`, no v1.7 promotion scheduled; C1's agreement with the legal platform label **49/63 = 77.8%** with the HOSTILE nights split exactly 11 STRONG / 11 NOTSTRONG, so C1 carries more independent information than §10 threat 2 assumed — and it is **not** promoted to a second primary on that. **The partition is never re-specified to SMA50-only** (PREREG §5.3 R2(i), DECISIONS item 12): that is a different arm chosen after the data existed, and it is a different question with its own id. **Re-check trigger is the H-074 measurement, not a date** — both Alpaca variables present in a desk session **and** a market-data coverage probe returning SPY split-adjusted daily bars from 2025-01-02 (coverage and first/last bar date only) — and satisfying that one trigger re-opens Q030 and Q032 together, in registration order. On re-entry (**`@registrar apply Q032`**) six things must be re-measured, none inherited: limb (a) on a freeze carrying **t+40** bars with numerator and denominator over the same period and item 3's cap applied; limb (b) on the **true** `close > SMA50 > SMA200` trend and the **true ≥ 250-session** tercile; the rarer-arm episode rate on the **t+40-matured** basis; the **nine-cell** composition and item 7's single permitted suppression revision (**cells added only**); limb (f)'s DP-50(a)/(b) sweep and the v1.7 check; and Correction 3's three inequalities **re-solved from the trading calendar against DP-43's ceiling measured from the new lock date**. **PREREG.md and DECISIONS.md are preserved as the drafted and decided record** (`state.json` `PREREG_DRAFT` → `DEFERRED`; no `PREREG_LOCKED`, no `schedule.json`, no `eval.py`, no `results/`, **no outcome of any kind read**), and **where the drafted record below and `DECISIONS.md` disagree, `DECISIONS.md` "## Record — 2026-09-14" and "## Schedule" govern** — every provisional date (the drafted 2027-04-12 / 2027-05-24 and the recomputed 2027-06-28 / 2027-08-09) is **struck in full and never inherited** (the Q025 / Q030 precedent). The question number **Q032 is consumed and not reused**; **`G` leaves both correction sets while deferred** (H-062 / Q025 precedent), so **F2 falls from 17 to 16** (Q023 2 + Q027 2 + Q029's 10 + Q031 2) and the **F1 companion set from 28 to 27** (Q006 2 + Q024 6 + Q025 2 + Q029 16 + Q031's `D1ᴮ` 1) — no locked PREREG cites Q032, so **no locked file is edited**; the unlocked **Q033** draft's §7 counts are corrected (F1 29 → 28, F2 18 → 17) and **Q034** registers no primary and no BH set, so nothing there changes. **C1 is withdrawn as a blocking companion to Q023's E1; Q023 is unaffected and is not edited.** Q032's R2 rider (SPY from 2025-01-02; the t+40 bar horizon) is **withdrawn and HELD**, and Q027's and Q031's own freeze requests and pins are unaffected. The drafted record follows, as written on 2026-09-14 and **not** a registration: drafted as Q032 (`research/questions/Q032_regime_conditionality/PREREG.md`). Drafted on the **path** objective (rule 5, DP-01): **one primary, night-level, two-sided** — **P1** `G` = Q006's within-night control-adjusted L3-touch excess `E_t` (each pick's own printed swing target, 20 sessions from the session t+1 open — DP-03(b), DP-09, DP-42; the ten nearest same-night unpublished candidates matched on beta60 / atr_pct / runup20 with a synthetic target at the identical ATR distance, Q006 §3 verbatim) averaged over **BENIGN** nights minus the same over **HOSTILE** nights, **MPE ±10.0 pp** (H-079's own number; DP-20's larger-MPE clause for a difference of differences — the Q012 P2 / Q018 P4 / Q022 E1 / Q023 E1 precedent). **`m` = 1**, fixed at lock. **The partition is bar-only and fixed at this lock** (§2.2, DP-26): `trend_t` = UP iff SPY close > SMA50 > SMA200, DOWN iff close < SMA50 < SMA200, else MIXED; `vol_t` = expanding-window tercile of SPY's 20-session realised vol over every session ≤ t with ≥ 250 sessions of history; **BENIGN** = UP ∧ vol ∈ {LOW, MID}, **HOSTILE** = the other eight cells. Every input is a SPY bar dated ≤ t, so **a platform relabelling or a regime-engine repair cannot move a night between arms** (DP-50(a)) — which is why §9's DP-50(b) flag-off constraint on `services/market_regime/scorer.py` binds the companion only, not the primary. **H-079 names two regime definitions and the second is deliberately not a primary**, on an exposure fact rather than a preference: `market_regime_daily` has taken exactly **two** values in its whole point-in-time-legal history (`STEWARD_Q023_exposure.md` §3: 49 `strongly_bullish` / 13 `bullish` in 65 sessions, **zero** `neutral` / `bearish` / `risk_off`), so its only partition with exposure **is Q023's locked E1**, on a population ~95% identical (`publication_floor` = 80.0 since 2026-07-07; 90+ runs 2–3 picks a month). It is registered instead as **C1, a mandatory blocking companion** (§8 clause 6) that is corrected **jointly with Q023's E1 as one non-independent pair**, carries no q of its own, and **licenses nothing in any branch** (§9) — registering it as a primary would give the desk a second bite at a locked question's statistic. **Three clauses make this a conditionality test rather than a level test, and each can only block or relabel a positive:** clause 3 **`NO_EDGE_TO_CONDITION`** — if the pooled `mean_t E_t` is inside ±5.0 pp there is no edge for the tape to be conditional on, the verdict is INCONCLUSIVE with that label and routes to Q006 / Q027 (Q031 clause 1 applied to tape instead of time); clause 4 splits a confirmed `G` into **`WORKS_IN_BENIGN`** (BENIGN's own level > +5.0 pp — the only label that licenses a lane rule) and **`HOSTILE_ONLY`** (the edge is merely less negative in calm uptrends — guide line only, no flag, no card field, no sizing); clause 8 the **80% HOSTILE composition guard**, which *renames* the verdict after the sub-state carrying it rather than moving any floor. **Clause 5 is H-079's own reproduction limb** — same sign in both halves, **split by session index at lock** (Half A = sessions 1–60, Half B = 61–119; re-cut 1–75 / 76–149 if the extension fires), blocking at whatever count it has. **Three units are given three names because two of them are called "episode" elsewhere on this desk:** the **contributing night** (floor unit), the **tape-episode** (Q023 §2.5's construction on the bar-only label — the block and permutation unit, and H-079's own "regime labels permuted across episodes" baseline, 10,000 permutations, seed 20260914), and the **symbol-episode** (DP-51 only, gaps ≤ 10 sessions). **DP-51 applies in full and is a decision clause:** both the tape-episode block CI and the symbol-episode CI must exclude 0, and clearing MPE on the first but not the second is INCONCLUSIVE, never CONFIRMED. **Window is prospective-only — pick nights 2026-09-15..2027-03-05 (119 elapsed sessions), Q027's window adopted session for session** so **one successor freeze pair serves both**, with **one added scope requirement that is load-bearing: SPY split-adjusted daily bars from 2025-01-02** — `manifest_prices_v001` starts 2026-01-31, so **`SMA200` exists nowhere in any manifest pinned today** and no arm could be assigned without it; if that history cannot be obtained at the decision pass (the Q030 / H-074 Alpaca-provisioning blocker) **Q032 is DEFERRED on that ground and the partition is never re-specified to SMA50-only after the fact**. The sealed stretch has been read for this hypothesis (the 2026-09-10 / 09-12 weeklies' regime-stratified cells are what put H-079 on the backlog) and prints once as a labelled post-hoc panel, 2026-06-01..2026-08-12 **split at 2026-07-06** (the `_enforce_ladder_monotonic` ship), entering no verdict, no half, no stratum, no q. PROSPECTIVELY_CONFIRMED reachable by construction (DP-24 satisfied, DP-31 does not apply). Decision **Monday 2027-04-12**, one automatic DP-13 extension to pick nights ..2027-04-19 decided **Monday 2027-05-24** (hard stop), then DEFERRED — 6.9 and 8.3 months from lock, inside DP-43's ceiling. **R1 is counts-only and BLOCKING for the lock**, because no desk report has ever counted a bar-only tape partition and floor B (20 contributing nights in the rarer arm) rests on the one rate that is a placeholder: the drafted 0.9538 contributing-nights-per-session is **borrowed** from Q023 on a population this one contains (conservative), but the 0.20 rarer-arm rate and the 3.0 nights-per-episode conversion are **Q023's platform-label numbers used as planning placeholders for a different variable**. R1 measures the split under an **SMA50-only** variant, registered at lock as a **one-sided bound and used as one** — adding `SMA50 > SMA200` can only move nights out of UP, so the count is a **lower bound on HOSTILE** and an **upper bound on BENIGN**, and **if the upper-bounded arm is the one that fails the gate, Q032 defers rather than locking on a bound that runs the wrong way** (DP-45). Lock-or-DEFER gate (DP-43's ceiling solved at this lock date → 225 admissible elapsed sessions): contributing nights **≥ 0.36**/session, rarer-arm nights **≥ 0.09**, rarer-arm gate-counting tape-episodes **≥ 0.023**. **F2's correction set at this lock is Q023 (2) + Q027 (2) + Q029's 10 companion IC endpoints + Q031 (2) + Q032 (1) = 17 and never falls below 17**, with `G` **additionally corrected in F1** (Q006 2 + Q024 6 + Q025 2 + Q029 16 + Q031's `D1ᴮ` 1 + 1 = 28) and the **larger q quoted** — the Q015 / Q020 / Q029 / Q031 pattern, because a conditionality test on Q006's own statistic is not independent of Q006's. **Not merged into Q023** (one cell of this under the *other* definition, on the 80–90 band; its E1 is Q032's C1 and §7 forbids reading either as confirming the other), **nor Q006** (the level of `E_t`; its 2026-10-05 decision lands inside this window and changes no arm, MPE, clause or date here), **nor Q027** (same window, freeze, entry and clock, different claim), **nor Q031** (decay is the time-only alternative — Q031 §8 clause 7 routes `TAPE_COMPOSITION` **to this question**, and a large `G` beside a Q031 DECAY is not two findings), **nor Q022** (same two-stage shape, different night-level arm). **No rule-14 exception is requested** (DP-41): every input is a 16:05 ET candidate field, a SPY or pick bar dated ≤ t, the trading calendar, or the t+1 open used **only** as the entry price. **§9's NULL branch is as consequential as its positive one** — it retires regime as an explanation for weak stretches (H-079's own FAIL action), labels the weekly's regime panels as composition rather than signal, and replaces "it was a difficult tape" with the measured HOSTILE-arm band. **Three open decisions go to the Decision-maker** (§11: which definition carries the primary; how the lock gate handles SPY's missing 200-session history; and whether the pooled HOSTILE arm is protected by the 80% renaming guard or split into two thin primaries). Original entry: Haci's H9 "Regime conditionality": does SAS have an edge only in identifiable environments — PASS the same frozen strategy works reliably in pre-defined regimes and weakens predictably elsewhere, reproduced OOS; FAIL the regime partition does not reproduce → do not invent regime explanations for bad periods. Rule 7 already stratifies every result by `market_regime_daily`, and Q023 tests one band × one label prospectively; H-079 is the general form as a **primary**: the regime × selection-edge interaction on all published picks — `E_t` (Q006's within-night control-adjusted L3-touch rate) averaged per regime cell, the contrast between cells the PREREG names in advance, MPE ±10.0 pp (a difference of differences, DP-20's larger-MPE clause), reproduced with the same sign in two independent windows (the first and second halves of the prospective window, split at lock). Two regime definitions, both fixed at lock: the platform's point-in-time label (v1.2, legal from 2026-06-09 — FREEZE_v001 §7, PI-005) and a bar-only proxy the desk computes from data ≤ t (SPY against its 50- and 200-session means; a realised-vol tercile), which is knowledge-time-legal on every night and immune to platform relabelling. Regime nights arrive in runs, so the episode-block bootstrap and the ≥ 5-episodes-per-arm gate from Q023 §2.5 bind (DP-51 / H-070). Baseline: the pooled edge with regime labels permuted across episodes. Why real: April–May and June–August already disagree on nearly every descriptive cut in the weeklies. Why noise: regime and calendar are confounded in a five-month history, and the desk has read the sealed regime cells (weekly 2026-09-12 §3) — prospective-only window. Related: Q023 (one cell of this), H-082 (decay is the time-only alternative), every rule-7 stratum. Source: Master Hypothesis Program, H9.
- [x] H-080 (F2) — **DEFERRED 2026-09-14** (registrar, autonomous run, DP-40..48), entry in `research/questions/DEFERRED.md`, **not registered and not drafted — no PREREG, no directory, no question number consumed (Q033 stays free; the H-041 precedent)**. Deferred on that file's **second admission ground (DP-43's 12-month ceiling)** with a **second, structural limb that no amount of waiting fixes**. **Limb 1, the arithmetic:** both primaries live above 90, and on the all-candidates population H-080 itself specifies, `STEWARD_Q027_exposure.md` (d) measures **18 rows with `overall_score ≥ 90` on 17 of 48 contributing nights** (2026-06-01..2026-09-10, 71 elapsed sessions) — an elite-present rate of **0.2394 per elapsed session**, which is an **upper** bound on the both-arms rate primary 2 needs (`[85,90)` appears on 33 of 48; the joint has never been measured, ≈ 0.165 under independence) and an upper bound on the *current* rate (published elite runs **11 → 4 → 0 picks** by month, `STEWARD_Q015_exposure.md` (9); PI-010's series is 8/13/12/8/3/2; Q005 INCONCLUSIVE on cause). Against DP-21 that gives **160 elite-present nights as written** (80 for the `c*`-dispersion primary across ≥ 4 non-overlapping windows at 20 each, plus 80 for the held-out `≥ 90` vs `85–89` contrast on disjoint nights) = 668 sessions → decision ≈ **2029-06-18, ~21 months past the ceiling**; and **80 nights on the most generous reading anyone could defend** (primary 2's nights taken as the union of the training windows) = 334 sessions → ≈ **2028-02-21, still ~5.4 months past**. A lock today would need **0.727/session** as written, **0.364/session** at the loosest reading. **Limb 2, why no accrual date is named:** the band carries **1.06 rows per elite-present night and zero on the median contributing night**, so on a typical night every cut between one elite row's score and the next score below it induces the identical partition — `c*` is decided by the tie-break, not by data, and the ±3-point PASS band (Haci's own "nearby range") cannot be resolved at any night rate. The permutation null H-080 names is correct and does not repair it: it would return a very wide null wander, making the verdict **INCONCLUSIVE by construction**, which DP-45 forbids registering. **The backlog's suspected cause is corrected by measurement:** the ATR-elite cap fires on **2 of 4,020 rows (0.05%), both at 79.9, none at 84.9**, so the cap is not why the band is thin; and `publication_floor` is irrelevant to a population of all candidates ≥ 70. **Rejected before deferring:** merging into **Q027** (its band secondary fixes five bands at lock and blocks on an *inversion*, never searches a cut or reports an argmax's dispersion — H-010's line above already routes an inversion at the 90 line *to* H-080 rather than answering it), **Q015** (whose `90+` arm is itself demoted and SUPPRESSED at 14 nights, and whose §1 names this as a separate future question), **Q005** (elite *count*, reads no outcome), **Q031 / Q032** (level over time, level by tape — neither locates a boundary); primary-1-only (worse arithmetic, limb 2 in full); published picks only (15 picks / 14 nights, strictly worse); truncating the grid below 90 or widening ±3 to ±8 (DP-25, DP-45 — a different hypothesis); pooling rows across nights (rule 6); sealed training windows (the 88–90 vs 90+ finding and the weekly 90+ return series are sealed reads at this exact boundary, **and** the 2026-07-08 publication-gate ship doubles `d*_t` mid-stretch); DP-13's extension (short by 60–140 nights, not marginal); and demoting the `≥ 90` arm to descriptive (it **is** both primaries — the H-062 / H-041 precedent). **Re-check triggers are two measured counts on a then-current freeze over a trailing quarter, both required, neither a date:** (1) nights carrying ≥ 1 candidate `≥ 90` **and** ≥ 1 in `[85,90)` per elapsed session **≥ 0.73** (≥ 0.36 triggers a re-read of the entry, not a lock) — a stricter form of Q015's own ≥ 0.5 trigger, which governs for Q015 but not here; (2) **median rows per contributing night with `overall_score` in `[87,93]` ≥ 3** (today: 0). The realistic path to either is EN-016's wider candidate universe or a scoring change that restores the upper tail — both of which would also split the window under DP-06 / DP-50(a). **F2's correction set is unchanged at 17** (Q023 2 + Q027 2 + Q029's 10 + Q031 2 + Q032 1); H-080's primaries do not join it while deferred (H-062 / Q025 precedent). **Consequence recorded for H-083**, which sequences itself after H-080 and routes H-080's FAIL branch into itself: no threshold verdict is coming, so H-083 either states that in its own §1 and stands on the published v1.6 slate as its baseline already says, or waits for the triggers — it may not cite a descriptive band read as a substitute. **No outcome of any kind was read** (counts only, pinned freeze, no live query — DP-50(c)); **no rule-14 exception is involved** (DP-41). **Caveat carried forward:** any future PREREG on H-080 is post-hoc with respect to `manifest_v001` and must run prospective-only. Original entry: Haci's H10 "Threshold stability": is 90 a real boundary or a historical artifact — PASS 90 or a nearby range stays near-optimal across independent walk-forward windows; FAIL the optimal cut moves substantially between windows → drop the hard threshold and use categorical setups (H-083). Walk-forward on all candidates ≥ 70 (the qualification threshold, volatilx `services/super_agent_select_models.py:85`): in each training window find the cut `c*` maximising the within-night control-adjusted L3-touch excess (Q006 `E_t`) of candidates ≥ c over candidates < c; primaries: the dispersion of `c*` across ≥ 4 non-overlapping windows (PASS if every `c*` lies within ±3 points of 90 — Haci's "nearby range", fixed at lock) and the ≥ 90 vs 85–89 contrast on the held-out window, MPE +5.0 pp (DP-20), same sign in ≥ 2 windows. Facts the PREREG carries: the 88–90 band underperforms 90+ (CLAUDE.md); the ATR-elite block caps bull scores at 84.9 for ATR-only projections (`:129-131`); the publication floor of 80 dates from 2026-07-07 (`:91`); the elite count is falling — 8 / 13 / 12 / 8 / 3 / 2 picks per month (PI-010; Q005 INCONCLUSIVE on cause) — so the ≥ 90 cell may not reach 20 contributing nights per window, and the registrar sizes on the Steward's elite rate; if the floor lands outside DP-43's ceiling the question defers with Q015's trigger (≥ 0.5 elite-contributing nights per session over a trailing quarter). Baseline: the same walk-forward with scores permuted within night — the "optimal" cut of pure noise wanders by construction, and that dispersion is the null. Why real: 90 was set by hand and the score distribution has drifted (Q005). Why noise: sealed band returns have been printed (H-010), so the sealed period is post-hoc for the band contrast; thin elite cells make `c*` noisy even under a true threshold. Related: Q015 (85–90 vs 90+), H-002 (the 70 boundary), H-010 / H-073, H-083. Source: Master Hypothesis Program, H10.
- [x] H-082 (F2) — **registered as Q031** (`research/questions/Q031_edge_decay/PREREG.md`; registrar, autonomous run 2026-09-14, DP-40..48). Registered on the **path** objective (rule 5, DP-01): **two primaries, one per series, both night-level, both two-sided** — **P1** `D1ᴬ` = the mean per-night Spearman IC of `overall_score` with the path-outcome rank (Q027's `IC_t`, at the night's common ATR distance `d*_t`, 20 sessions from the t+1 open) in the window's **second** block minus the **first**, **MPE `|D1ᴬ| ≥ 0.05`**; **P2** `D1ᴮ` = the mean per-night **control-adjusted L3-touch excess** of the published slate (Q006 §3's `Delta_t`: each pick's own printed L3 distance, the ten nearest same-night unpublished candidates matched on beta60 / atr_pct / runup20 with a synthetic target at the identical ATR distance) on the DP-09 20-session clock, same block contrast, **MPE ±5.0 pp** (DP-20). **`m = 2`.** **The whole question runs on Q027's machinery by reference** (§2.0: §2.1's funnel, §2.2's `d*_t` and its thin-slate / absent-slate rules, §2.3's entry and clock, §2.4's exclusions, §2.5's contributing-night and episode rules, §4.1's `IC_t`) and on Q006's for the selection series — **one machinery, three questions**, so the only difference between Q027 and Q031 is the statistic taken over the night series. **The estimator is a deliberate re-specification of the entry's "CUSUM primary", recorded rather than silently applied:** the magnitude lives on **three blocks fixed at this lock by session index and proportion** (W1 = sessions 1–61 reference, W2 = 62–122 test, W3 = 123–153 persistence holdout; 40/40/20, the proportions re-applied at `record` when the window lengthened), because a magnitude read at a **data-chosen** change point is biased away from zero by its own search and has no sample size knowable at lock, so no floor could be written for it — while the **CUSUM is kept in the role it can honestly fill** as §8 clause 9 (is there a break, and where; `τ̂` prints in every branch). The entry's *"shuffle nights within episodes"* null is likewise re-specified to a **circular block permutation** of the night series (expected block length 10 sessions, the desk's standing value), because a plain shuffle destroys the series' autocorrelation and would call a slowly-drifting but stationary series a break; the episode is a cross-sectional unit and DP-51 puts it in the CIs, where it belongs. **Four clauses make this a decay test rather than a level test, and each can only *block* a positive:** clause 1 **`NO_EDGE_TO_DECAY`** — if the reference block's own level is below the edge MPE the endpoint is INCONCLUSIVE with that label, decides nothing about decay and is routed to Q027 / Q006, because a flat line at zero is not the stability of an edge; clause 5 **persistence** (`D2 = W3 − W1` must carry the same sign at ≥ MPE — H-082's own *"a single bad quarter is INCONCLUSIVE by construction"*); clause 6 the **level gate** `SHRUNK_NOT_GONE` (the later blocks' own level must have fallen below the edge MPE — a fall from excellent to good licenses a monitor and nothing else); clause 7 **`TAPE_COMPOSITION`** (the coarse `tape_sign_t`-reweighted contrast must hold, else the result is routed to **H-079**, whose question it then is). **DP-20's doubling clause is considered and declined with the reason** (§4.2): `D1ᴮ` is formally a difference of differences, but at 10.0 pp a complete collapse of a mid-sized edge would be unconfirmable by construction — a validity failure, not conservatism (the Q029 reasoning) — and the same argument binds harder on the IC, where the level MPE *is* 0.05; clause 6's level gate is registered in its place and is strictly harder than the bare MPE. **Window is prospective-only — pick nights 2026-09-15..2027-04-23 (153 elapsed sessions, sized at `record` on the *slower* of the two measured rates)** — and the sealed stretch is **never the reference block**, for two independent reasons: it has been read for this hypothesis (the weekly §7 rolling four-week 90+ trend, H-010's April→August fade, Q005's elite-rate halving are what *motivated* H-082), and it sits on the far side of the 2026-07-08 publication-gate ship that doubled `d*_t` from ≈ 1.06 to ≈ 2.10 ATR, so a contrast across it would measure the ship. It prints once as a labelled post-hoc panel (2026-06-01..2026-08-12, split at 2026-07-06) entering no verdict, no block, no CI comparison and no q. **Sized on the borrowed measured rate, named as borrowed:** series A's contributing-night definition **is** Q027 §2.5's, so R1(a) of `research/reports/STEWARD_Q027_exposure.md` applies unchanged — **0.6761 per elapsed session (48/71)**, re-confirmed bit-for-bit by R1(e); **series B was measured for the first time at 0.6620 (47/71)** (`research/reports/STEWARD_Q031_exposure.md`). Lock-or-DEFER gate **≥ 0.45**, derived **per block** rather than as a total — the DP-43 ceiling solved backwards at the actual lock date gives a last window end of session 225, whose 40/40/20 cuts need max(80/180, 20/45) = 0.4445 — **cleared by 52% (A) and 49% (B), so Q031 locks**; the 0.9577 eligible-night proxy is refused as an extrapolation past the freeze's own forward-bar horizon (DP-45, Q027's own refusal), and it would have decided ≈ 2027-03-29. **Floors: 100 contributing nights per series** — 80 across W1 ∪ W2 (DP-21, per primary) plus 20 in W3 (DP-21, cell floor, blocking at whatever count it has). Decision **Monday 2027-06-07** (2027-04-23 + 20 sessions + one week → 2027-05-31, Memorial Day, skipped **out** to the next Monday), one automatic DP-13 extension to pick nights ..2027-06-07 (session 183) decided **Monday 2027-07-19** (hard stop), then DEFERRED — 8.7 and 10.1 months from lock, inside DP-43's ceiling by 3.3 months; PROSPECTIVELY_CONFIRMED reachable by construction (DP-24 satisfied, DP-31 does not apply). **`eval.py` is committed no later than 2027-03-08 and not touched afterwards, and that deadline is a validity requirement rather than housekeeping**: Q027 and Q029 decide on **2027-04-12** and will print this question's own `S^A` series for all but the final three sessions of W1 ∪ W2 about eight weeks before Q031 decides, so a script written after that date would be written by someone who has seen two thirds of the series (§10 threat 6 states the residual risk that remains even so). **DP-51 applies in full and is a decision clause**: two CIs per primary, the episode being one symbol's run of appearances at gaps ≤ 10 sessions, and clearing MPE on the date-clustered CI but not the episode-clustered one is INCONCLUSIVE, never CONFIRMED — sharper here than in Q027, because a single long episode can sit almost entirely inside one block and move that block's mean on its own. **R1 (counts only) was made BLOCKING for the lock** — not for the lock-or-DEFER gate, which series A clears alone, but because **DP-43 settles an arm's demotion at lock and never afterwards** — and is **satisfied and closed** (`research/reports/STEWARD_Q031_exposure.md`, 2026-09-14): series B **0.6620/session (47/71)** under the registered ≥ 3-valid-picks / ≥ 5-matched-controls rule **and identically** under Q006's weaker ≥ 1-pick rule and the drop-the-thin-pick variant (pool depth min 37 / median 54 / max 60, so the stricter floor **costs nothing measured** and was not relaxed on that finding); 35 invalid targets of 383 published rows; 47 nights contributing to both series; commit sweep **NONE** (`HEAD` `d19c9a9`) with **no v1.7 promotion date** inside 2026-09-15..2027-07-19. The measured rate fired the **second** of three pre-fixed branches: the window end moved **out** for both series (149 → 153 sessions), the blocks were re-cut at the registered proportions, the decision date moved out two weeks, **`m = 2` stands and P2 is not demoted**; no floor was reduced in any branch. **R2** is the only routed item still open (successor freezes, due before 2027-06-07, blocking nothing — DP-23), a **superset of Q027's successor freeze** (same population, window extended to 2027-04-23), built once and serving both. **Not merged into Q027** (which asks whether `IC_t` has a level; Q031 asks whether the level moves — they share the nights of W1 ∪ W2 and the entire `S^A` construction, so §7 forbids reading either as corroborating the other, and Q027 §10 threat 11 names this question as where its series is watched over time), **nor Q006** (the level of `S^B` on sealed nights; its 2026-10-05 decision lands inside W1 and **changes nothing here** — no block, no MPE, no clause), **nor Q029** (which owns the per-layer ICs: Q031's attribution limb computes the same statistic as a time series and licenses nothing about any layer, DP-29), **nor H-079** (regime conditionality, decay's principal competitor, which clause 7 routes to rather than answers), **nor H-014** (configuration brackets, to which Q031's per-night `config_json` panel is descriptive input, never a test). **F2's correction set at this lock is Q023 (2) + Q027 (2) + Q029's 10 companion IC endpoints + Q031 (2) = 16 and never falls below 16**, with **`D1ᴮ` additionally corrected in F1** (Q006 2 + Q024 6 + Q025 2 + Q029 16 + 1 = 27) and the **larger q quoted** — the Q015 / Q020 / Q029 pattern, because a decay test on Q006's own statistic is not independent of Q006's. **§9 draws a line the entry's own action word invites crossing:** a confirmed DECAY **does not by itself retire or retrain any component** — the attribution limb is descriptive by construction and Q029 owns the layer endpoints, so a retirement brief needs all three of a DECAY verdict here, a layer whose own series moves with it, and Q029's verdict on that layer; what a DECAY does license is **EN-017**, the nightly edge-decay monitor, built with *this* question's measured machinery, block length, seed and MPEs rather than invented ones. **A STABLE verdict is equally consequential and equally actionable**: the desk stops explaining weak stretches as decay, the measured W1 bootstrap band replaces the phrase "within normal variation" with a number in the weekly, and EN-017 is built anyway — a monitor is worth more when the series is known to be stable. **§9 carries a DP-50(b) constraint tighter than any other question's:** any change to the SAS weights, timeframe multipliers, `qualification_threshold`, the ATR-elite caps, the GEX offset or the scoring enable-flags — **including any promotion of the v1.7 score** — is flag-off until **2027-06-07** (2027-07-19 if the extension fires), extending Q027's and Q029's identical constraint by about **eight** weeks and making it the tightest such constraint on the board, because for a decay question a scoring ship does not merely split the data, it *becomes* the explanation for what the question measures; if it ships anyway the window is cut at the ship date, out and never in. As of the lock the DP-50 sweep since `fa70688` returns **NONE**. **All fifteen decisions are settled** (`DECISIONS.md`, decision-maker, decide + record passes 2026-09-14; applied to the PREREG by the registrar the same day, `schedule.json` written with `pin_at_decision: true`): the five drafted §11 items all went the Registrar's way — the decay MPEs with the doubling declined and clause 6's level gate registered in its place; the fixed-block estimator over the CUSUM magnitude, because rule 6's floors cannot be written for a data-chosen segment; W3 at DP-21's cell floor of 20 rather than 80, since `D2` can only block a DECAY and never produce one; series B as a second primary with its demotion settled at lock; and locking now, prospectively — **three of them DEFAULTED** (#5 lock now, #6 the prospective-only window start, #7 the window end and decision date, all the same cost: waiting, and the `NO_EDGE_TO_DECAY` risk inside it). **Fourteen corrections applied, none weakening a floor, an MPE, a gate or a clause, and every date moving out**: the series-B "nothing changes" threshold corrected from 0.671 (a total over the window) to **0.6723** (the per-block floor — a rate of 0.6715 would have passed the drafted test while delivering 79.9 nights against an 80-night floor); the window end re-solved as **the smallest window whose *rounded* 40/40/20 cuts meet every per-block floor for the slower series simultaneously** (153, not the 152 a sum of independent floor dates gives, and not 149); the block table given **one projection column per series, never pooled**; the lock-or-DEFER gate re-derived per block at the actual lock date; the holiday list extended with 2027-07-05 and 2027-09-06 and the **Memorial-Day skip shown**; and `eval.py`'s commit deadline deliberately **left at 2027-03-08** rather than moved out to the new W2/W3 boundary, the earlier date being the stricter one (DP-45). Original entry: Haci's H12 "Edge decay": is recent weakness temporary or structural — PASS rolling IC / expectancy recovers or stays within historical variation; FAIL persistent deterioration across independent windows → retire or retrain the affected component. Series, night-level from 2026-06-01: H-073's per-night IC and Q006's `E_t` (control-adjusted L3-touch excess), plus H-075's per-layer ICs so a fall can be attributed to a component. Primary: a pre-registered change-point test (CUSUM on the night series; permutation null by shuffling nights within episodes, DP-51) with FAIL declared only if the post-change mean sits below the pre-change mean by more than the MPE (5.0 pp on `E_t`, DP-20; ρ 0.05 on the IC) **and** the deficit persists in the next independent window; a single bad quarter is INCONCLUSIVE by construction. Baseline: the series' own historical variation (block bootstrap of the pre-window). Known: the weekly §7 rolling four-week 90+ trend and H-010's snapshot show the 90+ return lead fading from April to August, and Q005 shows the elite-candidate rate halving (0.50 → 0.245 per night, p = 0.016) with cause INCONCLUSIVE — sealed reads, so the sealed stretch is a labelled post-hoc panel and the decision rests on prospective nights. Why real: PI-010 and Q005 already show the score distribution moving; a weaker tape and a drifting score are both candidate causes. Why noise: five months is one regime cycle, and the return version is confounded with the tape — which is why the control-adjusted `E_t` is the series. Related: Q005, H-014 (configuration brackets), H-079 (regime is the competing explanation), PI-010, EN-017 (the monitor). Source: Master Hypothesis Program, H12.

## F3 — Unused signals
- [x] H-020 (F3) — **merged into Q029** (`research/questions/Q029_layer_value_ablation/PREREG.md`; registrar, autonomous run 2026-09-14; DP-29). GEX is one of Q029's seven layers and, because its base weight is **0.0** in v1.6 (`services/super_agent_select_models.py:9-17` — kept in the dict so the subscore is still computed for audit visibility), its test is **addition, not removal**: E1_gex / E2_gex are the marginal and partial per-night Spearman ICs of `gex_alignment_score` with the path outcome over all 16:05 candidates, and E3_gex re-scores the night with GEX **restored to its nominal weight** and re-selects the slate. The addition weight is **measured, not assumed** — R1b(c) read `scoring_weights.gex_alignment` from the last `sas_runs.config_json` dated before the v1.5 ship of 2026-05-14 and **measured 12.0** (2026-05-08; only 12.0 and 0.0 ever appear in the frozen history), so the fallback branch did not fire and is struck; the code comment's own reallocation arithmetic (technical 20→27, projection 26→29, catalyst 8→10 = 12) is kept as a corroborating remark only, and a `{5, 12, 24}` sensitivity curve is printed as a companion that never decides. GEX is **EVALUABLE on all three endpoints** (99.00% non-null, 32 distinct values, within-night variance on 100% of segment nights) and is an ADDITION test on 68 of 68 nights. **Two re-specifications of the entry as filed, recorded rather than silently applied:** the baseline is **not** "GEX-missing picks" — a GEX-missing pick differs from a GEX-covered one in coverage, liquidity and option-chain density, so that contrast is confounded and the +5 GEX-missingness offset (`services/super_agent_select_scoring.py:1379-1399`) sits directly on it; Q029's baselines are the within-night subscore permutation (B1), the residual permutation (B2), the placebo swap (B3) and the rule-5 distance-matched control (B4). And the endpoint is **not** "day-lane outcome" but L3 within 20 sessions from the t+1 open (DP-09, DP-42), with `best_timeframe` carried as a reported stratum for E1/E2 — the lane multipliers (`:37-66`: GEX 1.0 / 0.9 / 0.6 across short / swing / long) are what would make a lane effect visible, and they are printed. **Filed in F1, not F3** (DP-29: family follows the deciding endpoint's subject, which is which candidates reach the slate), so **F3's correction set is unchanged at Q021 alone** and H-020 is not counted in two families. Not merged into Q021 (pin risk, a different GEX quantity, `gex_context.pin_risk`, on a two-sided ±2 ATR / 60-session endpoint) — Q029 §7 does not list it because Q021 tests no subscore; the two share only the word GEX. Original entry: GEX alignment (weight 0) vs day-lane outcome — baseline: GEX-missing picks
- [x] H-021 — DEFERRED 2026-09-14 (`research/questions/DEFERRED.md`, entry H-021; registrar, autonomous run, first admission ground; not drafted, no question number consumed, not merged — DP-29). Blocker: no freeze holds implied volatility. The frozen 16:05 `context_json` copies top contracts but drops `iv` (volatilx `services/symbol_context_builder.py:274-291`). `whale_ledger` is an empty option-contract ledger. The one platform table with a per-contract IV, `uoa_contract_daily.iv` (`services/uoa_screener.py:1067-1069`, `:1457`), is unfrozen, has no `freeze_config.availability` declaration, and is deleted and re-inserted from a later snapshot on any `force` re-run (`:1282-1287`; a single-symbol `run_uoa_range.py --force` wipes the whole date). `gex_contract_snapshot.iv` is off by default (`services/gex.py:35`). Triggers (either): (1) the Steward freezes `uoa_contract_daily` with `created_at`, declares availability, and a counts-only probe over a trailing quarter measures ≥ 95% point-in-time nights, ≥ 90% ATM-IV coverage for picks **and** for same-night controls (within 10 pp of each other), and a contributing-night rate at the DP-43 gate re-solved at re-entry; or (2) EN-011's options EOD history lands and passes the same coverage tests. On re-entry the endpoint is re-specified to the path (rule 5, DP-42), not "realized return". F3 set unchanged at Q021 (1). Original entry: IV/RV ratio at selection vs realized return by lane — new data needed
- [x] H-022 — DEFERRED 2026-09-14 (`research/questions/DEFERRED.md`, entry H-022; registrar, autonomous run, first admission ground; not drafted, no question number consumed, not merged — DP-29). Blocker: neither the desk nor the platform holds any insider-transaction (Form 4) record. `whale_watch_ledger` is 0 rows (FREEZE_v001 §5) and is an option-contract outcome ledger anyway (`manifest_v001.json:432-462`). The universe's `whale_watch` "insider" source is an **options-flow screen**: OTM ≥ 20%, DTE 10–45, single print ≥ $75K, spike ≥ 6× (`services/candidate_universe_builder.py:51-109`, `routers/uoa_screener.py:311-329`, `services/super_agent_select_models.py:114-120`), scored in the flow layer (`services/super_agent_select_scoring.py:761-800`). No ORM table stores Form 4 data. FMP `insider-trading/latest` is fetched live for reports and never persisted (`ai_agents/fmp_client.py:139-150`). The routine/opportunistic label needs each insider's own ≥ 3-year history, and knowledge time is the SEC acceptance datetime, not the trade date. Triggers (both required): (1) a sha256-pinned Form 4 freeze for every candidate symbol from ≥ 36 months before the window, with reporting-owner CIK, transaction code, transaction date and acceptance datetime (procurement, Haci's call); (2) a counts-only Steward probe on it measuring nights with ≥ 1 opportunistic P-coded purchase accepted ≤ 16:05 ET within the look-back and ≥ 3 matched controls, at the DP-43 gate re-solved at re-entry. F3 set unchanged at Q021 (1). Naming hazard: on this platform "insider watch" means options flow. Original entry: Opportunistic vs routine insider buys inside window — new data needed
- [x] H-061 (F3) — **registered as Q021** (`research/questions/Q021_pin_risk_two_sided/PREREG.md`; registrar, autonomous run 2026-09-13). **Q017 was superseded before lock and never run:** its draft PREREG was committed before `@registrar apply` could fold in the `DECISIONS.md` amendments, and the locked-PREREG guard forbids editing a committed PREREG, so the question was re-registered as **Q021** with every amendment folded in — identical hypothesis, population, arms, endpoint, MPE, window (pick nights 2026-06-01..2027-04-30), decision date (Monday 2027-08-09), extension and decision rule. **No outcome was read for either question**; Q017 is recorded in `research/questions/DEFERRED.md` as a registration-mechanics supersession, not a research deferral. **Not merged into Q016** although it shares Q016's endpoint exactly (`TWO_SIDED` at ±2 ATR / 60 sessions from `C_t`): Q016 conditions on `beta60` and `atr_pct` terciles and tests no GEX quantity anywhere, so the hypothesis is untested by any locked PREREG. The shared endpoint is priced in instead — Q017 sits in **F3** with a **required companion BH correction across F7's 15 primaries**, and §4 makes the within-ATR-tercile and within-beta-tercile decomposition mandatory, so a Q017 positive beside a Q016 ATR result can never be counted as two confirmations (§7). One primary: the within-night paired PIN − NOPIN contrast on published, **GEX-computable** picks. Two registrar findings shape the draft: (a) `pin_risk = false` conflates "not pinned" with "not measurable" — the degraded snapshot path writes `regime="UNKNOWN"`, empty walls and `pin_risk=False` (volatilx `services/gex.py:676-698`) and `_pin_risk` returns False on an empty wall list (`:340-344`) — so §2 carries a computability filter and the conflation is filed as a platform-issue candidate under DP-07; (b) `gex_symbol_daily` is upserted in place (`services/gex.py:605-629`, `:823-840`), so the feature is read from the candidate row's 16:05 `context_json.gex_context` snapshot and **no rule-14 exception is requested** (DP-41 respected). The dominant threat is that pin risk is mostly a statement about the option chain — strike spacing in percent falls with price, so pinned names skew high-priced, densely-struck and low-ATR, exactly the cohort EXPLORE_001 §D found most two-sided — so §8 clause 7 requires the **matched** contrast (controls matched on `log C_t` / `atr_pct` / `beta60` / `runup20`, all non-pinned) to exceed MPE in the same sign, or the verdict is INCONCLUSIVE. Window pick nights 2026-06-01..2027-04-30, decision Monday **2027-08-09**, one DP-13 extension to ..2027-06-14 decided Monday 2027-09-20, then DEFERRED. **Lock is conditional on R1:** at a planning rate of 0.35 contributing nights per session the initial decision date sits ~11 months after lock against DP-43's 12-month ceiling, so if the Steward measures below **0.32 contributing nights per session** the question goes to DEFERRED at `record` instead of locking (§5). **Resolved at `record` 2026-09-13:** the Steward measured **0.868–0.909 contributing nights per session** (`research/reports/STEWARD_Q017_exposure.md` item 3), far above both the 0.35 plan and the 0.32 DEFERRED floor, so §5's third branch fires — **no DEFERRED, no date moves, the window stands** (DP-43, DP-45), and the schedule is pinned in `research/questions/Q021_pin_risk_two_sided/schedule.json` (decision 2027-08-09, single DP-13 extension to 2027-09-20, hard stop 2027-09-20). The binding decision record is `research/questions/Q021_pin_risk_two_sided/DECISIONS.md` — Q017's file carried over unchanged but for the question id, with the Steward's exposure report keeping its `STEWARD_Q017_exposure.md` filename — and its amendments **are** folded into Q021's PREREG.md (the larger-of-two-q rule, the |SMD| > 0.25 disclosure, the bearish-PIN suppression at lock, the measured funnel and run-rate, the 98.1% snapshot-vs-table agreement, the EVALUABLE clause-7 feasibility with CTRA dropped-and-counted, and the DP-49 / DP-50 sentences). **Not merged into any other question** (DP-29). Original entry: GEX pin risk at selection (gex_context.pin_risk = true) goes with two-sided paths: pinned picks touch both +2 ATR and −2 ATR within 60 sessions more often — baseline: pin_risk = false picks; all candidates with pin_risk = false — in-sample n: 26 picks / 16 nights with pin risk (all candidates: 248 / 37 nights); direction: more two-sided among pinned picks, small difference across all candidates — why it might be real: dealer hedging near large gamma strikes pulls price back toward the strike from both sides — why it might be noise: GEX context missing for about half of picks; pin effects are short-horizon while the window is 60 sessions; tiny n. Source: EXPLORE_001 §C/§D.

## F4 — Price behaviour after selection (Explorer seeds)
- [x] H-030 — **registered as Q007** (`research/questions/Q007_gap_at_open/PREREG.md`; bookkeeping 2026-09-13). Original: Day-1 gap >2% at open vs flat open → subsequent realized return
- [x] H-031 — **registered as Q008** (`research/questions/Q008_fast_start_l4/PREREG.md`; bookkeeping 2026-09-13). Original: L1 within 2 days → L4 probability
- [x] H-033 — **DEFERRED (Q020, needs DP-05(c) grant)** — drafted as Q020 (`research/questions/Q020_l4_pullback_readvance/PREREG.md`; registrar, autonomous run 2026-09-13) and sent to `research/questions/DEFERRED.md` **before lock** by `DECISIONS.md` item 1 (DP-41): the EVENT classifier reads `prices_daily_split` / `prices_hourly_raw` bars from sessions t+1…t+40, on **published picks and the unpublished control pool alike**, to assign population membership, control-set membership and the decision basis `X_b = O_{b+1}` — a decision time of 09:30 ET on session `b+1` for that one input — which is a rule-14 exception the desk may not grant itself. **Not registered, not locked, and not counted in the F4 or F6 correction sets while deferred** (F4 is 9: Q007 4 + Q008 3 + Q013 2; F6 is 15). Haci lifts it by adding that scope to DP-05 as **(c)**, after which Q020 re-enters at the front of the queue with DECISIONS items 2, 3, 4 and 8 binding, Corrections 1–4 applied and the schedule recomputed from the new lock date. **The drafted design below stands as written.** Drafted as an **event-matched** contrast, because the conditioning event is the price path itself — L4 first touched in sessions 1..40 from the pick-night close `C_t` (DP-11, an already-held position), then L2 touched at or after that touch inside the same 40-session swing window (`_LADDER_LEVEL_WINDOW`, volatilx `services/sas_conviction_card.py:194`) — so **DP-12 governs**: the primary control is **condition-first, match-second** (same-night unpublished candidates carrying synthetic L2/L4 at the *same* ATR distances from their own close, kept only if they complete the same round trip, then the 10 nearest of those on `beta60`/`atr_pct`/`runup20`, minimum 3), and the unmatched total effect is descriptive and licenses no rule. Matching first and filtering second was rejected: it empties most control sets for want of ten look-alikes that happen to round-trip. **Decision basis `X_b` = the open of the session after the pullback completes** — and because the bars that locate `X_b` also choose the population and both arms, the draft's "no rule-14 exception is requested" reading was **not adopted**: it rested on a sibling draft (Q019, unlocked), while the locked question on this shape (Q009 §6) holds that no session-t+1 quantity enters any **eligibility filter**, and DP-05 records Haci declining the standing later-clock-for-classification licence on 2026-09-13. Q008's DP-05(b) grant bought the same kind of input for **2** sessions; Q020 needs it for **40**, on both arms — hence the DP-05(c) request and the deferral. Two primaries, both required in the same sign: **E1** the event-matched re-advance rate (L4 touched again within 20 sessions of `X_b`, MPE +5.0 pp, DP-20) and **E2** the event-matched realized ATR of plan RE-ADD (MPE 0.25 ATR, DP-10), with an **absolute gate** — a `playbook/` "hold / re-add" line needs E2's own expectation `mean_t mean_E[r_p]` to have its CI clear of zero, because "beats a look-alike" is not "pays" (Q008 §8 carve-out, mirrored). **Not merged into Q008, Q009/Q010 or Q015:** Q008 stops at the *first* L4 touch and its own §7 calls H-033 "the after-L4 path"; Q009 §7 calls it "the favourable-side mirror" of stop-then-target; Q015 tests L3-then-counter-level within a score band with **no third leg**. §7 states all three overlaps and forbids counting them as independent confirmations. Filed in **F4** with a required **F6 companion correction on E2** (q ≤ 0.10 in both families, larger q quoted; the Q015 pattern). **Lock on re-entry stays conditional on R1** (§5), which is **HELD** while the question is deferred — it measures the very classifier in dispute: no exposure has ever been measured for this event, so if the Steward then measures below the re-solved threshold (**0.31 matched-contributing nights per session** on the drafted dates) the initial decision date passes DP-43's 12-month ceiling and Q020 stays deferred on that ground too (H-062 precedent). Drafted schedule at a 0.35/session planning rate, **provisional and recomputed from the new lock date on re-entry** (DECISIONS item 8, moving out only): pick nights 2026-06-01..2027-04-30, decision **Monday 2027-08-02**, one automatic DP-13 extension to ..**2027-06-14** (Monday; the drafted 2027-06-12 was a Saturday — DECISIONS Correction 2) decided **Monday 2027-09-20**, then DEFERRED; binding maturity 60 sessions, uniform across every eligible pick so nothing is right-censored by its own timing. Original entry: L4 touched → retrace to L2 → re-advance (Haci's example 1)
- [x] H-034 (F4) — **registered as Q022** (`research/questions/Q022_sector_cluster_nights/PREREG.md`; registrar, autonomous run 2026-09-13). **Testable after all, and the reason EXPLORE_001 §8 said otherwise is worth recording:** that verdict ("not testable from this freeze (industry 12% populated)") is correct about the **`industry` column** — descriptive-only, written from the FMP profile when fundamental enrichment is on (volatilx `services/symbol_context_builder.py:525`), its two siblings `industry_rotation_active` / `industry_rotation_bonus` dead and always NULL, and **no `sector` column in the frozen table at all** (the migration is archived at `scripts/archive/migrate_sas_sector_columns.py:27-31`; the columns were renamed to `industry*` and the bonus deleted in platform commit `66cb63d` — volatilx `docs/SUPER_AGENT_SELECT_PROCESS.md` §8.1). It is wrong about the platform: a second, nearly complete symbol → SPDR-sector-ETF artifact is committed in the repo — `data/sp500_sectors.json`, S&P 500 merged with R2K (`scripts/bootstrap_sp500_sectors.py:11`, `:37-55`), read in production by `services/sector_breadth.py:78-80` — and it is dated **`_meta.last_synced = 2026-05-17`**, before every pick night in the window, so it is prior information and **no rule-14 exception is requested** (DP-41). It is pinned by sha256 at platform SHA `fa70688…` and never re-synced for the life of the question. PI-007 stands unchanged; this question routes around it rather than waiting on it. **One primary** (m = 1): **E1**, the night-level arm contrast of the *within-night control-adjusted* L3-touch rate — per pick, touch of the printed swing t1 within 20 sessions from the session t+1 open (DP-03(b), DP-09, DP-42) minus the same for its 10 nearest same-night unpublished look-alikes carrying a synthetic target at the identical ATR distance (Q006 §3 construction verbatim); averaged per night into `E_t`; E1 = mean over CLUSTER nights − mean over DIVERSIFIED nights. **That two-stage shape is the answer to "the arm is a property of the whole night"**: the arm cannot be assigned within a night, so the within-night control does the tape-removal and the between-night contrast is run on already tape-adjusted numbers — a night on which everything ran gives `E_t ≈ 0` exactly like a night on which nothing did. **MPE ±10.0 pp, not DP-20's +5.0** — E1 is a difference of differences and the desk doubles the MPE of the components (Q012 P2, Q018 P4; DP-20's larger-MPE clause, one-line reason in §8), and at ~80 nights a 5 pp bar would return INCONCLUSIVE by construction rather than by evidence. Registered **two-sided with no predicted direction** — H-034 names none and no sealed outcome exists (§6 contamination check: the weeklies and dailies carry no sector, HHI or concentration figure anywhere; "elites are mostly memory/semis" is a composition fact, not an outcome). Arm rule is deterministic and coverage-safe: CLUSTER iff `m_t ≥ 4`, DIVERSIFIED iff `m_t + u_t ≤ 3`, else UNDETERMINED and counted — unmapped symbols can cost a DIVERSIFIED night but can never manufacture a CLUSTER one. **§8 clause 7 is a hard gate (DP-45, Q021 pattern): the sector-matched contrast (controls drawn from same-night unpublished candidates in the *same* sector ETF) must share E1's sign and clear the same 10.0 pp, with an EVALUABLE test defined numerically — if it cannot be computed, the verdict is INCONCLUSIVE, never CONFIRMED on the sector-agnostic control alone**, because a concentrated slate is first of all a bet on one sector. Dispersion / co-movement ("eight picks are one bet") is a **descriptive** secondary — DP-44 forbids inventing an MPE for it and §9 forbids any rule resting on it. **Not merged into Q006** (whose picks-vs-control excess is exactly `E_t`; Q022 adds only the arm contrast, and §7 forbids reading a positive E1 as a second confirmation of a selection edge), **nor into Q007/Q008** (both condition on the price path itself under DP-12; Q022 conditions on a 16:05 property of the slate), **nor into Q013 or Q018**; §7 states all five overlaps. **Lock was conditional on R1, which was a blocker here rather than a formality** (§5): no arm split had ever been measured, so a **rarer arm below 0.085 nights per elapsed session** or eligible nights below **0.30 per session** would have sent Q022 to DEFERRED at `record` with the projected date named (H-062 precedent) instead of locking. **Resolved at `record` 2026-09-13** (`research/reports/STEWARD_Q022_exposure.md`, counts only on the pinned freeze, DP-50(c)): **CLUSTER 17 / DIVERSIFIED 15 / UNDETERMINED 0** on the 32 `k_t = 8` nights of the corrected in-window period, rarer arm **0.326/session** and eligible nights **0.696/session** — both gate lines clear by 2.3–4.2×, so **Q022 LOCKS**. Every date moved **out**, none in: the start rule returned **2026-07-08** (`1765a6f` shipped 2026-07-07 21:43:53 ET, four hours *after* that night's run finished; 2026-07-06 is an excluded manual re-run), `66cb63d` ships 2026-05-14 so §10.9 does not bind, and the measured eligible rate is **slower** than the struck 0.85 planning rate, so the window is re-sized to the 80-night floor at the **one-sided 90% lower bound** of the measured rate (0.6088/session) rather than at its point estimate — which would have made DP-13's single extension the plan rather than the fallback. Final: pick nights **2026-07-08..2027-01-13** (132 elapsed sessions), decision **Monday 2027-02-22**, one automatic DP-13 extension to ..**2027-02-26** decided **Monday 2027-04-05**, then DEFERRED — 5.4 and 6.7 months from lock, inside DP-43's ceiling; `research/questions/Q022_sector_cluster_nights/schedule.json`. The sector blob is pinned **by sha256 `c4d12610…0201`** (content of `4171b1a`, 2026-05-17, the file's only commit ever; byte-identical at `fa70688`, a locator only) and never re-synced; **BRK.B and BF.B are never hand-mapped** (they are the only two unmapped symbols in 431 candidates; the bias is one-directional and disclosed). Suppressed at lock, counts only: every **non-XLK `dominant_sector_t`** cell and the dose cells `m_t = {5}` / `{6+}`. **B3 is projected evaluable but expressly not pre-certified** — §8 clause 7 reads `eval.py`'s measured counts at the pass, and if B3 is not evaluable the verdict is INCONCLUSIVE, never CONFIRMED on B1 alone. **Both open §11 items are settled** (window start DEFAULTED to A under DP-43; the literal `k_t = 8` denominator DECIDED under DP-25, with `k_t ≥ 6` or a relative cut left as a successor hypothesis) and the binding record is `research/questions/Q022_sector_cluster_nights/DECISIONS.md`, whose twelve corrections **are** folded into the PREREG. One routed item stays open by design: **R2**, the successor freezes, due before the decision pass and not a blocker for lock (DP-23). Original entry: Sector cluster nights (≥4 of 8 same sector) vs diversified nights
- [x] H-063 (F4) — **merged into Q007** (`research/questions/Q007_gap_at_open/PREREG.md` §7; DECISIONS.md item 7; DP-29). Its ATR-scaled gap buckets and its L3-before-(−1 ATR) race are carried there as secondaries, descriptive; F4 still counts 7 hypotheses and H-063 is not a second registered question in the family correction. Original entry: Gap against → skip or delay the spread: when the next open is ≥ 0.25 ATR against the pick, L3 is touched less often from the open and the adverse −1 ATR move comes first more often than for flat opens (extends H-030 from return to the path objective and a skip decision) — baseline: flat-open picks (|gap| < 0.25 ATR) — in-sample n: 39 picks / 19 nights (flat: 152 / 34); direction: gap-against worse — why it might be real: overnight news or a failed breakout against the thesis — why it might be noise: small cell; the gap threshold is arbitrary; big favourable gaps (15 picks) also looked worse, so it may be a |gap| effect. Source: EXPLORE_001 §B/§E.
- [x] H-064 (F4) — **registered as Q013** (`research/questions/Q013_earnings_proximity/PREREG.md`; drafted by the desk in autonomous mode 2026-09-13, DP-46/DP-47; decisions applied from `DECISIONS.md` 2026-09-13 — no open items. The Steward's R1 returned: (a) **PASS**, the arm-label columns are live-path 16:05 ET writes, so no rule-14 exception is needed and DP-41's DEFERRED branch does not fire; (b) 23 contributing nights of 48 at 0.4510/session, both floors inside DP-43's 12-month ceiling, so Q013 locks. Window pick nights 2026-06-01..2027-02-12, decision date Monday **2027-03-22**, one automatic DP-13 extension to ..2027-03-30 decided Monday 2027-05-10, no hard stop, DEFERRED thereafter; `schedule.json` written). Original: Earnings within 0–3 sessions after the pick: deeper drawdown from the open and a lower L3 touch rate than picks whose report is 4–20 sessions out → skip rule, and no spreads held across the print — baseline: picks with the next report 4–20 sessions out — in-sample n: 30 picks / 18 nights; direction: worse path — why it might be real: binary event risk plus IV crush; in-sample the catalyst layer scored a bogus market-wide earnings date (catalyst_event_score flat for every pick; volatilx scripts/rescore_sas_earnings_corrected.py:1-24, fixed 69ef05f 2026-06-01), so such picks were never penalised — why it might be noise: report dates come from the later *_corrected backfill (knowledge-time caveat: scheduled dates were probably public, but verify); after the fix the sealed period may hold too few such picks for the 20-night cell floor. Related to H-041 but a different decision. Source: EXPLORE_001 §G.

## F5 — Cross-engine interaction
- [x] H-040 — **registered as Q014** (`research/questions/Q014_uoa_persistence/PREREG.md`; registrar, autonomous run 2026-09-13; decisions applied from its DECISIONS.md, nothing open, ready to lock `PREREG_LOCKED --by desk`; decision date Monday **2027-04-05**, one DP-13 extension to Monday 2027-05-17 — `schedule.json`). Registered on the **path** objective, not on return: two primaries — E1, the distance-matched excess L3-touch rate (20 sessions from the next open, DP-09) of picks with ≥ 3 strong same-direction UOA days in the prior 5 sessions minus the same for picks with 0, paired within night; and E2, published picks vs same-night **unpublished** symbols that also carry ≥ 3 such days, distance-matched at the identical ATR distance. Fixed-horizon return is printed and decides nothing (rule 5, DP-01). "UOA appearance" is defined as a bucket score ≥ 70 with a direction-matched label, reconstructed to its 16:05 value because the next-morning OI-confirmation run overwrites `score_swing` / `score_long` in place (volatilx `services/uoa_screener.py:2236-2239`) — no rule-14 exception is requested (DP-41). Original entry: UOA appearances in prior 5 sessions (0 / 1–2 / 3+) × SAS-selected → return (Haci's example 2) — baselines: 3+ UOA not selected; selected with 0 UOA
- [x] H-041 (F5) — **DEFERRED** 2026-09-13 (`research/questions/DEFERRED.md`, entry H-041; registrar, autonomous run). **Would have been Q022; no question number is consumed and no F5 slot is spent** — the family correction set stays **Q014 only (2 primaries)**, as Q014 §7 records. The backlog's own first step is **done**: "broaden to all picks with earnings in window" is **Q013** (`research/questions/Q013_earnings_proximity/PREREG.md`, locked 2026-09-13), and it is Q013's measured funnel that shows the residue is not tractable. Blocker is **the unit, then exposure** (DP-43's 12-month ceiling), not measurability of the level: the outcome H-041 names is the *post-earnings* path, which is **one realization per (symbol, scheduled print)**, so a symbol selected on four nights in the run-up window has one outcome, not four — counting selection rows would enter the same forward bars 2–4× and hardest in the **treatment** arm (repeat selection *is* the treatment), which is rule 6's aggregation rule one level up. On the honest event unit (one row per symbol-print, the last selection before the report; repeat = ≥ 2 selections inside the 10-day run-up vs single = exactly 1; same-night single events as the baseline), the measured earnings-proximate book is **86 of 371 eligible picks with the report ≤ 7 sessions out (23.2%, 1.79/window night, June 3 picks in 20 nights vs July 30 in 20 — `STEWARD_Q013_exposure.md` §1/§2/§4)** ≈ **50–56 distinct events over 48 nights**, giving **≈ 0.185 contributing nights/session** → decision date ≈ **2028-04-03**, about **6.7 months past** the 2027-09-13 ceiling. Q003's cells on pick rows give ≈ 0.20/session (≈ 2028-02-21); **only** the reading that keeps duplicate outcomes (repeat = ≥ 1 prior appearance, pick rows) reaches ≈ 0.32/session and an inside-ceiling date, and it was **rejected under DP-45** along with dropping the within-night pairing (≈ 0.30/session, but it gives up H-041's own baseline and confounds the contrast with the reporting season in a population defined by it). The bar is ≈ 0.28/session. **Not merged** into Q013 (arms are `k ≤ 3` vs `4–20` with no selection-history dimension anywhere; its §7 already calls H-041 "a different decision"), Q003 (all picks, L3 within 40 sessions, no report condition, no post-print stretch) or Q014 (DP-29). **Second, independent blocker on the endpoint, named in the entry and not cured by waiting:** the desk holds the scheduled report *date* only — no BMO/AMC flag (Q013 §10 threat 4) and no as-reported source after 2026-05-30 (threat 8) — so the print cannot be located to the session; the only entry safe under both conventions is the `k+2` open, which discards the first post-print session, the one the hypothesis is about. **Re-check triggers:** (1) accrual — the floor comes inside a 12-month ceiling once ≈ 36 contributing nights have accrued from 2026-06-01, ≈ **2027-03-01**; (2) any freeze where the Steward measures **≥ 0.28 event-unit contributing nights per session over a trailing quarter** (the counts-only measurement to request is spelled out in the entry, and **that count is what overturns the deferral** — the rates here are the registrar's arithmetic on Q013's funnel, offered to be falsified); (3) a BMO/AMC field plus an as-reported earnings-date table covering the window, which removes the second blocker. **No data was unsealed** — counts come from `STEWARD_Q013_exposure.md` and EXPLORE_001 §G's coverage row; the in-sample streak-bin excess figures play no part, and any future PREREG leaning on the streak-bin structure is post-hoc w.r.t. `manifest_v001` and must say so. Original entry: Repeat selection within 10 days before earnings → post-earnings return (Haci's example 3) — expected n very small; broaden to all picks with earnings in window first

## F6 — Exits and execution
- [x] H-050 (F6) — **registered as Q019** (`research/questions/Q019_conviction_monitor_exit/PREREG.md`; registrar, autonomous run 2026-09-13). Registered as a **damage-matched** contrast, because the trigger is itself part of the price path — the technical arm fires on structure breaks, level breaches and bias flips measured against the qualifying-day snapshot (volatilx `services/conviction_monitor_service.py:328-620`) — so **DP-12 governs**: the primary control is unflagged published picks **matched on the move already completed** (`dam = dir × (Close_s − C_t)/ATR`, ±0.25 ATR caliper, same evaluation session, graded over the flagged pick's own calendar stretch), and the unmatched total effect is descriptive and licenses no rule. Without that the question would only re-find "stocks that have fallen keep falling". Two primaries on the stretch the rule avoids — **P1** the avoided move in ATR per flagged trade (MPE 0.25, DP-10) and **P2** the adverse-first race at ±1 ATR from the exit basis (MPE +5.0 pp, DP-20) — and **both must confirm in the same sign** before any rule follows. Entry `C_t` (DP-11, already-held position; the estimands are entry-free by construction), **exit basis the open of session s+1** — the monitor writes at 16:02 ET, two minutes after the close, and the freeze declares it `lag_sessions: 1`, "feature only for t+1 decisions" — so **no rule-14 exception is requested** (DP-41). Trigger is the subscriber-facing `age_adjusted_severity = 'EXIT'` (`routers/conviction_monitor.py:157-204`), which the age decay confines to days 1–2; the undecayed `overall_tier` and WATCH-or-worse are registered secondaries that never decide (WATCH conflates "weakened" with "data missing" — `polarity_unavailable_coverage_low` → WATCH, `:184-185`). **Not merged into Q015 or Q012:** Q015 contrasts exit-at-the-L3-touch vs hold-through inside one band and Q012 a recycle plan vs the committed scale-out over a 60-session budget; neither reads a monitor row, and Q019 is the only F6 question whose exit is triggered by a signal computed *after* the pick night. §7 states the overlap and forbids counting them as independent confirmations. **Lock is conditional on R1** (§5): no exposure has ever been measured for this table (EXPLORE_001 printed its shape only), so if the Steward measures below **0.26 matched-contributing nights per session** the initial decision date passes DP-43's 12-month ceiling and the question goes to DEFERRED at `record`. R1 also dates the tier-logic changes ("Fix 1–6") from the platform git history — a column that changed meaning mid-window is two features (DP-06, DP-50(a)) and the window start moves after it. Drafted schedule at a 0.35/session planning rate: pick nights 2026-06-01..2027-05-03, decision **Monday 2027-05-24**, one automatic DP-13 extension to ..2027-06-15 decided **Monday 2027-07-12**, then DEFERRED. **Resolved at `record` 2026-09-13 (`research/reports/STEWARD_Q019_exposure.md`, counts only on the pinned freeze, DP-50(c)): Q019 LOCKS on the unmodified drafted schedule.** `A` = **42** matched-contributing nights over **61** elapsed sessions → `r` = **0.6885**/elapsed session, nearly 2× the 0.35 plan and far above the 0.26 DEFER floor, so the ceiling test passes and — because a faster rate never pulls a date in (DP-43, DP-45) — no date moves; the registered window projects ≈ 160 contributing nights against an 80-night floor, and that ≈ six-month over-wait is named on the board rather than paid by moving a locked date. The last tier-logic ship is **2026-05-16** (three same-day commits, the only ones the service has ever had), fifteen days before the window start, so the start stays **2026-06-01**. Decisions and corrections are folded into the PREREG (`DECISIONS.md`, 2026-09-13); `schedule.json` written. **Three measured claim-scope restrictions travel with the verdict** (PREREG §1/§8/§9): the EXIT tier as measured is the **technical arm alone** (284 of 284 flags; `polarity_tier` is `WATCH` on every in-window row and `polarity_unavailable_coverage_low` sits on 100% of them — filed as **PI-014**, a platform defect and not a Q019 finding), it is a **day-1-or-2** event (zero primary flags at days 3–5, by the age decay's design), and the matched estimate speaks for **ordinary-damage** flags only (195 of 284 dropped for want of a caliper-matched control, the dropped tail the more damaged one, mean `dam` −0.94 vs −0.17 ATR — DP-12's cost paid, the caliper not widened). Five sub-cells are **SUPPRESSED at lock**, counts only: bearish, 90+ and `< 80` on accrual, and the polarity-only / both firing arms and primary-trigger age 3–5 structurally. R2 (successor freezes, with the monthly polarity composition and the dated SHA of any restoration) stays open by design, due before the decision pass and not a blocker for lock (DP-23). Original entry: Conviction Monitor exit vs fixed +10d hold
- [x] H-051 (F6) — **registered as Q018** (`research/questions/Q018_lane_choice_by_band/PREREG.md`; registrar, autonomous run 2026-09-13). Registered as a **within-pick paired contrast of the three printed lane plans** — the same pick, the same entry (session t+1 open), the platform's own day / swing / long targets, each on its own 20 / 40 / 60-session grading window (`services/sas_conviction_card.py:194`), 50% at the lane's t1 and 50% at its t2 with the remainder at the window close, **no stop** (DP-02) — so "realized return" becomes the plan's realized **ATR per trade** (DP-01, DP-10, DP-44) and the fixed-horizon return prints but never decides. Four primaries: P1 swing−day, P2 long−swing, P3 long−day (MPE 0.25 ATR each), P4 the band gradient `[long−day]_HIGH − [long−day]_LOW` on both-band nights (MPE 0.50 ATR, difference of differences, Q012 P2 precedent). **A lane is named best only if both of its pairwise contrasts confirm** (§8). **Not merged into Q012**, which contrasts recycle-vs-committed-scale-out on one lane-free plan pair, nor into Q015, which contrasts exit timing inside one lane; §7 states both overlaps and forbids counting them as two confirmations. **Window is prospective-only — pick nights ≥ 2026-09-14** — because the sealed period has already been read for this hypothesis (the snapshot below, plus the standing "short DTE underperforms" finding): the sealed panel is printed post-hoc and enters no verdict, and PROSPECTIVELY_CONFIRMED is reachable by construction (DP-24 satisfied, DP-31 does not apply). Window ends **2027-03-31** (sized on P4's 0.6000 both-band nights per session, `STEWARD_Q012_exposure.md`, whose eligibility funnel is identical), decision **Monday 2027-07-12**, one automatic DP-13 extension to pick nights ..2027-05-12 decided **Monday 2027-08-16**, then DEFERRED — 9.9 and 11.1 months from lock, inside DP-43's ceiling. No rule-14 exception is requested (DP-41): lane plans are written before `finished_at` (`services/super_agent_select_service.py:155-161`). **Verdict speaks for 80–90 only** — 90+ is a descriptive sub-cell and `<80` is structurally near-absent (1 published row in 579). Original entry: Lane choice: which lane's plan (day/swing/long) maximises realized return per score band
  - Weekly snapshot 2026-09-12 (descriptive, NON_QUOTABLE, sealed data seen): in Jun–Aug the short lane was the weakest on T+20 next-open return, −2.24% [−5.52, +0.91] (50 nights), vs swing +1.09% [−1.15, +3.21] (41) and long +0.13% [−2.84, +2.88] (36); short-lane picks also had L1 already passed at the open most often (24% vs 14% swing, 13% long). The lanes use different ladder distances and different natural horizons, so a fixed T+20 comparison favours no lane fairly; register on each lane's own window. **This snapshot is why Q018's window starts after the lock** (Q018 §6, §10 threat 7): it ranked the lanes on sealed nights, so those nights are not a blind test — note also that it grouped *different picks* by `best_timeframe`, whereas Q018 grades *the same pick* under all three plans.
- [x] H-052 — **registered as Q003** (`research/questions/Q003_repeat_selection/PREREG.md`; bookkeeping 2026-09-13). Original: Repeat-selection streak length vs continuation (extends existing finding)
- [x] H-032 Stop-hit then target within window (whipsaw rate) by stop distance — **registered as Q009** (`research/questions/Q009_stop_whipsaw/PREREG.md`). **Re-filed from F4 to F6 on 2026-09-13** (DP-29: family follows the primary endpoint's subject, and both primaries are about using the printed stop as an *exit*; Q009 DECISIONS.md item 8) — F4 drops to 6 hypotheses and H-032 is not counted in both families. Its "by stop distance" half is untestable in the registered window (measured TIGHT 282 / MID 21 / WIDE 0, max stop distance 1.70 ATR — `research/reports/STEWARD_Q009_exposure.md` §R1(b) + v003 addendum), so it is registered descriptive-only; a stop-*width* question needs its own PREREG and a window that contains wide stops. The **prospective replication is Q010** (`research/questions/Q010_stop_whipsaw_prospective/PREREG.md`), locked at the same commit under DP-31 — it is the same hypothesis, not a new one, and is **not** counted as a second F6 question in the BH correction.
- [x] H-078 — **DEFERRED 2026-09-14** (registrar, autonomous run, DP-40..48; admitted under DP-47 "new data needed"), entry in `research/questions/DEFERRED.md`, **not registered and not drafted — no PREREG, no directory, no question number consumed (Q036 stays free)**. Two blockers, both named in the entry: **(1) the desk holds no record of any fill Haci has ever made** (`playbook/` is a README; `whale_watch_ledger` is empty; the Alpaca *trading* API is out of scope under rule 1) — it needs one immutable broker export, CSV, one row per fill, every fill in the window and not only the ones on picks, with `fill_timestamp` (ISO-8601 + offset, to the second), `symbol`, `asset_class`, `occ_symbol`, `side`, `position_effect`, `quantity`, `fill_price`, `commission`, `fees`, `order_id`, `fill_id`, dropped at `playbook/trades/` or named in `INBOX.md` and sha256-pinned by the Steward; **(2) the after-costs arms need a cost model in money units** (commission per leg, half-spread paid at the t+1 open, slippage on a scale-out leg) — **R-4 / DP-44: the desk does not invent a money-unit MPE**, so the "conservative default" this line once contemplated is *not* taken (DP-45 takes the stricter of the two rules). **Before costs, ROBOT vs DUMB is already locked and is not registered twice (DP-29):** Q024's clause-7 money gate (§4, ATR per trade, decides 2027-05-17) and Q006 `E1`/`E2` (2026-10-05). Re-entry is two measured triggers, not a date: **A** — Haci answers the three cost questions, they become a DP entry, and the after-costs question registers as an **F6 successor after Q006 decides (2026-10-05)**, prospective-only, inheriting Q024's clause-7 plan verbatim with a cost line; **B** — the fills CSV present *and* pinned *and* its contributing-night count measured against the rule-6 floors, which adds the FOUNDER arm (descriptive at lock if short). Original: (F6) — **new data needed: Haci's own fills** — Haci's H8 "Founder vs Robot vs Dumb": can the edge be harvested — PASS robot VolatilX beats NULL after costs and risk, and the founder comparison becomes diagnostic; FAIL the robot does not beat NULL → prediction quality is not becoming realisable returns. Three arms on the same pick nights: ROBOT = mechanical execution of the platform's committed plan (t+1 open entry, SELECT scale-out `[0,15,30,15,25,15]` on the flattened ladder, no stop — DP-02, DP-10), **after costs**; DUMB = the same plan on Q024's benchmark arms and on Q006's matched control; FOUNDER = Haci's realised trades on the same names. ROBOT vs DUMB before costs is Q024's clause-7 money gate and Q006's E2; what H-078 adds is the cost line (spread, slippage, commission per leg) — money units are R-4, so in autonomous mode the desk takes a conservative default (the stricter of two published estimates), records it as DEFAULTED, and Haci overturns it on the board. FOUNDER needs a trade export the desk does not hold (`playbook/` has only a README; the Alpaca trading API is blocked for the desk, rule 1): symbol, side, quantity, price and timestamp per fill, dropped as a CSV under `playbook/trades/` or named in INBOX. Until then the FOUNDER arm sits in DEFERRED.md with that file named, and the ROBOT-after-costs arm registers as an F6 successor when Q006 decides (2026-10-05). Why real: a target-touch edge can be eaten by entry slippage on a gapping open (Q004's L1-already-passed rate) and by scale-out commissions on a ~1 ATR move. Why noise: founder trades are discretionary and few; costs are an assumption until measured. Related: Q024, Q006, Q004, Q018, Q012. Source: Master Hypothesis Program, H8.

## F7 — Path, entry timing & volatility (Haci's trading style)
Source for all entries: research/reports/explore/EXPLORE_001.md (in-sample only; strong Apr–May tape; retro nights 2026-05-11..14 excluded). Path metrics per the rule-5 amendment. "Matched control" = the 10 nearest same-night unpublished candidates on beta60 / ATR% / 20-session run-up, synthetic target at the same ATR distance and direction.
- [x] H-053 (F7) — **merged into Q006** (`research/questions/Q006_control_cohort_path/PREREG.md` §4 secondaries, §3 B1, §10 threat 5; locked 2026-09-11 commit `c52fa16`; DP-29). What Q006 reports: every ladder level **L1–L6 on its own lane window** (20 / 40 / 60 sessions), from the next-session open, against the **same** distance-matched control carrying a synthetic target at the identical ATR distance and direction (§3 B1) — so the per-level profile H-053 predicts (near-zero excess at L1/L2, larger at L4–L6) is printed there, beside the level's ATR-distance tercile cut (§10 threat 5), the platform close-basis rate and the counter-direction touches (§4). What is carried as **descriptive**: all of it except **L3** — only E1 (L3 within 40 sessions) and E2 (L3 within 5) decide; L1, L2, L4, L5, L6 print raw p only and never decide (§4, §7). The decisive half of the L1/L2 claim is already registered inside F7 as **Q002 S1/S2** (L1/L2 within 2 sessions vs the identical control), with the full-lane-window rates as its §4 secondaries and B3 ("is this just distance?"). **Not counted twice:** H-053 contributes no primary endpoint to either BH correction — F7's registered questions stay Q002, Q004, Q011, and F1's stay Q006. **Residue, not drafted today:** no locked PREREG makes the deep levels or the "excess rises with the level's ATR distance" *shape* a primary endpoint; that is a real new question and needs its own PREREG — registrable on nights **after Q006's 2026-10-05 decision date** (a version built on this manifest would be post-hoc once Q006's per-level secondaries have been read), with L5/L6's 60-session maturity plus DP-21/DP-24 putting its decision date in ≈ Feb 2027, inside the DP-43 ceiling. The ladder-placement defect itself is **PI-009** (`HACI_DECIDED:research`); the product change is **EN-004** / **TI-013**. Original entry: Ladder touches are mostly distance: L1/L2 sit inside one normal day's range, so their touch rate should not beat a distance-matched control, and any SAS excess should sit in the deep levels (L4–L6) → ATR-scaled ladder; stop quoting L1/L2 hit rates without the control — baseline: matched control at the same ATR distance, same lane window — in-sample n: 280 picks / 35 nights (1,660 level rows); direction: small excess at L1–L3, larger at L4–L6 — why it might be real: ladder prices are LLM-written from projection levels with no ATR scaling (volatilx ai_agents/principal_agent.py:530-575; fallback 0.5%/1% steps at :897-911), and in ATR units high-ATR names get closer targets; near levels get hit by ordinary noise, deep levels need a trend — why it might be noise: one strong tape; high-beta picks in an up-tape reach far levels regardless; a 3-feature match may be incomplete; L5/L6 need 60-session windows (11 matured sealed nights today).
- [x] H-054 (F7) — **registered as Q004** (`research/questions/Q004_entry_basis/PREREG.md`, with H-057; bookkeeping 2026-09-13). Original: Actionable-basis hit rates: many L1 (and some L2) touches happen before a next-day buyer can enter, so L1/L2 hit rates on a next-open or 10:00 ET entry basis are materially below the close-basis rate the platform publishes — baseline: close-basis rate on the same picks (entry_ref = spot_close, volatilx services/sas_excursion.py:336-354) — in-sample n: 280 picks / 35 nights; direction: large share of L1 already passed at the open, more by 10:00 ET, more for elite — why it might be real: L1 is closer than the usual overnight gap plus first-hour range — why it might be noise: mechanical, but the size depends on the tape's gap behaviour; elite n is tiny.
- [x] H-055 (F7) — **DEFERRED** (`research/questions/DEFERRED.md`: needs an options price freeze; see ENHANCEMENTS.md EN-011) — bookkeeping 2026-09-13. Original: Leg out at the touch: for a vertical opened at the next open with the short strike at L3, closing at the first touch of L3 beats holding to the session-20 (≈ expiry) close; a sizeable share of picks that touch L3 close back below it by session 20 — baseline: hold-to-session-20 settlement of the same spread (stock-path proxy until option prices exist; then real option P&L) — in-sample n: 280 picks / 35 nights; direction: touch rate well above session-20 settle rate — why it might be real: targets sit at extension/resistance levels where supply appears; post-touch mean reversion (sas_excursion ran_then_dipped pattern) — why it might be noise: the stock proxy ignores IV, theta and strike width; in a strong tape many touches keep running; overlapping windows across nights.
- [x] H-056 (F7) — **equity-path half registered as Q002**; the DTE-recommendation half is in `research/questions/DEFERRED.md` (no options price history) — bookkeeping 2026-09-13. Original: Speed: SAS picks reach swing targets (L3/L4) in fewer sessions than matched controls at the same ATR distance, even where 40-session touch rates are similar → option DTE chosen from the target's ATR distance instead of a flat 21–35 DTE — baseline: days to first touch for the matched control — in-sample n: 280 picks / 35 nights; direction: picks faster at L3/L4, no difference at L2 — why it might be real: flow-led, breakout-style selection front-loads the move — why it might be noise: speed conditional on touching is survivor-biased (pre-register an unconditional version, e.g., touch within 5/10 sessions); strong tape.
- [x] H-057 (F7) — **registered as Q004** (`research/questions/Q004_entry_basis/PREREG.md`, with H-054; bookkeeping 2026-09-13). Original: After-hours entry at publication vs next-day open: an AH fill (keyed to sas_runs.finished_at, not a fixed 21:00 UTC) captures L1/L2 moves that happen overnight, and its adverse-first rate (−1 ATR before L3) is no worse than an open entry; the AH advantage is concentrated in elite picks — baseline: next-open entry on the same picks; for the elite leg, non-elite picks — in-sample n: 211 picks / 31 nights with an actionable AH price (elite 12 / 11 nights); direction: AH ≥ open on the L3 race; elite day-1 close above the AH price more often than non-elite — why it might be real: overnight drift after flow-driven selection; elite picks gap in their favour — why it might be noise: AH liquidity is thin and publication time varied (20:4x–23:1x UTC in-sample); the elite leg cannot reach the 20-night cell floor for months.
- [x] H-058 (F7) — **registered as Q011** (`research/questions/Q011_day1_dip_limit/PREREG.md`; locked by the desk in autonomous mode 2026-09-13, DP-46; decisions in its DECISIONS.md). Original: Don't buy the day-1 dip: resting limit orders 0.25–0.5 ATR against the pick-night close often fill on day 1, and the filled picks do worse on the L3-before-(−1 ATR) race than a market entry at the open — adverse selection — baseline: next-open market entry on the same picks — in-sample n: 280 picks / 35 nights (≈150 fills at 0.25 ATR, ≈100 at 0.5 ATR); direction: filled limits worse on the race — why it might be real: a day-1 dip is information against the thesis, and limits fill preferentially on losers — why it might be noise: the race ignores the better fill price on eventual winners; strong tape.
- [x] H-059 (F7) — **registered as Q016** (`research/questions/Q016_two_sided_beta_atr/PREREG.md`; registrar, autonomous run 2026-09-13). Two co-primaries, both two-sided, both on H-059's own metric and clock (DP-25): the within-night **beta60** tercile gradient and the within-night **atr_pct** tercile gradient in the rate at which a published pick touches **both** `C_t + 2 ATR` and `C_t − 2 ATR` inside 60 sessions. H-059's two named baselines both survive as registered: the low tercile of the same night's published picks (B1, the paired primary contrast) and beta-matched same-night unpublished candidates (B2 — distance-matched by construction, since the level is 2× each row's *own* ATR from its *own* close, and carrying a hard §8 gate). **Registered although the in-sample read went the other way** (EXPLORE_001 §D: no monotone beta gradient, 0.42/0.44/0.32, and the *low*-ATR tercile most two-sided at 0.54 vs 0.39) — that read is April–May, a one-way tape, it is quoted in this entry, and the window therefore starts 2026-06-01; a confirmed negative retires the belief from `playbook/` and is written up with the same weight as a positive. **The 60-session maturity is the binding constraint:** only ~11 in-window nights have matured on the current freeze, so the question is almost entirely prospective — decision date **Monday 2027-02-22**, single DP-13 extension to **2027-04-05**, DEFERRED after that (DP-43; both inside the 12-month ceiling). Not merged with Q015: same intuition, different level (ATR band vs printed L3/stop), clock (60 vs 20 sessions), conditioning variable (volatility vs score band) and no ordering requirement — §7 forbids counting them as two confirmations. Original entry: Haci's belief: high-beta / high-ATR picks are two-sided more often, touching both +2 ATR and −2 ATR within 60 sessions — baseline: low-beta tercile of picks, and beta-matched same-night unpublished candidates — in-sample n: 280 picks / 35 nights (all candidates 2,076 / 37 nights); direction in-sample: NOT supported in ATR units — high-beta picks realised wider range but it was one-directional; low-ATR picks were the most two-sided — why it might be real: high beta amplifies market swings in both directions, which a choppy tape would reveal — why it might be noise: April–May was a one-way tape, and the weaker Jun–Aug tape may flip the sign; register precisely because the tape changed.
- [x] H-065 (F7) — **registered as Q002** (`research/questions/Q002_speed_to_target/PREREG.md`; bookkeeping 2026-09-13). Original: Haci's thesis, 2026-09-10: the platform's differentiator is *speed to target*, not target distance. L1/L2 are reachable by many stocks, but SAS picks reach them (and L3) in fewer sessions than distance-matched same-night candidates, unconditionally (touch within 2 / 5 / 10 sessions, not days-to-touch among touchers) — baseline: matched control at the same ATR distance — in-sample n: 280 picks / 35 nights; direction (EXPLORE_001 §E/H-056): picks faster at L3/L4 — why it might be real: flow- and momentum-led selection catches names already in motion, so the first sessions carry the move — why it might be noise: survivor bias if measured only among touchers; a one-way tape front-loads everything; speed at L1/L2 was not separately measured in-sample.
- [x] H-066 (F7 → **F6**) — **registered as Q012** (`research/questions/Q012_capital_recycling/PREREG.md`; registrar, autonomous run 2026-09-13; decisions in its DECISIONS.md, measured exposure in `research/reports/STEWARD_Q012_exposure.md`). **Re-filed from F7 to F6 Exits and execution under DP-29** — both money primaries contrast two *exit plans* on the same picks, which is F6's subject (the Q009 re-filing is the precedent) — and it is **not counted in F7's family arithmetic**; Q012 §7 nevertheless computes an **F7 companion q** over its 3 primaries and requires q ≤ 0.10 in both families, which is a stricter reporting requirement, not a second family membership. Registered as three primaries on published picks scoring **80–90** over a **60-session capital budget identical for both plans**: P1 the money (recycle minus committed scale-out, ATR per capital slot, MPE 0.25), P2 the band gradient (HIGH's plan gap minus LOW's, on both-band nights, MPE **0.50** as a difference of differences), P3 picks minus distance-matched unpublished controls under the identical recycle plan (MPE 0.25). **H-066's elite (90+) half carries no verdict here** — demoted to descriptive at lock under DP-43 (20 contributing ELITE nights measured through 2026-09-09 on a 10/6/3/1 trend) and it needs its own PREREG once elite counts allow; `<80` is descriptive against a labelled 80-85 stand-in and is SUPPRESSED at 1 measured night. Window 2026-06-01..2026-12-10, decision **Monday 2027-03-22**, single DP-13 extension to **Monday 2027-05-03**, then DEFERRED. Original entry: Capital recycling by score band: a "quick-exit" plan (enter at the actionable price, exit at the first L1 or L2 touch, cap 5 sessions, redeploy) on sub-elite picks produces more target-touches per unit of capital-time than holding the same picks for L3/L4, while elite (90+) picks reward holding for L3–L6 — baseline: the same picks under the committed scale-out plan; the matched control under the quick-exit plan — in-sample n: 280 picks / 35 nights, elite 15 / 13 — why it might be real: near targets are hit fast (H-065) and the deep-target edge in-sample was concentrated in elite/deep levels (H-053), so band should decide hold length — why it might be noise: elite cell is tiny and sector-concentrated; turnover costs and the unfilled-entry rate are not in the stock-path proxy; per-trade return of a quick exit is small, so slippage matters.
- [x] H-060 (F7) — **registered as Q015** (`research/questions/Q015_band_85_90_updown/PREREG.md`; registrar, autonomous run 2026-09-13). **Both baselines H-060 names are unavailable and are demoted to descriptive at lock (DP-43), so Q015's primary comparator is the adjacent traded band 80–85:** `<80` is closed by configuration — `SuperAgentSelectConfig.publication_floor = 80.0` (2026-07-07, volatilx `services/super_agent_select_models.py:86-91`) drops sub-80 bulls to the dark set, which DP-28 excludes from every arm, and the band had already stopped appearing after May (weekly 2026-09-12 §2: 1 pick since June); `90+` is measured at 18 contributing nights in 50 matured Jun–Aug nights and is shrinking (DATA_NOTES: 12/8/3/2 elite picks per month since June), so as a *primary* comparator its 80-night floor lands between ~10 and ~24 months out on a cell carrying ~1 pick a night. **Residue, not drafted:** a properly powered 85–90 vs 90+ contrast is a separate future question, registrable when the Steward measures ≥ 0.5 elite-contributing nights per session over a trailing quarter (H-062's re-check-trigger pattern). Q015 registers three primaries — the unconditional touch-L3-then-revisit-the-swing-stop rate (band contrast), and exit-at-the-touch minus hold-through in ATR (band contrast, and within 85–90) — and stays in F7 with an F6 companion correction for the two exit-plan primaries (DP-29 ambiguity, §7); H-060 is counted once. Original entry: Up-then-down in the 85–90 band: 85–90 picks touch L3 and then revisit the swing counter level more often than <80 picks, so holding through hurts this band and exiting at the touch helps — baseline: <80 band and 90+ band — in-sample n: 44 picks / 28 nights (85–90) vs 100 / 24 (<80), 15 / 13 (90+); direction: more up-then-down in 85–90 — why it might be real: may be the path face of the known 88–90 underperformance; higher-scored non-elite picks are more extended at selection — why it might be noise: small cell, arbitrary band edges, the 90+ comparison n is tiny; mechanism: weak.

## F8 — System validity (Haci's Master Hypothesis Program, 2026-09-14)
Source for all entries: Haci's sixteen-item program from his own Claude + ChatGPT analysis, filed verbatim with its PASS / FAIL / "what we do" columns and the H0–H14 → H-069..H-084 mapping in `research/reports/INBOX_2026-09-14_master_hypothesis_program.md`. Items that belong to an existing family by their primary endpoint's subject (DP-29) are filed there — H-071 / H-072 / H-075 in F1, H-073 / H-079 / H-080 / H-082 in F2, H-078 in F6 — and this family holds the rest: the integrity and independence checks, the discovery step before selection, and the architecture questions. BH correction (rule 8) runs across the F8 members that carry primaries — H-074, H-081, H-083. **State at the close of the 2026-09-14 desk run: H-074 is DEFERRED and its primary leaves the set while deferred** (H-062 / Q025 precedent); **H-081 is registered as Q033 and contributes exactly one** — P1 (`A`, the direction excess) locks alone on 2026-09-14 and its interaction half `V` is **deferred at the lock** on the H-074 provisioning blocker (`research/questions/DEFERRED.md`, "Q033 / H-081"), so **F8 = 1 at Q033's lock and never below it**; **H-083 was drafted as Q035 and is DEFERRED at its own `record` (2026-09-14) without ever locking, so its two primaries never join the set and F8 stays at 1** (`research/questions/DEFERRED.md`, "Q035 / H-083"; the H-062 / Q025 / H-074 precedent — a question that never locked never enters the correction set). (Q035's own draft §7 counts Q033 as 2 and itself as 2, and therefore F8 as 4; both halves of that figure are superseded — `V` was deferred at Q033's lock and Q035 never locked — it was settled at Q035's own `record`, not here, and no locked file is edited either way.) H-069, H-070, H-076, H-077 and H-084 are diagnostics with no primary and no MPE (the Q005 pattern) and enter no correction set — H-084 is drafted as Q034 and enters none. All sixteen jump the queue under DP-47 (Haci's own items) in the order set in `research/lib/desk_queue.py` PRIORITY: H-069 first, because its FAIL branch stops everything else.
- [x] H-069 (F8, diagnostic — PASS / FAIL, no MPE, no BH) — **registered as Q026** (`research/questions/Q026_point_in_time_integrity/PREREG.md`; registrar, autonomous run 2026-09-14, DP-40..48). Drafted on the **Q005 diagnostic pattern**: no primary endpoint, no MPE (DP-10 / DP-20 / DP-44 cited as inapplicable — the threshold is **zero material rows**), no p-value and therefore **no BH** — it enters no correction set and F8's remains H-074 / H-081 / H-083. Runs on the **sealed `manifest_v001` + `manifest_prices_v001` now**: **DP-31 applies on its own terms** — the hypothesis *is* about the sealed period's integrity, no outcome column of any table is read (§2.4), so nothing can be unsealed by running it; PROSPECTIVELY_CONFIRMED is unreachable by design (post-lock projection **0 nights, exactly** — the population closed four days before the lock) and DP-31's successor-question clause resolves, **for a diagnostic only**, to the standing per-freeze re-run in §9 rather than a successor PREREG, because no branch of this question can ever produce a tradeable verdict. **DP-51 does not apply and is stated as not applying**: it attaches to a primary endpoint's CI, and there is no CI, no bootstrap and no sampling distribution to cluster. **Census, not sample** (113/113 nights, 107,185 selection rows across 11 tables, 584,231 price rows / 436 symbols) — rule 6's floors do not bind in their usual form and none is waived; this is the strictest possible reading of any floor, and no Steward exposure report or R1 gate is needed because nothing accrues. Two baselines: **B1** the declared `availability` block (the null — PASS means the declaration is true everywhere a locked question relies on it) and **B2** a **Gate 0 positive control** the census must independently re-find before any other line is read (the 4 KT-audit re-run nights, the 07-06 late `market_regime` write, the pre-06-09 backfill, the Jan–Mar `uoa`/`gex`/`projection_bull` late block, the `gex` 05-13..15 within-night append, the 2026-06-26 `stats_json` disagreement); Gate 0 failing ⇒ INCONCLUSIVE and nothing else is interpreted. Five channels: **A** write-time census per (table, night); **B** column-level materiality against the **21 questions frozen as a list at lock** (the 20 LOCKED/PINNED plus Q005 as a closed-question row; Q001/Q017/Q020/Q025 named out), classed MATERIAL / MATERIAL_MITIGATED / INDETERMINATE / IMMATERIAL with the resolution step reading the platform repo at the frozen SHA; **C** internal corroboration (`sas_runs.stats_json` vs the frozen row set — the only channel that can see a traceless mutation like 2026-06-26); **D** restatement anachronism in the price freeze (raw/split factor changes after a pick night, material only for **stored price levels**, ratio features being invariant) plus the `outcome_correction_ledger` appendix; **E** declaration coverage, producing a proposed **column-level availability block** (PI-013's `uoa_symbol` is the known counterexample). **Exclusions are an input to materiality, never a filter on the census, and are read per question from the file each locked PREREG itself cites** (DP-22) — which is the substantive half of the test: Q006 cites v001 and Q007 cites v002 while 2026-06-26 is excluded only in v003. Decision rule: **PASS ⇒ NULL** (M = 0 **and** U = 0), **M = 0 but U > 0 ⇒ INCONCLUSIVE** (DP-45, never PASS), **M ≥ 1 ⇒ FAIL / HISTORICALLY_CONFIRMED**, scoped per row — with a numeric **STOP-THE-DESK** escalation to Haci's "re-run from zero" only at ≥ 20% of a question's in-window nights on a core selection column or ≥ 5 of the 21 questions. §9 opens with DP-31's clause: **no guide line, lane rule, playbook entry, product change or quotable number follows from either verdict**, which is labelled `HISTORICAL_ONLY`; the consequences are internal (Red Team flag — never an edit, DP-22; a new exclusions version or a re-cut freeze; no verdict ledgered on a contaminated pin; a PI against the writer, DP-07) plus EN-001 / EN-015 / PI-013 filed to Haci under DP-48, with DP-49 binding any brief that follows. Decision date **Monday 2026-09-21** (nearest practical Monday; DP-13's extension and DEFERRED fallback are inapplicable — no floor can come up short on a census of mature frozen data; DP-23 inapplicable — no successor freeze needed). **All eight decisions are settled** (`DECISIONS.md`, decision-maker, 2026-09-14; applied to the PREREG by the registrar the same day, `schedule.json` written): the six drafted items plus two raised at decide. Notably **the provenance-query item went against the Registrar's recommendation** — a live read-only query is **forbidden even for provenance** (rule 4, DP-50(c)); attribution comes from the pinned parquet and the read-only platform repo at `fa70688` only, and a column set the code cannot close stays `CONTAMINATION_UNRESOLVED` ⇒ INCONCLUSIVE. Also fixed: §2.2 is the authoritative question list and `eval.py` fails loudly on a mismatch, with out-of-window nights of the four prospective questions carried as **`ADVISORY`** rows that enter neither M nor U nor either limb; MATERIAL_MITIGATED requires a row-by-row reconstruction at **relative tolerance 1e-6** or the whole tuple is MATERIAL; the register becomes a **standing per-freeze Steward step** with the 20-row hand sample **retained, not retired**; the **Researcher** writes `eval.py` (rule 9) and the **Steward** runs it; Channel D rows count in M (limb (b) always, limb (a) only on a core column); and the Gate 0 fix-and-re-run loop is bounded at **two passes inside a 2026-10-05 hard stop**. **Not merged into Q005** (a score decomposition that reads no timestamp and audits no availability claim). Original entry: Haci's H0 "Point-in-time integrity": did every input exist when SAS made the prediction — PASS zero material leakage; FAIL any material future-data contamination → stop, repair the pipeline, re-run from zero. What exists: FREEZE_v001 §7 confirmed the declared availability of every table on a 15–20-row sample and corrected two (`market_regime` before 06-09; `sas_excursion` recomputed weeks later); the KT audit (`research/reports/KT_AUDIT_manifest_v001_rerun_nights.md`) found four re-run nights whose 16:05 candidate set is unrecoverable (now `exclusions_v002`) and the 2026-06-26 post-write mutation (`exclusions_v003`); DP-06 splits the catalyst layer at 2026-06-01 (the rescore); PI-013 records `uoa_symbol_daily.score_swing / score_long` overwritten next morning; the `oi_confirm_*` columns are declared lag 1; earnings dates come from a later corrected backfill (H-064). H-069 is the **full-population** version of §7 — every row, every declared column, run by the Data Steward on the pinned freeze: (1) per table, `created_at` / `updated_at` / `computed_at` against 16:05 ET of `trading_date` plus the declared lag, counted per night; (2) per column a locked PREREG reads as a feature, whether any cell on any in-window night was written after that night's decision time — **that, and only that, is "material"**; (3) a leakage register (table, column, nights, rows, which QNNN reads it). PASS = the register's material section is empty; FAIL = one row in it → the affected question is flagged to the Red Team (never edited, DP-22), the Steward re-cuts its freeze with the cells nulled or the nights excluded, a PI is filed for the writer, and no verdict is ledgered on the contaminated pin. The platform-side controls are EN-001 (append-only run history), EN-015 (knowledge-time stamp) and PI-013's point-in-time copy. n: all 113 frozen nights, every table in `manifest_v001` and `manifest_prices_v001`. Why it might FAIL: three write paths are known to rewrite rows in place and the platform has no snapshot table. Why it might PASS: every known instance is already excluded, split or declared, so the first pass enumerates rather than discovers. Registers as a diagnostic PREREG (Q005 pattern) so the register is pinned and ledgered; **runs first**. Source: Master Hypothesis Program, H0.
- [x] H-070 — implemented as DP-51 (standing sensitivity; first live application Q006 red-team sign-off, 2026-10-05); no PREREG. Registrar, autonomous run 2026-09-14 (DP-40..48): not drafted and not merged into a question — the hypothesis is a **property of every question's CI**, not a population with an entry basis, a ladder target and a baseline, so it has no rule-5 endpoint of its own and no rule-6 n ("n: not applicable" as filed). It binds every future primary through DP-51's §8 two-CI clause and every locked primary through the Red Team's sign-off computation (DP-22 forbids editing the locked PREREGs). F8's correction set is unchanged at H-074 / H-081 / H-083. (F8, cross-cutting — a standing sensitivity, not a stand-alone question) — Haci's H0B "Independent signal episodes": is performance still strong when repeated same-symbol selections are clustered rather than counted independently — PASS the edge survives episode clustering / a clustered bootstrap; FAIL the advantage disappears materially → the historical sample overstated its independence. Rule 6's unit is the night, which absorbs same-night correlation; it does not absorb the same symbol re-selected on consecutive nights sharing the same forward bars — Q003 studies that repetition as a hypothesis, H-041's deferral reasoned on the event unit, and Q023 already block-bootstraps regime episodes. Implemented as **DP-51** (added 2026-09-14): every PREREG drafted from now reports each primary twice in §8 — the registered date-clustered CI and an **episode-clustered** CI, where an episode is one symbol's run of selections with gaps ≤ 10 sessions (the TI-003 / Q003 first-time-pick window) and the bootstrap resamples episodes and nights jointly; a primary CONFIRMED on the first and not on the second is INCONCLUSIVE, never CONFIRMED. For PREREGs locked before DP-51 (Q002–Q024) the clause cannot be added (DP-22), so the Red Team computes the episode-clustered CI at sign-off from `results/` and treats a failure as grounds to change the verdict, as it did on Q005. Retroactive re-run: nothing yet — the only verdict on record is Q005, a diagnostic. First live test: Q006, 2026-10-05. n: not applicable. Related: Q003, H-041, Q023 §2.5, rule 6. Source: Master Hypothesis Program, H0B.
- [x] H-074 — registered as Q030 (2026-09-14, `@registrar apply Q030`; history: DEFERRAL LIFTED 2026-09-14, earlier Q030 drafted; universe price freeze needs Alpaca credentials in the desk session). Both trigger limbs measured and cleared: credentials **present** (direct presence test) and coverage **97.55%** — 2,346 of 2,405 blob symbols with ≥ 60 daily bars, against the ≥ 90% gate (`research/reports/STEWARD_Q030_universe_coverage_probe.md`); blob re-verified at 2,405 entries and sha256 `c4d12610…0201` against the **committed** bytes of `4171b1a`. Next: R2(iii) pins `manifest_prices_universe_vNNN`, R2(iv)'s counts-only dry run fixes the suppression list, then `@registrar apply Q030` folds items 1–13 and Corrections 1–7 before lock; schedule re-derived from the actual lock date, out only. Original entry follows — **not registered**, entry in `research/questions/DEFERRED.md` (registrar, autonomous run 2026-09-14, DP-40..48), first admission ground. The R2 lock gate failed on the **credentials limb**: `ALPACA_API_KEY` / `ALPACA_SECRET_KEY` **ABSENT** from the desk's session (`research/reports/STEWARD_Q030_universe_freeze.md` §(i), confirmed independently by the coordinator), so `manifest_prices_universe_v001` — daily bars for all 2,405 base symbols — was not built and **coverage is unmeasured, not failed** (no probe ran). The **rate limb passed** at **0.6761 all-candidates contributing nights per elapsed session (48/71)** against the 0.36 gate; the pinned blob re-verified (2,405 entries, sha256 `c4d12610…0201`); `n_t ≥ 1` on **48/48** sealed nights (min/median/max 45/60.5/68), while **`b_t` and `m_t` are both UNAVAILABLE** — the only pinned price freeze covers **436** symbols (candidates + benchmarks), not `B`. **Re-check trigger is a measurement, not a date:** a desk session with both Alpaca variables present **and** a Steward trailing-60-session probe over all 2,405 symbols returning **≥ 90%** with ≥ 60 bars; then the R2 build proceeds, Q030 re-enters the queue at the front with `DECISIONS.md` binding, and the schedule is re-derived from the actual lock date (**out only**). **PREREG.md and DECISIONS.md are preserved as the drafted record** (`state.json` `PREREG_DRAFT` → `DEFERRED`; no `PREREG_LOCKED`, no `schedule.json`); the question number **Q030 is consumed and not reused**; **F8's BH correction set does not contain Q030 while deferred** (H-062 / Q025 precedent), so F8 carries no registered primary today. The drafted record follows, as written on 2026-09-14 and **not** a registration: drafted as Q030 (`research/questions/Q030_universe_discovery_recall/PREREG.md`). Drafted on the **path** objective (rule 5, DP-01): a mover is a base-universe symbol whose session high **first touches** `C_t + 3 × ATR14` at some session in **t+1..t+20** — a gap through at a session open is a touch, and the t+20 close-to-close version is printed and decides nothing. **One primary, two-sided: E1 = mean(`recall_t`) / mean(`e_t`)**, where `recall_t = |M_t ∩ U_t^B| / |M_t|` and `e_t = |U_t^B| / |B_t|` is the exact hypergeometric expectation of a same-size uniform draw from the night's gradeable base universe — the ratio-of-means form, because a per-night lift is violently right-skewed when `m_t` is small, with the night still the unit of inference (rule 6). **MPE lift ≥ 2.00** (Haci's own H4 number, "ideally ≥2× lift", read as the threshold not the aspiration — DP-45), mirror ≤ 0.50, **plus a materiality floor `D = mean(recall_t − e_t) ≥ +1.0 pp`**: **DP-20 is considered and explicitly not applied** (it governs touch rates on 40–70% bases; +5.0 pp on this endpoint's ≈ 2.4% null base rate silently demands a lift above 3, a different hypothesis), and since DP-44 has no ratio unit, **"lift MPE unit" is §11 item 2, a standing rule proposed to the Decision-maker** (the Q027 IC-MPE pattern). **Rule 5's distance match is the construction, not an add-on**: every symbol in `B` is asked to travel the same number of *its own* ATRs, on the same clock, whether or not it was ever a candidate. **Four baselines:** B1 the uniform random `n_t`-draw from `B_t` (10,000 randomization draws, seed 20260914 — Monte Carlo, adds no n, and centred on lift 1 by construction); **B2 sector-matched** and **B3 liquidity-matched** random universes, both **blocking in one direction only** — a question-level CONFIRMED is blocked if either matched lift sits below 1.00 with its CI excluding 1.00 (the universe may not *lose* to a matched random draw), and neither carries an MPE; B4 the one-day-stale universe, descriptive. **DP-51 applies and is a gate**: CI 1 date-clustered, CI 2 joint symbol-episode × night (episode = a symbol's run of appearances in `B_t` / `U_t` with gaps ≤ 10 sessions; for a continuously listed base symbol that is one episode spanning the window, so CI 2 is in effect a symbol-clustered bootstrap), and clearing MPE on CI 1 but not CI 2 is **INCONCLUSIVE, never CONFIRMED** — acute here because one 3-ATR run makes the same symbol a mover on up to **twenty consecutive nights**. **Population reuses Q027 by reference**: the same successor selection freeze, the same add-only successor exclusions file, the same window, maturity and schedule, so one freeze and one price fetch serve both (`manifest_prices_universe_v001` is a strict superset of Q027's R2 price freeze) — an efficiency, not a statistical link (§7: the two share **no endpoint**). `U_t` = the distinct symbols in `sas_candidates` for night t with **no score, qualification, publication or direction filter** (DP-28 is stated as inapplicable — Q030 has no published arm), corroborated per night against `sas_runs.stats_json.universe_count` at a 2% tolerance (`services/super_agent_select_service.py:445`, `:735-746`), which is the only defence against a built-but-unpersisted universe entry being scored as "never found". **Two corrections to this entry, recorded not silently applied:** the pinned blob carries **2,405** mapped symbols, not 2,252 (`STEWARD_Q024_…`, sector coverage; `eval.py` fails loudly on any other count or sha256); and the primary's random-draw denominator is **`n_t = |U_t ∩ B_t|`**, not `|U_t|`, because a candidate outside `B` can never appear in `M_t` — with the as-filed `|U_t|` version carried as a **blocking** companion (§8 clause 7) so the pair is strictly the stricter test (§11 item 1). **Window is prospective-only — pick nights 2026-09-15..2027-01-26 (92 elapsed sessions)** — on two independent grounds: the weeklies have read sealed pick outcomes against the numerator's ingredients (DP-45), and the universe's own composition changed inside the sealed period (**bear projection v1 rejoined 2026-07-02, `5b716a2`**, `services/candidate_universe_builder.py:120-148`), so `U_t` is not one feature across it; the sealed stretch 2026-06-01..2026-08-12 prints once as a labelled post-hoc panel entering no verdict, **split at 2026-07-02**, computed by the same byte-identical `eval.py` at the decision pass. PROSPECTIVELY_CONFIRMED is reachable by construction (DP-24 satisfied, DP-31 does not apply). Decision **Monday 2027-03-08**, one automatic DP-13 extension to pick nights ..2027-03-10 decided **Monday 2027-04-19** (hard stop), then DEFERRED — 5.8 and 7.2 months from lock, well inside DP-43's ceiling. **No second exposure count is routed** (§5.1): the candidate-side contributing-night rate comes from `research/reports/STEWARD_Q027_exposure.md`, whose all-candidates funnel is **strictly narrower** than Q030's night rule and is therefore a conservative lower bound; the planning rate is Q023's measured 0.9538/session, named as borrowed, with an 8-session cushion rather than an invented haircut. **R2 is BLOCKING FOR THE LOCK** and is the real risk: `manifest_prices_universe_v001` — daily split and raw bars for all 2,405 base symbols plus every in-window candidate symbol — has never been built, and the Q024 R1(d) precedent is that **the Steward's environment had no Alpaca credentials**; the lock-or-DEFER gate is credentials present **and** ≥ 90% of the 2,405 symbols returning ≥ 60 bars **and** an R1 night rate ≥ 0.36/session, any limb failing → DEFERRED with the blocker named. **No rule-14 exception is requested** (DP-41): universe membership and `source_membership_json` are 16:05 ET writes, the blob predates every in-window night by four months, and every bar used for a mover level is dated ≤ t. **The mid-window split rule is narrower than every other question's**: a change to the universe builder, `uoa_screener.max_symbols` (today 40, `services/uoa_screener.py:351`), the bulletin list sizes, the insider-watch parameters or which projection tables feed the universe splits the window (DP-06/DP-50(a)) — **a scoring-weight, threshold or publication-floor change does not**, because the universe is built upstream of scoring. **Not merged into Q006** (published picks vs unpublished controls *inside* the pool), **nor Q024** (whose sector-random arm draws from the same-night **candidate pool**, not from `B` — different populations, different questions), **nor Q027** (ranking inside `U_t`; the universe could hold every mover and rank them backwards, or hold few and rank those perfectly), **nor Q022** (same pinned blob, not the same test). **F8's BH correction set at this lock is Q030 (1) and never falls below 1**; H-081 and H-083 join when they lock. `m = 1` inside the question is **fixed at lock** and the §4.3 descriptive list **closes at lock** — the forking-path surface here (seven source feeds × two directions × three ATR thresholds × two denominators) is the largest of any registered question, and the whole source table prints whatever its shape. Seven open decisions are listed in §11 for `@decision-maker decide Q030`. Original entry: Haci's H4 "Discovery": can VolatilX find major opportunities before they move — PASS candidate-universe recall materially exceeds random / base-universe expectation, ideally ≥ 2× lift; FAIL the best future movers rarely enter the universe → candidate generation is the bottleneck, not ranking. This is the step **before** every selection question: Q006 and Q024 draw their controls from the night's candidate pool, so they cannot see what the pool missed. Universe U_t: every row in `sas_candidates` for night t (≈ 57 per night; 6,479 rows over 113 runs), built by `build_candidate_universe` (volatilx `services/candidate_universe_builder.py:71`) from the UOA day / swing / long bulletins, the whale-watch insider list and the bullish and bearish projection lists (`:90-135`; the screener's own `max_symbols = 40` cap, `services/uoa_screener.py:351`, is one reason the pool is small). Base universe B: the 2,252 symbols in `data/sp500_sectors.json` (S&P 500 ∪ R2K; sha256 `c4d12610…0201`, the Q022 / Q024 pin, dated 2026-05-17 — point-in-time only from that date). Movers M_t: symbols in B whose 20-session move from t's close exceeds +3 × their own ATR (bulls; bears separately at −3 ATR), computed from a **Steward price freeze over all of B** — daily bars from Alpaca market data, allowed under rule 1, pinned as `manifest_prices_universe_v001` before drafting (fetchable, so a Steward task, not a deferral). Primary: recall_t = |M_t ∩ U_t| / |M_t| minus the random expectation |U_t| / |B|, per night, MPE = lift ≥ 2 (Haci's number; recorded as a DP entry at draft) with a night-bootstrap CI; secondaries: the same against a sector-matched random universe of the same size (H-072's lesson), and recall by source (which of the five feeds finds the movers). Baseline: random draws of |U_t| symbols from B, and sector-matched draws. Window: nights ≥ 2026-06-01 (DP-06 / DP-43), which also satisfies the blob's 2026-05-17 date. Why real: the feeds are flow- and projection-led and should over-sample names about to move. Why noise: B is a fixed snapshot (delistings and post-May additions missing — a one-directional bias to disclose); a ±3 ATR mover set is a few percent of B, so per-night recall is a small count and needs the 80-night floor; recall of movers says nothing about *ranking* them (H-073). Related: EN-016 (larger universe retained), Q024's sector-random arm, Q022. Source: Master Hypothesis Program, H4.
- [x] H-076 (F8, diagnostic — reads no outcome column; no primary, no BH) — **registered as Q028** (`research/questions/Q028_layer_signal_independence/PREREG.md`; registrar, autonomous run 2026-09-14, DP-40..48). Drafted on the **Q005 / Q026 diagnostic pattern**: no primary endpoint, no MPE (DP-10 / DP-20 / DP-44 cited as inapplicable — the 0.80 / 3.0 / 0.50 / 0.60 / 0.70×K thresholds serve the MPE's role and are fixed at draft), no p-value and therefore **no BH** — it enters no correction set and F8's remains H-074 / H-081 / H-083. Runs on the **sealed `manifest_v001` now** (no price manifest at all): **DP-31 applies on its own terms** — no outcome column and no price bar is read (§2.4), so nothing can be unsealed by running it; PROSPECTIVELY_CONFIRMED is unreachable by design (post-lock projection **0 nights, exactly**) and DP-31's successor clause resolves, **for a diagnostic only**, to §9's standing re-run — triggered by a new freeze **or** a `sas_runs.config_json` hash change, because the object under test is the engine's architecture, not the tape. **DP-51 applies and is a gate, not a footnote**: every structural statistic carries both the night-clustered and the episode-clustered CI (episode = one symbol's run of *candidate appearances* with gaps ≤ 10 sessions), and a threshold crossing that holds on the first and not the second is INCONCLUSIVE — it bites here because the candidate universe re-selects the same names nightly. Population: **all 16:05 candidate rows, published or not** (DP-28 considered and applied only as a descriptive stratum, since there is no published arm), 102 of 113 nights after `exclusions_v003` (34 in-sample / 68 sealed), ≈ 57 rows per night. Three baselines: **B1** the architecture's own claim (identity matrix, N_eff = K, every R² = 0 — DISTINCT means B1 survives), **B2** a within-night independent permutation reference (2,000 draws, identical marginals, ties, clamping and missingness — the control for "is this just clamping and sparsity?"; Monte Carlo, adds no n), and **B3** a known-redundant positive control (`overall_score` reconstructed from that night's effective weights; `completeness_score` from the available-weight share) which is also Gate 0's target. Four statistics per cell, all with both CIs: **S1** top-two PC variance share, **S2** N_eff participation ratio (raw, /K, and excess over B2), **S3** each layer's R² on the others, **S4** the full pairwise Spearman matrix printed in full always (the anti-cherry-pick rule), plus S5 weight-carried redundancy. Cells: pooled, **P0 / P1 split at 2026-06-01** (DP-06 and rule 7 coincide with `in_sample_end`; a structure that holds in one period only is not a result), `best_timeframe` strata, and the monthly N_eff panel. **REDUNDANT** needs (a) S1 ≥ 0.80 or (b) N_eff ≤ 3.0 or (c) ≥ 3 layers at R² ≥ 0.50 — each on both CI bounds, in pooled *and* both periods — **or (d) K ≤ 4**, fewer than five layers carrying usable within-night variation, which is a live possibility given PI-008's forced-zero smart-money layer and `enable_fundamental_enrichment` defaulting `False`. **Two corrections to this entry, recorded not silently applied:** `component_scores_json` is a column of **`market_regime_daily`** (volatilx `models.py:1912`; four night-level regime components, `services/market_regime/scorer.py:506`), not of `sas_candidates` — it cannot enter a within-night cross-section, so Tier 2 reads **`score_details_json`** instead (`services/super_agent_select_service.py:306-336`), as §11 item 1; and the structure limb **cannot deliver H6's composite FAIL alone** (H-076's own text conjoins it with H-075's partial IC), so §8's verdict sentence carries "structure only, contribution untested" and **no architecture change follows from this question**, with the joint H6 answer assembled by the Reporter from ledgered verdicts (the H-077 pattern). Decision date **Monday 2026-09-28**, hard stop **2026-10-12** for the bounded two-pass Gate 0 loop; DP-13 and DP-23 inapplicable (nothing accrues, no successor freeze). **All nine decisions are settled** (`DECISIONS.md`, decision-maker, 2026-09-14; applied to the PREREG by the registrar the same day, `schedule.json` written): the five drafted items plus four raised at decide, **no routed request, no DEFAULTED item, none of ASK class**. The Registrar's option was taken on all five drafted items and **four were tightened**: Gate 0's items (b)–(e) are restated as **binary, row-level conditions** — (b) on **every included row with no domain carve-out** at R² ≥ 0.90 with unreconstructible rows kept in the denominator, (c)/(d) as **row-by-row identities against each night's `config_json` and the code at `fa70688` at relative tolerance 1e-6** (smart-money effective weight exactly 0 where the flag is off or absent; `gex_alignment` base weight exactly 0.0), (e) as a **script-correctness gate** on the mandatory 2026-06-01 catalyst split, with every share and distribution **printed, not gated** (the Q026 tolerance lesson: a gate without a number and a failure branch would be judged with the rows in view, which rule 9 forbids); the **pairwise-complete matrix becomes a binding companion** — a PSD pairwise matrix crossing a criterion the other way makes that criterion INCONCLUSIVE, a non-PSD one prints its smallest eigenvalue and is uncomputable for S1/S2; **each `K`-dependent criterion must hold at the floor that works against it** — (d) at 70% **and** 50%, DISTINCT's `K ≥ 5` at 70% **and** 90% — with the REDUNDANT route labelled (`correlation route` / `dead-layer route`) and every dropped layer named with its null share, distinct-value count, **cause at `fa70688`** and **summed effective weight**; and the verdict carries the fixed label **"structure limb only — contribution untested (H-075)"** beside `HISTORICAL_ONLY` and `NON_QUOTABLE` wherever it is restated. Also fixed: the **Researcher** writes `eval.py` once (rule 9) **and runs the 2026-09-28 pass**, the **Data Steward** owns the standing per-freeze / per-config-hash re-run only; a fixed RNG **seed `20260914`** with 2,000 draws for B2 and 2,000 resamples for each bootstrap, with seed, draw counts and library versions printed in the register header; the 113-night sensitivity **never promotes and never demotes** a verdict line; and the **DP-50 sweep at lock** records that no repair after the 2026-09-10 freeze reaches a row (including `2d5776c`, which touches a table this question never reads) while every code reference is read at `fa70688`, never HEAD. **Not merged into Q005** (a decomposition of the elite *count*, which reads no correlation) **or Q027** (H-073, whether the score orders *outcomes*). Original entry: Haci's H6 "Signal independence": are the agents ten signals or repeated versions of three or four factors — PASS low-to-moderate conditional redundancy plus incremental contribution; FAIL highly correlated layers and no incremental information → collapse the architecture into fewer factors. Structure half (this entry): on all 16:05 candidates, the correlation and PCA structure of the seven layer subscores (`flow_strength_score`, `technical_structure_score`, `gex_alignment_score`, `projection_score`, `fundamental_quality_score`, `catalyst_event_score`, `smart_money_confirmation_score`) and of the agent-level fields in `component_scores_json`, within night and pooled; the effective number of factors (participation ratio) per month; conditional redundancy = each layer's R² on the other six; stratified by `best_timeframe`, because the timeframe multipliers change the effective weights (`_effective_dimension_weights`, volatilx `services/super_agent_select_scoring.py:682`). Contribution half: H-075's per-layer IC **given the others** (partial IC) — decided there, not here. PASS / FAIL thresholds fixed at draft (e.g. FAIL if the first two components explain ≥ 80% of subscore variance and no layer's partial IC clears H-075's bar). Reads only 16:05 columns, so it can run on the sealed period without contaminating any outcome question (the Q005 argument). Output feeds H-083's setup definitions. n: 6,479 candidate rows / 113 nights. Why real: projection 29 / technical 27 / flow 24 dominate the weights and all three read the same price action. Why noise: subscores are clamped to 0–100 and several are sparse (GEX missing for about half of picks; PI-008's forced-zero layer), which inflates apparent redundancy. Related: H-075, Q005, H-083. Source: Master Hypothesis Program, H6.
- [x] H-077 — **NOT A QUESTION: synthesis to be written by the Reporter once the ledgered verdicts it needs exist** — first cut once **Q006** (2026-10-05), **Q002** (2026-10-12), **Q004** (2026-10-26) and **Q011** (2026-12-14) have ledgered verdicts; the full selection-vs-execution table additionally once **Q024** (2027-05-17), **Q019** (2027-05-24), **Q012** (2027-03-22), **Q018** (2027-07-12) and **Q015** (2027-08-30) do. **No PREREG, no eval.py, no family correction, no question number consumed** (Q036 stays free): it states no new hypothesis, defines no population, and adds only ATR-per-trade figures already decided elsewhere — a second test of them would be the same statistic counted twice (DP-29). Output `research/reports/SYNTHESIS_selection_vs_execution.md`, from ledgered verdicts only, **NON_QUOTABLE until a prospective verdict exists** (rule 12), and it may cite no question that has not yet decided. (registrar, autonomous run 2026-09-14) Original: (F8, synthesis — no new test; written by the Reporter from ledgered verdicts) — Haci's H7 "Selection vs execution": where does the economic value come from — quantify the incremental ATR per trade from selection and from execution separately → decides whether Product A (the picks), Product B (the execution guidance) or both deserve the focus. Selection value = the confirmed excess of published picks over their matched control under the platform's committed plan (Q006 E1 / E2; Q024's money gate); execution value = the confirmed gain of the best registered plan over the committed plan on the same picks (Q004 entry basis, Q011 dip limit, Q018 lane choice, Q012 recycling, Q015 exit at the touch, Q019 monitor exit) — each already in ATR per trade (DP-10) on the same picks, so the two add. Delivered as `research/reports/SYNTHESIS_selection_vs_execution.md`: a first cut after Q006 / Q002 / Q004 / Q011 decide (2026-10-05 .. 2026-12-14), the full table after Q012 / Q018 (2027-03-22 / 2027-07-12); per band and per regime, only from ledgered verdicts, NON_QUOTABLE until a prospective verdict exists (rule 12). No PREREG, no family correction. Related: every F1 and F6 question. Source: Master Hypothesis Program, H7.
- [x] H-081 (F8) — **registered as Q033** (`research/questions/Q033_direction_vs_magnitude/PREREG.md`; registrar, autonomous run 2026-09-14, DP-40..48). **One substitution, made under rule 5 / DP-01 and recorded rather than silently applied:** H-081 defines direction as *"the sign of the 20-session move"*, which is a fixed-horizon close-to-close quantity and therefore **cannot decide a verdict** — the registered direction endpoint is the **path** form of the same idea, which side of a symmetric **±0.5 ATR band around the t+1 open** is touched **first** within 20 sessions, and H-081's filed sign version is printed beside it as a named descriptive companion that decides nothing in any branch. The path form is also the stricter test of the hypothesis: being symmetric by construction it cannot smuggle target *placement* into a *direction* claim, which matters because the printed ladder is not ATR-scaled (`ai_agents/principal_agent.py:530-575`, PI-009 / H-053). **Registered with P1 alone (`m = 1` fixed at lock); P2 is DEFERRED at the lock** on the H-074 provisioning blocker — §2.2's tape arm cannot be assigned without SPY split-adjusted daily bars from 2025-01-02, which sit in no pinned manifest, and R1 limb (f) found `ALPACA_API_KEY` / `ALPACA_SECRET_KEY` both unset in the desk session (`STEWARD_Q033_exposure.md` §(f); entry in `research/questions/DEFERRED.md`, "Q033 / H-081", with a second independent ground — the rarer-arm episode limb at 0.0141 against 0.025 — and the H-074 re-entry trigger cited by id). **Q033 itself is not deferred and no question number is consumed**; `V` returns only as a successor question with its own id, inheriting every definition verbatim, never by editing Q033. **The two endpoints as designed, both two-sided:** **P1** `A` = the pooled per-night control-adjusted own-side-first rate (**MPE ±5.0 pp**, DP-20 and H-081's own number) — registered as a primary, not merely a precondition, because `directional_pct` is a claim the platform makes on its own surfaces and must carry its own verdict including a NULL one; **P2** `V = G_μ − G_α` = the tape-arm contrast of the **magnitude** excess (first touch of the pick's own printed L3 distance within 20 sessions — Q006 `Delta_t` on the DP-09 clock) minus the same contrast of the direction excess (**MPE ±10.0 pp**, DP-20's larger-MPE clause with the Q012 P2 / Q018 P4 / Q022 E1 / Q023 E1 / Q032 P1 precedent: at ~100 nights a 5 pp bar on a *triple* difference returns INCONCLUSIVE by construction rather than by evidence). Both series run on the **identical** contributing nights, picks and control sets and are **resampled together night by night**, so `V`'s CI carries their positive correlation instead of being needlessly wide. **The cells are a bar-only SPY tape partition** (BENIGN = `close > SMA50 > SMA200` ∧ 20-session realised vol not in the expanding top tercile; HOSTILE otherwise), specified in full rather than by reference and identical to Q032 §2.2 — no platform column assigns an arm, so a regime-engine repair cannot move a night between cells (DP-50(a)) — with H-081's "windows" fixed at lock as the **four** arm × calendar-half stability cells and the 80% HOSTILE composition guard carried verbatim. **Three level clauses keep a conditionality contrast from being reported as a level finding:** `NO_EXCESS_TO_COMPARE` (both pooled levels inside ±5.0 pp ⇒ P2 INCONCLUSIVE, routed to Q006 and P1), `MAGNITUDE_IS_THE_CONDITIONAL_ONE` vs `BOTH_CONDITIONAL` (labelled by `|G_α|`), and **`BOTH_MOVE_TOGETHER`** — H-081's own FAIL reading, which is a real finding here and **licenses nothing on its own**, because whether the common movement is decay in time or conditionality on the tape is Q031's and Q032's question. **`G_μ` is Q032's primary `G`, the same statistic on the same nights**, and was registered as a one-direction blocker that may never confirm anything here (§8 clause 8) — the Q032-C1 / Q023-E1 pattern. **With P2 deferred it is not computed at all**, and neither is the arm, the nine-cell composition, the four stability cells, the 80% guard, the tape-episode bootstrap or the label permutation; every read registered "per arm" prints **pooled and per half**, the magnitude series `μ_t` survives as a **descriptive** companion that decides nothing, and the ±0.5 ATR band, the ±10.0 pp MPE and the level clauses stand as designed in `DEFERRED.md` for a successor to inherit verbatim. **The SMA50-only proxy is substituted for the registered partition nowhere** — inside this question or any successor edit of it. Window **prospective-only, pick nights 2026-09-15..2027-04-30 (158 elapsed sessions)** — the sealed period has already been read for this hypothesis (the standing `directional_pct ≈ base rate` finding, EXPLORE_001's per-level and speed reads, and H-081's own "the desk expects the opposite" line), so it prints once as a labelled post-hoc panel split at 2026-07-06 and enters no verdict — decision **Monday 2027-07-12**, single DP-13 extension to pick nights ..2027-06-14 decided **Monday 2027-08-23** (hard stop), **9.9 and 11.3 months** from lock, inside DP-43's ceiling of 2027-09-14. **Both dates are five weeks later than the draft advertised, and the five weeks are arithmetic, not caution:** the decision date is computed on the **40-session binding maturity** §2.3 / §2.5 register — a contributing night needs t+40 bars — not on the primary's 20-session clock, and the draft's holiday list was missing 2027-06-18 (DECISIONS Corrections 1 and 2). The successor price freeze therefore carries daily bars through **2027-06-29** (extension path: 2027-08-11), and the DP-50(b) flag-off window runs to 2027-07-12 / 2027-08-23. **Exposure is measured, not borrowed, and the slower measured rate is the one used** (DP-45): 0.6620 contributing nights per elapsed session on the identical funnel (`STEWARD_Q031_exposure.md` §(a) — not Q032's borrowed 0.9538 from a narrower band funnel), control-pool depth min 37 with the ≥ 5-control floor costing zero nights. **R1 was BLOCKING FOR LOCK and is ANSWERED AND CLOSED** (`research/reports/STEWARD_Q033_exposure.md`, six limbs, counts only, no live query): limb (a) — **the only limb that gates P1** — **PASSES at 0.6620 contributing nights per elapsed session against a re-solved floor of 0.40, 65% headroom**, with `r₄₀ = 0.8710` and item 6's cap binding at `min(r₄₀, 0.6620)`; **limb (f) FAILS** (credentials absent, probe not attempted) and fires the registered branch — **P2 deferred, P1 locks alone**; the rarer-arm limbs (≥ 0.10 nights, ≥ 0.025 episodes) were sized for floors **B** and **D**, which are `V`'s, and **travel to `DEFERRED.md` with it** at their governing strict t+40-matured reading (0.0141 — short); limb (d) measured **7 of 550 picks excluded by the `d_L3 ∈ [0.25, 10]` bound and 3.89% (20 of 514 valid) carrying `d_L3 < 0.5`** — the band stays 0.5 ATR and the bound stays [0.25, 10], unmoved in either direction, because the conflation biases the deferred `V` toward zero; limb (e) returned **NONE** at HEAD `d19c9a9` with no v1.7 promotion scheduled through 2027-08-23. **The window is not pulled in to the binding floor's session 121** and the Steward's informational HOSTILE-richness note is not acted on — it is a bounded proxy for a partition this question no longer computes (DP-43, DP-45). **The §5.1 placeholders are struck, not replaced**, and rule 7's stratification falls back to the platform's point-in-time `market_regime_daily` label, descriptively, carrying no q and blocking nothing. Family **F8 as filed**, **F8 = 1** at this lock and never below it, with `A` additionally corrected across **F1 (28)** and the larger q quoted — which is F1's in every branch, so `m = 1` loosens nothing; **F2 carries no Q033 endpoint while `V` is deferred (17 → 16)**, and no locked PREREG records `V`, so no locked file is edited. **Not merged into Q006** (the *level* of the magnitude excess), **Q031** (whether that level decays in *time*), **Q032** (whether it is conditional on the tape — whose primary is one of `V`'s two terms) or **Q009/Q010** (the printed-stop race, an exit question; nothing here is an exit, DP-02): Q033 owns exactly two things no other question owns, the direction series and the contrast between the two series' tape sensitivity, and a confirmed `V` is explicitly **not** a second confirmation of any of them. **All decisions are settled** (`DECISIONS.md`, decision-maker `decide` + `record`, 2026-09-14: 33 items, Corrections 1–22; applied to the PREREG by the registrar the same day, `schedule.json` written). The four drafted items all went the Registrar's way — the **±0.5 ATR** band with ±1.0 as a non-deciding sensitivity, an unresolved race scored **0** with the pick kept in the denominator, the **equivalence NULL** reading (both CIs entirely inside ±MPE, which can only make a NULL harder), and **F8 as filed** — and five items were DEFAULTED under DP-43 / DP-45: the pre-lock SPY probe (**probe now, defer the endpoint today, rather than discover it at the 2027 decision pass**), the prospective-only window start, the window end and dates, **the branch itself (defer P2, lock P1)**, and the final schedule. `eval.py` is committed no later than **Monday 2027-04-05** — Q027 and Q029 decide 2027-04-12 on this window, this freeze, this entry and this clock. **No branch of this run may be described as H-081 passing or failing**: with `V` unmeasured, what P1 can settle on its own is whether published picks go their own way more often than their same-night look-alikes, with the matched-control rate beside it. Original entry: Haci's H11 "Direction vs expansion": does VolatilX predict direction better than magnitude — PASS direction stays stable while target / magnitude attainment varies systematically with the environment; FAIL both degrade together → selection itself is deteriorating. On published picks vs Q006's distance-matched control, from the t+1 open: **direction** = the sign of the 20-session move agrees with the pick's direction (excess over control, pp); **magnitude** = the first-touch rate of the printed L3 distance within 20 sessions (Q006 E1) and realised |move| in ATR against the printed target distance. Primary: the interaction — the direction excess's between-window variance against the magnitude excess's, across the rule-7 calendar / regime cells, two-sided, MPE ±5.0 pp on each excess (DP-20), with "stable" defined at lock as the excess keeping its sign in every window. Baseline: the matched control's own direction and touch rates. Known prior: the platform's own `directional_pct` grade sits at base rate (CLAUDE.md), and EXPLORE_001 found the in-sample excess in speed and deep levels (H-065, H-053) — so the desk expects the *opposite* of Haci's PASS and registers two-sided. Why real: a flow-led signal can be right about a move's existence and wrong about its size. Why noise: direction at 20 sessions is a coin with a tape-driven bias; the control removes the tape only to the extent of the three-feature match. Related: Q006, Q002, Q004, H-079. Source: Master Hypothesis Program, H11.
- [x] H-083 — DEFERRED (Q035 drafted; the projection layer — v1.6 weight 29, the largest — is unscored, `available: false`, on 72.2% of `selected` and 74.3% of `capped_by_max_output` main-lane rows, so the night's publishable choice set cannot be reconstructed) — **not registered**, entry in `research/questions/DEFERRED.md` (registrar, autonomous run 2026-09-14, DP-40..48), **second admission ground as amended** — the blocker is a platform data-completeness gap, not elapsed time. The lock-or-DEFER gate failed on **three of seven limbs, all testability limbs and none time-curable** (`research/reports/STEWARD_Q035_exposure.md`, R1, counts only, `manifest_v001` + `manifest_prices_v001`, 71 elapsed sessions): **(c) reorder room** median `|P_t|` **5** and `|P_t| ≥ 9` on **31.7%** of nights against floors of ≥ 11 and ≥ 80%; **(d) reorder rate 0.2683** against ≥ 0.50; **(f) reconstruction correctness undefined on its own registered population** — **0 of 41** qualifying nights, **0.00** on the only computable proxy, against ≥ 0.90. Independently, **(b)** the rarest setup (C, catalyst) measures **0.0845**/session on the governing full-window reading against ≥ 0.09, projecting floor B at **≈ 237 elapsed sessions** against DP-43's **225**-session ceiling — two grounds, same answer. **What was not the problem, recorded so a re-attempt does not look there:** (a) contributing nights **0.5775**/session against ≥ 0.36, (e) partition viability **0.615** margin share and **85.4%** of nights carrying ≥ 4 varying layers, (g) the DP-50(a)/(b) commit sweep since `fa70688` — **NONE**, and the mixed-row correction verified right against the code and numerically inert (0 of 442 mixed rows ever reach the reason set or the slate). **Five measured re-entry triggers** (median `|P_t| ≥ 11` and ≥ 9 on ≥ 80% of nights · reorder rate ≥ 0.50 · reconstruction ≥ 0.90 on a non-empty population · **projection non-null on ≥ 70% of both reason sets, the driver** · the unchanged 0.36 / 0.09 rate gates on the full-window reading), measured by the Steward on a successor freeze over a trailing quarter; **no calendar date is named, because accrual does not move any of them**. **Q035 is not re-registered on a weaker partition, a merged setup, a widened `P_t` or a reduced floor — a different construction is a different id.** **F8 is unchanged at 1** (Q033's P1 alone): Q035 never locked, so its two primaries never enter the correction set. **No verdict, no `eval.py`, no `fit.py`, no artefact, no `results/`, no successor freeze** (R2 and R3 withdrawn; the 2026-10-05 script deadline void); the question number **Q035 is consumed**. The driver is filed as **`research/PLATFORM_ISSUES.md` PI-015** (high, OPEN, recommendation only, repo-only check under DP-49, DP-50(b) ship timing named). **The draft is preserved as written; where it and `DECISIONS.md` "# RECORD — 2026-09-14" disagree, the RECORD governs.** Original registration note, kept for the record: **the setups are defined and frozen in this file, at draft, from the code at `fa70688`** — which is what the BACKLOG line asked for: dominant layer = the argmax of the seven layer subscores **z-standardised within that night's publishable set** (a weighted-contribution argmax would hand setup C, at weight 10, to projection/technical/flow at 29/27/24 by construction), with a zero-variance layer excluded from the argmax on that night (the PI-008 forced-zero and `enable_fundamental_enrichment=False` signatures), **A** = flow, **B** = projection *or* technical (the continuation pair; splitting them would create a fourth setup H-083 does not name, DP-25), **C** = catalyst, and a fourth residual bucket **O** (GEX / fundamental / smart-money) that is **never a setup cell** and whose rows are handed back to the universal rule. **Neither predecessor is waited for, and §1.1 says so in the file:** H-080 is DEFERRED today so **no threshold verdict is coming and its FAIL branch will not fire** — Q035 stands on the published v1.6 slate exactly as H-083's own baseline names it, and the 80 floor / 90 line / 8-slot cap are held as **fixed configuration identical in both arms**, with a change to any of them splitting the window (§2.7); H-076/Q028 decides 2026-09-28 and its two degeneracy checks (≥ 4 layers carrying within-night variation; is the dominant-layer argmax better than a coin flip) are **carried inside Q035 itself** as R1 limb (e) and Gate 0.1, measured on frozen data *before* the lock, so Q028 is read at the decision pass as **corroboration only and never as a ground to re-specify**. **One substitution, recorded rather than applied silently:** H-083's endpoint names the printed L3 (Q006 `E_t`), and **the printed L3 does not exist for the setup arm** — a ladder is written only for rows the platform publishes, so a promoted name has no target and an arm-dependent construct would confound a claim about *name* selection with one about *target placement* (PI-009 / H-053: the ladder is not ATR-scaled). The registered level is a **uniform 2.0 × the row's own ATR14 on both sides**, distance-matched by construction (rule 5), with 1.0 and 3.0 ATR as pre-declared descriptive sub-rows. **The population is the line the whole question turns on, and it is a deliberate, recorded deviation from DP-28's literal predicate** (§11 item 1, flagged for the Red Team): `services/super_agent_select_scoring.py:1509-1548` shows `qualified IS TRUE` and `selected_rank IS NOT NULL` are **the same event** — rows above the publication floor and above `min_completeness` that lose only to the 8-slot cap are marked `capped_by_max_output` (`:1533-1537`), and they, not the dark lane, are the reorder room; under DP-28 read verbatim both arms are the identical eight rows and the hypothesis is untestable. `P_t` = `qualification_reason IN ('selected','capped_by_max_output')`, bull lane only (the lanes have separate thresholds and caps, and mixing them would let a direction-mix change masquerade as a ranking result — H-062), with `selected_dark`, `below_threshold_*`, `below_completeness_*` and every bear row excluded and counted. **Two primaries, both two-sided, MPE ±5.0 pp (DP-20/DP-44), `m = 2` fixed at lock, resampled jointly on identical nights:** **P1** the **fitted** ranker H-083 asks for — `S_i = (1−w_s)·overall + w_s·L_s`, `w_O ≡ 0`, `w ∈ {0,.25,.5,.75,1}³` chosen once by exhaustive grid **after the lock commit** (rule 3) on **2026-06-01..2026-08-12** (post-DP-06 so setup C is one feature, matured on v001, ≈ 50 nights — *not* the desk's pre-06-01 in-sample split as the line words it, because DP-06 makes the catalyst layer a different feature there; §11 item 7), pinned by sha256 and never refit, with a **+5.0 pp in-sample shrinkage guard** and an `FIT_RETURNS_UNIVERSAL` early stop that closes P1 as NULL/`HISTORICAL_ONLY` at the fit and drops it out of the correction set; and **P2** the **pure** ranker `w ≡ 1` — **parameter-free, fitted on nothing, fixed in the file at draft** — which is the form of H13 that cannot be argued with and is what carries the question if the fit fails, returns the baseline, or is disputed. `Δ_t = R_S,t − R_U,t` is H-083's control-adjusted difference **with the common pool term written out and cancelled** (both excesses printed), the night is the unit, and **nights where the two slates come out identical contribute `Δ_t = 0`** (excluding them would let a ranker that moves three nights in ten be reported as though it moved ten; §11 item 6). **Four baselines, two of them blocking in one direction only:** B1 the published v1.6 slate (reconstructed from the platform's own `(-overall_score, symbol)` sort so both arms come from the identical screened pool, with the reconstruction's ≥ 0.90 agreement a Gate 0 limb), **B2 random-8 from `P_t`** — beating B1 but not B2 is relabelled **`UNIVERSAL_BELOW_RANDOM`**, is not evidence for setups, and is routed to Q027 — **B3 the setup-label-shuffled ranker**, the most likely benign explanation of a positive, whose failure is relabelled **`LAYER_BLEND_NOT_SETUP`** and licenses a different, smaller product change that is *not* registered here; and B4 the pool level, descriptive. **DP-51 is a gate** (episode = a symbol's run of appearances in **either** slate, gaps ≤ 10 sessions; clearing MPE on CI-1 and not CI-2 is INCONCLUSIVE), and the **larger** of the sign-flip p and the label-shuffle p is the p that enters BH. **Six-limb Gate 0**, including `PARTITION_COLLAPSED` (two of three setups short of 20 nights) and **`NO_ROOM_TO_REORDER`** (reorder rate < 0.25 ⇒ INCONCLUSIVE, never NULL — a NULL would be read as "setups do not help" when the measurement says the ranker barely ran; §11 item 5). **Window prospective-only, pick nights 2026-09-15..2027-03-31 (≈ 136 elapsed sessions)** — **no sealed-period panel for either primary**, because that stretch *is* P1's training set and P2's value on the same rows is a free look at the answer — decision **Monday 2027-05-10**, single DP-13 extension to pick nights ..2027-05-13 decided **Monday 2027-06-21** (hard stop), 7.9 and 9.2 months from lock, inside DP-43's ceiling. Exposure **measured, not borrowed, and the slower figure used** (DP-45): 0.6620 contributing nights/elapsed session (`STEWARD_Q031_exposure.md` §(a), reconfirmed `STEWARD_Q032_exposure.md` §(a)(ii)) projects ≈ 90 nights against floor A's 80. **R1 is BLOCKING FOR LOCK** and floor B (20 nights in the rarest setup) is the limb that can fail — the split of the reorder room three ways is **unmeasured** today, so seven counts-only limbs are routed with numeric gates (contributing-night rate ≥ 0.59; rarest-setup rate ≥ 0.15; median `|P_t|` ≥ 11; reorder rate ≥ 0.50; dominant-layer margin share ≥ 0.60 with ≥ 4 varying layers; reconstruction agreement ≥ 0.90; the DP-50 sweep), any limb short ⇒ DEFERRED with the measured number, never a re-specified partition or a reduced floor. **Rule 7 stratification uses `market_regime_daily` under the Q023/Q032 legality test, descriptively and never as an arm**, so a regime repair can move a stability row and never a verdict — chosen deliberately **instead of** Q032/Q033's bar-only SPY partition, which would have imported the Alpaca-credentials blocker that deferred Q030 and Q032 for a stratification that decides nothing; the successor price freeze is **daily-only** and is a strict **subset** of what Q027/Q031/Q033 already require, so this lock creates no new fetch. **F8 correction set at this lock = 4** (Q033's 2 + Q035's 2; Q030 DEFERRED and out, Q026/Q028/Q034 diagnostics). **Not merged into Q027** (whether `overall_score` orders outcomes at all — Q035 asks whether a *different* ranker builds a better eight; neither confirms the other), **H-075** (per-layer IC and *leave-one-layer-out* re-ranking; Q035 adds a blend and removes nothing), **Q028** (structure, no outcome), **Q006/Q002/Q015/Q018/Q012/Q009/Q010** (each holds the slate fixed and varies the rule; Q035 holds the rule fixed and varies the slate) or **Q005/Q024**. Seven open decisions in §11 for `@decision-maker decide Q035`. Original entry: (F8; behaviour — needs the setups defined and frozen before the test window) — Haci's H13 "A/B/C architecture": do setup-specific models outperform the universal 0–100 score — PASS frozen categorical setups materially improve OOS expectancy, calibration and stability; FAIL the universal score performs equally or better → keep the scoring architecture. Sequence: after H-076 (which factors exist) and H-080 (whether the threshold holds). Setups are assigned deterministically from 16:05 columns — e.g. A = flow-led (flow the dominant layer), B = projection-led continuation, C = catalyst / earnings-driven — using `determine_dominant_direction` / `determine_best_timeframe` (volatilx `services/super_agent_select_scoring.py:504`, `:606`) and `component_scores_json`; the within-setup ranking rule is fitted **once on the in-sample split, frozen in the PREREG (rule 9), and tested only on prospective nights**. Primary: the within-night control-adjusted L3-touch rate (Q006 `E_t`) of the setup-ranked top 8 minus the universal-score top 8 on the same nights, MPE ±5.0 pp (DP-20); secondaries: calibration (does "Pro / Elite" mean what it says within setup), ATR per trade under the committed plan (DP-10), and sign-stability across two windows. Baseline: the published v1.6 slate. The product change — setup labels on the pick card and per-setup scoring — is EN-018, gated on this verdict; H-080's FAIL branch also lands here. n: TBD, prospective (≈ 12 months at DP-43's ceiling). Why real: one score forces a breakout and an earnings play through the same weights. Why noise: any fitted setup model looks good in-sample, and three setups cut a thin nightly slate three ways (20-night cell floors bite). Related: H-076, H-080, H-073, EN-018. Source: Master Hypothesis Program, H13.
- [x] H-084 (F8, diagnostic — every objective pre-listed, none decides; no MPE, no BH) — **registered as Q034** (`research/questions/Q034_economic_objective/PREREG.md`; registrar, autonomous run 2026-09-14, DP-40..48). Drafted on the **Q005 / Q026 / Q028 diagnostic pattern**: no primary endpoint, no MPE (DP-10 / DP-20 / DP-44 cited as inapplicable — the 80-night, 20-night, 0.50-argmax, 0.05 `SET_SENSITIVE` and |SMD| ≤ 0.25 thresholds serve the MPE's role and are fixed at draft), **no q and therefore no BH** — it enters no correction set and F8's remains **Q033's 2** (H-074/Q030 DEFERRED, H-083 unregistered, Q026/Q028/Q034 diagnostics). **PROSPECTIVELY_CONFIRMED is declined in every branch** even though the window is prospective and DP-24 is satisfied: no primary, no MPE, no correction and nine simultaneous comparisons must not become a subscriber-facing basis (rule 10), so the verdict carries the fixed labels **`DIAGNOSTIC — no primary, no MPE, no BH`** and **`NON_QUOTABLE`** wherever it is restated, and the only thing a winner licenses is **a new PREREG on that objective** (§9). Label map: **SEPARATED** (one named strongest stable objective) ⇒ HISTORICALLY_CONFIRMED · **NO_SEPARATION** (stable but tied) ⇒ INCONCLUSIVE, tied leading set named in full · **NO_STABLE_OBJECTIVE** = H14's FAIL ⇒ NULL, which **does not retract a marketing claim on its own** — that sentence is about the *level* of the edge and belongs to Q006 / Q024's ledgered verdicts (the H-077 pattern). **The nine objectives close at draft** with exact definitions: O1/O2 5- and 20-session close-to-close return, **O3** direction as Q033's locked path form (±0.5 ATR own-side-first — H-081's filed fixed-horizon sign version prints as a companion and is not one of the nine), **O4** L3-before-printed-swing-stop on the DP-09 20-session clock (Q009's race, DP-30's stop, DP-02 — measured, never an exit), **O5** ATR per trade under Q012's **Plan H** (`BAND_EXITS`, single position, 60-session budget, no stop), **O6** adverse and **O7** favourable excursion in ATR at 20 sessions, **O8** the count of L1–L6 first-touched on **each level's own lane window** (the six per-level rates print as sub-rows that never enter the ranking, so one objective cannot occupy six of nine slots), **O9** L3 within 5 sessions (Q002's S3). All nine are pick-minus-**Q006 §3 B1 matched control** (rule 5's distance match, controls carrying every synthetic construct at identical ATR distances), entry **t+1 open** (DP-03(b)/DP-42, `C_t` printed as a sensitivity), **binding maturity 60 sessions uniform** so no objective is censored and all nine are measured on the same picks and nights. **Ranked on a unit-free within-night pooled-sd standardized excess `D`** with the native-unit `E` printed beside every row always; **separation requires the winner's CI to clear the runner-up's on both CIs (DP-51 is a gate) and an argmax share ≥ 0.50 in both joint bootstraps**, and the **9 × 9 objective-correlation matrix with `N_eff`** is printed and named beside any winner — a winner among nine measures that behave as three is a different statement. **Window is prospective-only, pick nights 2026-09-15..2027-04-30 (158 sessions)**, sharing Q027/Q031/Q032/Q033's window and R2 build with the **daily tail extended from t+40 to t+60**, and **no sealed-period panel is computed at all** — this register's rows *are* the endpoints of seven locked questions still in flight (Q006, Q002, Q004, Q011, Q012, Q018, Q009/Q010), so printing them early would be an interim look at their data (rule 3, rule 9); the Reporter cross-references their ledgered verdicts instead. Windows for "keeps its sign in every window" = the two calendar halves **and** the two bar-only SPY tape arms (Q032/Q033 §2.2 verbatim), each where it clears 20 contributing nights, plus — in **every** branch, added at decide (DECISIONS item 3) — the **two platform-label `market_regime_daily` arms** under Q032 §(d)'s legality test, so that the **`STABILITY_WITHOUT_TAPE_ARMS`** branch pre-registered for a failed SPY delivery (the Q030/H-074 credentials blocker, and on today's evidence the branch most likely to run) still runs **four** windows rather than two and cannot make STABLE easier to reach, and the partition is never re-specified after the fact, and `ARM_IS_ONE_STRETCH` qualifying a rarer arm that sits in < 3 tape-episodes. **Gate 0** (six binary construction checks: post-match |SMD| ≤ 0.25; nested-window and within-lane monotonicity identities; denominator identity; range identities; a printed 20-row sign-convention sample) ⇒ INCONCLUSIVE if any fails, bounded two passes. **Schedule settled and final** (`DECISIONS.md` `decide` + `record`, 2026-09-14; applied to the PREREG and `schedule.json` written the same day): window **pick nights 2026-09-15..2027-04-30 = 158 elapsed sessions**, **60-session maturity**, decision **Monday 2027-08-09**, single DP-13 extension to pick nights ..**2027-06-14** (session 188) decided **Monday 2027-09-20**, **which is the hard stop** — still short there, DEFERRED (10.9 and 12.2 months from lock; DP-43's ceiling binds the *initial* date, DECISIONS item 4). **`eval.py` committed by Monday 2027-04-05**, sha256 recorded, byte-identical at the extension run. **R1's lock gate is CLOSED — PASS on both limbs** (`STEWARD_Q034_exposure.md`): ranking-set rate **0.9091**, scheduling rate `min(0.9091, 0.6620) =` **0.6620** ≥ 0.44; rarer registered window **0.1972** (platform-label NOTSTRONG, the lower partition; bar-only HOSTILE 0.3099 cited) ≥ 0.11 — both measured *faster* than the placeholders, so **no date moved in** (DP-45), floor A (80 ranking-set nights, session 121) binds and the draft's "rarer window marginal at 20.9" was the placeholder's artefact (measured 31.2). R2 (successor freezes, daily bars through **2027-07-28**; **2027-09-09** on the extension path — Correction 1 adds Juneteenth 2027-06-18 to the calendar) stays routed and open, due before the decision date. **§2.4's ladder screen is corrected to the platform's own non-strict invariant** (`_ladder_is_monotonic`, `services/super_agent_select_service.py:180-192` / `:195-257` at `fa70688`; DECISIONS item 21) — the draft's "strictly monotone" was a mis-transcription that excluded 429/550 rows, 388 of them ties the platform blesses by design, and would have deferred the question; three guards ride with it (firing rate printed per window and per objective with `eval.py` failing loudly above the pre-fix 25.71%, the strict variant printed as a counted companion, tied levels graded on both for picks and controls alike). **Not merged into Q006 / Q002 / Q009 / Q010 / Q012 / Q033** — each owns one objective's *level* with its own MPE and correction, and §7 forbids any row here from confirming, strengthening or weakening any of them, or their verdicts from moving any line here. **All 21 decisions are settled** (`DECISIONS.md`, decision-maker, 2026-09-14 `decide` + `record`; applied to the PREREG by the registrar the same day, `schedule.json` written): the Registrar's option was taken on all five drafted items — the within-night pooled-sd scale with `E` printed always, **nothing computed on sealed nights at all**, stability over halves **and** arms, DP-43's ceiling on the initial date, and separation requiring both CIs **and** an argmax share ≥ 0.50 — with eight corrections applied (Juneteenth 2027-06-18 added to the calendar; the 80-night floor re-projected on the **ranking set** rather than the wider contributing-night rate; the fallback branch strengthened; F8's membership restated as **Q033's 1, or 2 only if Q033's SPY limb clears**, with Q030 and Q032 DEFERRED and out; §2.4 corrected to the platform's non-strict invariant; the placeholder sizing tables rebuilt on R1's measured rates). One item was **DEFAULTED on Haci's behalf** (the schedule, DP-43/DP-45) and its only cost is the wait; **item 20 (R2) stays routed and open**. Original entry: Haci's H14 "Economic objective": what does VolatilX actually predict best — determine the strongest stable objective among return, direction, target-before-stop, ATR per trade, MAE / MFE and first touch; FAIL no objective has persistent edge → Product A should not be marketed as a predictive system. For published picks vs Q006's matched control, from the t+1 open, the excess on **each** of: 5- and 20-session close-to-close return; 20-session direction (H-081); L3-before-printed-stop (Q009 / Q010's race); realised ATR per trade under the committed plan (DP-10); MAE and MFE in ATR within 20 sessions; first touch of L1–L6 on lane windows (rule 5's objective); and speed (touch within 5 sessions, Q002). Reported as one table with a night-bootstrap CI per objective and sign-consistency across the rule-7 windows; "strongest stable" = the largest standardised excess that keeps its sign in every window, named descriptively. Rule 5 fixes the desk's objective by how the pick is traded and H-084 does not move it; it reports whether that choice is also where the signal lives. Why real: a flow-led signal should show its edge in speed and path before it shows in a 20-session close. Why noise: nine objectives on one dataset is a garden of forking paths — the list closes at draft and every row prints, whatever its sign. Related: Q006 §4 (which already prints most of these for one question), Q002, Q009, H-081. Source: Master Hypothesis Program, H14.

## F9 — Multi-agent technical analysis reports (per-symbol, stocks and crypto; Haci's idea, 2026-09-14)

**A different engine from SAS.** These are the on-demand per-symbol analyses written to blob storage
(the `ai-reports` / jobs containers, `azure_storage.py:132-163`): seven timeframes (15m, 30m, 1h, 4h,
1d, 1wk, 1mo), each with a BUY / SELL / HOLD call, strength, confidence, stop and target, plus the
indicator detail behind it (RSI, MACD, OBV, Supertrend, Kalman, Bollinger, Fibonacci, Elliott wave)
and a consensus. The decision layer is `day_trading_agent.py:668-917`.

**Facts every F9 question inherits — read from the code and the sample, verify at the inventory:**
- **Entry is the spot price at analysis time**, never a trigger (`day_trading_agent.py:718`, `:737`).
  "Did it reach entry" is always yes; the testable things are the stop, the target, and the path.
- **SELL calls carry no target** in the sample (`take_profit: null`; `risk_reward: {}` on 30m and 1h).
  A bearish setup can only be graded against a synthetic level at a matched ATR distance.
- **The call is not a clean read of the technicals (PI-018).** A 71%-strength *bearish* 15m signal
  printed as **BUY**: strength ≥ 70 but confidence "medium" falls through both branches, and "price
  within 2% of a Fibonacci support" then turns the HOLD into BUY (`:749-791`). Support is checked before
  resistance, so on a tight Fibonacci grid the tie goes to BUY. Every F9 question must therefore
  test the **raw indicator bias** and the **final call** as separate inputs, never assume one is the other.
- **Timestamps are ET wall-clock without a zone** ("2026-09-11 10:01:49"); wave points are UTC. The
  zone is pinned before any bar is matched (rule 14).
- **Selection hazard.** If reports are requested by users, they cluster on names already moving, and
  "the analysis worked" becomes "momentum worked". The control must be built inside the analysed set.
- **Crypto trades 24/7** and has no session close: it is a registered split with its own horizons,
  never pooled with stocks. Outcome bars come from the Alpaca market-data host only.
- **Unit of inference:** the analysis day, with the same symbol re-analysed on nearby days treated as
  one episode (DP-51). Horizons per timeframe are a trading-style choice (R-2) and are fixed at lock.

- [ ] H-086 (F9) — **Do the setups work, per timeframe?** For each BUY (and, on synthetic levels, SELL)
  call: is the target touched before the stop, and how many bars or sessions after the analysis
  timestamp, at a registered horizon per timeframe (e.g. 15m/30m → same session, 1h → 2 sessions,
  4h → 5, 1d → 20, 1wk → 60, 1mo → descriptive only) — baseline: same-day HOLD reports on other symbols
  with synthetic stop and target at the identical ATR distances, and the same symbol's opposite-side
  level at the same distance — n: TBD (inventory first) — why real: stops and targets come from
  Fibonacci and ATR structure that can mark real support and resistance — why noise: entry is spot,
  short-timeframe targets sit inside ordinary intraday noise (the sample's 15m target is 0.38% away with
  risk/reward 0.82, the 1d risk/reward 0.38), and SELL targets do not exist. Source: INBOX 2026-09-14.
- [ ] H-087 (F9) — **Timeframe alignment.** Reports where ≥ 6 of 7 timeframes agree in direction vs mixed
  reports: first touch of +k ATR vs −k ATR at the day, swing and long horizons — baseline: mixed and HOLD
  reports on the same day, distance-matched — n: TBD — why real: multi-timeframe confluence is standard
  practice and is what Haci trades on — why noise: full alignment is rare by construction (the consensus
  needs ≥ 60% of timeframes) and concentrates in names already trending, so it may be momentum, not the
  analysis. Source: INBOX 2026-09-14.
- [ ] H-088 (F9) — **Divergence and the order of hits.** Short timeframes (15m–4h) bullish with the daily
  bearish (and the mirror): does price touch the short-horizon up level first and then the counter level
  before the daily horizon — i.e. "up first, then down"; are day and swing targets hit while the long
  target is not — reported as the order in which levels are first touched — baseline: aligned-bullish
  reports, distance-matched — n: TBD — why real: the bounce-inside-a-downtrend is a named, traded pattern
  — why noise: the exact pattern may be too rare to reach any floor, and order-of-hit statistics need
  many independent episodes. Source: INBOX 2026-09-14.
- [ ] H-089 (F9, **Explorer first — not a PREREG**) — **Which indicator states carry information about the
  path beyond the call?** OBV trend, volume vs its average, Supertrend direction and distance, Kalman
  slope and invalidation, MACD cross, RSI oversold/overbought, Bollinger position, Fibonacci trend and
  distance to level, Elliott pattern and confidence — each per timeframe — against the forward path. Seven
  timeframes × ~10 indicators is a forking-paths surface, so the Explorer mines the in-sample split only
  and the survivors are pre-registered as separate questions with BH correction. Why real: the raw
  indicators may hold signal that the decision layer throws away (PI-018). Why noise: most single-
  indicator correlations on daily moves are near zero, and anything found in-sample must survive a
  prospective window. Source: INBOX 2026-09-14.
- [ ] H-090 (F9) — **Calibration of strength and confidence.** Does per-timeframe strength (and consensus
  strength / confidence) rank target-touch rates monotonically — baseline: adjacent strength bands,
  demeaned within day — n: TBD — why real: strength is shown to subscribers as a quality number — why
  noise: strength is a vote share with a few discrete values, and the confidence gate interacts with the
  Fibonacci override (PI-018). Source: INBOX 2026-09-14.
