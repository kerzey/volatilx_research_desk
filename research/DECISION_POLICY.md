# Decision policy — standing answers for pre-registration decisions

Read by the Registrar before drafting and by the Decision-maker when it resolves a draft's
"Open decisions before lock". Purpose: a question that comes up in every PREREG is decided
**once**, the same way, and never put to Haci twice. Each entry has an id (`DP-nn`), the rule,
and where it came from. Nothing here overrides CLAUDE.md.

Two kinds of entry:
- **Confirmed** — Haci said so. Applied without asking.
- **Default** — a desk convention chosen for consistency. Applied without asking; Haci can
  overturn it at any time. When he does, edit the entry in place and note the date — don't add
  a second entry that contradicts it.

A third list, **Reserved for Haci**, names the decisions that are always asked.

How entries get added: when Haci answers a question the Decision-maker put to him, the
Decision-maker appends it here as Confirmed (naming the question it came from) if the answer
generalises beyond that one question. One-off answers stay in that question's `DECISIONS.md`.

## Confirmed (Haci)

| id | rule | source |
|---|---|---|
| DP-01 | The objective is the pick's price path: first touch of L1–L6 within the lane windows (day 20 / swing 40 / long 60 sessions), measured from a stated entry, against a distance-matched control. Fixed-horizon return is descriptive only. | CLAUDE.md rule 5 amendment, Haci 2026-09-10 |
| DP-02 | Stops are never assumed as exits. Adverse excursion and counter-direction touches are reported. A question *about* stops studies the platform's printed stop; it does not make the desk exit on it. | rule 5; Haci 2026-09-10: "I do not trade by considering stops" |
| DP-03 | Entry bases in scope: (a) pick-night after-hours, from the run's actual `finished_at` — Haci's partial after-hours buy on elite (90+) picks; (b) next-session open — his option-spread entry, legs closed as targets are touched. A target already passed at entry is not a hit. | Haci 2026-09-10; DATA_NOTES |
| DP-04 | A night is excluded when the run's `finished_at` is later than the next session's open (a manual re-run), or the run is dated on a non-session day. | Haci 2026-09-10; `exclusions_v001.json` |
| DP-05 | Rule-14 exceptions granted so far, each **scoped to the question that asked for it** and to nothing else: **(a) Q007** — the session t+1 official 09:30 ET open may be used as a conditioning variable for the **gap only**, i.e. gap-group assignment (Q007 §6.1); it does not extend to any other t+1 quantity. **(b) Q008** — bars through **16:00 ET on session t+2** may be used to assign the **fast-start F/S label only** (E4 set aside), for picks and matched controls alike; every other input stays at 16:05 ET on the pick night and every outcome is measured strictly after that classification moment. Neither grant is a general "later clock for classification" licence — Haci declined that standing rule on 2026-09-13. Every *new* rule-14 exception is asked (R-1). | Haci 2026-09-12 (a); **(b) added Haci 2026-09-13, Q008** — entry edited in place 2026-09-13 rather than duplicated, because the Q008 grant is question-scoped and creates no new standing rule |
| DP-06 | The catalyst layer is a different feature before and after 2026-06-01 (earnings fix `69ef05f`). Earnings questions are sealed-period only, or split at that date. | Haci 2026-09-10; DATA_NOTES |
| DP-07 | Platform defects are not research questions. They go to `PLATFORM_ISSUES.md`; Haci marks fix / research / accept. | Haci 2026-09-10 |
| DP-08 | Speed is part of the claim: the desk measures *when* a target is first touched (sessions from entry), not only whether. L1/L2 are reachable by many stocks; the edge to test is speed. | Haci 2026-09-10; H-065 / H-066 |
| DP-09 | When L3 is the primary level, the primary clock is **L3 within 20 sessions measured from the stated entry** (the spread horizon, ≈ expiry), not the platform's 40-session swing-lane window; the 40-session result is reported alongside, descriptively, wherever it has matured. Sample floors and decision dates are computed on the 20-session window. | Haci 2026-09-12, Q007; wording generalised in place 2026-09-13, Q007 — was "when the entry basis is the next-session open spread", which no longer fits after Haci moved Q007's entry to the pick-night close: the clock rule is about the level and the horizon, not the entry basis |
| DP-10 | MPE for any **per-trade endpoint denominated in ATR**: **0.25 ATR per trade**. Set once, applies to every such endpoint; smaller values are not accepted and a larger one needs a one-line reason from the Registrar. | Haci 2026-09-12, Q007 |
| DP-11 | When a question measures what happens to a position that is **already held** when the signal appears, the entry is the **pick-night regular-session close `C_t`**, used as the proxy for the DP-03(a) after-hours fill, for picks and distance-matched controls alike (only the close exists for controls, and rule 5 requires one basis on both sides). The real after-hours price (`E_AH`, Q004's definition verbatim) is a **picks-only descriptive sensitivity** that never decides. Questions whose subject *is* the entry basis (Q004) are unaffected. | Haci 2026-09-12, Q007: "I want to measure the movement of the stock from closed price of previous day not opening prices"; recorded 2026-09-13 |
| DP-12 | When the conditioning variable is itself **part of the price path** (an overnight gap, a fast start, any move already completed when the signal is read), the **primary** contrast uses a control matched on that variable; the **total effect is descriptive** and licenses no rule. The cost travels with the rule: matched pools are smaller, picks below the control floor are dropped and counted, and the sample floor arrives later — that later date is an R-3 for Haci, never a reason to loosen the floor or to fall back to the total effect. | Haci 2026-09-13, Q007: "Strip the gap out" |
| DP-13 | When a question's sample floors are **projected to be marginal** at its decision date, it carries **one pre-agreed automatic extension and a DEFERRED fallback, decided at lock**: if any gate (the ≥ 80-contributing-night floor, or DP-24's post-lock-nights clause, **per primary endpoint**) is short at the decision date, the window extends **once**, automatically and with no new question to Haci, to the extension date named in the PREREG; if a gate is still short after that single extension, the question goes to **DEFERRED** rather than running under-powered. The extension fires on the **measured counts printed by `eval.py`**, never on a projection or run-rate, and no gate is ever reduced. This is the standing answer to this class of R-3 from now on — it does **not** remove R-3 for the **initial** window and decision date, which is still asked; it removes only the second, "it came up short, now what" question. | Haci 2026-09-13, Q008 |

## Defaults (desk convention; Haci can overturn)

| id | rule | why | first applied |
|---|---|---|---|
| DP-20 | MPE for a control-adjusted or within-night **touch-rate** endpoint: **+5.0 pp**. The Registrar may propose a larger MPE for an endpoint with no control (a raw arm contrast) with a one-line reason; a smaller one is not accepted. | consistency across F7; in force in Q006, locked by Haci | Q006, 2026-09-11 |
| DP-21 | Rule 6 floors read as **80 contributing nights per primary endpoint** and 20 per stratum cell. "80 eligible nights with ≥ 20 contributing" is the weaker reading and is not used. | the stricter of the two readings already in drafts (Q008 vs Q007) | 2026-09-12 |
| DP-22 | Exclusions: a draft cites the **newest** exclusions file. When the KT audit or the Steward finds new re-run nights, the Steward issues the next `exclusions_vNNN.json` *before* the batch is locked, and drafts cite it. Locked PREREGs are not edited; the file they cite stands. | one list, not per-question lists | 2026-09-12 |
| DP-23 | Successor freezes: a question whose floors need nights after `manifest_v001` is locked **now**, pinned to v001, with its successor manifests pinned at the decision date (the Q006 pattern). Successor price freezes always include **all candidate symbols, published and unpublished**, plus hourly bars for published symbols. | Q006 precedent; every path question needs the control cohort | 2026-09-12 |
| DP-24 | PROSPECTIVELY_CONFIRMED needs **≥ 30 contributing nights dated after the lock commit**. If the chosen window cannot supply 30, the decision date moves (that move is R-3); the clause does not weaken. | Q002 / Q004 precedent | 2026-09-12 |
| DP-25 | Units and cuts come from the BACKLOG hypothesis as written (H-030's "2%" is percent, not ATR). The Registrar does not re-unit a hypothesis; a re-unit is a new hypothesis. | the test stays the one that was proposed | 2026-09-12 |
| DP-26 | Registrar-chosen bands and clocks (a 1% "flat" band, TIGHT/MID/WIDE ATR buckets, a 2-session fast-start clock) stand as drafted, provided they were not derived from sealed outcomes. A gap-through at entry is never a hit. | conventions, not findings; rule 5 | 2026-09-12 |
| DP-27 | Ties: a same-session stop and target that hourly bars cannot order count **stop-first** — the outcome less favourable to the hypothesis. | conservative | 2026-09-12 |
| DP-28 | Publication predicate: `qualified IS TRUE AND selected_rank IS NOT NULL`; dark-lane rows are excluded from every arm. A locked PREREG with a looser predicate is flagged to the Red Team for that question's review, never edited. | Q008 draft finding | 2026-09-12 |
| DP-29 | Family is assigned by the primary endpoint's subject (exits → F6, path after selection → F4, calibration → F2). An overlapping BACKLOG hypothesis is marked *merged into QNNN* rather than counted twice. | rule 8 bookkeeping | 2026-09-12 |
| DP-30 | Stop line for a stop question: the printed stop of the lane whose target is the primary endpoint. | technical consistency | 2026-09-12 |
| DP-31 | **HISTORICAL_ONLY.** When the window chosen for a question cannot supply DP-24's 30 post-lock contributing nights, three things happen together and none is optional: §8 states that PROSPECTIVELY_CONFIRMED is **unreachable from that run by design**, with the measured post-lock projection printed; §9 opens by saying that **no** guide line, product change, brief or quotable number follows from its verdict (rule 10), and the verdict carries the label `HISTORICAL_ONLY` wherever it is restated, ledgered or quoted; and a **successor prospective question — a replication inheriting every definition and the byte-identical `eval.py`, on nights after the first question's decision date — is drafted and locked at the same commit**, so the tradeable verdict stays reachable without an interim look. Every future ASK that offers such a window states this ceiling in that option's consequence line, in these terms, rather than re-explaining it. DP-24's 30 is never reduced to fit the window. | a shorter window is a legitimate choice about *his* waiting, but its price is the prospective track; writing the price into the file once stops a historical verdict being read as tradeable, and locking the successor before the numbers are seen is what keeps the prospective half blind (rule 3) | Q009, 2026-09-13 (Haci chose a historical-only window knowingly; the handling is the desk's, not his answer) |

## Autonomous mode (Defaults; Haci can overturn) — in force from 2026-09-13

Haci asked for a desk that works the backlog and comes back with results instead of questions.
These entries say what the desk does with the decisions it would once have put to him. They are
Defaults: he can overturn any of them by editing the row in place. The board
(`research/BOARD.md` §7) lists every decision taken under them, per question.

| id | rule | why | first applied |
|---|---|---|---|
| DP-40 | **Ask after, not before.** In autonomous mode (`/desk-run`, the nightly `desk` routine, or any run not marked `--ask`) no decision is put to Haci before lock. An item that would have been an ASK is **DEFAULTED**: the Registrar's recommended option is applied unless a DECIDE ground gives a different answer, or the options differ only in strictness (then the stricter). DECISIONS.md records it as `DEFAULTED` with the option not taken and the reason in one line; the board lists every DEFAULTED item. The locked question stands (rule 3); Haci overturns by asking for a successor question, never by editing. | a wrong default costs one successor question; a queue blocked on a question costs weeks | 2026-09-13 |
| DP-41 | **R-1 in autonomous mode:** the desk grants no new rule-14 exception. If a hypothesis cannot be tested without one, it goes to `questions/DEFERRED.md` with the exception spelled out (table, column, time needed). Haci grants it by adding the scope to DP-05; the question then re-enters the queue at the front. | knowledge time is a validity rule, not a preference | 2026-09-13 |
| DP-42 | **R-2 defaults (how he trades):** primary level = the level the hypothesis names; otherwise L3 on the DP-09 clock (20 sessions from entry) for swing-lane questions, L2 within 20 sessions for day-lane questions, L4 within 40 sessions for questions about deep targets. Lane = the hypothesis's lane, else swing. "Elite" = `overall_score ≥ 90`. Entry basis by DP-03 / DP-11. Option structures stay out of scope until an options freeze exists (DEFERRED.md; ENHANCEMENTS.md EN-011). | the values already locked in Q002–Q010; consistency across a family beats local fit | 2026-09-13 |
| DP-43 | **R-3 defaults (waiting vs sample):** window = pick nights ≥ 2026-06-01 (DP-06) through the earliest date at which every primary endpoint's contributing nights are projected ≥ 80 and post-lock nights ≥ 30 (DP-21, DP-24), projected from the Steward's exposure count at the measured run-rate (≈ 21 pick nights a month, less exclusions), plus the endpoint's maturity window and a one-week freeze margin; decision date = the first Monday on or after that. DP-13's single extension = +30 sessions. An arm projected below 20 contributing nights at the decision date is demoted to descriptive at lock, never dropped afterwards. If the initial decision date lands more than 12 months after lock, the question goes to DEFERRED.md with that date named instead of being locked. A shorter window is never chosen to reach a date sooner; DP-31 (HISTORICAL_ONLY) applies only when the hypothesis is itself about the sealed period. | the desk's job is to wait correctly; the 12-month ceiling keeps the family correction from filling with questions the platform will have outgrown | 2026-09-13 |
| DP-44 | **R-4 defaults (MPE units):** touch-rate and touch-within-k-sessions endpoints use DP-20 (+5.0 pp); per-trade ATR endpoints use DP-10 (0.25 ATR); a per-trade endpoint in percent-of-price units uses 0.25 ATR converted at the pick's own ATR% (still DP-10, not a new number). Sessions-to-touch differences are descriptive (no MPE); the unconditional touch-within-k form decides. No money-unit MPE is invented — a hypothesis that needs one is DEFERRED with the question stated. | his costs are encoded once and reused, never guessed | 2026-09-13 |
| DP-45 | **R-5 in autonomous mode:** when the desk cannot state a choice without hedging, it takes the option less likely to reach CONFIRMED, records both options and the reason in DECISIONS.md as `DEFAULTED (R-5)`, and the board shows it. It never takes the option that reaches a decision date sooner or a floor more easily. | conservative by construction | 2026-09-13 |
| DP-46 | **Lock authority.** The desk commits the PREREG and advances `PREREG_LOCKED --by desk` once DECISIONS.md has no pending item and `@registrar apply` has run; `schedule.json` (decision date, extension, hard stop) is written at the same time and the Steward pins the dataset in the same cycle. Haci's review moves to after the lock, via the board. The controller keeps `HUMAN_APPROVED`, `IMPLEMENTED_FLAG_OFF` and `RELEASE_APPROVED` for `--by haci`; those do not move. | the lock's value is that it precedes the data, not who types it | 2026-09-13 |
| DP-47 | **Registration order.** INBOX items first (each becomes an H-entry and jumps the queue), then the `PRIORITY` list in `research/lib/desk_queue.py` (EXPLORE_001's ranking — Haci-style F7 first, testable-soonest first), then remaining BACKLOG order. "new data needed" hypotheses go straight to DEFERRED.md with the data named. A hypothesis a locked PREREG already covers is marked *merged into QNNN* (DP-29), not registered twice. At most `--max-register` (default 2) new questions per cycle. | a fixed order the desk cannot re-argue every night | 2026-09-13 |
| DP-48 | **Issues, enhancements, trade ideas.** The desk files issues (PI), enhancements (EN) and trade ideas (TI) with a recommendation and leaves the decision to Haci. Asking for the prompt (`/desk-run prompt <id>`) *is* the decision: the desk sets `HACI_DECIDED:fix` / `HACI_DECIDED:build` and writes the brief in the same step. After he implements, `/desk-run verify <id> <sha>` runs the brief's before/after check; the desk never assumes a change landed. Nothing behaviour-changing is built subscriber-facing before its gate (ENHANCEMENTS.md), but an `INTERNAL_TOOL` brief (flag-off, Haci-only) may be written at any stage. | he wants lists, not questions; his ask is the approval | 2026-09-13 |
| DP-49 | **A brief never hands the coding agent a database step.** The only database configured in the platform repo is **production**; the desk's read-only role does not exist there. So every check a brief asks the *coding agent* to run must be satisfiable from the repo alone — the test suite, a pure-function import, `git show --stat`, a file diff. Anything that reads or writes a table belongs in the brief's verification section, addressed to the Data Steward on `$RESEARCH_DB_URL`, or to Haci where a write is required. A "prove you didn't change file X" check is written as the diff that proves it, never as "run X twice and compare". Where the desk holds a frozen manifest covering the affected table, the brief names that parquet as the before-snapshot instead of asking anyone to capture one. | PI-001: the coding agent correctly refused §4(b) and Step B, and was right — the brief had asked it to run a script against production. Six fix briefs are queued behind it with the same shape. | PI-001, 2026-09-13 |

## Reserved for Haci (asked in `--ask` mode; defaulted under DP-40..45 in autonomous mode)

- **R-1** Any new rule-14 (knowledge-time) exception.
- **R-2** Any choice that encodes how he trades: a horizon that differs from the platform's lane
  window (e.g. L3 within 20 sessions for a spread), which target level is primary, the lane, an
  option structure, what counts as "elite".
- **R-3** Trade-offs between waiting and sample size: the decision date, dropping an arm or a
  primary endpoint, sending a question to DEFERRED.
- **R-4** MPEs in money or ATR units — they encode his costs and slippage. Once he sets one for a
  metric type it becomes a DP entry and is not asked again.
- **R-5** Anything the Decision-maker cannot settle with strong alignment — if its own write-up of
  the choice would need an "on the other hand", it asks.
