# VERIFY EN-002 (re-verification) -- speed-to-target internal card, corrected specification

Commit verified: 22a2e1b (platform repo), merged to main at d19c9a9 (Merge pull request
#27 from kerzey/feat/en-002-speed-to-target-internal); parent 2d5776c matches the brief BASE
value. Brief: research/briefs/EN-002_speed_to_target_internal_card.md.
Specification correction: research/reports/EN-002_SPEC_CORRECTION_2026-09-14.md.
Verified 2026-09-14 by the Data Steward under DP-49/DP-50, task OPS-EN002-REVERIFY
(research/learning/agenda.json).

This is a re-verification of the same implemented SHA against a corrected specification, not a
re-implementation and not a deployment claim. research/reports/VERIFY_EN-002.md (2026-09-13) is
preserved unmodified as the original, failed-specification receipt.

## PASS: picks=578 over 71 nights matches the corrected specification exactly, with the TJX exclusion named; all other checks re-confirm PASS

The corrected specification (EN-002_SPEC_CORRECTION_2026-09-14.md) distinguishes: 579 DP-28
published rows over 71 nights (the tool's before-state / input), 578 tier-eligible picks over 71
nights (the shipped payload's output under the Section 3.1 score-at-least-80 tier gate), and one
named exclusion -- TJX, 2026-06-11, overall_score = 79.5802, qualified = True, selected_rank = 8.0,
dropped by the router's tier-of gate before summarize_speed ever sees it. This re-verification
reproduces all three numbers exactly against the pinned freeze and the implemented code, with no
code, data, flag or scientific-endpoint change since 2026-09-13.

---

## 1. Repo half -- re-run of Section 4(a), (b), (c), (d) at 22a2e1b (DP-49, no database)

**(a) Touched-file list** -- name-only diff between 2d5776c and 22a2e1b:
```
app.py
data/sas_speed_control_reference.json
docs/README.md
docs/SAS_SPEED_TO_TARGET_INTERNAL.md
routers/admin_sas_speed.py
services/sas_speed_to_target.py
tests/test_sas_speed_to_target.py
```
Exactly the seven paths named in brief Section 4(a). PASS -- identical to the 2026-09-13 result.

**(b) Subscriber path diff** -- diff --stat between 2d5776c and 22a2e1b over the twelve named
files (services/sas_conviction_card.py, services/super_agent_select_public.py,
services/super_agent_select_report_service.py, services/super_agent_select_scoring.py,
services/super_agent_select_service.py, routers/performance.py, routers/super_agent_select.py,
templates/performance.html, models.py, db.py, core/feature_flags.py,
scripts/run_sas_ladder_nightly.py): empty output. PASS.

Six sha256 checks (before = 2d5776c, after = 22a2e1b), re-run:
```
services/sas_conviction_card.py       before=e6ce8fe0...deeb6d86b after=e6ce8fe0...deeb6d86b MATCH
routers/performance.py                before=46dd1bf9...ee291794a after=46dd1bf9...ee291794a MATCH
templates/performance.html            before=48497c5b...93017f0e487 after=48497c5b...93017f0e487 MATCH
models.py                             before=ff0605e9...708be8b502bf after=ff0605e9...708be8b502bf MATCH
scripts/run_sas_ladder_nightly.py     before=3883f1f5...96e85f95337ce3a29347fcb5e4fcbfdb after=3883f1f5...4fcbfdb MATCH
core/feature_flags.py                 before=3d37fdf7...c5e4fcbfdb after=3d37fdf7...c5e4fcbfdb MATCH
```
All six MATCH, byte-identical to the 2026-09-13 hashes. PASS.

**(c)** numstat of app.py between 2d5776c and 22a2e1b reads two lines added, zero removed (the
import and the router-registration line beside admin_outcome_coverage). PASS.

**Write/DDL-shaped SQL marker sweep (AC-8)** -- a full-diff text search for the write and
schema-mutation keywords the brief names (row-insert, row-update, row-delete, add-column,
create-all shaped tokens) returns nothing anywhere in the 2d5776c..22a2e1b diff. PASS.

**(d) Flag default and import purity**, re-run against the platform repo module at 22a2e1b:
```
EN-002 flag default OFF: OK; import-pure: OK
```
tool_enabled() is False with the flag unset; sys.modules shows neither db nor models imported as a
side effect of importing services.sas_speed_to_target. PASS.

**Haci-only router gating** -- routers/admin_sas_speed.py: the admin-token guard is the first
statement in get_sas_speed_to_target, raising 401 before the flag check and before any db use
(read verbatim at 22a2e1b, unchanged since 2026-09-13); same ADMIN_TOKEN env-var plus header model
as routers/admin_outcome_coverage.py. Not mounted on routers/performance.py -- confirmed by the
app.py diff in (c), which adds only the admin_sas_speed router.

## 2. Test suite (pure, offline, no DB)

Running the shipped offline test file directly (no pytest, no conftest, no DB connection):
```
EN-002: 9/9 checks passed
```
Matches the brief's expected last line exactly, unchanged from 2026-09-13. PASS.

## 3. Frozen-freeze replay against the corrected specification (DP-50(c), no live query)

research/data/v001_sas_candidates.parquet sha256 confirmed
af40ad8a48915f979cf36b0cc4d6526a7e9ecfbd523351ac4d8155b7a0aaa2f2 -- matches
research/data/manifest_v001.json exactly.

Filtered to the DP-28 published predicate (qualified is true, selected_rank not null,
trading_date on or after 2026-06-01): 579 rows over 71 nights -- matches Section 1 of the
brief and the corrected specification's stated input exactly.

Independently reconstructed the router's row-builder (level-price extraction, entry-reference
extraction, tier assignment, ladder-void rule -- read verbatim from routers/admin_sas_speed.py and
services/sas_reference.py at 22a2e1b, not reimplemented from memory) over the 579 published
parquet rows, as_of = 2026-09-10, passed_at_entry = None for every row (no next-open price in the
platform, as specified), and called the shipped summarize_speed function:

```
tier-eligible rows built: 578
excluded sub-80 rows: [(2026-06-11, 'TJX', 79.5802)]
picks: 578   nights: 71
```

- Published input = 579 rows / 71 nights -- MATCH (corrected specification section 1).
- Tier-eligible output = 578 picks / 71 nights -- MATCH (corrected specification section 1).
- One-row exclusion = TJX, 2026-06-11, overall_score = 79.5802 -- MATCH, identical row named
  in VERIFY_EN-002.md (2026-09-13) and EN-002_SPEC_CORRECTION_2026-09-14.md.
- Every cell: control_status equals AWAITING_Q002, control is null -- confirmed across all six
  tier-by-level cells (select/apex times L1/L2/L3). PASS.
- Entry basis reads "signal_close_step_proxy (NOT Q002's next-open entry; passed_at_entry not
  applied)", matching the brief's section 3.3 specification exactly. PASS.

Payload sha256 from this independently-reconstructed replay:
f739b49f833192d0149d0b3c8fccf60c1877ab7c45056117d98297e5893748f8.
This differs from the sha256 recorded in VERIFY_EN-002.md (944e59b3...). The two replay scripts
were written independently (neither is checked into the repo; both reconstruct the router logic
from routers/admin_sas_speed.py and services/sas_reference.py read verbatim); a byte-identical
JSON hash across two independently-authored reconstructions is not guaranteed by the brief and was
not named as a pass/fail criterion in the corrected specification -- only picks, nights, the
one-row exclusion and every cell's control_status are. All four of those match exactly. This
discrepancy is recorded rather than suppressed; it does not affect the PASS verdict below, which
rests on the named, reproducible counts and exclusion, not on hash equality between two different
replay implementations of the same shipped pure function.

One read-only query (brief section 8a, the sealed June/July/August month check): NOT RUN.
The research read-only database credential is not set in this session. Per CLAUDE.md rule 1 and
DP-49/DP-50, no other credential may be substituted. This is the same limitation recorded in
VERIFY_EN-002.md (2026-09-13); the Steward should re-run this specific query the next time the
credential is available in-session, to confirm the sealed June-August published, with-steps, and
nights counts (171/163/21, 177/168/22, 170/170/21) have not changed under a platform repair
(DP-50(a)).

Zero rows written to the database: confirmed structurally (no migration, no new model column, no
schema-mutation text anywhere in the diff, per the AC-8 sweep above). The router's one database
touch is a single read-only fetch-and-filter, unchanged since 2026-09-13.

## 4. Scope of this receipt

This receipt verifies the code at commit 22a2e1b against the corrected specification in
EN-002_SPEC_CORRECTION_2026-09-14.md only. It makes NO deployment claim and asserts NO flag
change: the internal-card feature flag remains default-OFF in the repo, and whether it is turned
on in the platform's application settings is Haci's decision alone (brief section 8b), not
addressed by this document. No platform code, data, flag, or scientific endpoint changed between
the 2026-09-13 and 2026-09-14 verifications -- only the specification's stated expectation (579
vs. 578) was corrected.

## Verdict

PASS: published input (579 rows / 71 nights), tier-eligible output (578 picks / 71 nights), and
the one-row TJX exclusion all match the corrected specification exactly; every inertness, purity,
security, control-stub and test check from brief Sections 4, 5, 7 (AC-1..9, AC-11) re-confirms
PASS with no change since 2026-09-13. The original VERIFY_EN-002.md FAIL receipt is preserved
unmodified as the record against the pre-correction specification.
