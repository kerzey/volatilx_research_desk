# Q037 — decisions before lock

Run: 2026-09-14 by decision-maker (**autonomous**, DP-40..48; DP-52..58 read for design and forecasting) · Source: PREREG.md "Open decisions before lock", **4 items** (+2 routed requests named in §5.6: R1 counts-only exposure, R2 successor freezes)

State checked: `state.json` = `PREREG_DRAFT` (registrar, 2026-09-14) — in scope. **Three of the four §11 items met a DECIDE ground** (one of them against the Registrar's recommendation, which DP-40 expressly allows); **one is DEFAULTED** under DP-43/DP-45 because it is R-3 territory (send the question to DEFERRED vs re-specify the primary). Three further items are **ROUTED** to the Data Steward and settle mechanically at `record` on R1's counts — the decision date and the lock-or-DEFER call (§5.5's 0.27 trigger), the demotion-at-lock list, and §8 clause 6's evaluability. **R1 blocks the lock; R2 does not** (DP-23).

| # | Decision | Bucket | Choice | Basis |
|---|---|---|---|---|
| 1 | WALL_IN_PATH side-matching (§11.1) | DECIDED | **Option A — any wall `w ∈ gex_context.top_walls` whose `strike` lies between `X_i` and `L_i` inclusive counts, with no requirement that `w.side` match the direction of travel.** "Travel side" keeps its geometric meaning only (`strike ≥ X_i` for bullish, `≤ X_i` for bearish, which the `[X_i, L_i]` band already encodes). **Option B (side-matched: `call_wall` for bullish, `put_wall` for bearish) is registered as the printed alternative reading** (§4 secondary 3) and R1 measures the WALL_IN_PATH share under both. §2.5, §4, §5.6 take A. | **DP-25** — H-091 as filed reads "the **nearest wall on the side of travel** sits between the entry and the lane's first target"; A is *exactly* that cut (if any travel-side wall lies in `[X_i, L_i]`, the nearest travel-side wall does too), while B adds a filter the hypothesis does not contain — a re-unit, i.e. a different hypothesis. **Code ground (§11.1's premise checked and not sustained):** the −10 "Nearby gamma wall may interfere with follow-through" deduction keys on `abs(nearest_wall.dist_pct) <= 0.01` **alone**, with no side test and no path test (`volatilx services/super_agent_select_scoring.py:906-912`), and `side` is merely `"call_wall" if gex_net >= 0 else "put_wall"` (`services/gex.py:333`) — the sign of net gamma at a strike, not the platform's statement about resistance, so B tests a registrar generalisation, not the warning as printed. **DP-45** points the same way: A widens D and thins C (the scarce arm, §10.1), diluting the dampened arm toward the population mean and making the floors *harder*, so A is neither the easier CONFIRMED nor the easier floor. **DP-26** — a convention fixed before any outcome, with the alternative always printed. |
| 2 | DP-58 interim look at 60 contributing nights (§11.2) | DECIDED | **Option A — no interim look is registered.** §5.5 and §8 stand as drafted: fixed look **2027-06-07 at α = 0.05**, no interim, and the template's "no interim outcome looks, ever" governs. Because DP-58 requires the design to be declared **before the lock or not at all**, this choice is irreversible after lock and is recorded as such. | **DP-58** (declare before lock or not at all; absent a registered design the no-interim template stands); **DP-45** — the two options differ only in strictness and A is the stricter: it removes the early stop-for-success exit entirely, where B adds a CONFIRMED path at 60 nights. Registrar's recommendation agrees (DP-40). **Q021 §8** precedent: one fixed look, no interim, on the sibling F3 question. §5.4's own arithmetic seals it — at the central rate the 60-night point arrives with barely 30 post-lock nights, so the interim would be a mechanical "continue" bought with α. |
| 3 | Thin "neither" (C) arm: DEFERRED vs switch the primary to the complement (§11.3) | **DEFAULTED** | **Option A — DEFERRED.** If R1 measures the scarcer endpoint below **0.27 contributing nights per eligible session**, Q037 is **not locked** and goes to `research/questions/DEFERRED.md` with the measured rate, the projected floor date and the re-entry trigger (§5.5). The primary **never** switches to the complement `D − (C ∪ M)`; the complement stays a printed secondary (§4 secondary 1) in every branch, at the decision pass as well as at lock. §3, §4 and §5.5 stand as drafted. | **DP-43** — an under-powered question is DEFERRED, never rescued by re-specifying its primary; **DP-25** — the filed contrast is the conjunction against its clean complement, and M is a mixture whose composition would decide the result; **DP-45**; not taken: **switch the primary to D vs C∪M** |
| 4 | E1 (L1) as co-primary (§11.4) | DECIDED | **Option A — keep P1 (L1) and P2 (L3) as co-primaries, m = 2 within the question, F3's correction set = 3 primaries** (Q021's 1 + Q037's 2). §4, §7, §8 stand as drafted, with the §8 headline sentence tightened by Correction 2 below. | **DP-42** — the primary level is the level the hypothesis names, and H-091 names both the day lane's first target and the swing lane's; **DP-25** — the levels are taken as filed (only the L3 *clock* is re-specified, by the Confirmed DP-09, which outranks the DP-25 default and is already recorded in §2.4); **Q021 DECISIONS #4** is the same rule applied the other way (H-061 named one clock, so one primary); **DP-45** — with 3 F3 primaries the leading BH threshold is `0.10 × 1/3 = 0.033` against `0.05` at 2, so keeping E1 is the stricter bar for whichever endpoint leads, and §10.3's ceiling threat is a reason to *report* the speed companion, not to drop an endpoint the hypothesis names. |
| 5 | Window end, decision date, extension date, lock-or-DEFER | ROUTED → data-steward | waits on **R1 item (3)** — the measured contributing-night rate per eligible session on the scarcer endpoint, against §5.5's three branches (**< 0.27 → DEFERRED, not locked**; **0.27–0.35 → both dates move out**, locking only if the resulting decision date is on or before 2027-09-14; **≥ 0.35 → nothing changes**, the drafted window 2026-06-01..2027-04-30, decision **2027-06-07**, single DP-13 extension to ..2027-06-14 decided **2027-07-26**, then DEFERRED). Settled at `record`; **R1 may push the dates out and can never pull them in.** | DP-43; DP-45; DP-13; §5.5's own numeric trigger |
| 6 | Demotion / suppression at lock (DP-43) | ROUTED → data-steward | waits on **R1 items (1)–(3)** — any arm or sub-cell projected below **20 contributing nights** at the decision date is **demoted to descriptive at lock and never dropped afterwards**. On today's counts the **bearish** side of every direction-crossed cell is already SUPPRESSED at lock (14 bearish of 550 published rows, §5.4/§10.12); R1's D/M/C counts per endpoint decide whether anything else joins it, **per endpoint**, since arm membership is endpoint-specific (§2.5). Suppression restricts **affirmative reporting only** and never removes a cell from §8 clause 5 or clause 7. | DP-43; DP-45; Q021 DECISIONS #7 |
| 7 | §8 clause 6 (S2) and B2 feasibility | ROUTED → data-steward | waits on **R1 items (4)–(5)** — B2 pool sizes per night, the share of eligible picks obtaining a **full 10-neighbour** match, and how many D picks obtain a same-night same-direction C match for the S2 pairs. If S2 cannot be computed the clause is **not evaluable and the verdict is INCONCLUSIVE**, never CONFIRMED on B1 alone (§8 clause 6, as drafted — unchanged by this pass); rows lost to a truncated history are **dropped and counted**, never imputed. Re-checked at the decision pass either way. | rule 5; §8 clause 6; Q021 DECISIONS #8 |
| 8 | Successor freezes | ROUTED → data-steward | waits on **R2** — `manifest_v002` / `manifest_prices_v002` covering pick nights after 2026-09-10 through **2027-04-30**, due before the decision pass of **2027-06-07**; a second pair **only if** the DP-13 extension fires, then through **2027-06-14** for the **2027-07-26** pass. **Not a blocker for lock** (DP-23). | DP-23; DP-50(a) |

## Corrections to silent choices

Three, all small; the Registrar applies them with the table. Nothing in the draft contradicts a DP entry on MPE, floors, exclusions, predicate, entry basis, clock or freeze scope (see the checked list below).

1. **§7, third overlap bullet — "Q022 (F7)" is wrong: Q022 is filed in **F4 Price behaviour after selection** (`research/questions/Q022_sector_cluster_nights/PREREG.md` §1, DP-29).** Fix the family letter. The substance of the bullet stands and nothing in F3's correction set moves: F3 remains **3 primaries** (Q021 1 + Q037 2). Checked at the same time and **no companion BH correction is owed** under the Q021 DECISIONS #1 rule — that rule fires only when a new primary is *byte-identical* to a locked question's in another family (same metric, same clock, **same anchor**, overlapping nights), and Q022's E1 is a *between-night* contrast anchored on the **session t+1 open** (Q022 §4 rule 2) while Q037's P1/P2 are *within-night* D − C contrasts anchored on **`C_t`** (DP-11). Different anchor and different contrast, so one correction set, not two.
2. **§8, "the question's headline verdict is the stronger-evidenced primary" — add the restatement rule.** As drafted it is correct but restatable as a bare "Q037 CONFIRMED". It must read: the headline **names the endpoint** (P1/L1 or P2/L3) and states the other endpoint's verdict beside it, with **both** raw p and **both** BH q quoted, in the REPORT, the LEDGER line and any brief; a single-endpoint result is never restated as a question-level CONFIRMED. (The Q012 §7 / Q021 DECISIONS #1 principle: the report may not quote the friendlier of two numbers.)
3. **§2.5 and §4 — apply decision 1's wording.** WALL_IN_PATH is defined without any `side`-field requirement; the side-matched reading appears only in §4 secondary 3 and in R1's item (2), and §10.6's caveat (`side` is the sign of net gamma at the strike, `services/gex.py:333`, not an option type) is cited at the definition rather than only in the threat list.

**Checked and *not* corrections** — each already matches policy: MPE **5.0 pp** on both primaries and on the S2 gate, in the same units (DP-20, DP-44); no invented money-unit MPE, sessions-to-touch descriptive (DP-44); `exclusions_v003.json`, the newest file, read from JSON with no hard-coded dates (DP-22); floors read as **≥ 80 contributing nights per primary endpoint** and 20 per cell — the stricter reading (DP-21); **≥ 30 post-lock contributing nights**, not reduced (DP-24); publication predicate `qualified IS TRUE AND selected_rank IS NOT NULL` with dark-lane rows out of every arm but retained in the control pool (DP-28); entry `X_i = C_t` for picks and controls alike with the next-open basis a printed sensitivity (DP-11, DP-03); L3 on the 20-session clock with the 40-session cell descriptive (DP-09); inclusive-endpoint and gap-through conventions fixed before any outcome (DP-26, DP-27's tie principle); the **episode-clustered CI as a gate**, not a footnote (DP-51, §8 clause 3); **no rule-14 exception requested or needed** — the feature is the 16:05 `context_json` snapshot, not the upserted `gex_symbol_daily` table (DP-05 untouched, DP-41 respected); successor freezes covering **all** candidate symbols, published and unpublished, plus hourly bars for published symbols (DP-23); 20-session basis labelled **NON_QUOTABLE** (rule 12); DP-31 correctly **not** invoked (PROSPECTIVELY_CONFIRMED is reachable by design); the DP-49 sentence in §9 and the DP-50(a)/(b)/(c) clauses in §5.5/§9/§10.14 present; the sample-size, precision, dependence, multiplicity and strata justification in §5.4 (DP-57); the counts-only funnel separating elapsed / eligible / matured nights with the maturity leg added **once** (DP-53, DP-56); the in-sample exclusion and prior-exposure disclosure in §6 and the historical-companion caveat (DP-55). The extension date sitting past the initial ceiling is correct as drafted — DP-43's 12-month ceiling governs the **initial** decision date (2027-06-07 is ≈ 8.8 months after lock), and DP-13's single extension is a standing answer to a shortfall, not a new choice.

## Defaulted on Haci's behalf

- #3 Thin "neither" arm — chose **DEFERRED if R1 measures below 0.27 contributing nights per eligible session, with the complement staying a printed secondary**; not taken: **switch the primary to D vs C∪M** — DP-43. Overturn = successor question.

That is the only one. Items 1, 2 and 4 each met a DECIDE ground (DP-25 with the code read; DP-58 with DP-45; DP-42 with DP-25 and the Q021 precedent), so none needed his word, and items 5–8 are mechanical on the Steward's counts. **Worth him seeing plainly:** #3 is the branch on which this question produces **no answer at all** rather than a weaker one — if the clean "neither" arm is too rare, the desk waits for a platform change instead of testing the conjunction against a mixture it cannot interpret. Nothing here is pending on him.

## Routed requests

### data-steward — R1 (counts-only exposure report) — **blocks the lock** (§5.5 trigger)

Q037 (does the SAS report's own GEX dampening warning predict a weaker path; PREREG §5.6 request R1) needs an exposure count on the **frozen** data — `manifest_v001` / `manifest_prices_v001` / `manifest_prices_universe_v001`, never a live query (DP-50(c)) — and it is **counts only, with no outcome of any kind**: no touch, no hit rate, no excess, no return, no arm difference, and no bar dated after a pick night may be read except to establish that a bar *exists*, for maturity and gradeability accounting. Population: published picks (`qualified IS TRUE AND selected_rank IS NOT NULL`, DP-28; dark-lane rows excluded from both arms but retained in the control pool) on pick nights **2026-06-01..2026-08-11** for every quantity that needs a matured t+20 window, and **2026-06-01..2026-09-10** for the pick-night-only quantities, with the nights in `research/data/exclusions_v003.json` (`manual_runs` ∪ `non_session_runs` ∪ `uncorroborated_publication_runs`) and DP-04 re-run nights removed, and with the two cohorts reported **separately** so the maturity lag is added once and the immature tail is not counted as attrition (DP-53). Please return: **(1)** the §2.6 funnel night by night — eligible picks after each exclusion, **NOT_COMPUTABLE rows broken out by failing clause** (`gex_context` empty or `available` false, null `spot_close`, `regime ∉ {POS_GAMMA, NEG_GAMMA}`, `quality.contracts_used < 1`, empty `top_walls`, null `nearest_wall.dist_pct`), **day-lane and swing-lane target availability separately** (`public_payload_json.lane_plans.{day_trading,swing_trading}.targets[0]` missing or empty — the day-lane count is unmeasured anywhere on the desk today), the price-scale screen (`L_i / C_t` outside `[0.5, 2.0]`), **passed-at-entry** rows per endpoint (bullish `L_i ≤ C_t`, bearish `L_i ≥ C_t`), the **2% spot-basis guard** (`|gex_context.spot_close / C_t − 1| > 2%`), symbols with fewer than 60 daily bars dated ≤ t in `prices_daily_split`, picks with no `C_t`, ungradeable picks, nights where more than 25% of eligible picks are ungradeable, and immature nights; **(2)** the **POS_GAMMA / NEG_GAMMA split** of GEX-computable published rows per night, and the **WALL_IN_PATH share per endpoint under both readings** — the registered primary reading **A** (any wall in `top_walls` whose `strike` lies in the closed band between `C_t` and that endpoint's first target, no `side` requirement) and the alternative reading **B** (additionally `side = "call_wall"` for bullish travel, `"put_wall"` for bearish), reported side by side, since B is a registered printed secondary; **(3)** the resulting **D / M / C counts per night per endpoint** (D = POS_GAMMA ∧ WALL_IN_PATH, C = NEG_GAMMA ∧ ¬WALL_IN_PATH, M = exactly one condition) and the **contributing-night count and per-eligible-session rate per endpoint** on the §2.7 definition (a night carrying ≥ 1 eligible D pick **and** ≥ 1 eligible C pick, each with a full matched control set), given **both** on the matured cohort (2026-06-01..2026-08-11) and on the pick-night-only extended proxy (through 2026-09-10), with the two labelled — this is the number that decides whether Q037 locks at all: on the **scarcer** endpoint, **below 0.27 contributing nights per eligible session the question is not locked and goes to DEFERRED**; **0.27–0.35 pushes the window end and both dates out**; **at or above 0.35 nothing changes** (DP-43, DP-45 — a fast rate never pulls a date in and never shortens a window); **(4)** the same GEX coverage for **unpublished** candidates — B2 pool size per night, the share of eligible picks obtaining a **full 10-nearest-neighbour** match on `log C_t` / `atr_pct` / `beta60` / `runup20` (ATR14 desk-computed from `prices_daily_split` bars ≤ t, the `03_build_core.py:71-74` construction — the platform's `atr_pct` column is split-corrupted, PI-003, and must not be used), the pool's distributions on those four features, and the **B3 arm counts** (D / M / C among unpublished GEX-computable candidates per night under reading A); **(5)** **S2 matched-pair feasibility** — per night per endpoint, how many **D** picks obtain a same-night, same-direction **C** match on `log C_t` / `atr_pct` / `beta60` / `runup20` / `k` / `(L−X)/X`, 1:1 without replacement, and how many are left unmatched, since §8 clause 6 is **unevaluable → INCONCLUSIVE** if the pairs cannot be built; **(6)** the **agreement rate** between `context_json.gex_context` and `v001_gex_symbol` for the same symbol-date on the **wall list and the regime label**, with the disagreements listed (the table is upserted in place, `services/gex.py:605-629`, `:823-840`, so this is a data-quality figure only — the snapshot is the feature either way), plus published rows with no `gex_symbol_daily` row at all; and **(7)** the **truncated-history scan** (the CTRA signature — a symbol whose daily bars stop mid-freeze while it keeps appearing in `sas_candidates`; CTRA's bars stop 2026-05-06) over every in-window candidate symbol, together with the **DP-50(a) in-window commit check** from the read-only platform repo for any commit touching `services/gex.py` — `_compute_walls` (`:322-337`), the degraded-snapshot path (`:676-698`) and the upsert (`:605-629`, `:823-840`) — with SHA and date, because a repair there makes the arm label two different features split at the ship date (the DP-06 pattern) and must be recorded in `research/data/DATA_NOTES.md` before the successor freeze is built. Item (3) settles the window end, the decision date, the DP-13 extension date and the lock-or-DEFER call at `record`; items (1)–(3) settle which arms and sub-cells are demoted to descriptive at lock (DP-43); item (5) is the one exposure fact that can change §8. **R1 may push the decision date out; it can never pull it in**, and no floor is lowered to meet a date. (Bookkeeping: Q021's R3 — the one-line CTRA entry in `DATA_NOTES.md` — is still open, and item (7) here is the third time the scan has been paid for; the note would let the next question cite it instead.)

### data-steward — R2 (successor freezes) — due before the decision pass, **not** a blocker for lock

Q037 (PREREG §5.5, request R2, DP-23) needs successor freezes before its decision pass on **Monday 2027-06-07** (the date is provisional until R1 item (3) is read, and can only move **out**). Please build and pin `manifest_v002` (selections; same SQL and same exclusion criterion as v001) and `manifest_prices_v002` (same Alpaca queries) covering pick nights after **2026-09-10** through the registered window end **2027-04-30** — and a **second** pair through **2027-06-14** for the **2027-07-26** pass **only if** the DP-13 extension fires. Five scope requirements, none optional. (1) The daily symbol list is extended to **every candidate symbol on every in-window night, published and unpublished** — B2 and B3 need the unpublished symbols' pick-night closes, their **60 prior daily bars** (ATR14, beta60, runup20) and their **20 forward daily bars** (40 for the descriptive swing cell) — and is never assumed from v001's symbol list. (2) **Hourly bars for all published symbols** over t+1..t+40, for the §4 same-session ordering, which is descriptive only. (3) **`gex_symbol_daily` for every in-window night**, for the §6 disagreement audit only — it is never the feature. (4) A **targeted truncated-history check** (the CTRA signature) over every in-window candidate symbol before the freeze is called complete, reported as a list rather than left to be rediscovered by an exposure count. (5) Under **DP-50(a)**, report any platform commit between the freezes that rewrites historical rows in `sas_candidates.context_json` or changes `services/gex.py`'s wall or regime logic, with a dated `research/data/DATA_NOTES.md` entry naming the column, the date range and the ship SHA — the successor freeze re-runs the same SQL against the same tables, so such a repair silently changes values v001 recorded differently, and this question would then treat the arm feature as two features split at that date. The freeze must carry **40 forward sessions beyond the last included pick night** (through the 2027-06-25 session for the 2027-04-30 window end, so the descriptive 40-session L3 cell is covered as well as the deciding 20), and please re-confirm every date in §5.5 session by session from the trading calendar at the same time — the holiday list used was 2026-11-26, 2026-12-25, 2027-01-01, 2027-01-18, 2027-02-15, 2027-03-26, 2027-05-31, 2027-06-18, 2027-07-05, 2027-09-06. Rows whose bars are missing are **excluded and counted, never back-filled or imputed**.

## Schedule

**Not yet filled — R1 is outstanding and blocks the lock.** Drafted values, carried here so the `record` pass has one place to confirm or move them:

decision_date: **2027-06-07 (provisional)** · extension_date: **2027-07-26 (provisional)** · hard_stop: **2027-07-26** (= the extension date; a gate still short on `eval.py`'s measured counts that day sends Q037 to **DEFERRED** — there is no second extension and a gate shortfall is never INCONCLUSIVE) · rule: **exposure-driven** · window: pick nights **2026-06-01..2027-04-30** · gates: **≥ 80 contributing nights per primary endpoint** (DP-21) **and ≥ 30 contributing nights dated after the lock commit** (DP-24), on `eval.py`'s measured counts, never on a projection · ceiling: 2027-06-07 is ≈ **8.8 months** after a 2026-09-14 lock, inside DP-43's 12 months.

Arithmetic re-checked at this pass and agreeing with the draft: window end 2027-04-30 → t+20 = **2027-05-28** → +1 calendar week = 2027-06-04 (Friday) → first Monday = **2027-06-07**; extension window end **2027-06-14** (30th session after 2027-04-30, 2027-05-31 removed) → t+20 = **2027-07-14** → +1 week = 2027-07-21 (Wednesday) → first Monday = **2027-07-26**. At `record` the drafted dates stand unless R1's measured rate pushes them out under §5.5's second branch; **DP-53 governs how the projection is computed** (fully observable cohort, exclusions retained in the denominator, maturity added once, scenarios and uncertainty shown separately) **and is not a licence to shorten a registered window or to prefer an earlier date** — DP-43 and DP-45 still forbid pulling a date in on a fast rate.

## Standing rules added

_none._ Nothing here is Haci's word: items 1, 2 and 4 were DECIDED on existing entries, the platform source and precedent; item 3 was DEFAULTED under DP-43; items 5–8 settle mechanically on the Steward's counts. **A DEFAULTED item adds no DP entry** (DP-40).

## Standing rules proposed

- **When a draft's recommended cut cites the platform's own behaviour, the code is read before the cut is accepted.** §11.1 recommended side-matching because `side` is "the platform's own label for what the printed warning calls call wall resistance"; the deduction that prints that warning tests `abs(nearest_wall.dist_pct) <= 0.01` and nothing else (`super_agent_select_scoring.py:906-912`), and `side` is only `sign(gex_net)` (`gex.py:333`). Proposed as one clause on DP-25: *a re-spec justified by "this is what the platform does" is applied only after the cited lines are read; otherwise the filed cut stands.*
- **A question with two co-primaries names the endpoint in every restatement of its verdict.** Correction 2 here, Q012 §7 and Q021 DECISIONS #1 are three instances of the same rule — the report may not quote the friendlier of two numbers, and "Q037 CONFIRMED" without an endpoint name is exactly that.
- **DP-53's relationship to DP-43/DP-45 should be stated in one line where a schedule is set**, not re-argued per question: DP-53 changes how a forecast is *computed* (observable cohort, maturity once, uncertainty shown); it does not authorise shortening a registered window or preferring an earlier decision date, which DP-45 still forbids.
