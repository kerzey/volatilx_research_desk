# Q022 — decisions before lock
Run: 2026-09-13 by decision-maker (autonomous) · Source: PREREG.md "Open decisions before lock", 2 items (+ 9 items decided or routed here: the two routed requests named in §5, and seven items the draft settles silently or leaves to `record`)
**Record pass: 2026-09-13 (autonomous)** · Source: `research/reports/STEWARD_Q022_exposure.md` (R1, counts only, on the pinned freeze; DP-50(c)). **Q022 LOCKS**, on a schedule that moves **out** of the drafted one on every date. Measured: window start **2026-07-08**; arm split **CLUSTER 17 / DIVERSIFIED 15 / UNDETERMINED 0** on 46 elapsed sessions; eligible-night rate **0.696**/session, rarer arm **0.326**/session, conversion to contributing **18 of 18** on the matured slice. Both §5 gate lines clear by 2.3–4.2×. Every ROUTED item R1 settles is settled below and **no item is pending**; five further items (12–16) are decided here on the measured counts. R2 (successor freezes) stays open **by design** — due before the decision pass, never a blocker for lock (DP-23) — and is restated at record with the fixed dates and one added reporting clause.

State checked: `state.json` = `PREREG_DRAFT` (registrar, 2026-09-13) — in scope.

**One item is DEFAULTED** (the window start, which is where this question spends Haci's waiting — Q018 #1's shape, DP-43). **Eight are DECIDED** on existing entries and locked precedent. **Two are ROUTED** to the Data Steward: R1 (counts only) is **blocking for lock** — the draft's §5 DEFERRED gate turns on its numbers and no desk report has ever counted a sector arm split — and R2 (successor freezes) is due at the decision date and is not a blocker (DP-23).

**Q022 does not lock at `decide`.** The arm split is unmeasured: EXPLORE_001 §8 declared H-034 untestable on the `industry` column and nothing since has counted CLUSTER nights. Whether this question locks at all, and on what dates, is settled at `record` from R1 — the §5 gate (rarer arm ≥ 0.085 nights/elapsed session, eligible nights ≥ 0.30) against DP-43's 12-month ceiling. A rate **faster** than plan changes nothing: no date is ever pulled in (DP-43, DP-45).

**Resolved at `record`, the same day. LOCK.** The arm is real and it varies: 17 CLUSTER and 15 DIVERSIFIED nights on the 32 `k_t = 8` nights of the corrected in-window period, with **zero** UNDETERMINED — §10.1's "the arm may be almost constant" is measured and closed as a feasibility matter. The rarer arm runs at **0.326** nights per elapsed session against a **0.085** gate and eligible nights at **0.696** against **0.30**, so the DEFERRED branch is closed. But the eligible-night rate is **slower than the 0.85 planning assumption**, which fires item 9's *second* branch: **both dates move out**. The drafted window end 2026-11-13 projects ≈ 64–66 of the 80 contributing nights the floor needs — short — so the drafted schedule would have made **DP-13's single extension the plan rather than the fallback**, which is not what DP-13 is for. The window end is therefore re-derived so the 80-night floor is projected met **inside the primary window**, at the conservative end of the measured rate (item 10): window **2026-07-08..2027-01-13**, decision **Monday 2027-02-22**, one automatic extension to **2027-02-26** decided **Monday 2027-04-05**, then DEFERRED. Every one of those four dates is **later** than the drafted one; none was pulled in, and no floor, gate, MPE or arm was touched.

**Rule 14: no exception is requested and none is needed.** The sector mapping is a committed repo artifact dated before every in-window pick night; DP-05 is untouched and DP-41 is not engaged (item 4, which tightens *which* blob is pinned).

| # | Decision | Bucket | Choice | Basis |
|---|---|---|---|---|
| 1 | Window start | **DEFAULTED** | **Option A — pick nights from 2026-07-06**, i.e. after `_enforce_ladder_monotonic` (`5fa3db4`, 2026-07-06) and the `publication_floor = 80.0` codification (`1765a6f`, 2026-07-07), stated as the rule in item 3 rather than as a bare date. §2.2, §5 and §6 stand as drafted; the ≈ 18 sealed sessions of 2026-06-09..2026-07-03 are **not** printed as a descriptive panel — they are a different ladder mechanism and a different publication predicate, not a weaker reading of the same one. **Measured at `record`: the rule of item 3 returns `window_start` = 2026-07-08** — 2026-07-06 is an excluded manual re-run and the 2026-07-07 run finished four hours *before* `1765a6f` shipped — so the start moved **out** by two sessions and the discarded sealed period is ≈ 22 sessions, not ≈ 18. | **DP-43**; not taken: 2026-06-09 start, ~18 more sessions, blends two ladder mechanisms |
| 2 | The "of 8" denominator | DECIDED | **Option A — the primary is nights with exactly `k_t = 8` published picks.** Nights with `k_t ≠ 8` are counted by value of `k_t` (**both tails**: the Q018 exposure figure of 8.07 published rows per night implies `k_t > 8` nights exist, and they are excluded and counted exactly like short slates), reported in the funnel, and enter no arm. A `k_t ≥ 6` primary, or a **relative** cut (`m_t / k_t ≥ 0.5`), is a **successor hypothesis**, never a switch at the decision pass. §2.3 step 2 and §10.10 stand. | **DP-25** — "≥ 4 of 8" is H-034's cut as written and the Registrar does not re-unit a hypothesis; **DP-45** — A is the smaller, slower-accruing population, and B would reach the floors sooner by admitting nights whose short slate is mechanically pushed toward DIVERSIFIED, confounding the arm with slate size; §2.3's own argument |
| 3 | The exact window start, as a rule | DECIDED | `window_start` = **the first non-excluded pick night whose nightly run begins after the last population-defining ship among `5fa3db4` and `1765a6f`**, with the ship timestamps dated by R1(a) in ET and compared against `sas_runs.finished_at` on the adjacent nights. Expected first graded night **2026-07-07 or 2026-07-08** (2026-07-06 is itself excluded by `exclusions_v003.json`). If R1(a) dates `66cb63d` (industry-rotation-bonus removal) on or after that night, §10.9 binds and the start moves after **that** commit instead. **Measured at `record`: `window_start` = 2026-07-08.** `5fa3db4` ships 2026-07-06 15:13:42 ET; `1765a6f` ships 2026-07-07 21:43:53 ET while that night's run had already finished at 17:46:04 ET; the first run to *begin* after both is 2026-07-08 at 17:19:37 ET. `66cb63d` ships 2026-05-14 10:24:45 ET, ~8 weeks pre-window → **§10.9 does not bind**. The rule stays in §6 as the basis and re-fires on any later dated ship that changes which picks are published. The rule moves the start **out, never in**, and the whole §5 schedule is recomputed at whatever start it fixes. | Ground 3 (one defensible answer) + **DP-06 / DP-50(a)**: a mechanism that changed mid-window is two features, and the printed ladder *is* the primary level while the publication predicate *is* the arm; **Q019 DECISIONS #3** verbatim in form ("first pick night after the last such change"); **DP-45** — the rule can only delay |
| 4 | Which `sp500_sectors.json` blob is pinned, and its rule-14 status | DECIDED | **No rule-14 exception is needed or requested** (DP-05 untouched; DP-41 respected): a committed mapping dated **2026-05-17**, before every in-window pick night, is prior information in the strict sense, is never re-synced for the life of the question, and classifies nothing with a later clock. **Tightened:** the pinned artifact is **the blob in force at the window start** — the content of the last platform commit touching `data/sp500_sectors.json` dated ≤ `window_start` — **not** automatically the content at manifest SHA `fa70688…`. R1(a) returns both; if they differ, the **earlier** blob is used, the differing symbols are listed in the report, and `manifest_v002` records that blob's sha256. `eval.py` fails loudly on a sha256 mismatch (§2.1 as drafted). **Measured at `record`: they do not differ** — one commit ever (`4171b1a`, 2026-05-17), byte-identical at `fa70688`, sha256 `c4d12610…0201`. The contingency is recorded as **not engaged** rather than deleted (item 12); it is the rule that would bind a successor question. | rule 14 read literally (a mid-window re-sync is knowledge of a later reclassification, even where `_meta.last_synced` was not bumped); **DP-45** — the stricter of the two pins; **DP-23** (the artifact travels with the successor manifest) |
| 5 | MPE ±10.0 pp on E1 | DECIDED | **Stands at ±10.0 pp**, with the Registrar's reason as written. **DP-44** routes a touch-rate endpoint to **DP-20** (+5.0 pp) *including* DP-20's own clause permitting a **larger** MPE with a one-line reason, and "E1 is a difference of differences" is the desk's standing reason for exactly this uplift: **Q007 §8** (10.0 pp = 2 × DP-20), **Q014 §8 E1** (10.0 pp, same shape, same wording), **Q012 P2** and **Q018 P4** (0.50 ATR = 2 × DP-10). The same 10.0 pp governs **§8 clause 7's B3 gate**. **The MPE is never lowered at the decision pass**, in any branch, including one where 10.0 pp turns a CI-excludes-zero result into INCONCLUSIVE. | **DP-44 → DP-20**'s larger-MPE clause; **Q007 §8**, **Q014 §8**, **Q012 §8 P2**, **Q018 §8 P4**; **DP-45** (the larger MPE is the stricter option and no smaller one is admissible) |
| 6 | Arm demotion and sub-cell suppression at lock | DECIDED | **The two arms are not demotable.** DP-43's demotion clause reaches only the §4/§6 sub-cells: E1 *is* the arm contrast, so an arm below 20 contributing nights leaves no endpoint — it fires the §5 gate (DEFERRED at `record`) or, post-lock, the single DP-13 extension and then DEFERRED. §5's clause stands as drafted. **Rule fixed now, applied at `record` to R1(b)(c)(d)(e)'s measured splits:** any *reported sub-cell* projected below 20 contributing nights at the decision date is **SUPPRESSED at lock** — counts only, no point estimate, no sign, no "directionally" — and **stays suppressed even if it clears 20 measured nights at the decision pass**. Suppression restricts affirmative reporting only: it never removes a cell from **§8 clause 6** (any tape or regime cell with ≥ 20 contributing nights beyond MPE in the opposite sign still blocks CONFIRMED) or from **clause 7**, and never by itself makes E1 INCONCLUSIVE. Expected suppressions to be confirmed at `record`: bearish direction, the 90+ band (PI-010's falling elite count), and every non-XLK `dominant_sector_t` cell. **Applied at `record` — see item 14 for the closed list.** Two of the three expectations are confirmed and fixed (every non-XLK dominant sector, plus the `m_t = {5}` and `{6+}` dose cells, which the draft did not anticipate); **bearish and 90+ are *not* fixed at lock** because R1 returned no direction or band counts, so they fall to the standing ≥ 20-measured-night rule instead — strictly binding, never loosened. | **DP-43** (demotion decided at lock, never afterwards), **DP-21** (20 per cell), **Q019 DECISIONS #6/#9** and **Q017 DECISIONS #7** (a)–(d) verbatim |
| 7 | Exclusions inside a window that outruns every exclusions file | DECIDED | `eval.py` unions **`research/data/exclusions_v003.json`** (the newest file, DP-22) with the **add-only successor exclusions file** issued with the successor selection freeze — the identical three lists (`manual_runs`, `non_session_runs`, `uncorroborated_publication_runs`), built by the identical criteria, covering nights after 2026-09-10. The successor file may only **add** nights; no night is ever removed from a v003 list; the **criteria** are fixed at this lock even though the **dates** cannot be. DP-04 applies mechanically on top. Both paths are `eval.py` inputs — no hard-coded file name, no hard-coded date. | **DP-22** read as "one list, one set of criteria" — its "cite the newest file" clause presumes a sealed window, and Q022's runs to **2027-01-13** (extended **2027-02-26**) at the dates fixed at `record`, four to five months past v003 — a longer reach than the draft assumed, which strengthens this item rather than changing it; **Q019 DECISIONS #5** and **Q018 DECISIONS #4**, same construction, same cycle |
| 8 | The F4 correction set at the decision pass | DECIDED | BH runs over the primary endpoints of **F4 questions locked by the decision pass**, and the set **never shrinks below the 10 registered at this lock** (Q007 4, Q008 3, Q013 2, Q022 1). If **Q020 (H-033)** returns from `DEFERRED.md` — it needs a DP-05(c) rule-14 grant — and locks before Q022's decision pass, **its primaries join the set** and the count rises. A question leaving F4 after this lock does not lower Q022's denominator. §7 gains that sentence. | rule 8; **DP-29** (H-034 counted once, in F4); **Q018 DECISIONS** "a family correction set is stated symmetrically and never shrinks" |
| 9 | The DEFERRED gate — the inequality, not the number | DECIDED | **The gate is the inequality and it is re-solved at `record`** at whatever start item 3 fixes and whatever lock date applies. The drafted thresholds (**rarer arm ≥ 0.085** nights per elapsed session, **eligible CLUSTER + DIVERSIFIED nights ≥ 0.30**) are that inequality's solution on a 277-session ceiling window from a 2026-07-06 start and a ~2026-09-14 lock, and they are **stricter** than the bare arithmetic (20/277 = 0.072; 80/277 = 0.289) — kept. Three branches, decided now so none is argued with counts in view: **below either line → Q022 is not locked and goes to `research/questions/DEFERRED.md`** with the measured rates and the projected floor date (the H-062 form); **inside the lines but slower than the planning rate (0.85 eligible / 0.25 rarer-arm share) → both dates move out** to the recomputed date and the question locks on those; **faster than plan → nothing changes**. | **DP-43** (12-month ceiling; window sized at the measured rate; never shortened), **DP-45** (a fast rate licenses nothing and never pulls a date in), **Q019 DECISIONS #4** third branch |
| 10 | Window end, decision date, extension date, **lock-or-DEFER** | **DECIDED at `record`** (was ROUTED → data-steward; R1 landed) | **LOCK.** Window start **2026-07-08** (item 3's rule, measured). Window **pick nights 2026-07-08..2027-01-13** = **132 elapsed sessions**; decision **Monday 2027-02-22**; one automatic DP-13 extension to window end **2027-02-26** (+30 sessions), decided **Monday 2027-04-05**; then DEFERRED. **Sizing, stated so it can be checked:** contributing-night rate = eligible-night rate, because conversion was **18 of 18** on the matured slice; measured `r` = 32/46 = **0.6957**/elapsed session. At the point estimate the 80th contributing night lands at session **115** (2026-12-17) — a projection of **80.0** against a floor of 80, i.e. a coin flip on whether the extension fires, which would make **DP-13 the plan instead of the fallback**. The window is therefore sized at the **one-sided 90% lower bound** of the measured rate (normal approximation on 32 of 46: `0.6957 − 1.2816 × 0.0678` = **0.6088**/session), giving `80 / 0.6088` = **131.4 → 132 sessions** = **2027-01-13**; at the point estimate that window projects **≈ 92** contributing nights, a **15%** margin over the floor. Neither other floor binds: 20 rarer-arm nights arrive at session ≈ 85 even at the lower bound (0.2375/session), and DP-24's 30 post-lock nights at session ≈ 96, ≈ 2026-11-19 (85 post-lock sessions × 0.6088 ≈ 52). Decision date = last pick night **+ 20 sessions** (2027-02-11) **+ one week** (2027-02-18) → first Monday on or after = **2027-02-22**. Extension: 2027-01-13 + 30 sessions = **2027-02-26** (Presidents Day 2027-02-15 removed) → + 20 sessions = **2027-03-29** (Good Friday 2027-03-26 removed) → + one week = **2027-04-05**, already a Monday. **The margin is a *sizing* convention, not a gate:** the floors are unchanged and fire on `eval.py`'s measured counts at the pass, never on this projection (DP-13). | **DP-43** (window sized at the measured rate to the floor, plus maturity and the one-week margin; ceiling cleared at 5.4 and 6.7 months from lock), **DP-45** (the longer window; a date is never pulled in and the floor is never "reached" by leaning on the extension), **DP-13** (one extension, then DEFERRED), item 9's **second branch** (measured 0.696 < the 0.85 planning rate → both dates move out), item 6 (an arm short of 20 is not demoted — it fires the gate; neither arm is short) |
| 11 | Successor freezes | ROUTED → data-steward — **restated at `record`** | waits on **R2** — `manifest_v002` (selections + the pinned `sp500_sectors.json` blob and its sha256 + the add-only successor exclusions file) and `manifest_prices_v002`, covering pick nights **2026-07-08..2027-01-13**, due **before the decision pass 2027-02-22**, not a blocker for lock (DP-23). A second pair **only if** the DP-13 extension fires (window to **2027-02-26**, pass **2027-04-05**). R2's session-by-session calendar re-confirmation may move the §5 dates **out, never in**. **One clause added at record:** the funnel counts for the sub-cells R1 did not break out — direction, score band, `best_timeframe` lane — reported with the freeze as **information, not a gate** (item 14). | — |
| 12 | The pinned sector blob, fixed by content not by locator | **DECIDED at `record`** | The artifact is **sha256 `c4d12610ac95a8a83a0fc2365d02b4963d6a4169a9352acf2578164111390201`** — the content of platform commit **`4171b1a`, 2026-05-17**, the **only** commit ever to touch `data/sp500_sectors.json`, and **byte-identical** to the content at manifest SHA `fa70688` (`git diff` empty). Item 4's contingency (if the window-start blob and the `fa70688` blob differ, use the earlier one and list the differing symbols) **does not engage, and is recorded as not engaged** rather than deleted — it is the rule that would bind a successor question. The blob is **identified by its sha256** and `fa70688` is retained only as a locator; `manifest_v002` records the sha256 as a static artifact and `eval.py` fails loudly on a mismatch (§2.1). The mapping holds **2,405** symbol→sector-ETF entries over the eleven SPDR sectors. **Never re-synced for the life of the question**, including if the platform's quarterly sync runs mid-window. | item 4 as written; **DP-23** (the artifact travels with the successor manifest); rule 14 (the blob predates every in-window pick night by seven weeks, so it is prior information in the strict sense); **DP-05 untouched, DP-41 respected** — no exception requested |
| 13 | **BRK.B and BF.B** — the unmapped-symbol rule | **DECIDED at `record`** | **No hand-map, no desk-side supplement, no override — ever.** BRK.B and BF.B are the **only** two unmapped symbols in 431 distinct candidates across the entire frozen history (published coverage 99.6% overall / 99.5% in-window; all-candidate coverage 99.75%); the pinned blob's bootstrap script does not carry the period-suffix dual-class format. They are counted in **`u_t`** exactly as §2.3 step 3 says, they are listed by name with their appearance counts in the funnel (published: BRK.B ×3, two in-window; all candidates: BRK.B ×8, BF.B ×7), and a night they push to **UNDETERMINED** is counted and reported, never rescued. Three reasons and each is sufficient: (i) a desk-side supplement breaks the **sha256 pin** that makes the study reproducible (§2.1) and turns a frozen artifact into a desk-maintained one; (ii) it would be a classification rule invented **after looking at which symbols failed**, and a hand-map is a judgment in the inner loop (rule 9); (iii) the bias is already one-directional and disclosed — an unmapped symbol can only cost a DIVERSIFIED night, never manufacture a CLUSTER one (§2.3, §10.3) — so leaving them unmapped is the conservative side. **Measured cost: nil.** Exactly **one** `k_t = 8` night in the whole report period carries `u_t = 1` and none carries `u_t ≥ 2`; **UNDETERMINED = 0** everywhere. The rule is general for this question: the unmapped set is whatever the pinned blob does not contain, on every night including post-lock ones. | §2.3 as drafted; **DP-45** (the stricter side of a disclosed one-directional bias); rule 9 (no tuning while looking); rule 3 (the pin is what makes the arm reproducible) |
| 14 | Which sub-cells are **SUPPRESSED at lock** (item 6's rule, applied to R1's measured splits) | **DECIDED at `record`** | **Four projections are decidable from R1 and they close the list; counts only — no point estimate, no sign, no "directionally", no q-value.** Projected to the registered window end (132 sessions) at the measured per-session rates: **(a) every non-XLK `dominant_sector_t` cell — SUPPRESSED.** XLF 3/46 = 0.065 → ≈ **9**; XLV 1/46 = 0.022 → ≈ **3**; the other eight SPDR sectors have **zero** observed CLUSTER nights. **XLK clears** (13/46 = 0.283 → ≈ **37**) and is the only reportable dominant-sector cell. **(b) The dose–response cells `m_t = {5}` and `m_t = {6+}` — SUPPRESSED** (3/46 and 3/46 = 0.065 each → ≈ **9** each). **`m_t ∈ {2,3}` clears** (15/46 → ≈ **43**) and **`m_t = {4}` clears** (11/46 → ≈ **32**), so the dose–response is reportable as a three-point shape at most and never as a monotone ladder over six cells. **(c) Both arms clear and neither is demotable** — CLUSTER 0.370/session → ≈ **49**, DIVERSIFIED 0.326 → ≈ **43** (≈ 41 and ≈ 36 at the lower-bound rate). **(d) UNDETERMINED is an empty cell, not a suppressed one** — 0 nights measured; it is printed as a measured zero. **Cells R1 did not break out are *not* pre-suppressed and are not left loose:** direction, score band, `best_timeframe` lane, tape stratum and regime label are governed by the standing rule §4/§5 already carry — **reported only at ≥ 20 measured contributing nights, SUPPRESSED below, counts printed either way**. Pre-suppressing a cell the desk has not counted would be an invented number; the standing rule is strictly binding in every branch, so nothing is loosened. The tape and regime strata *cannot* be pre-suppressed even in principle — the expanding-window terciles are defined by sessions that have not happened. **Decided now and never revisited:** a cell on this list stays suppressed **even if it clears 20 measured nights at the decision pass**. **Suppression restricts affirmative reporting only** — it never removes a cell from **§8 clause 6** (a tape or regime cell with ≥ 20 contributing nights beyond MPE in the opposite sign still blocks CONFIRMED) or from **clause 7**, and never by itself makes E1 INCONCLUSIVE (§8 clause 1). | **DP-43** (demotion/suppression decided at lock, never afterwards), **DP-21** (20 per cell), item 6's four clauses verbatim, **Q019 DECISIONS #9** and **Q017 DECISIONS #7** (a)–(d) |
| 15 | The DP-50(a)/(b) sweep result | **DECIDED at `record`** | **Clean — §5's DP-50(a) clause stands unfired and the window is not split.** The platform's `main` HEAD **is** `fa70688`: **zero** commits have merged since the freeze SHA. The one commit that exists (`2d5776c`, 2026-09-13, PI-001 backfill-window floor) is on an **unmerged** branch, touches only the nightly-pipeline entrypoint and a new test, restates nothing, and concerns `uoa_symbol_daily.fwd_return_*` — a table this question **bans outright** (§6). It touches no selection or publication path, no lane-plan writer, and rewrites no historical `sas_candidates` or price row, so it cannot change the population, the arm label or the lineage. **The clause is retained, not retired:** any repair merged between now and `manifest_v002` that rewrites historical rows gets a dated `DATA_NOTES.md` entry and splits this window as two features (DP-06 pattern), and any change to **which picks are published** changes the **arm** — R2 carries that sweep forward. | **DP-50(a)** and **(b)** applied and reported; **DP-06** pattern named for the successor period |
| 16 | The two feasibility gates R1 sized — B3, and the pick-level screens | **DECIDED at `record`** | **Nothing changes; both stay as drafted and are measured at the pass, not now.** **B3 (§8 clause 7):** projected evaluable but **not certified** — 80.9% of in-window picks keep ≥ 3 same-sector controls (CLUSTER 83.1% / DIVERSIFIED 78.3%) and 31 of 32 nights retain ≥ 50% of their picks (CLUSTER 17/17, DIVERSIFIED 14/15), against a gate of ≥ 20 contributing nights per arm under B3 pools **and** ≥ 50% pick retention. The gate reads `eval.py`'s measured counts at the decision pass and **is not weakened, pre-certified or converted into a projection**; §10.8's thin-sector risk stands, and **if B3 is not evaluable the verdict is INCONCLUSIVE, never CONFIRMED on B1 alone**. **B1:** not binding anywhere — 100% of picks keep ≥ 3 matched controls, pool sizes 37–60. **§2.4 screens:** the price-scale screen reproduces the DATA_NOTES signature exactly (19 of 542 = 3.5%: **APH 15**, CRWD 2, KLAC 1, MNST 1) and the screen stays symbol-agnostic as drafted — it is a rule, not that list. No swing-lane target: 8 of 550 (1.45%). `outcome_target_invalid`: 4. Below 60 prior bars / no `C_t` / no t+1 open: **0**. **CTRA:** the Q016 §10.8 signature is confirmed and is literally CTRA (last bar 2026-05-06, still appearing as a candidate through 2026-09-10, **never published**, already excluded by the ≥ 60-prior-bar gate on every date after 2026-05-06) — no action, no repair, and the R2 rescan stands. | §8 clause 7 and §2.4 as drafted; **DP-45** (a projected-feasible gate is not a passed gate); **DP-07** (CTRA is an observation here, not a research question) |

## Corrections to silent choices

**Twelve — five at `decide`, seven more at `record` (6–12, from the Steward's measured counts).** The Registrar applies all twelve at `apply`, with the rest of this file. Everything else was checked against the policy and already matches (list at the end of this section).

Five at `decide`.

1. **§2.3 step 1 and §5 — the exclusions clause must name the union, not `exclusions_v003.json` alone** (item 7). The drafted window runs two to four months past v003's coverage; leaving the post-freeze nights to DP-04 alone silently drops the `uncorroborated_publication_runs` and `non_session_runs` protections the sealed nights get. **Must read:** "`exclusions_v003.json` **unioned with the add-only successor exclusions file issued with `manifest_v002`**, identical three lists, identical criteria, add-only; both paths are `eval.py` inputs".
2. **§2.2, §5 and §6 — the window start is a rule, not the literal date** (item 3). Every place that prints "pick nights 2026-07-06" gains "**or the first non-excluded pick night whose run begins after the last of `5fa3db4` / `1765a6f` / `66cb63d`, whichever is later — dated by R1(a), moving out only**". The draft already excludes 2026-07-06 itself; the point of the repair is the **2026-07-07 ship of `1765a6f`**, which the draft's own §6 names and then does not carry into the start date. — **Refined at `record` by Corrections 6 and 7: the rule ran and returned 2026-07-08. §6 keeps the rule as the stated *basis* (it re-fires if a later ship changes which picks are published — see the Schedule's "one live route back to DEFERRED"); every other section prints the literal measured date.**
3. **§2.1, §5 R1(a) and §6's knowledge-time table — the pinned artifact is the blob in force at the window start** (item 4), not "the content at platform SHA `fa70688…`" simpliciter. `fa70688` is dated ~2026-09-10, inside the window; if the file moved between 2026-05-17 and the window start the earlier blob is still prior information, and if it moved *after* the window start the `fa70688` content is not. **Must read:** "the content of the last platform commit touching `data/sp500_sectors.json` dated ≤ `window_start`, recorded with its sha256 in `manifest_v002`; `eval.py` fails loudly on a mismatch." — **Refined at `record` by Correction 10: that commit is `4171b1a` (2026-05-17), the file's only commit ever, and its content is byte-identical to `fa70688`'s. The PREREG names the **sha256** as the identity and keeps this rule as the basis; the "if they differ" contingency is recorded as **not engaged**, not deleted (item 12).**
4. **§5 R1 gains the DP-50(a)/(b) commit sweep.** The draft asks R1 to date `66cb63d` and to check the mapping file's history but never asks the standard sweep. **Must read:** R1 reports **every** platform commit since manifest SHA `fa70688` that touches the selection or publication path, the lane-plan writer, or which picks are published, and **any repair that rewrites historical `sas_candidates` or price rows** — with SHA and date, so §5's DP-50(a) clause has something to act on (a repair inside the window splits the column as two features, the DP-06 pattern; a repair that changes which picks are published changes the **arm**). — **Answered at `record`: the sweep is clean (main HEAD = `fa70688`, zero merged commits). Correction 12 writes that result into §5; the clause stays live for the period between the two freezes and R2 carries it forward.**
5. **§5's planning paragraph — the 0.85 eligible-night haircut is provisional and is labelled so** (item 2). The Q018 exposure headline (541 published rows on 67 nights = **8.07 per night**) is evidence that `k_t > 8` nights exist, which the `k_t = 8` primary also discards; the haircut is therefore a guess in **both** directions and R1(c) is what replaces it. **Must read:** "planning rate, to be replaced at `record` by R1(c)/(d)'s measured `k_t` distribution (both tails) and arm split; it is never used to defend a date after the measurement lands." — **superseded at `record` by Correction 8: the measurement landed, so the planning rates are struck outright rather than labelled. The Registrar applies 8, not this sentence.**

Seven at `record`. All seven are the measurement replacing an assumption; none changes an arm, a level, a clock, a control, an MPE, a gate or a floor.

6. **Every printed window date is replaced by the measured one — §2.2, §2.3 step 1, §5, §6, §11.1.** Start **2026-07-08** (not 2026-07-06); window end **2027-01-13** (not 2026-11-13); decision **Monday 2027-02-22** (not 2026-12-21); extension window end **2027-02-26** (not 2026-12-29); extension decision **Monday 2027-04-05** (not 2027-02-08). §2.3 step 1's sentence "In-window today that removes **2026-07-06** itself — the window therefore effectively opens on the next session" **must go**: 2026-07-06 and 2026-07-07 are now both **outside** the window, and the first graded night is the window start itself. §5's "the end is the 94th session after it at the planning rate" **must read** "the end is the **132nd elapsed session** from the start, sized so the 80-contributing-night floor is projected met **inside the primary window** at the conservative end of the measured rate (DECISIONS item 10)". §5's "3.2 and 4.8 months from a 2026-09-14 lock" **must read** "**5.4 and 6.7 months**".
7. **§6's window-start rationale must carry the measured ship clock, not the commit dates.** `5fa3db4` ships **2026-07-06 15:13:42 ET**; `1765a6f` ships **2026-07-07 21:43:53 ET** — and the 2026-07-07 run **started 17:35:09 and finished 17:46:04 ET, about four hours before that ship**, which is precisely why the start is not 2026-07-07. The first non-excluded pick night whose run **begins** after both ships is **2026-07-08** (run started 17:19:37 ET). 2026-07-06 is excluded anyway (late-finishing manual re-run, `exclusions_v003.json`). **§10.9 is not triggered and says so:** `66cb63d` (industry-rotation-bonus removal) ships **2026-05-14 10:24:45 ET**, roughly eight weeks before the window opens — the threat is measured and closed, not merely listed.
8. **§5's planning paragraph and R1 bullet list are replaced by the measured numbers, with the source named.** Cite `research/reports/STEWARD_Q022_exposure.md` (2026-09-13). Strike the 0.85 / 0.25 planning assumptions (Correction 5 anticipated this) and print: eligible-night rate **0.696**/elapsed session (32 of 46), rarer arm **0.326** (15 of 46), conversion to contributing **18 of 18** on the matured sub-window 2026-07-08..2026-08-11, `k_t` distribution in-window (4: 1, 5: 1, **8: 32**, 9: 11, 10: 1 — **both tails confirmed**, `k_t > 8` is 12 of 46 over the report period), mapping coverage **99.5–99.9%**, **UNDETERMINED = 0**. The §5 gate lines (0.085 / 0.30) stay printed **as the inequality that was tested and cleared**, with the measured rates beside them — they are not deleted just because they passed.
9. **§6's Half A / Half B dates move with the window.** **Half A = pick nights 2026-07-08..2026-09-11** (**47** elapsed sessions, ≈ **33** contributing nights projected); **Half B = 2026-09-14..2027-01-13** (**85** sessions, ≈ **59** projected); extension nights **2027-01-14..2027-02-26** join Half B. **A disclosure goes with it, because extending the window grows only Half B:** at the measured split Half A carries ≈ **17 CLUSTER / ≈ 16 DIVERSIFIED** — below 20 *per arm within that half*. §8 clause 5 is a **sign test on the half-level `E1`** and imposes no per-arm-per-half floor, so it stays exactly as drafted and is evaluable; the halves are **not** sub-cells and are not on item 14's suppression list. `eval.py` prints the per-arm night count for each half beside the half-level estimate so the asymmetry is read with the number, not around it. No gate is added and none is relaxed.
10. **§2.1, §6's knowledge-time table and the PREREG header identify the artifact by sha256, not by platform SHA.** **Must read:** "`data/sp500_sectors.json`, sha256 **`c4d12610ac95a8a83a0fc2365d02b4963d6a4169a9352acf2578164111390201`**, the content of platform commit **`4171b1a` (2026-05-17)** — the file's only commit — byte-identical at `fa70688`, which is retained as a locator only; recorded as a static artifact in `manifest_v002`; `eval.py` fails loudly on a mismatch." Item 4's earlier-blob contingency is **recorded as not engaged**, not deleted (DECISIONS item 12).
11. **§10.1 and §10.3 are measured threats now and must be written as measured.** §10.1 ("the arm may be almost constant"): **measured 17/15 in-window, 28/26 over 2026-06-01..2026-09-10, UNDETERMINED 0** — closed as a *feasibility* matter; the pre-window split (11/11) is added as the informational cross-check it is. §10.3 (mapping coverage is the single point of failure): **measured 99.5–99.9%, two unmapped symbols ever (BRK.B, BF.B), one night with `u_t = 1`, none with `u_t ≥ 2`** — the one-directional bias disclosure **stands unchanged** and gains DECISIONS item 13's no-hand-map rule. **§10.2 (arm correlated with the tape) is untouched and is now the question's live threat**, which the report must say plainly: the one thing R1 could not measure is the thing most likely to make this INCONCLUSIVE.
12. **§5 gains the DP-50(a) sweep result as a dated fact** (item 15): "as of 2026-09-13 the platform's `main` HEAD **is** the manifest SHA `fa70688` — zero merged commits since the freeze; the single unmerged commit `2d5776c` touches only the `uoa_symbol_daily` backfill window, a table this question bans. The clause stays live for the period between the freezes and is carried forward by R2."

Checked and **not** corrections — each already matches the policy: entry basis the **session t+1 official open** for picks and every control alike, DP-11 correctly **not** fired (nothing here is a position already held) with the close basis a sensitivity that never decides (**DP-03(b)**, **DP-42**; Q006 §4, Q002, Q003, Q013 §4, Q014 §4 precedent); **primary level L3 on the 20-session clock** with the platform's 40-session window descriptive (**DP-42**, **DP-09**); publication predicate `qualified IS TRUE AND selected_rank IS NOT NULL`, dark-lane rows in no arm but available to the control pools (**DP-28**); the B1 construction **reused verbatim from Q006 §3** — 10 nearest, `beta60`/`atr_pct`/`runup20`, night median and MAD, Euclidean, with replacement, ties by symbol ascending — with Q013 §3's ≥ 3-controls drop-and-count (**DP-45**: the pick is dropped, never imputed); a level at or through the entry **never a hit**, applied identically to every synthetic control target (**DP-26**, **DP-03**); no stop assumed, counter-direction touches reported not used as exits (**DP-02**), and **DP-30** not engaged; fixed-horizon return descriptive in every branch (**DP-01**, rule 5); floors read as **≥ 80 contributing nights on the primary endpoint** and 20 per cell, the stricter reading (**DP-21**); **≥ 30 contributing nights dated after the lock commit**, never reduced, and **DP-31 correctly not invoked** because PROSPECTIVELY_CONFIRMED is reachable from this run by design (**DP-24**, **DP-31**); one automatic extension then DEFERRED, fired on `eval.py`'s **measured** counts and never on a projection (**DP-13**); successor freezes covering **every** candidate symbol, published and unpublished, plus hourly bars for published symbols (**DP-23**); registrar conventions fixed before any outcome is seen — the 10-session block length, the within-month permutation, the expanding-window tape terciles, the HHI and the ±1.5 ATR synthetic-level companion (**DP-26**), with none of them promotable at the decision pass (§7); regime used only for nights ≥ 2026-06-09, `v1.2`, same-evening rows (rule 7, FREEZE_v001 §7); every basis **NON_QUOTABLE** (rule 12) and nothing quotable before PROSPECTIVELY_CONFIRMED (rule 10); dispersion, dose–response and sessions-to-touch **descriptive with no invented MPE** and no money-unit MPE anywhere (**DP-44**); family **F4**, H-034 counted once (**DP-29**); the exposure count specified on the pinned freeze rather than a live query (**DP-50(c)**); and **no rule-14 exception requested or needed** (**DP-05** untouched, **DP-41** respected).

## Defaulted on Haci's behalf

- #1 Window start — chose **pick nights from 2026-07-08** (measured at `record`: the first run beginning after both population-defining ships), window to **2027-01-13**, decision **Monday 2027-02-22**; not taken: 2026-06-09 start, ~18 more sealed sessions, decides sooner — DP-43. Overturn = successor question.

**Updated at `record` and this is the line the board shows.** The choice is unchanged; its price is now measured rather than estimated. Waiting for one ladder mechanism and one publication predicate costs **≈ 22 sealed sessions** (2026-06-09..2026-07-07, in which the arm split was an even 11/11 — so the discarded nights were usable, not degenerate), and the slower-than-planned eligible-night rate then pushes the decision from the drafted 2026-12-21 to **2027-02-22**, about **nine weeks** further out. The alternative was not a better question, it was a **blended** one: two ladder mechanisms (cross-lane non-monotonicity 25.71% → 0.00%) and two publication predicates inside one arm definition. DP-43 and DP-45 forbid paying the wait back by moving a date in.

Nothing else was defaulted. Item 2 — the Registrar's other open item — met a DECIDE ground (DP-25 takes H-034's cut as written) and is not a question about how Haci trades; items 3–9 are applications of existing entries and locked precedent. One consequence of #1 belongs on the board with it: **the desk is choosing to wait roughly three extra weeks and to throw away ≈ 18 sessions of already-frozen nights** rather than blend two ladder mechanisms and two publication predicates into one arm definition. DP-43 and DP-45 forbid paying that back by moving a date in, and if R1 measures a thin arm split the same choice is part of why Q022 may be DEFERRED rather than locked. No floor, arm, gate, MPE or clause was weakened anywhere in this file; items 3, 4 and 6 each tighten one.

## Routed requests

### data-steward — R1 (counts only) — **ANSWERED 2026-09-13** → `research/reports/STEWARD_Q022_exposure.md`

**Closed. Nothing outstanding.** Every item (a)–(h) was returned on the pinned freeze with no live query behind any number (DP-50(c)) and no outcome of any kind. Settled here as items 10, 12, 13, 14, 15 and 16. The request is kept verbatim below as the record of what was asked and on what basis the schedule was fixed.

#### R1 as sent, kept verbatim (was: **BLOCKING FOR LOCK**; settles the schedule and the lock-or-DEFER call)

Q022 (sector-cluster nights, PREREG §5 request R1) needs a **counts-only** exposure measurement on the **frozen** data — `research/data/manifest_v001.json` + `research/data/manifest_prices_v001.json`, plus read-only `git log` / `git show` in the platform repo — and **never a live query** (DP-50(c)); it is **blocking for lock**, because no desk report has ever counted a sector arm split and the §5 DEFERRED gate turns on these numbers. **No outcome of any kind:** no touch, no first-touch date, no return, no excess, no arm difference, and no sector-versus-outcome cross-tab of any shape; forward bars may be read **only to establish that a bar exists** (maturity and gradeability accounting), never for a value. Population: published picks (`qualified IS TRUE AND selected_rank IS NOT NULL`, DP-28; dark-lane rows counted in the funnel and available to the control pools but in no arm), with the nights in `research/data/exclusions_v003.json` (`manual_runs` ∪ `non_session_runs` ∪ `uncorroborated_publication_runs`) and DP-04 late-`finished_at` nights removed; **denominator for every rate = elapsed sessions (calendar sessions minus holidays, exclusions *not* pre-removed — the Q019 Correction 3 convention, so the rates are comparable across questions)**. Please return, all by month and for the period as a whole: **(a) the artifact and the ships** — `git log --follow -- data/sp500_sectors.json` with every commit SHA and date, the **blob in force at the window start** (the last such commit dated ≤ the start) with its sha256, whether that blob is byte-identical to the content at manifest SHA `fa70688…` and, if not, the symbols added, removed or reclassified between them; the file's `_meta.last_synced`; the **commit timestamps in ET** of `5fa3db4` (`_enforce_ladder_monotonic`), `1765a6f` (`publication_floor = 80.0`) and `66cb63d` (industry-rotation-bonus removal), each compared against `sas_runs.finished_at` on the adjacent nights, so the **window start** of DECISIONS item 3 is a measured date and not an assumption; and the **DP-50(a)/(b) sweep**: every commit since `fa70688` touching the selection or publication path, the lane-plan writer, or which picks are published, plus any repair that rewrites historical `sas_candidates` or price rows. **(b) Mapping coverage** — the share of published picks whose symbol is in the pinned mapping, and the same for the unpublished candidate pool, with the full list of unmapped published symbols and their appearance counts. **(c) The `k_t` distribution** (published picks per night after DP-28 and the exclusions), **broken out on both tails** — `k_t < 8` and `k_t > 8` — since the Q018 headline of 8.07 published rows per night implies both exist. **(d) The arm split, the number this question lives on** — CLUSTER (`m_t ≥ 4`), DIVERSIFIED (`m_t + u_t ≤ 3`) and UNDETERMINED night counts on PREREG §2.3's rule, per month and **per elapsed session**, with the `m_t` distribution, the `u_t` distribution and the distribution of `dominant_sector_t` on CLUSTER nights; report this over **2026-06-01..2026-09-10**, with the in-window sub-period (from the item-3 start) broken out separately and the pre-window part marked informational — the arm label is a 16:05 ET property with no outcome attached, so the longer span costs nothing and makes the gate decision far less fragile. **(e) B3 feasibility** — per published pick, the number of same-night unpublished candidates carrying the same sector ETF with ≥ 60 prior daily bars; the share of picks with ≥ 3; the share of nights retaining ≥ 50% of their picks; each broken out by arm (this is §8 clause 7's EVALUABLE test, and if it fails the verdict can only be INCONCLUSIVE). **(f) B1 control-pool coverage** on the Q006 §3 construction (pool size per pick, share of picks with ≥ 3 matched controls), plus the targeted truncated-history scan for the CTRA signature (a symbol whose daily bars stop mid-freeze while it keeps appearing as a candidate, Q016 §10.8). **(g)** Swing-lane availability in `public_payload_json.lane_plans` and the §2.4 price-scale screen's drop count, by symbol. **(h) The contributing-night rate** per elapsed session on §2.3's full definition (CLUSTER or DIVERSIFIED, matured, ≥ 5 gradeable picks each with a valid control set), measured on the matured sub-window only — pick nights from the item-3 start through the last night whose session **t+20** has matured on `manifest_prices_v001` (≈ 2026-08-12; please confirm from the calendar) — with the maturity-independent parts of (b)–(g) reported through **2026-09-10**. **The call this request decides:** Q022 locks only if the **rarer arm** runs at **≥ 0.085 nights per elapsed session** and eligible (CLUSTER + DIVERSIFIED) nights at **≥ 0.30 per elapsed session**; below either line the 20-per-arm or 80-night floor cannot be reached inside DP-43's 12-month ceiling and **Q022 goes to `research/questions/DEFERRED.md`** with the measured rates and the projected date named, instead of being locked. Above the lines, the window end, decision date and DP-13 extension date are recomputed from the measured rates and **move out only, never in** — a rate faster than the 0.85 / 0.25 planning assumptions changes nothing (DP-43, DP-45). Please also flag any sub-cell (direction, score band, `best_timeframe`, tape stratum, regime label, `dominant_sector_t`) projecting below 20 contributing nights at the decision date, for the item-6 suppression list. Report to `research/reports/STEWARD_Q022_exposure.md`.

### data-steward — R2 (successor freezes) — **restated at `record` with the fixed dates**; due before the decision pass **2027-02-22**, **not** a blocker for lock

**Dates, now fixed (DECISIONS item 10):** pick nights **2026-07-08 .. 2027-01-13**, forward bars carrying **20 sessions beyond 2027-01-13** (i.e. through **2027-02-11**), delivered in time for the decision pass on **Monday 2027-02-22**. A **second** pair only if the DP-13 extension fires: pick nights through **2027-02-26**, forward bars through **2027-03-29**, pass **Monday 2027-04-05**. **The static artifact is fixed by content:** `data/sp500_sectors.json`, sha256 `c4d12610ac95a8a83a0fc2365d02b4963d6a4169a9352acf2578164111390201` (commit `4171b1a`, 2026-05-17; byte-identical at `fa70688`) — record the sha256, do not re-sync, and fail the build if the content has moved. **One clause added at record:** please also return the funnel counts for the sub-cells R1 did not break out — **direction (bullish/bearish), score band (80–85 / 85–90 / 90+), `best_timeframe` lane** — per elapsed session and per month, as **information for the report, not a gate**: they are governed by the standing ≥ 20-measured-night rule (DECISIONS item 14) and no date, floor or suppression depends on them. **Carry forward the DP-50(a)/(b) sweep from `fa70688`**, which was clean at 2026-09-13 (main HEAD = `fa70688`): any repair merged before `manifest_v002` that rewrites historical `sas_candidates` or price rows gets a dated `DATA_NOTES.md` entry and splits this window as two features (DP-06 pattern), and any change to **which picks are published** changes the **arm** and must be reported with its SHA and ET timestamp against `sas_runs.finished_at`, exactly as R1(a) did for `1765a6f`. Repeat the CTRA-signature scan.

The original request, unchanged in substance:

Q022 needs `manifest_v002` (selections, same SQL, same exclusion criteria, **plus the pinned `data/sp500_sectors.json` blob of DECISIONS item 4 recorded with its sha256 as a static artifact, plus the add-only successor exclusions file of item 7**) and `manifest_prices_v002` (same Alpaca queries), covering pick nights from the item-3 window start through the window end fixed at `record`, with the daily symbol list extended to **every candidate on every in-window night, published and unpublished** — B1 and B3 need the unpublished symbols' closes, their 60 prior bars and their 20 forward bars — **plus hourly bars for published symbols**, carrying **20 forward sessions beyond the last included pick night**, and repeating the CTRA-signature scan of R1(f). Build a second pair **only if** the DP-13 extension fires. Please re-confirm the exact session-count dates (last pick night + 20 sessions, then the first Monday on or after that plus one week) from the trading calendar when the freeze is built; that re-confirmation may move the §5 dates **out, never in**. Carry forward the DP-50(a) sweep of R1(a) for the period between the two freezes: any repair that rewrote historical rows gets a dated `DATA_NOTES.md` entry and splits this question's window as two features (DP-50(a), the DP-06 pattern), and any change to which picks are published changes the **arm** and must be reported with its SHA and date.

## Schedule

**FINAL — filled at `record` (2026-09-13) on `research/reports/STEWARD_Q022_exposure.md`. Every value below is final for the lock; none is provisional and no item is pending.** The Registrar writes them verbatim to `research/questions/Q022_sector_cluster_nights/schedule.json` at `apply` / lock (DP-46), with `check_from` = the decision date so the queue surfaces it (the Q021 convention). R2's session-by-session calendar re-confirmation when the successor freezes are built may move these dates **out, never in**.

decision_date: **2027-02-22** (Monday) · extension_date: **2027-04-05** (Monday) · hard_stop: **none** (DP-13's single automatic extension, then DEFERRED, replaces it — the 2027-04-05 pass is the last one) · rule: **fixed** (see the note below — the dates were *derived* exposure-driven at `record` from the measured rate and are **not** re-derived again) · window: pick nights **2026-07-08 .. 2027-01-13** (extended: .. **2027-02-26**) · extended: **false** · maturity: **20 sessions** (L3 on the DP-09 clock) · gates: **≥ 80 contributing nights**, **≥ 20 in each arm**, **≥ 20 per reported sub-cell**, **≥ 30 contributing nights dated after the lock commit** (DP-21, DP-24), on `eval.py`'s measured counts at the single decision pass · ceiling: **cleared** — 2027-02-22 is **5.4 months** after the 2026-09-14 lock and the extended date 2027-04-05 is **6.7 months**, both well inside DP-43's 12 months (ceiling date **2027-09-14**).

- **Why `rule: fixed` and not `exposure-driven`.** The window end **was** set by accrual — that is what this record pass did — but having been set at the measured rate it is now a **fixed calendar date**. It cannot move with the rate any more: DP-43 and DP-45 forbid pulling it in if accrual runs fast, and only R2's calendar re-confirmation or a DP-50(a) repair can push it out. Calling it exposure-driven would imply it still tracks accrual, which it does not. The one accrual-sensitive mechanism left is DP-13's single extension, which fires on `eval.py`'s **measured counts** at the pass and never on a rate or a projection (Q019's reasoning, same cycle).
- **Arithmetic, re-derived independently at `record` and in agreement with the Steward.** Elapsed sessions **2026-07-08..2026-09-10 = 46** (July 8–31 **18**, with the observed Independence Day Friday 2026-07-03 already before the start; August **21**; September 1–10 **7**, Labor Day Monday 2026-09-07 removed) — the Steward's count exactly, and the exclusions are **not** pre-removed (the Q019 Correction 3 convention). Eligible `k_t = 8` CLUSTER/DIVERSIFIED nights **32** → `r` = 32/46 = **0.6957**/session; conversion to contributing **18 of 18** on the matured slice, so the contributing rate is the eligible rate. Lower bound `r₉₀` = 0.6957 − 1.2816 × √(0.6957 × 0.3043 / 46) = 0.6957 − 0.0869 = **0.6088**. `80 / 0.6088` = 131.4 → **132 elapsed sessions** from 2026-07-08 → **2027-01-13** (session 92 = 2026-11-13, the drafted end, confirming the Steward's session index; 112 = 2026-12-14; 115 = 2026-12-17; 122 = 2026-12-29, the drafted extension end). + 20 sessions = **2027-02-11** (MLK Monday 2027-01-18 removed) → + one week = 2027-02-18 → first Monday on or after = **Monday 2027-02-22**. Extension: 2027-01-13 + 30 sessions = **2027-02-26** (Presidents Day Monday 2027-02-15 removed) → + 20 sessions = **2027-03-29** (Good Friday 2027-03-26 removed) → + one week = **Monday 2027-04-05**.
- **Both gates, projected on measured counts — the 80-night floor binds and it is met inside the primary window.** At `r` the registered window projects ≈ **92** contributing nights (≈ 49 CLUSTER / ≈ 43 DIVERSIFIED) against a floor of 80 and 20-per-arm; at `r₉₀` ≈ **80** / ≈ 41 / ≈ 36. DP-24's 30 post-lock nights land near session **90** (≈ 2026-11-10) at `r`, or session **96** (≈ 2026-11-19) at `r₉₀` — the Steward's own ≈ 2026-11-10 projection, and ≈ **59** post-lock contributing nights are projected by the window end (≈ 52 at `r₉₀`), so the boundary question of whether the 2026-09-14 pick night itself counts as post-lock is immaterial; `eval.py` decides it mechanically by comparing `finished_at` to the lock commit. **PROSPECTIVELY_CONFIRMED is reachable from this run by design, DP-31 does not apply, and no successor replication question is needed** — as §5 anticipated. The gates still fire on `eval.py`'s counts at the pass, never on this projection (DP-13).
- **Why the window is not 115 sessions.** At the point estimate the 80th contributing night lands at session 115 (2026-12-17) and the projection is **80.0 against a floor of 80** — a coin flip, which would make **DP-13's single extension the plan rather than the fallback for a measured shortfall**. The drafted end (2026-11-13, 92 sessions) is worse still: ≈ 64–66 of 80, short by construction. Sizing at the lower bound of the measured rate is the only reading on which "the floor is projected met inside the primary window" is true with margin rather than by luck, and it is the option that reaches the decision date **later** (DP-45). The floors themselves are untouched: the margin is a sizing convention, not a gate, and no gate was reduced to fit any date.
- **Lock-or-DEFER, settled (item 9).** Rarer arm **0.326 ≥ 0.085** and eligible nights **0.696 ≥ 0.30** — both lines clear by 2.3–4.2×, so the DEFERRED branch does not fire and **Q022 LOCKS**. Item 3's start rule fired and moved the start **out** to 2026-07-08; §10.9 did not fire (`66cb63d` ships 2026-05-14, ~8 weeks pre-window). Item 9's **second** branch applies — measured eligible rate 0.696 is **slower** than the 0.85 planning rate, so **both dates move out**; the rarer-arm *share* of eligible nights came in **faster** than plan (0.469 vs 0.25) and, correctly, licenses nothing (DP-45).
- **Suppression at lock (item 14), fixed now and never revisited.** Counts only: **every non-XLK `dominant_sector_t` cell** (XLF ≈ 9, XLV ≈ 3, the other eight sectors zero; **XLK ≈ 37 clears**) and the dose–response cells **`m_t = {5}`** and **`m_t = {6+}`** (≈ 9 each; `{2,3}` ≈ 43 and `{4}` ≈ 32 clear). **UNDETERMINED is a measured zero**, printed as such. Both arms clear 20 by a wide margin and **neither is demotable** — an arm shortfall fires the gate, never a demotion (item 6). Direction, band, lane, tape stratum and regime cells are **not** pre-suppressed and are **not** loose: the standing ≥ 20-measured-night rule in §4/§5 binds them in every branch. Suppression restricts affirmative reporting only and never touches §8 clauses 6 or 7.
- **DEFERRED fallback.** If any gate is still short after the single extension at **2027-04-05**, Q022 goes to `research/questions/DEFERRED.md` with the measured counts. No second extension, no reduced gate, and a gate shortfall is **not** INCONCLUSIVE (§8). On the measured rate this is remote; it is kept because the gate fires on counts, not on a rate.
- **The one live route back to DEFERRED, sized.** A **dated** platform ship inside the window that changes **which picks are published** — the publication predicate, the publication floor, the slate size — changes the **arm itself**, and fires item 3's start rule: the start moves to the first pick night after the ship and the whole schedule is recomputed at the measured rate. A restart costs the full **132 + 20 sessions + one week ≈ 227 calendar days**, so the latest survivable ship is ≈ **2027-01-30** (227 days before the 2027-09-14 ceiling). **That date falls after the window closes, so every in-window ship is survivable — barely:** a ship on the last in-window night, 2027-01-13, recomputes to a decision near **2027-08-30**, about two weeks inside the ceiling. A ship after the window end changes no night that enters the study and fires nothing. The sweep was clean at **2026-09-13** (main HEAD = `fa70688`); R2 carries it forward, and a ship is caught by R2's timestamp check rather than discovered at the pass.
- **Family bookkeeping re-checked at `record`.** F4's correction set stays at **10** primaries (Q007 4, Q008 3, Q013 2, Q022 1): Q023, registered in the same cycle, is **F2 Calibration**, and Q020/H-033 is still DEFERRED pending a DP-05 rule-14 grant. Item 8's clause is unchanged — the set never shrinks below 10, and it rises if Q020 returns and locks before 2027-02-22.
- **Not DEFERRED now.** The initial decision date is 5.4 months out, inside DP-43's 12-month ceiling, so the ceiling clause does not fire.

## Standing rules added

_none._ Nothing here is Haci's word: item 1 was **DEFAULTED** under DP-43, which adds no DP entry (DP-40), and items 2–9 are applications of DP-25, DP-45, DP-06/DP-50(a), DP-05/DP-41, DP-44/DP-20, DP-43, DP-21, DP-22, DP-29 and existing precedent. **At `record`: still none** — a DEFAULTED item never becomes a DP entry whatever the measurement says about it, and items 10 and 12–16 are applications of DP-43, DP-45, DP-13, DP-21, DP-23, DP-50(a)/(c) and rules 3 and 9, not new policy. The generalisable ones are listed below as **proposals**, for him to confirm or drop.

## Standing rules proposed

- **A question whose arm label comes from a static repo artifact pins the blob *in force at the window start*, not the blob at the manifest's platform SHA.** The manifest SHA is dated inside the window by construction, so pinning it can import a reclassification the desk could not have known — a look-ahead that no validator would catch, because the file is not a table. Q022 is the first question to read one; it will not be the last (`sp500_sectors.json`, exemplar libraries, any committed config the desk conditions on).
- **A night-level arm whose cut is stated as "m of k" takes the hypothesis's literal denominator; a relative cut is a separate hypothesis.** DP-25 already says the cut is not re-united; the addition is that with an *absolute* cut a short (or long) slate is mechanically pushed toward one arm, so mixing denominators confounds the arm with slate size. Worth stating once so it is not re-derived per question.
- **A two-arm contrast has no demotable arm.** DP-43's demotion clause is written for sub-cells; applied to an arm it would leave a primary endpoint with one side. Q019 and Q022 each decided this separately — the arm shortfall is a floor failure (extension, then DEFERRED), never a demotion to descriptive.

_Added at `record` (2026-09-13) — proposals, not policy; a DEFAULTED or record-only item adds no DP entry (DP-40):_

- **A window is sized to the floor at the *lower bound* of the measured rate, not at its point estimate — because DP-13's extension is a fallback for a measured shortfall, not a plan.** A window sized to the point estimate has a ~50% chance of firing the extension, which costs a second successor freeze pair, six more weeks, and turns a one-pass design into a two-pass one. Q022's point-estimate window (115 sessions) projected 80.0 against a floor of 80; the lower-bound window (132) projects ≈ 92. The convention is one line — "size at the one-sided 90% lower bound of the measured per-session rate" — it only ever moves a date **out**, and it is the difference between a schedule and a hope. Worth a DP entry so the next question does not re-argue the margin from scratch.
- **A suppression list fixed at lock covers only the cells the exposure report actually counted; the rest fall to the standing ≥ 20-measured-night rule, and the exposure request should say which cells it must break out.** Q022's R1 returned `dominant_sector_t` and `m_t` but not direction, band or lane, so three of the draft's "expected suppressions" could not be fixed at lock and had to be left to the measured rule. Nothing was loosened, but the split is avoidable: the sub-cell list belongs in the counts-only request as an explicit enumeration, the way the arm split and the `k_t` tails already are.
- **An artifact the arm label depends on is pinned by sha256 and never supplemented by the desk, even where the gap is obviously a formatting bug.** BRK.B and BF.B fail Q022's mapping because the bootstrap script does not carry dual-class period suffixes — an error anyone can see. Hand-mapping them would be a classification rule invented after seeing which symbols failed, would break the pin that makes the study reproducible, and would move the bias off its disclosed one-directional side. The correct response is a **platform** enhancement (fix the bootstrap, or write a native sector field on the candidate row — §9 already names it), never a desk-side patch inside the study.
