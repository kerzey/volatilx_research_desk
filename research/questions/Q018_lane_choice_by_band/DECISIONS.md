# Q018 — decisions before lock
Run: 2026-09-13 by decision-maker (autonomous) · Source: PREREG.md "Open decisions before lock", 3 items (+2 routed requests named in §5: R1 counts-only, R2 successor freezes)
Record: 2026-09-13 by decision-maker (autonomous) from `research/reports/STEWARD_Q018_exposure.md` (R1 returned in full) and the two dated 2026-09-13 entries in `research/data/DATA_NOTES.md` — **R1 is settled (item 7)**, three record-only items are added (items 9, 10, 11), R2 is extended and R3 opened, and `## Schedule` is final. **No date moved** (item (4) reconciliation is an exact match to the borrowed rates). R2 stays open by design, due at the decision date, and blocks nothing. **Nothing is left pending or provisional; Q018 is ready for `@registrar apply Q018`.**

State checked: `state.json` = `PREREG_DRAFT` (registrar, 2026-09-13). Of the three open items, **two met a DECIDE ground** (item 2 on DP-42 → DP-03(b) with Q012 §2 locked precedent; item 3 on rule 14 with Q012 §4 / Q014 §2 locked precedent) and **one is R-3 territory** — the primary window start, which buys blindness with roughly four more months of Haci's waiting — and is **DEFAULTED** under DP-43 for the board. Three further items are decided here because the numbers they need already exist; two requests are routed.

**Does `record` need a fresh exposure count? No.** `research/reports/STEWARD_Q012_exposure.md` §2 measures the **stage-for-stage identical** eligibility funnel (three lane plans present → all six targets present → no level at/through `C_t` → `outcome_target_invalid` null → ≥ 60 daily bars ≤ t → session t+1 bar exists), on the same pinned freeze, with the same `exclusions_v003.json`, and its contributing-night unit (≥ 1 eligible LOW-or-HIGH pick) and both-band unit are the units Q018's P1–P3 and P4 use. Its three rates — **0.9429** contributing, **0.6000** both-band, **0.2857** ELITE nights per elapsed session — are therefore Q018's rates, and no Q018 window night exists in any freeze yet (the window opens 2026-09-14), so a fresh count could not measure the population in any case. The Q018-specific R1 is **counts only and decides nothing**; the schedule below is filled now, from the borrowed numbers, and is re-confirmed session by session by the Steward when the successor freezes are built (**out, never in** — DP-43, DP-45).

| # | Decision | Bucket | Choice | Basis |
|---|---|---|---|---|
| 1 | Primary window start | **DEFAULTED** | **Option A** — primary window is **pick nights ≥ 2026-09-14 only** (fully blind, post-lock); the 2026-06-01..2026-09-10 version of every primary is printed **only** as an explicitly labelled post-hoc descriptive panel that enters no verdict, no half, no stratum test and no q. Decision date ≈ **Monday 2027-07-12**. | DP-43; not taken: window open from 2026-06-01, sealed part descriptive, decides ~4 months sooner |
| 2 | Entry basis for all three plans | DECIDED | **Option A** — the session t+1 **official** regular-session open `O_1` (`prices_daily_split.o`), identically for picks and for every B2 control against its own `O_1`; the lane plans' own printed `entry` levels are never used to fill (that is Q011's subject) and are reported descriptively; `C_t` (DP-03(a) proxy) and `E_AH` stay printed as the B4 reconciliation sensitivity and decide nothing. | DP-42 ("entry basis by DP-03 / DP-11") → **DP-03(b)**, the next-session open; **DP-11 does not apply** — its trigger is a position *already held* when the signal appears, and no lane plan here is held before entry, and DP-03(a) is scoped to his after-hours buy on **elite (90+)** picks, which is not this question's population (80–90); **Q012 §2/§4 locked** uses `O_1` as "the entry basis for every trade", and Q018 is Q012's F6 sibling on the same picks |
| 3 | A lane target at or through `O_1` at entry | DECIDED | **Option A** — that half of the position is recorded **unfillable** (never a hit), rides to the lane's own window close `C_{t+W}` and is counted per lane; the pick stays in all three arms. Option B (drop the pick from every arm) is printed as a **named sensitivity** with its counts and B-restricted estimates, and **never decides**. | **rule 14 + PREREG §6's own enforcement** — option B filters *eligibility* on a session t+1 price, and §6 requires the eligible-pick set to be written before any t+1 bar is loaded, so B is not available without a rule-14 carve-out that DP-41 would not grant; **DP-26** (a gap-through at entry is never a hit); **Q012 §4 locked** ("unfillable … recorded, counted, and closed with the remainder") and **Q014 §2 locked** ("Excluding them would filter the denominator on a session-t+1 price (rule 14)"). A post-hoc restriction printed as a sensitivity is not eligibility filtering and is allowed |
| 4 | Exclusions inside a window that postdates every exclusions file | DECIDED | `eval.py` unions **`research/data/exclusions_v003.json`** with the **successor exclusions file issued with the successor selection freeze** (`exclusions_v00N`, the identical three lists built by the identical criteria, covering the post-lock nights only). The successor file may **only add** nights; no night is ever removed from a v003 list, and the **criteria** are fixed at this lock even though the **dates** cannot be. DP-04 continues to apply mechanically on top. Paths stay `eval.py` inputs — no hard-coded file name. | DP-22 read as "one list, built by one set of criteria" — its "cite the newest file" clause presumes a sealed window; a prospective-only window has no file at lock, and leaving the post-lock nights covered by DP-04 alone would silently drop the `uncorroborated_publication_runs` and `non_session_runs` protections that the sealed period gets. Add-only, so the set can never be loosened after lock |
| 5 | Sub-cell demotion at lock (DP-43) | DECIDED | Nothing to demote among the arms: the three lanes are simulations of **one** pick set, so a night missing any lane is simply **non-contributing** (§2), never a thin arm. **REF (<80) is SUPPRESSED at lock** (structural — 1 published row in 579, publication floor ≈ 80). **ELITE (90+) is descriptive at lock** and carries no verdict regardless of its count; its print-or-suppress status is decided at the decision pass on `eval.py`'s own 20-night count (measured 20 cumulative nights on a declining 10 / 6 / 3 / 1 monthly trend — `STEWARD_Q012_exposure.md` §5, genuinely undetermined). Bear, regime, tape, ATR-tercile and lane-distance cells keep the §5 SUPPRESSED-below-20 rule on measured counts. **BH stays at m = 4 in every branch.** | DP-43 (demotion decided at lock, never dropped afterwards); DP-42 (elite = `overall_score ≥ 90`); DP-21 (20 per cell); the draft already reads this way — recorded so it is not re-argued at the decision pass |
| 6 | Window end, decision date, extension date, DEFERRED fallback | DECIDED | **Window: pick nights 2026-09-14 .. 2027-03-31** (137 elapsed sessions). **Decision date: Monday 2027-07-12.** One automatic DP-13 extension to window end **2027-05-12**, decided **Monday 2027-08-16**; then **DEFERRED** if P1's or P3's gates are still short, with P2/P4 shortfalls reported INCONCLUSIVE (floor). No second extension, no gate reduced. | DP-43's recipe applied to the borrowed measured rates: **P4 binds** at 0.6000 both-band nights per elapsed session → the 80th both-band night projects at session 134 = **2027-03-25**; the draft's month-end **2027-03-31** is four sessions **later** (out, never in), adds ≈ 2 both-band nights of margin and does **not** move the decision date. Arithmetic re-derived independently at this run (below). P1–P3 (129 projected) and DP-24 (all ~129 post-lock) are not binding. 9.97 and 11.1 months from lock — inside DP-43's 12-month ceiling |
| 7 | Lane-assignment, lane-distance and unfillable counts | **DECIDED (R1 returned, settled at record)** | **Settled, and no date moves.** R1's item (4) re-derives Q018's own contributing-night definitions on the frozen data and matches `STEWARD_Q012_exposure.md` §4 **exactly** — contributing **66/70 = 0.9429**, both-band **42/70 = 0.6000**, ELITE **20/70 = 0.2857** per elapsed session, month-by-month identical (Jun 19/14/14/10, Jul 20/8/8/6, Aug 21/14/14/3, Sep(partial) 6/6/6/1) — so the 80th both-band night stays at **session 134 of 137 ≈ 2027-03-25**, the window stays **2026-09-14..2027-03-31**, the decision date stays **Monday 2027-07-12** and the DP-13 extension stays **..2027-05-12 / Monday 2027-08-16**. Items (1)–(3) are folded into the PREREG as counts (Corrections, added at record): `best_timeframe` on 495 eligible 80–90 picks = **short 343 / swing 97 / long 55**, non-degenerate by band and by month (differs-from-day 30.71%, differs-from-swing 80.40%, differs-from-long 88.89%); per-lane ATR-distance quartiles day t1 median **0.367** → long t2 median **6.367**; ≥ 1 lane target at/through the t+1 open by lane = **day 14.14% / swing 2.02% / long 0.00%**. | DP-43, DP-45 (a matched rate licenses nothing and no date moves **in**); DP-50(c) (counts taken from the pinned freeze, never a live query); `STEWARD_Q018_exposure.md` §§1–4 |
| 8 | Successor freezes | ROUTED → data-steward (**open, due 2027-07-12**) | waits on **R2** (below, **extended at record** with the payload-scale screen of item 10, the model-version surveillance of item 9 and the 2026-07-06 split) — due **at the decision date**, not a blocker for lock (DP-23). It can move the §5 dates **out, never in** | — |
| 9 | The DP-50(a) mid-window ships vs the primary window | **DECIDED (record)** | **The primary is unaffected: every ship precedes the window.** `5fa3db4` (2026-07-06, `_enforce_ladder_monotonic`), `f718b34` (07-06), `1765a6f` (2026-07-07, the 80-point publication floor), `83b571d` (2026-07-09, the gpt-5.6 upgrade on the lane-plan writer file), the bear/dark-lane build-out (2026-06-28..07-06) and `a475f2b`/`fa70688` (2026-09-05, the lane-window ruling the PREREG cites and the pinned SHA) **all land before pick night 2026-09-14**, so the primary window sits on **one** side of each: no DP-06 split falls inside it, all four primaries are measured on a single feature regime, and no arm, gate, floor or date moves. Three consequences are written into the PREREG (Corrections): **(a)** the **descriptive sealed panel** (§6/§8), which does span them, is **split at 2026-07-06 into two sub-panels that are never blended** — 2026-06-01..**2026-07-03** (no cross-lane re-sort; **25.71%**, 36/140, non-monotonic ladders) and **2026-07-07**..2026-09-10 (**0.00%**, 0/355), with 2026-07-06 already an excluded night in `exclusions_v003` so the boundary is clean, and the 2026-07-07..07-08 nights additionally flagged as pre-`83b571d` writer-model nights; each sub-panel keeps the §2 screen of item 10, is SUPPRESSED below 20 contributing nights, and still enters **no** verdict, half, stratum test or q. **(b)** threat 2's "historically ~28%" is struck — in the post-ship primary window every published ladder is cross-lane monotonic **by construction**, the pre-re-sort ladder is **not recoverable** from the frozen payload, so "the share of payloads re-sorted" is **not an available in-window mitigation**; threat 2 is restated in its stronger form and rests on the ATR-distance distribution, the lane-distance terciles, B2 and clause 8. **(c)** the DP-50(a) surveillance list in §5 R2 and threat 10 must name **model/provider version changes** to `ai_agents/principal_agent.py`, not only prompt or logic edits. | DP-50(a) + DP-06 pattern (a repair splits a column into two features at the ship date — here the split falls outside the primary); the `DATA_NOTES.md` 2026-09-13 rule ("split at 2026-07-06 **or** state the blend") read at its **stricter** limb; DP-45; `STEWARD_Q018_exposure.md` §2 / §5 |
| 10 | The lane-plan price-scale mismatch — how §2 handles it | **DECIDED (record)** | **The stricter option: a hard §2 eligibility screen that drops and counts, not a computation-hygiene rule.** A pick is eligible only if its payload's own price levels are on the same scale as `C_t`, tested **before any session t+1 bar is loaded** from **pick-night data only** (payload + `prices_daily_split` bars ≤ t), so the screen is knowledge-time-legal and needs no rule-14 carve-out (DP-41 untouched): **(i)** `ρ = median(the three lane plans' printed entry levels) / C_t` must satisfy **0.80 ≤ ρ ≤ 1.25**; a pick carrying no printed `entry` on any lane fails; **(ii)** every one of the six targets and the three stops/invalidations must satisfy **0.5 ≤ level / C_t ≤ 2.0**; **(iii)** ATR backstop: the nearest target's distance `d = dir × (T − C_t)/ATR` ≤ **10 ATR** and the furthest ≤ **25 ATR** (measured clean maxima are 3.325 and 16.163 — `STEWARD_Q018_exposure.md` §2). A pick failing **any** limb is **dropped from all three arms and from B2, counted in the funnel under its own reason code `payload_scale_mismatch`, and reported by symbol, band and month**; it is **never rescaled, repaired or imputed** (§2's measurement-failure treatment), and a night losing > 25% of its otherwise-eligible picks to the screen is dropped under §2's existing 25% rule. The screen applies identically to the sealed descriptive panel. Measured sealed-period incidence: **17 of 495 picks (3.4%)** — APH 14/14 at ~2.0×, KLAC 10.18×, CRWD 4.07×, MNST 2.02×. | `DATA_NOTES.md` 2026-09-13 ("lane_plans price levels on the wrong split scale"; its own rule says screen the ratio before trusting it — the eligibility form is the stricter reading and is taken, DP-45); rule 14 + §6's write-the-eligible-set-first enforcement (pick-night inputs only, unlike item 3's t+1-price filter); §2's "excluded and counted, never back-filled or imputed"; PI-003 precedent (a corrupted platform price quantity is not used, not corrected) |
| 11 | The F6 correction-set size (cross-question flag from Q019's decide pass) | **DECIDED (record)** | **§7 must read 15, not 11**, itemised: **Q003 (2) + Q009 (2) + Q012 (3) + Q015's 2 F6-companion primaries + Q018 (4) + Q019 (H-050, 2) = 15**; **Q010 excluded** as Q009's prospective replication; H-051 counted once, in F6. The draft's 11 omits **Q003's 2** (`Q003_repeat_selection/PREREG.md` header and §7 file it in F6) and **Q019's 2** (locked in this same desk cycle, and Q019 §7 already counts Q018's 4 — the count must be symmetric). §7's standing clause is kept: the set is **recomputed at the decision pass over the F6 primaries locked by then**, and **m never falls below 15**. | DP-29 (family by the primary endpoint's subject; H-051 once); `Q019_conviction_monitor_exit/DECISIONS.md` item 6 and Q019 §7; DP-45 — of {13, 15} the larger m is the stricter BH bar and is taken |

## Corrections to silent choices

Four corrections. Three are bookkeeping in the exposure numbers the draft borrows; one applies a policy entry added **today** that the draft's §9 does not yet carry. Everything else was checked against the policy and already matches.

- **§5, second bullet — the run-rate denominators are mislabelled.** The draft prints "**66 of 67 contributing nights = 0.9429 per session**" and "**42 of 67 both-band nights = 0.6000 per session**". Both rates are correct but the denominator is not 67: `STEWARD_Q012_exposure.md` §4 computes them over **70 elapsed sessions** (2026-06-01..2026-09-09), of which 67 are non-excluded — 66/70 = 0.9429 and 42/70 = 0.6000, while 42/67 = 0.6269. Must read "**66 of 70 elapsed sessions (67 non-excluded) = 0.9429 per elapsed session**" and "**42 of 70 = 0.6000 per elapsed session**", and the projection line must state that the window's **137 is likewise elapsed sessions** (calendar minus holidays, exclusions **not** pre-removed), which is the matching basis. This is not cosmetic: recomputing on 67 would give 0.627, pull the 80th both-band night forward and shorten the window — the move DP-43 and DP-45 forbid.
- **§5, second bullet and the header — the funnel figure conflates two tests.** "517 eligible of 541 published rows (3.0% removed by the six-level test)" reads as 24 rows removed by that test. The measured breakdown is **16 of 541 (3.0%) removed by the ladder/six-target test** (8 carrying no lane plan at all, all on 2026-06-02; 8 with a lane present but a target missing) and a **further 8 by the wrong-side-of-`C_t` and `outcome_target_invalid` tests** — **24 in all (4.4%)**. Print it that way in the header and in the results funnel; §2's exclusion bullet and §10 threat 13 already have it right.
- **§2 and §5 — the exclusions clause as drafted cannot cover the window** (item 4 above): "`exclusions_v003.json` (or its successor, cited at the decision pass only if issued *before* this file is locked)" leaves every post-lock night covered by DP-04 alone, because no successor file can exist before a lock that precedes the whole window. Replace with item 4's union rule (v003 ∪ the add-only successor file built by the identical criteria, issued with the successor selection freeze, path passed to `eval.py` as an input).
- **§9, the implementer bullet and the `PLATFORM_ISSUES.md` filing — two sentences required by DP-49 and DP-50(b) that postdate the draft's §9.** (a) **DP-49:** every check the `recommended_lane` brief hands the **coding agent** must be satisfiable from the platform repo alone (test suite, pure-function import, `git show --stat`, a file diff); anything that reads or writes a table belongs in the brief's verification section, addressed to the Data Steward on `$RESEARCH_DB_URL`, or to Haci where a write is required. (b) **DP-50(b):** the "all four NULL → lane-plan generation is a candidate platform issue" filing must name the in-flight constraint in the PI entry itself — a repair to the lane-plan writer (`ai_agents/principal_agent.py:546-554`, `:897-911`), to `_enforce_ladder_monotonic` (`services/super_agent_select_service.py:195-215`), to `best_timeframe` or to the publication predicate is **flag-off until Q018's decision date** (the PI-011 / Q010 pattern), and if it ships inside the window anyway it takes a dated `DATA_NOTES.md` entry and the column becomes **two features split at the ship date** (DP-06 pattern, DP-50(a)). §10 threat 10 and §5 already state the desk-side half; the PI filing is where the constraint has to be written down.

### Added at record (R1 returned + the two 2026-09-13 `DATA_NOTES.md` entries) — the Registrar applies these with the rest

- **§7, the F6 correction set — 11 → 15** (item 11). Replace "Q009 (H-032, 2) + Q012 (H-066, 3) + Q015's 2 F6-companion primaries + Q018 (H-051, 4) = **11**" with "**Q003 (2) + Q009 (H-032, 2) + Q012 (H-066, 3) + Q015's 2 F6-companion primaries + Q018 (H-051, 4) + Q019 (H-050, 2) = 15**; Q010 is excluded as Q009's prospective replication; H-051 is counted once, in F6." Keep the existing clause that the set is computed at the decision pass over the F6 primaries locked by then, and add that **m never falls below 15**. *Reconciliation with `Q019/DECISIONS.md` item 6, which says Q018 "should read 13":* 13 is the set with Q003 restored but **without Q019 itself**; Q019 is registered and locked in this same desk cycle and its own §7 already counts Q018's 4, so the symmetric — and, for a BH bar, the stricter — number is **15**, and the never-shrinks clause makes the difference immaterial at the decision pass in 2027. Q019 §7 already reads 15 and needs no change; no locked file is edited, because Q018 is still `PREREG_DRAFT`.
- **§2, a new eligibility bullet — the payload price-scale screen** (item 10), placed with the other pick-night exclusions and *before* the t+1 bullets, with its three limbs, its reason code `payload_scale_mismatch`, the "never rescaled, repaired or imputed" clause, the > 25% night-drop clause, and the measured sealed incidence (17 of 495 picks, 3.4%; APH 14/14 ~2.0×, KLAC 10.18×, CRWD 4.07×, MNST 2.02× — `DATA_NOTES.md` 2026-09-13). **§4** adds one line: a pick dropped by the screen takes its B2 control set with it, so the paired arms keep one pick set. **§6's knowledge-time table** gains a row — *payload lane levels vs `C_t` / ATR14, `sas_candidates.public_payload_json` + `prices_daily_split` bars ≤ t, available pick night 16:05 ET, use: eligibility* — and §6's enforcement sentence explicitly covers the screen, which is computed with the eligible-pick set before any t+1 bar is loaded. **§10** gains threat **15**: the printed lane levels are not always on the split-adjusted scale, the wrong-side-of-`C_t` test does not catch it, the screen is what catches it, and the four named symbols are not asserted to be the complete list (no full sweep has been done).
- **§10 threat 2 and §4's secondary list — strike "historically ~28%"** (item 9). Threat 2 must read: the share is **two regimes, not one — 25.71% (36/140) before 2026-07-06 and 0.00% (0/355) on/after**, the ship being `5fa3db4` (`DATA_NOTES.md` 2026-09-13; `STEWARD_Q018_exposure.md` §2); **the primary window lies entirely in the post-ship regime**, so every published ladder in it is cross-lane monotonic **by construction** and the pre-re-sort ladder is not recoverable from the frozen payload. Therefore §4's secondary "the fraction of payloads whose flattened ladder was re-sorted across lanes" is **not measurable in-window** and is restated as: measured **only** on the sealed panel's pre-2026-07-06 sub-panel, and replaced in-window by (a) the per-pick cross-lane ordering check on the window's own payloads (expected 0% non-monotonic; printed as a verification, not a mitigation), (b) the per-lane ATR-distance distribution and (c) the lane-distance tercile cells. Threat 2's conclusion becomes **stronger, not weaker**: in this window "day" *is* the two nearest levels for **every** pick, by code, so a CONFIRMED lane result is a statement about nearest / middle / furthest target pairs on three different clocks, and only B2 (identical ATR distances, identical windows, no selection) with clause 8 separates that from a selection edge. §1's paragraph citing `_enforce_ladder_monotonic` ("in a large share of payloads") is corrected the same way: in the primary window it is **all** payloads.
- **§6 and §8 — the sealed descriptive panel is split at 2026-07-06 and never blended** (item 9a). Two sub-panels: **2026-06-01..2026-07-03** (pre-`5fa3db4`, no cross-lane re-sort, 25.71% non-monotonic) and **2026-07-07..2026-09-10** (post-ship, 0.00%), 2026-07-06 being an excluded night in `exclusions_v003` so the boundary needs no tie-break; the 2026-07-07..2026-07-08 nights are flagged as the last nights before `83b571d`'s writer-model upgrade. Each sub-panel carries the §2 screen, is SUPPRESSED below 20 contributing nights, and enters **no** verdict, half, stratum test or q — unchanged in that respect. The `DATA_NOTES.md` rule's alternative limb ("or state explicitly that it is reporting a blend of two mechanisms") is **not** taken.
- **§5 R2 and §10 threat 10 — the DP-50(a) surveillance list names model/provider version changes** (item 9c). `83b571d` (2026-07-09, "upgrading agents to gpt 5.6") shows that a model upgrade on `ai_agents/principal_agent.py` lands as an ordinary commit and changes lane-plan generation even against an unchanged prompt; the diff could not be inspected (`git show`/`git diff` blocked for the platform repo this session), so it is flagged, not asserted. Both places must therefore say "any change to the lane-plan writer **including its model or provider version**", and the Red Team item stands: confirm whether `:546-554` / `:897-911` changed on that commit.
- **§5, the R1 paragraph — R1 is answered.** Replace "Routed request R1 … NOT a blocker for lock" with a citation of `research/reports/STEWARD_Q018_exposure.md` (2026-09-13) and its measured counts: 541 published rows on 67 non-excluded nights of 70 elapsed sessions; 24 removed (4.4%) = 16 on the ladder/six-target test + 8 on wrong-side-of-`C_t` (7) and `outcome_target_invalid` (1); **517 eligible = 495 LOW/HIGH + 21 ELITE + 1 REF**; `best_timeframe` short 343 / swing 97 / long 55 with the three differs-from-lane shares (0.3071 / 0.8040 / 0.8889); the per-lane ATR-distance quartiles (day t1 0.237/0.367/0.526 … long t2 5.151/6.367/8.071, on 478 picks after the item-10 screen); the at/through-open shares day 14.14% / swing 2.02% / long 0.00%; and the item (4) reconciliation as an exact match. **§3's B3 paragraph** cites these counts instead of the weekly 2026-09-12 §4 figures (474/160/65), which are a **different population** (the W20 population, not the six-level-eligible 80–90 cohort) and may be quoted only as a shared "skewed short" shape. **§10 threat 11** is answered by the same numbers: `best_timeframe` is **not** degenerate — all three lanes appear in both bands and every month — and B3 stays descriptive.
- **§10 threat 14 and §11 item 3** — the at/through-open counts are now measured, not expected: **day 14.14% (70/495), swing 2.02%, long 0.00%**; by band HIGH 13.70% / LOW 14.22% on the day lane; excluding the 17 scale-mismatch rows moves day to 14.64%, so the finding is not an artefact of the scale issue. Item 3's B-sensitivity (drop the pick) therefore removes ≈ 14% of picks and is printed with that count.
- **Cross-question flag for the Red Team, not an edit — `Q012_capital_recycling/PREREG.md` §7 (locked, DATASET_PINNED).** It states "F6 holds H-050, H-051, H-032 and H-066" and "registered F6 questions at registration: Q009 (2) … and Q012 (3)", omitting **H-052 / Q003 (2 primaries)**, which was locked on 2026-09-11, two days **before** Q012 — so its F6 correction set is undercounted by 2 at registration. Q003 §7 and Q009 §7 both list H-052 correctly. **The locked file stands** (rule 3; DP-22's principle); Q012's §7 also says the set is applied by the Reporter across the family's registered primaries, so the Reporter should compute Q012's F6 q over the corrected set at its decision pass. Flagged here for the Red Team's Q012 review; no Q012 file is edited from this run.
- **§2, the REF bullet** — "1 published row in 579" gains its cause: the single sub-80 published row (**TJX, 2026-06-11, 79.58**) **predates** the formal 80-point floor, which `1765a6f` codified on 2026-07-07 (`SuperAgentSelectConfig.publication_floor = 80.0`); no sub-80 bull was published after it. REF is therefore structurally absent in the primary window **by code**, not merely by observation — which is the reason it is SUPPRESSED rather than merely thin.

Checked and **not** corrections — each already matches the policy: MPE **0.25 ATR per trade** on P1–P3, the standing number for a per-trade ATR-denominated endpoint (DP-10, DP-44) and **0.50 ATR on P4**, larger with the stated difference-of-differences reason and never smaller (DP-10; Q012 P2 / Q007 §8 precedent); no money-unit MPE invented or asked (DP-44); sessions-to-first-touch descriptive with no MPE (DP-44); floors read as **≥ 80 contributing nights per primary endpoint** and 20 per cell, the stricter reading, with P4 counting both-band nights (DP-21); **≥ 30 post-lock contributing nights** never reduced and satisfied by construction (DP-24), so DP-31 is correctly **not** invoked; publication predicate `qualified IS TRUE AND selected_rank IS NOT NULL` with dark-lane rows out of every arm but retained in the B2 pool (DP-28); newest exclusions file cited and read from JSON with no hard-coded dates (DP-22); family **F6** by the primary endpoint's subject, H-051 counted once, Q010 excluded as a replication, F6 = 11 primaries consistent with Q012 §7 and Q015 §7 (DP-29); stops measured and never used as exits, with the stop-exit version descriptive and same-session ties resolved stop-first (DP-02, DP-27, DP-30 not engaged); elite = `overall_score ≥ 90` (DP-42); units and cuts taken from H-051 as written, with "realized return" rendered as the plan's realized ATR per trade under DP-01 rather than re-united (DP-25); registrar conventions fixed before any outcome is seen — the 50/50 lane shape, the 20-session block length, the band cuts, the expanding-window tape terciles (DP-26); successor freezes covering **all** candidate symbols plus hourly bars for published symbols (DP-23); every 20/40/60-session basis labelled NON_QUOTABLE (rule 12); **no rule-14 exception requested or needed** — the lane plans are written before `finished_at` (`services/super_agent_select_service.py:155-161`), so DP-05 is untouched and DP-41 respected; DP-50(a)/(b)/(c) carried in §5 and §10 threat 10, with the exposure count taken from the pinned freeze and never a live query.

## Defaulted on Haci's behalf

- #1 Primary window start — chose **post-lock only, pick nights ≥ 2026-09-14, decision Monday 2027-07-12**; not taken: window open from 2026-06-01, sealed part descriptive, decides ~4 months sooner — DP-43. Overturn = successor question.

Nothing else was defaulted. Items 2 and 3 each met a DECIDE ground (DP-42 → DP-03(b) with Q012 §2 locked; rule 14 with Q012 §4 and Q014 §2 locked), and items 4–6 are bookkeeping or the mechanical DP-43 recipe on numbers already measured. Item 1 is the one place where the desk spent roughly **four more months of his waiting** to keep the test blind: the sealed nights have already been read for this exact hypothesis — the weekly snapshot of 2026-09-12 §4 ranked the three lanes on Jun–Aug outcomes and that ranking is written into the H-051 BACKLOG line, on top of the standing "short DTE underperforms" finding — so option B would decide sooner on nights that cannot serve as a blind test of a lane ordering the desk has seen. DP-43 forbids the shorter route to a date, and DP-45 forbids taking it because the date comes sooner. No floor, arm or gate was weakened anywhere in this file.

**At record (2026-09-13): still one — item 1, unchanged.** Nothing new was defaulted. Items 7–11 were settled on DECIDE grounds: item 7 by DP-43's arithmetic on a reconciliation that came back an **exact** match (so no date could move even in principle), item 9 by the DP-06 pattern under DP-50(a) on ship dates that all fall outside the window, item 10 by the stricter limb of a rule the Steward has already written into `DATA_NOTES.md`, item 11 by DP-29 with Q019's locked-in-the-same-cycle count. Two places where a looser choice was available were resolved by rule rather than preference: the price-scale mismatch could have been handled by rescaling the payload or by screening only the distance computation (both keep more picks — **not taken**; the pick is dropped and counted), and the F6 set could have been left at 13 (a lower BH bar — **not taken**; 15 stands). Item 1's default is what makes item 9 cheap: because the window starts after the lock, every DP-50(a) ship the Steward found falls **outside** the primary, and the mid-window feature split that would otherwise have forced a DP-06 cut through the arms does not arise.

## Routed requests

### data-steward — R1 (counts only) — **RETURNED 2026-09-13**, `research/reports/STEWARD_Q018_exposure.md`

Answered in full, counts only, no outcome read, from the pinned freeze (DP-50(c)). Headline numbers used at record: 541 published rows / 67 non-excluded nights of 70 elapsed sessions; 24 removed by the six-level test (4.4%) → **517 eligible = 495 LOW/HIGH + 21 ELITE + 1 REF**; `best_timeframe` **short 343 / swing 97 / long 55**, non-degenerate by band and month; per-lane ATR-distance quartiles (day t1 median **0.367** → long t2 median **6.367**, on 478 picks after the item-10 screen); at/through-open shares **day 14.14% / swing 2.02% / long 0.00%**; item (4) reconciliation an **exact** match to `STEWARD_Q012_exposure.md` §4 (0.9429 / 0.6000 / 0.2857, month-by-month identical) — **no date moves**, and a matching rate could not have moved one **in** in any case (DP-43, DP-45). Two findings beyond the request: the `_enforce_ladder_monotonic` regime split at **2026-07-06** (25.71% → 0.00%) and the `lane_plans` **price-scale mismatch** (17 of 495 picks; APH 14/14 ~2.0×, KLAC 10.18×, CRWD 4.07×, MNST 2.02×), both now dated entries in `DATA_NOTES.md` and both handled in items 9 and 10. The request as sent:

Q018 (lane choice by score band, PREREG §5 request R1) needs a **counts-only** measurement on the **frozen** data — `research/data/manifest_v001.json` + `research/data/manifest_prices_v001.json`, never a live query (DP-50(c)) — with **no outcome of any kind**: no touch, no plan result, no return, no excursion, no lane comparison, and no bar dated after a pick's own session t+1 read for anything but establishing that a bar *exists*. Population: published picks (`qualified IS TRUE AND selected_rank IS NOT NULL`, DP-28; dark-lane rows excluded from every arm but retained in the B2 pool) on pick nights **2026-06-01 .. 2026-09-09**, with the nights in `research/data/exclusions_v003.json` (`manual_runs` ∪ `non_session_runs` ∪ `uncorroborated_publication_runs`) and DP-04 re-run nights removed, restricted to the **80 ≤ `overall_score` < 90** picks that pass the six-level eligibility test `STEWARD_Q012_exposure.md` §2 already defines. Please return: **(1)** the `best_timeframe` distribution among those eligible picks, **by band (LOW 80–85 / HIGH 85–90) and by month**, plus the share of picks whose assigned lane differs from each fixed lane — this is B3's non-degeneracy check against the weekly 2026-09-12 §4 counts (474 short / 160 swing / 65 long) and it decides nothing, since B3 is descriptive; **(2)** the per-lane **ATR distance** distribution of each lane's t1 and t2, `d = dir × (T − C_t) / ATR` with **ATR14 desk-computed from `prices_daily_split` bars dated ≤ t** (the platform's `atr_pct` is split-corrupted — PI-003 / DATA_NOTES — and must not be used), reported per lane as quartiles plus the day/swing/long overlap share, together with the **share of published payloads whose flattened six-level ladder was not price-monotonic before `_enforce_ladder_monotonic` re-sorted it** (`services/super_agent_select_service.py:195-215`; the historical figure the desk carries is ≈ 28%, and §10 threat 2 turns on it); **(3)** the share of eligible picks with **≥ 1 lane target at or through the session t+1 official open**, **by lane** — a counts-only fact that feeds the §11 item 3 sensitivity and, per DECISIONS item 3, never enters eligibility; and **(4)** one reconciliation line the schedule rests on: re-derive, on **Q018's own** contributing-night definitions — a contributing night carries ≥ 1 eligible 80–90 pick **gradeable in all three lanes**, and a both-band night carries ≥ 1 such pick in **each** of LOW and HIGH — the contributing-night, both-band and ELITE rates **per elapsed session over the 70 elapsed sessions 2026-06-01..2026-09-09**, and confirm they equal `STEWARD_Q012_exposure.md` §4's **0.9429 / 0.6000 / 0.2857**; if any differs, state by how much and recompute the 80th both-band night, because that rate is what sets Q018's window end (2027-03-31) and decision date (Monday 2027-07-12) and **a slower rate moves both out while a faster one changes nothing** (DP-43, DP-45 — a date never moves in and no floor is lowered to meet one). Please also add, under **DP-50(a)**, any commit in the read-only platform repo between 2026-06-01 and the freeze date touching the lane-plan writer (`ai_agents/principal_agent.py:546-554`, `:897-911`), `_enforce_ladder_monotonic` (`:195-215`), `best_timeframe`, `services/sas_conviction_card.py:182-195` or the publication predicate, with SHA and date — a repair there makes the lane plans two different features split at the ship date and must reach `DATA_NOTES.md` before the successor freeze is built.

### data-steward — R2 (successor freezes) — due at the decision date, **not** a blocker for lock — **extended at record (2026-09-13)**

**Three clauses added at record, to be read as part of the request below.** (v) **Payload price-scale screen (DECISIONS item 10).** When the successor selection freeze is built, please report — counts only, alongside the freeze — the number of in-window picks failing the §2 scale screen (`ρ = median(lane entries)/C_t` outside [0.80, 1.25]; any of the six targets or three stops/invalidations outside [0.5, 2.0] × `C_t`; nearest-target distance > 10 ATR or furthest > 25 ATR), **by symbol, band and month**, and flag any night losing > 25% of its otherwise-eligible picks to it. These rows are dropped and counted, never rescaled or repaired; a rising incidence is a platform issue (a PI filing distinct from PI-003), not a reason to relax the screen. (vi) **Model- and provider-version surveillance.** The DP-50(a) commit report must treat a **model or provider version change** on `ai_agents/principal_agent.py` (e.g. `83b571d`, 2026-07-09, "upgrading agents to gpt 5.6") as a lane-plan-writer change in its own right, with SHA and date, even when the prompt text at `:546-554` / `:897-911` is untouched — it changes lane-plan generation against an identical prompt and would split this question's arms mid-window (DP-06 pattern). (vii) **The 2026-07-06 split is a fact about the sealed period, not the window.** `_enforce_ladder_monotonic` shipped before the window opens, so the successor freeze needs no split for it; please confirm at build time that no **new** cross-lane ladder enforcement, publication-floor or `best_timeframe` change shipped **inside** 2026-09-14..2027-03-31, and say so explicitly in the freeze report even when the answer is "none".

Q018 (PREREG §5, request R2, DP-23) needs successor freezes before its decision pass on **Monday 2027-07-12**. Please build and pin `manifest_v00N` (selections; the same SQL and the same exclusion criteria as v001) and `manifest_prices_v00N` (the same Alpaca queries) covering pick nights **2026-09-14 .. 2027-03-31**, and — only if the single DP-13 extension fires — a second pair covering **.. 2027-05-12**, built then and not before. Four scope requirements, none optional: **(i)** the daily symbol list covers **every candidate symbol on every in-window night, published and unpublished** — B2's distance-matched controls need the unpublished symbols' own pick-night closes, their 60 prior daily bars (ATR14, beta60, runup20) and their own forward bars — and is never assumed from v001's symbol list; **(ii)** daily bars run **60 forward sessions beyond the last included pick night**, i.e. through the **2027-06-25** session for the 2027-03-31 window end (≈ 2027-08-09 for the extended end), since the long lane's 60-session window is the binding maturity clock for all three lanes; **(iii)** **hourly bars scoped to at least the published-pick symbols**, as `manifest_prices_v001` was — they are needed only for the descriptive stop sensitivity and the `E_AH` price, so a narrower hourly scope costs a secondary and never a primary, but say so explicitly if it changes so the report can state it; and **(iv)** a **successor exclusions file** (`exclusions_v00N`, DECISIONS item 4) issued with the selection freeze, applying the **identical three criteria** — `manual_runs`, `non_session_runs`, `uncorroborated_publication_runs` — to the post-lock nights, **add-only**: it may add nights to the set Q018 excludes and may never remove one from v003's lists. Rows whose bars are missing are **excluded and counted, never back-filled or imputed**. Please also re-confirm every §5 date **session by session from the trading calendar** when the freeze is built — the holiday list used here is 2026-11-26, 2026-12-25, 2027-01-01, 2027-01-18, 2027-02-15, 2027-03-26, 2027-05-31, 2027-06-18, 2027-07-05 — and report, under **DP-50(a)**, any platform commit between the freezes that rewrites historical rows in `sas_candidates.public_payload_json`, changes `best_timeframe`, the lane-plan writer, `_enforce_ladder_monotonic`, `BAND_EXITS` or the publication predicate, with a dated `DATA_NOTES.md` entry naming the column, the date range and the ship SHA: the successor freeze re-runs the same SQL against the same tables, so such a repair silently changes what this question's arms mean and splits the feature at the ship date (DP-06 pattern). A correction to any date may move it **out, never in**.

### data-steward — R3 (counts only, opened at record) — **non-blocking; can move the window end out, never in**

Q018 (opened at `record`, 2026-09-13, from DECISIONS item 10) asks for **one counts-only re-measurement** on the **pinned** freeze (`manifest_v001` + `manifest_prices_v001`, `exclusions_v003.json`, never a live query — DP-50(c)), at your convenience and **before** the successor freeze is built rather than at the decision date: please recompute the three rates you confirmed in `STEWARD_Q018_exposure.md` §4 — contributing nights, both-band nights and ELITE nights per elapsed session over the 70 elapsed sessions 2026-06-01..2026-09-09 — **with the §2 payload price-scale screen applied** (a pick is ineligible if `ρ = median(the three lane plans' printed entry levels)/C_t` falls outside [0.80, 1.25], if any of its six targets or three stops/invalidations falls outside [0.5, 2.0] × `C_t`, or if its nearest target sits beyond 10 ATR or its furthest beyond 25 ATR, with ATR14 desk-computed from `prices_daily_split` bars ≤ t), and report how many **nights** — not only picks — the screen removes from each of the three counts, i.e. nights whose only eligible 80–90 pick, or whose only LOW or only HIGH pick, was a scale-mismatch row. No outcome of any kind, no bar after a pick's own session t+1 beyond establishing that a bar exists. The screen can only **reduce** eligibility, so this can only slow the measured rate: if the both-band rate falls below **0.6000**, recompute the 80th both-band night and the schedule **moves out** — never in — and if it is unchanged, nothing happens and the schedule stands (DP-43, DP-45; no floor is lowered and no date is pulled forward). Please also say, from the frozen payloads alone, whether the four symbols already named (APH, KLAC, CRWD, MNST) are the complete list on this population or whether the screen catches others, since `DATA_NOTES.md` explicitly declines to assume the list is closed.

## Schedule

**FINAL — confirmed at `record` (2026-09-13) against `research/reports/STEWARD_Q018_exposure.md` §4, whose item (4) reconciliation on Q018's own definitions is an **exact** match to the borrowed rates (0.9429 / 0.6000 / 0.2857, month-by-month identical). No date moved; none could have moved **in** (DP-43, DP-45).** Written verbatim to `research/questions/Q018_lane_choice_by_band/schedule.json` by the Registrar at `apply` / lock (DP-46). R2's calendar re-confirmation and R3's screened recount may still move these dates **out, never in**.

decision_date: **2027-07-12** (Monday) · extension_date: **2027-08-16** (Monday) · hard_stop: **none** (DP-13's single automatic extension, then DEFERRED, replaces it) · rule: **exposure-driven** (the window end is set by accrual to P4's 80 both-band nights, not by a fixed calendar end; the decision date then follows from the 60-session maturity plus a one-week freeze margin, first Monday on or after) · window: pick nights **2026-09-14 .. 2027-03-31** (extended: .. **2027-05-12**) · extended: **false** · maturity: **60 sessions** (the long lane's window, binding for all three arms) · gates: **≥ 80 contributing nights per primary endpoint** (P1–P3: ≥ 1 eligible 80–90 pick gradeable in all three lanes; P4: a both-band night — DP-21) **and ≥ 30 contributing nights dated after the lock commit** (DP-24, satisfied by construction), on `eval.py`'s measured counts · ceiling: **cleared — 2027-07-12 is 9.97 months after a 2026-09-13 lock, inside DP-43's 12 months; the extended date, 2027-08-16, is 11.1 months.**

- **Arithmetic, re-derived independently at `decide` and re-confirmed at `record`.** Window 2026-09-14..2027-03-31 = **137 elapsed sessions** (Sep 13, Oct 22, Nov 20, Dec 22, Jan 19, Feb 19, Mar 22; holidays 2026-11-26, 2026-12-25, 2027-01-01, 2027-01-18, 2027-02-15, 2027-03-26). P4 at the measured **0.6000** both-band nights per elapsed session → the 80th lands at session 133.3, i.e. the **134th session = 2027-03-25**; the registered end 2027-03-31 is four sessions later (≈ 82 projected both-band nights) and does not change the decision date. P1–P3 at **0.9429** → ≈ **129** contributing nights, reached ≈ 2027-01-22, not binding; DP-24's 30 post-lock nights land ≈ mid-November 2026, not binding. Window end → 60th forward session **2027-06-25** → +1 week = 2027-07-02 → first Monday = 2027-07-05, **the observed Independence Day holiday**, so the date moves **out** to **Monday 2027-07-12**. Extension: 2027-03-31 + 30 sessions = **2027-05-12** → 60th forward session **2027-08-09** → +1 week → **Monday 2027-08-16**. The Steward's report restates every one of these as confirmed.
- **Demotion at lock, re-checked at record (DP-43).** Unchanged from item 5: nothing among the arms is demoted — the three lanes are simulations of one pick set, so a night missing a lane is **non-contributing**, never a thin arm. **REF is SUPPRESSED** (structural: 1 published row in 579, and the 80-point floor was codified 2026-07-07, so it is absent by code in the window). **ELITE is descriptive at lock** and carries no verdict; its print-or-suppress status is decided at the decision pass on `eval.py`'s own count against 20 nights — the measured sealed figure is 20 ELITE nights on a declining monthly trend (10 / 6 / 3 / 1), i.e. genuinely undetermined. **BH stays at m = 4 within the question** in every branch, inside an **F6 set of 15** (item 11).
- **What the price-scale screen (item 10) may do to these dates.** The screen removes picks, and removes a *night* only where every eligible 80–90 pick on it (or every LOW, or every HIGH) is a scale-mismatch row; sealed incidence is **3.4% of picks**. It can therefore only slow accrual, never speed it. The dates are **not** re-projected downward for it now, and they are **not** moved in on any account; if it bites, the shortfall fires the single **DP-13 extension** on `eval.py`'s measured counts, exactly as a slow tape would. R3 measures the screened rates for the record.
- **DEFERRED fallback.** If **P1's or P3's** gates are still short after the single extension at 2027-08-16, Q018 goes to `research/questions/DEFERRED.md` with the measured counts; **P2/P4** shortfalls after the extension are reported **INCONCLUSIVE (floor)**. No second extension, no gate reduced.
- **Not DEFERRED now.** The initial decision date is 9.97 months out, inside DP-43's 12-month ceiling, so the ceiling clause does not fire.

Arithmetic re-derived independently at this run. Window 2026-09-14..2027-03-31 = **137 elapsed sessions** (Sep 13, Oct 22, Nov 20, Dec 22, Jan 19, Feb 19, Mar 22, after the holidays above). P4 at 0.6000/elapsed session → 80 both-band nights at session 133.3, i.e. the **134th session = 2027-03-25**; the registered end 2027-03-31 is four sessions later (≈ 82 projected both-band nights) and does not change the decision date. P1–P3 at 0.9429 → ≈ **129** contributing nights, reached around 2027-01-22, not binding; DP-24's 30 post-lock nights land mid-November 2026, not binding. Window end 2027-03-31 → 60th forward session = **2027-06-25** → +1 week = 2027-07-02 → first Monday = 2027-07-05, **the observed Independence Day holiday**, so the date moves **out** to **Monday 2027-07-12**. Extension: 2027-03-31 + 30 sessions = **2027-05-12** → 60th forward session = **2027-08-09** → +1 week = **Monday 2027-08-16**. Both agree with the draft.

## Standing rules added
_none._ Nothing here is Haci's word: items 2–6 were DECIDED on existing entries and locked precedent, items 7–11 on existing entries plus the Steward's measured counts, and item 1 was DEFAULTED under DP-43, which adds no DP entry (DP-40). **At record: still none** — a DEFAULTED item never becomes a DP entry, and the three record-only decisions are applications of DP-06, DP-29, DP-45 and DP-50(a), not new policy.

## Standing rules proposed
- **A hypothesis whose arms have already been ranked on the sealed period by a desk report starts its primary window after the lock.** Q018's window start is not a preference about waiting; it is the only honest reading of rule 3 once a weekly snapshot has published the ordering the question tests. Stated once, it removes this R-3 from every future question whose BACKLOG line quotes an outcome.
- **A window that postdates every exclusions file unions the pinned file with an add-only successor file built by the identical criteria** (DECISIONS item 4). DP-22 presumes a sealed window; prospective-only questions are now common enough (Q010, Q018) that the rule should say so rather than being re-derived.
- **Eligibility never conditions on a session t+1 price.** A level at or through the entry is scored unfillable and the pick stays in the denominator, with the drop-it version printed as a sensitivity that never decides. Q012 §4, Q013 §2, Q014 §2 and now Q018 all decided this separately; it generalises DP-26's gap-through clause from *scoring* to *eligibility*.

_Added at record (2026-09-13) — proposed, not policy; a DEFAULTED or record-only item adds no DP entry (DP-40):_

- **A payload's own printed price levels are screened against `C_t` before they are trusted, and a failing row is dropped and counted, never rescaled.** Any study that mixes a `public_payload_json` price level with a `prices_daily_split` close or ATR applies the item-10 screen (entry-ratio band, level-ratio band, ATR backstop) as a **pick-night eligibility test**, because the platform's lane levels are sometimes on the raw rather than the split-adjusted scale (`DATA_NOTES.md` 2026-09-13; APH systematic). This is the payload analogue of PI-003's rule for `atr_pct` and belongs beside it rather than being re-derived per question.
- **A DP-50(a) commit sweep names model and provider version changes, not only code edits.** Where an LLM writes a column the desk measures, a model upgrade splits the feature exactly as a prompt change would; the sweep must report it with SHA and date even when no prompt line moved.
- **A post-hoc descriptive panel spanning a platform ship date is split at that date or not printed.** Q018's sealed panel takes the split (2026-07-06); "state that it blends two mechanisms" is the weaker limb of the `DATA_NOTES.md` rule and should not be available once a measured split exists.
- **A family correction set is stated symmetrically across the questions registered in one cycle.** Q018 and Q019 are locked in the same cycle and each counts the other's primaries; the count is recomputed at each decision pass over the F6 primaries locked by then and **never shrinks** below the number registered at lock (item 11, with Q003's 2 restored).
