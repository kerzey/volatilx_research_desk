# Q038 — decisions before lock

Run: 2026-09-15 by decision-maker (**autonomous**, DP-40..48; DP-52..58 read for design, forecasting and the interim) · Source: PREREG.md "Open decisions before lock", **4 items** (+2 routed requests named in §5.7: R1 counts-only exposure, R2 successor freezes)

State checked: `state.json` = `PREREG_DRAFT` (registrar, 2026-09-15) — in scope. **Three of the four §11 items met a DECIDE ground**; **one is DEFAULTED (R-5)** — and it is defaulted *against* the Registrar's recommendation, because the two options differ only in strictness and DP-45 takes the stricter. Four further items are **ROUTED** to the Data Steward and settle mechanically at `record` on R1's counts: the window end, the decision and extension dates and the lock-or-DEFER call (§5.6's 0.29 / 0.55 trigger), the demotion-and-suppression list (§4(d)), §8 clause 6's three-group-night evaluability, and §8 clause 7's B3 evaluability under decision 3. **R1 blocks the lock; R2 does not** (DP-23).

| # | Decision | Bucket | Choice | Basis |
|---|---|---|---|---|
| 1 | Which tier field defines the terminal state (§11.1) | DECIDED | **Option A — `overall_tier`, the undecayed tier, on the pick's latest in-scope monitor row** (`last_i`, dated ≥ t+4). `age_adjusted_severity` is **not** the classifier; it is printed as §4 secondary 3 — the same three groups built from the displayed field, with the day-5 cross-tab of the two fields, **re-measured at every freeze** so §2.3's bijection claim is measured and never assumed. §2.3, §4 and R1 item (2) stand as drafted. | **Technical ground with one defensible answer** (charter ground 3): the age decay shows days 3–5 one step softer (`services/conviction_monitor_service.py:50-56`, `:807-811`), so under B a day-5 row can never display EXIT and **`G_C` is structurally empty** — B is not a stricter option, it is an untestable one. **DP-25** — H-097 as filed asks about picks "whose tier is still EXIT", and `overall_tier = worst(polarity, technical)` (`:792-805`) is that field. **DP-26** — a convention fixed before any bar is read, with the alternative printed. **Threat 6 (§10.6)** — classifying on the decayed field would build the clock into the partition. With HOLD unreachable (PI-014) the day-5 map EXIT→WATCH, WATCH→HOLD is a bijection on the realized support, so A *is* the displayed partition relabelled — a fact R1 item (2) measures rather than assumes. |
| 2 | Extreme contrast + monotonicity gate, or four adjacent co-primaries (§11.2) | DECIDED | **Option A — `G_C − G_A` per endpoint, m = 2 within the question**, with §8 clause 6's monotonicity gate (both adjacent steps carry the extreme contrast's sign, neither beyond MPE in the opposite sign, **≥ 20 three-group nights or the gate is not evaluable and the verdict is INCONCLUSIVE**). The two adjacent contrasts `G_B − G_A` and `G_C − G_B` keep their CIs and stay §4 secondary 1, printed whether the gate passes or fails. §4, §7 and §8 stand as drafted. | **DP-20 / DP-44** — the MPE is **5.0 pp on the registered endpoint**; option B requires each adjacent step to clear 5.0 pp, i.e. **10 pp across the ordering**, which is a larger MPE on the registered contrast adopted silently and without the one-line reason DP-20 demands. **DP-25** — H-097 as filed is one ordering claim (worse … better, "with the softened group sitting in between"), not four independent tests. **Rule 8 / §7** — B raises the within-question m to 4 and F6's correction set to 19, a multiplicity change that penalises every other F6 primary for a re-specification no DP supports. **DP-57** — §5.4's MDE is 11–14 pp on the *extreme* contrast; the adjacent steps are roughly half the effect on the three-group-night subset only, so B is not a stricter test but an unpowered one. A is not the lenient option either: the gate makes a non-evaluable ordering INCONCLUSIVE, which a bare two-group design would not. |
| 3 | B3 (`S2`, the within-published `dam5` pairing): one-way blocker or two-way gate (§11.3) | **DEFAULTED (R-5)** | **Option B — two-way gate.** §8 clause 7 becomes: CONFIRMED requires B3 to clear **≥ 20 contributing nights** and to be **beyond MPE (5.0 pp) in the same sign** as the primary; **B3 inside MPE, beyond MPE in the opposite sign, or not evaluable (< 20 contributing nights) makes the endpoint INCONCLUSIVE**. B3 can still **never create** a CONFIRMED and takes no BH slot — it is a gate, not a primary, so **m stays 2** (§7 unchanged), it prints raw p only, and §5.4's power note gains one line saying the gate is a thin statistic whose failure is a likely route to INCONCLUSIVE. §3 B3, §8 clause 7, §5.4 and §10.2 take this wording. The DP-58 interim inherits it (§5.5 requires every §8 clause on the interim data). | **DP-45** — the options differ **only in strictness** (B is uniformly harder: it adds a requirement and removes none), and the charter and DP-45 both take the stricter and never the option more likely to reach CONFIRMED. **Consistency inside this file** — §8 clause 6 already treats a non-evaluable gate as INCONCLUSIVE; a second gate on the same slate reading the other way would be arbitrary. **Q019 (locked) §3/§8** — the within-published damage pairing is *decisive* in the locked sibling on the same table; Q038 moved the pool to unpublished candidates for depth (H-097), so requiring the Q019-style read to agree is what keeps the two questions commensurable. Hedge recorded, hence R-5: the Registrar's 68.7 % drop-rate argument (Q019's measured published-to-published caliper loss) is real, which is why routed item 8 makes B3's projected evaluability an R1 number **before** the lock rather than a surprise at the decision pass. **Not taken: one-way blocker (contradiction only).** |
| 4 | `G_B` when a pick softens and re-prints EXIT (§11.4) | DECIDED | **Option A — the terminal state decides: a pick whose `last_i` is EXIT is in `G_C` whatever it did on days 1–4.** The census reading ("ever softened on a later day") is printed as §4 secondary 4 with the overlap against §2.3's partition counted, and the EXIT-day dose cells (secondary 5) carry the within-`G_C` detail. §2.3 and §4 stand as drafted. | **Technical ground with one defensible answer** (charter ground 3): §2.3 and §4 compute per-night group means, which require **disjoint** groups; option B puts a soften-then-re-EXIT pick in both `G_B` and `G_C`, so the groups are not a partition, picks are double-counted in `mean_G[·]`, and the monotonicity gate of decision 2 becomes uninterpretable. **DP-25** — H-097 as filed says "picks whose EXIT **softened before day five**", i.e. the terminal state. **DP-26** — a convention fixed before any outcome, with the alternative printed and counted. |
| 5 | Window end, decision date, extension date, lock-or-DEFER | ROUTED → data-steward | waits on **R1 item (5)** — the measured contributing-night rate per eligible session, against §5.6's three branches (**< 0.29 → not locked, DEFERRED** with the measured rate, the projected floor date and the re-entry trigger; **0.29–0.55 → the window end, the interim date and both decision dates move out**, locking only if the resulting decision date is on or before **2027-09-15**; **≥ 0.55 → nothing changes**: window 2026-06-01..2027-01-29, interim **2027-01-04**, decision **Monday 2027-03-01**, single DP-13 extension to ..2027-03-15 decided **Monday 2027-04-19**, then DEFERRED). Settled at `record`; **R1 may push the dates out and can never pull them in.** | DP-43; DP-45; DP-13; DP-53; §5.6's own numeric trigger |
| 6 | Demotion / suppression at lock (DP-43) | ROUTED → data-steward | waits on **R1 items (1)–(3), (7)** — any reported cell projected below **20 contributing nights** at the decision date is **demoted to descriptive at lock and never dropped afterwards**. Already suppressed at lock on measured counts: **bearish** (14 of 550 published rows), **score band `< 80`** (1 of 579), **the polarity firing arm** (0 rows, structural, PI-014). **Score band 90+** joins them unless R1 item (7) projects ≥ 20 contributing nights, with the projection printed either way (§4(d)). Suppression restricts **affirmative reporting only** and removes nothing from §8 clauses 5, 6 or 7. | DP-43; DP-45; §4(d) |
| 7 | §8 clause 6 — three-group-night evaluability | ROUTED → data-steward | waits on **R1 items (2), (4), (5)** — the projected count of **three-group nights** (≥ 1 matched pick in each of `G_A`, `G_B`, `G_C`). Below 20 at the decision date the monotonicity gate is **not evaluable and the verdict is INCONCLUSIVE**, never CONFIRMED on the extreme contrast alone (§8 clause 6, unchanged by this pass). The projection is printed in §5.2's funnel at lock so a designed-in INCONCLUSIVE is disclosed before the lock, not discovered at the decision pass. | §8 clause 6; DP-43; DP-57 |
| 8 | §8 clause 7 — B3 (`S2`) evaluability under decision 3 | ROUTED → data-steward | waits on **R1 item (6)** — how many `G_C` picks obtain a same-night, same-direction, ±0.25 ATR `dam5`-matched `G_A` pick, and the resulting **projected B3 contributing-night count at the decision date**. Decision 3 makes this material: if B3 is projected below 20 the two-way gate cannot be met and **CONFIRMED is unreachable from the run by design** — that projection is printed in §8 beside the gate (the DP-31 disclosure pattern) and, with NULL also out of reach on this window (§5.4), the §5.6 pre-lock trigger applies and Q038 is **DEFERRED rather than locked as a design that can only return INCONCLUSIVE**. Settled at `record`. | DP-43; DP-45; DP-57; §8 clause 7 as amended by decision 3 |
| 9 | Successor freezes | ROUTED → data-steward | waits on **R2** — `manifest_v002` / `manifest_prices_v002` (with the add-only successor exclusions file) covering pick nights after 2026-09-10, due **before the interim look of 2027-01-04** and again before the decision pass; a third pair **only if** the DP-13 extension fires. **Not a blocker for lock** (DP-23). | DP-23; DP-50(a) |

## Corrections to silent choices

Three. The Registrar applies them with the table.

1. **§5.2, §5.3 and §5.6 — the census-derived counts are design sizing, not exposure counts (DP-50(c)).** The terminal-state split "≈ 194 / ≈ 201 / ≈ 193" and the attrition scenarios behind the 0.85 / 0.70 / 0.55 rates are derived from a **live** operational read (`research/reports/monitor_counts_2026-09-15/REPORT.md`, rows to 2026-09-14). DP-50(c) is explicit that **exposure counts come from the pinned freeze, never from a live query**: live rows move under a repair and a live "before" is not reproducible. The draft already routes re-measurement to R1, so the fix is one of labelling and bindingness — every live-derived figure in §5.2/§5.3 carries the status **`DESIGN_SIZING_ONLY — NOT_YET_MEASURED (R1)`** in the table itself (not only in prose), and **no date, floor projection, suppression or lock-or-DEFER branch is settled on those numbers**; §5.6's trigger reads R1's frozen counts and nothing else. Rule 4 is what makes the census legal as an operational count; DP-50(c) is what stops it sizing a schedule.
2. **§5.5 and §8 — the interim is defined by a count, not by a date (DP-58).** DP-58 registers "one interim analysis at **60 contributing nights per primary endpoint**". §5.5 fixes the calendar date (2027-01-04) correctly but does not say what happens if the measured count is short on that date. Add: **if `eval.py`'s measured contributing nights per primary endpoint are below 60 at the interim freeze, the look is a mechanical *continue*** — the boundary test is not declared, nothing is estimated outside `results/interim/`, and the registered α spend stands. (The other short-count routes are already written: any reported cell under 20, or fewer than 30 post-lock nights, is likewise a continue.)
3. **§4 ordering rule 1 and §4 secondary 9 — the tie-reading sensitivity becomes a one-way blocker (DP-27).** DP-27 resolves an unorderable same-session pair to *the outcome less favourable to the hypothesis*; the draft applies it literally and symmetrically (adverse-first for every group and arm), which is right — an asymmetric per-group tie rule would break rule 5's single basis on both sides — but on the sealed portion **every unpublished control's same-session tie is resolved by rule** (hourly bars for 227 of 436 symbols, §10.8), so the convention is doing real work. The opposite reading (favourable-first) must therefore be more than a printed sensitivity: **if it flips the sign of a primary, that endpoint is INCONCLUSIVE.** It can never create a CONFIRMED, it takes no BH slot, and it is computed from the same `eval.py` pass. §4 ordering rule 1, §4 secondary 9 and §8's INCONCLUSIVE list take this wording.

**Checked and *not* corrections** — each already matches policy: MPE **5.0 pp on both primaries** and now on the clause-7 gate, same units (DP-20, DP-44), with no uplift and no invented money-unit MPE, and sessions-to-touch descriptive (DP-44); the filed "ATR move" correctly re-specified to the path-legal P2 with the fixed-horizon quantity printed as descriptive (rule 5, DP-01 outranking the DP-25 default, and the re-specification recorded in §4 rather than made silently); `exclusions_v003.json` — the newest file (DP-22) — unioned with add-only successor files, read as an input with no hard-coded dates; floors read as **≥ 80 contributing nights per primary endpoint** and 20 per cell (DP-21, the stricter reading), **≥ 30 post-lock contributing nights** not reduced (DP-24); publication predicate `qualified IS TRUE AND selected_rank IS NOT NULL`, dark-lane rows out of every group but retained in the control pool (DP-28); basis `Open_{t+6}` for picks and controls alike — the first executable moment after the classification, matching locked **Q019**'s `Open_{s+1}` construction, with `C_t` used as the DP-11 already-held reference for `dam5`, matching and the plan panel, and the t+5-close basis a picks-only non-executable sensitivity (DP-03, DP-11); DP-12 applied in full — matched primary, unmatched B4 descriptive, the dropped tail counted and printed, **the caliper never widened** (§10.12); family **F6** by the primary endpoint's subject, H-097 counted once (DP-29); the episode-clustered CI as a **gate**, not a footnote (DP-51, §8 clause 3); gap-through-at-open as a touch and the bands/clocks fixed before any outcome (DP-26); **no rule-14 exception requested or needed** — every input is at or before the moment it governs, with `Close_{t+5}` preceding the basis (DP-05 untouched, DP-41 respected); successor freezes covering **every** candidate symbol, published and unpublished, plus hourly bars for all of them, which is stricter than DP-23's minimum; regime labels only for nights ≥ 2026-06-09, `regime_version v1.2`, same-evening rows (CLAUDE.md, FREEZE_v001 §7); every number **NON_QUOTABLE** (rule 12); DP-31 correctly **not** invoked (≈ 52 post-lock contributing nights projected at the pessimistic rate, so PROSPECTIVELY_CONFIRMED is reachable by design) while the NULL-unreachability ceiling is disclosed in §5.4 and §8 in the same spirit; the DP-49 sentence in §9 and DP-50(a)/(b)/(c) in §5.7/§9/§10.9, including the **flag-off-until-2027-05-24** constraint on any PI-014 repair; DP-57's sample-size, precision, dependence, multiplicity and strata justification in §5.4, and DP-58's interim declared in both §5.5 and §8 with no floor waived; DP-53's counts-only funnel separating elapsed / eligible / matured nights with the maturity leg added **once** and scenarios shown separately; DP-55's prior-exposure disclosure and the **NOT_ESTIMABLE** historical companion; DP-52's INTERNAL / NON_QUOTABLE evidence card. Date arithmetic re-checked session by session and agreeing with the draft: window end 2027-01-29 (Fri) → t+15 = **2027-02-22** (2027-02-15 holiday) → +1 week → **Monday 2027-03-01**; +30 sessions → **2027-03-15** → t+15 = **2027-04-06** (2027-03-26 holiday) → +1 week → **Monday 2027-04-19**; interim window end 2026-12-01 → t+15 = 2026-12-22 → **Monday 2027-01-04**. The decision date is **5.5 months** after a 2026-09-15 lock, inside DP-43's 12-month ceiling, and DP-43's ceiling governs the initial date, not DP-13's single extension.

## Defaulted on Haci's behalf

- #3 B3 (`S2`), the within-published damage pairing — chose **two-way gate: CONFIRMED requires B3 to clear 20 contributing nights and beat 5.0 pp in the same sign; unevaluable or inside MPE ⇒ INCONCLUSIVE**; not taken: **one-way blocker (blocks only on the opposite sign)** — DP-45. Overturn = successor question.

That is the only one. Items 1, 2 and 4 each met a DECIDE ground (a structurally empty group; DP-20/DP-44's MPE; a partition that must be disjoint), so none needed his word, and items 5–9 are mechanical on the Steward's counts. **Worth him seeing plainly:** #3 is the item that decides how often this question can answer at all. The stricter gate means a thin within-published pairing — which Q019 measured at a **68.7 %** drop rate for exactly this construction — can send Q038 to INCONCLUSIVE even when the registered contrast is clean. The desk took it because the two options differ only in strictness, and it made the cost visible rather than hidden: routed item 8 puts B3's projected evaluability in front of the lock, and if the gate is projected unevaluable the question is **DEFERRED instead of locked**. Nothing here is pending on him.

## Routed requests

### data-steward — R1 (counts-only exposure report) — **blocks the lock** (§5.6 trigger)

Q038 (does the Conviction Monitor's five-day tier history order the forward path; PREREG §5.7 request R1) needs an exposure count on the **frozen** data — `manifest_v001`, `manifest_prices_v001`, `manifest_prices_universe_v001`, never a live query (DP-50(c); the 2026-09-15 census's live counts are design sizing only and settle nothing) — and it is **counts only, with no outcome of any kind**: no touch, no race, no rate, no return, no excursion, and above all **no tier-versus-outcome cross-tab of any shape**; a forward bar is read only to establish that it *exists*, for maturity and gradeability accounting. Population: published picks (`qualified IS TRUE AND selected_rank IS NOT NULL`, DP-28; dark-lane rows excluded from every group but retained in the control pool) on pick nights **2026-06-01..≈2026-08-18** for every quantity needing a matured t+15 window and **2026-06-01..2026-09-10** for the pick-night and monitor-row quantities, with the nights in `research/data/exclusions_v003.json` (`manual_runs` ∪ `non_session_runs` ∪ `uncorroborated_publication_runs`, in-window: 2026-06-26, 2026-07-02, 2026-07-06) and DP-04 re-run nights removed, the two cohorts reported **separately** so the maturity lag is added once and the immature tail is not counted twice as attrition (DP-53), and the exact **t+15 maturity boundary session** stated rather than inferred. Please return: **(1)** the §2.5 funnel night by night — eligible published picks after each exclusion, monitor rows failing the **point-in-time guard** (`created_at` not on the row's own `trading_date` before the next session's open, or `updated_at` more than one minute after `created_at`) broken out by failing clause, picks with no `C_t` / no `Close_{t+5}` / no `Open_{t+6}` / fewer than 60 daily bars dated ≤ t, ungradeable picks, nights with more than 25 % of eligible picks ungradeable, and immature nights; **(2)** the **terminal-state split `G_A` / `G_B` / `G_C` per night under both `overall_tier` and `age_adjusted_severity`** (the classifier is `overall_tier` on the latest in-scope row, decided this pass), with the **day-5 cross-tab of the two fields** so §2.3's bijection is measured and not assumed, plus the monthly `polarity_unavailable_coverage_low` share, `polarity_tier` distribution, `overall_tier` HOLD count and `technical_overall_tier = 'UNKNOWN'` share (§1's claim scope is a measured restriction); **(3)** the **in-scope-row-count distribution** per pick and the count failing the **≥ 4 distinct in-scope session rows / `last_i ≥ t+4`** rule, with the holiday-shortened four-row picks identified separately; **(4)** **B2 pool depth** — eligible same-night unpublished candidates per night after the ±0.25 ATR `dam5` caliper and the same-direction constraint, the share of picks obtaining **≥ 3** and a **full 10** controls **by group**, and the `dam5` distribution of the picks obtaining none against those retained (ATR14 and `dam5` desk-computed from `prices_daily_split` bars — the platform's `atr_pct` and `spot_close` are split-corrupted and must not be used, PI-003); **(5)** the resulting **contributing-night rate and three-group-night rate per eligible session, by month** (a contributing night carries ≥ 1 control-matched pick in `G_A` **and** ≥ 1 in `G_C`; a three-group night ≥ 1 in each of the three) — this is the number that decides whether Q038 locks at all: **below 0.29 contributing nights per eligible session the question is not locked and goes to DEFERRED** with the measured rate and a re-entry trigger; **0.29–0.55 pushes the window end, the interim date and both decision dates out**, locking only if the decision date is on or before 2027-09-15; **at or above 0.55 nothing changes** (DP-43, DP-45 — a fast rate never pulls a date in and never shortens a window); **(6)** **B3 (`S2`) feasibility, now material to the decision rule** — how many `G_C` picks obtain a same-night, same-direction, ±0.25 ATR `dam5`-matched `G_A` pick 1:1 without replacement, how many are left unmatched, and the **projected B3 contributing-night count at the decision date**, because this pass made clause 7 a **two-way gate** (CONFIRMED requires B3 beyond 5.0 pp in the same sign; unevaluable ⇒ INCONCLUSIVE), so a projection below 20 means CONFIRMED is unreachable by design and Q038 is DEFERRED rather than locked; **(7)** the **90+ and bearish counts per night** (the §4(d) suppression test — 90+ is suppressed at lock unless ≥ 20 contributing nights are projected) together with the score-band and direction composition of each group; **(8)** **hourly-bar coverage for the in-window unpublished candidate symbols** in `manifest_prices_v001` / the universe freeze, as a share of same-night control rows, since every uncovered row's same-session tie resolves adverse-first by DP-27 (§4 ordering rule 1, §10.8); and **(9)** a re-run of Q019 R1 item 7 — `git log --follow services/conviction_monitor_service.py` in the read-only platform repo — confirming **no tier-logic ship and no dated restoration of the polarity input inside the window**, with SHA and date for anything found, because such a date makes the tier two features split at the ship date (DP-06 pattern, DP-50(a)) and fires §5.6's window-start rule. Item (5) settles the window end, the interim date, both decision dates and the lock-or-DEFER call at `record`; items (1)–(3) and (7) settle which cells are demoted to descriptive at lock (DP-43); items (5) and (6) settle §8 clauses 6 and 7's evaluability, the two places where this design can be a foregone INCONCLUSIVE — and that must be visible **before** the lock, not at the decision pass. **R1 may push the decision date out; it can never pull it in**, and no floor is lowered to meet a date. A `readiness.json` written from `research/templates/READINESS_TEMPLATE.json` is welcome alongside the report, counts only, with every registered endpoint and its actual floor listed.

### data-steward — R2 (successor freezes) — due before the **interim** look, **not** a blocker for lock

Q038 (PREREG §5.7, request R2, DP-23) needs successor freezes before its **interim look on Monday 2027-01-04** and again before its decision pass on **Monday 2027-03-01** (both dates provisional until R1 item (5) is read, and both can only move **out**). Nights ≤ 2026-09-10 stay pinned to `manifest_v001` / `manifest_prices_v001`; later nights enter only through `manifest_v002` (selections, **including the `conviction_monitor` table key with `created_at` and `updated_at`** — the point-in-time guard is unenforceable without them — same SQL and same exclusion criterion, issued with the **add-only successor exclusions file**: identical lists by identical criteria over the post-2026-09-10 nights, which may only add nights and may never remove one from a v003 list) and `manifest_prices_v002` (same Alpaca queries). Five scope requirements, none optional. (1) The daily symbol list covers **every candidate symbol on every in-window night, published and unpublished** — B2 needs the unpublished symbols' `C_t`, `Close_{t+5}`, their **60 prior daily bars** (ATR14, beta60, runup20) and their **15 forward daily bars** (20 where §4's shape sensitivity is computed) — and is never assumed from v001's symbol list. (2) **Hourly bars for every in-window candidate symbol, published and unpublished**, for §4's same-session ordering; the sealed portion cannot supply them (227 of 436 symbols) and the asymmetry is otherwise carried into every successor night. (3) Each freeze carries **15 forward sessions beyond the last included pick night** (20 for the sensitivity cell), and every §5.6 date is re-confirmed session by session from the trading calendar at the same time — the holiday list used was 2026-11-26, 2026-12-25, 2027-01-01, 2027-01-18, 2027-02-15, 2027-03-26. (4) With each freeze, counts only: the monthly `polarity_unavailable_coverage_low` share, `polarity_tier` distribution, `overall_tier` HOLD count and `technical_overall_tier = 'UNKNOWN'` share, and the day-5 `overall_tier` × `age_adjusted_severity` cross-tab — §1's claim scope and §2.3's bijection are **re-measured** at every freeze, never assumed to hold. (5) Under **DP-50(a)**, report the **dated SHA** of any commit between the freezes that changes `services/conviction_monitor_service.py`'s tier logic or age decay, or restores the polarity input, or otherwise alters which picks receive EXIT, with a dated `research/data/DATA_NOTES.md` entry naming the column, the date range and the ship SHA — the successor freeze re-runs the same SQL against the same tables, so such a repair silently changes values v001 recorded differently, and that date fires §5.6's window-start rule mid-flight. Rows whose bars are missing are **excluded and counted, never back-filled or imputed**. A third pair is built **only if** the DP-13 extension fires, then through 2027-03-15 for the 2027-04-19 pass.

## Schedule

**Not yet filled — R1 is outstanding and blocks the lock.** Drafted values, carried here so the `record` pass has one place to confirm or move them:

decision_date: **2027-03-01 (provisional)** · extension_date: **2027-04-19 (provisional)** · hard_stop: **2027-04-19** (= the extension date; a gate still short on `eval.py`'s measured counts that day sends Q038 to **DEFERRED** — no second extension, no reduced floor, and a gate shortfall is never INCONCLUSIVE) · rule: **exposure-driven** · window: pick nights **2026-06-01..2027-01-29** · interim: **2027-01-04** (DP-58, success only, 60 contributing nights per primary endpoint) · gates: **≥ 80 contributing nights per primary endpoint** (DP-21), **≥ 30 contributing nights dated after the lock commit** (DP-24) and **≥ 20 three-group nights** (§8 clause 6), on `eval.py`'s measured counts, never on a projection · ceiling: 2027-03-01 is ≈ **5.5 months** after a 2026-09-15 lock, inside DP-43's 12 months; the latest decision date this question may carry is **2027-09-15**.

At `record` the drafted dates stand unless R1's measured rate pushes them out under §5.6's middle branch. **DP-53 governs how the projection is computed** — fully observable cohort, exclusions retained in the denominator, maturity added once, scenarios and uncertainty shown separately — **and is not a licence to shorten a registered window or to prefer an earlier date**; DP-43 and DP-45 still forbid pulling a date in on a fast rate.

## Standing rules added

_none._ Nothing here is Haci's word: items 1, 2 and 4 were DECIDED on existing entries, the platform source and the draft's own arithmetic; item 3 was DEFAULTED under DP-45; items 5–9 settle mechanically on the Steward's counts. **A DEFAULTED item adds no DP entry** (DP-40).

## Standing rules proposed

- **A gate whose evaluability is unknown is projected before the lock, not discovered at the decision pass.** Q038 now carries two (clause 6's three-group nights, clause 7's B3 pairs) and Q037 carried one (its S2 pairs). Proposed as one clause on DP-43: *every §8 clause that can be "not evaluable ⇒ INCONCLUSIVE" gets a projected count in the counts-only exposure request, and a question whose gates are all projected unevaluable is DEFERRED rather than locked.*
- **An option that empties an arm is not a stricter option, it is an untestable one.** Q038 §11.1's option B would have left `G_C` structurally empty via the age decay. Proposed as one clause on DP-45: *the "take the stricter" tie-break applies only between options that both admit a measurable contrast; an option that is unmeasurable by construction is rejected on validity, not weighed on strictness.*
- **Live operational counts may size nothing.** DP-50(c) already says exposure counts come from the pinned freeze; Q038's census-derived group split shows the failure mode is a table row that *looks* like an exposure count. Proposed as one clause on DP-50(c): *a live-derived count printed in a PREREG carries `DESIGN_SIZING_ONLY` in the table itself and can settle no date, floor, suppression or lock decision.*
