# Q024 — decisions before lock
Run: 2026-09-13 by decision-maker (autonomous) · Source: PREREG.md "Open decisions before lock", 4 items + 6 raised here (multiplicity, family denominator / id collision, arm-below-floor handling, dividend basis, schedule, successor freezes) = 10, **plus 7 settled at `record` = 17**

**Recorded 2026-09-13** (`record Q024`, autonomous) from `research/reports/STEWARD_Q024_sas_vs_simple_benchmarks_exposure.md`. Items 3, 6, 9 and 10 are settled below, items 11–17 are the calls R1's numbers raised, and `## Schedule` is **final**. **No item in this file is pending.**

State read: `state.json` = `PREREG_DRAFT` (registrar, 2026-09-14T03:11:13Z). No `results/` directory exists, no `eval.py` exists, and no outcome of any kind was read to write this file — every number cited below is a count, a date or a policy id. R1 is a counts-only report and was read as such.

| # | Decision | Bucket | Choice | Basis |
|---|---|---|---|---|
| 1 | Window start: prospective-only vs sealed start | DEFAULTED | Option **A** — pick nights ≥ 2026-09-14, entirely after the lock commit; the sealed stretch 2026-07-08..2026-09-11 is printed once as a labelled `POST_HOC — NOT A TEST` panel entering no estimate, CI, q or verdict | DP-45; Q018 §6, Q023 §6; not taken: sealed start 2026-07-08, ~2 months sooner, not blind |
| 2 | What "a simple technical rank" is | DECIDED | Option **A** — the price-only three-component composite of §3 B6 (SMA50 distance, Wilder RSI14, 20-session stochastic position; equal-weight cross-sectional percentiles, ties by symbol ascending) is the registered arm. The platform's `technical_structure_score` rank stays in §3 B7 as a descriptive comparator, printed every time, labelled "confirms nothing about H-003" | DP-26 (registrar convention, fixed before any outcome is read); Q024 §7's H-003 boundary; a SAS subscore cannot be an *alternative to* SAS |
| 3 | How the equal-weight S&P 500 arm is built | DECIDED · settled at `record` (**DEFAULTED**) | Option **A** stands — `RSP` added to the benchmark symbol list of `manifest_prices_v002` (and of the extension pair). R1(d) could not run the availability check (no Alpaca credentials in the Steward's environment), so **EW is registered as a conditional arm**: inclusion is settled once, by the Steward's **freeze-time RSP pinnability check, the first action of R2**, reported in writing and dated **before `manifest_prices_v002` is pinned and before `eval.py` exists** — pinnable → EW is a registered primary, `m = 6`; not pinnable → EW is deferred **as an arm** (`DEFERRED.md`), `m = 5`, no proxy ever substituted, F1's denominator stays at 10. Predicate and mechanics in `## Schedule` → "The EW (RSP) arm" | DP-45 — six arms is strictly the harder gate (question-level CONFIRMED needs *every* registered arm, `E_min` is a minimum over a larger set, step-down max-T runs over six correlated arms); not taken: defer EW now and lock at `m = 5`, which removes a hurdle before anyone knows whether it can be built. DP-23; `research/lib/freeze_prices.py:46` is a plain symbol list and is **not** rule-15 enforcement code; B rejected on data, C rejected — Haci named the arm |
| 4 | May the rank arms choose names SAS also published | DECIDED | Option **A** — the B4–B6 pool is every `sas_candidates` row on night `t` with ≥ 60 prior daily bars, **including the published picks**; the overlap share is printed per night and pooled, as a composition count and never as an outcome | charter ground 3 (one defensible answer: H-067's wording is "would a naive rank of the same list have done as well"); DP-45 direction — B removes the best names from the benchmark and manufactures a positive `E_a` |
| 5 | Multiplicity: step-down max-T vs rule 8's BH | DECIDED | **Both are computed, both are printed, and the stricter governs at every level.** Per arm: raw sign-flip p, BH q across F1's full registered primary set (item 6), and the step-down max-T adjusted p across the six arms — a CONFIRMED arm needs **both** q ≤ 0.10 **and** max-T adjusted p ≤ 0.10. Question level: CONFIRMED needs every registered arm to satisfy its full §8 clause list (1–7) **and** `E_min > +5.0 pp` with the max-T adjusted p ≤ 0.10 for the arm attaining the minimum | rule 8 (BH across the family is not optional and is not replaced by an FWER statistic); DP-45 (the intersection of two corrections is the harder gate) |
| 6 | F1's correction denominator, and the `Q024` id collision | DECIDED · **RESOLVED at `record`** | The collision is resolved: the H-068 draft is now `research/questions/Q025_exposure_attribution/` (`state.json` id `Q025`, `PREREG_DRAFT`, **m = 2** primaries E1/E2, F1), committed in the parallel session. This question keeps **Q024**. **F1's correction set at this lock = 10 primary endpoints — Q006 (2) + Q024 (6) + Q025 (2)** — and **never falls below 10**, including if the EW arm defers to `m = 5`. §7 and §8 clause 4 name **Q025** and **10**, not "H-068, unregistered" and 8 | DP-29; rule 8 (both drafts lock in the same cycle, so §7's own symmetric-set rule fires now); larger denominator = larger q = stricter |
| 7 | An arm that cannot reach the 80-night floor | DEFAULTED | An arm the Steward projects below **80** contributing nights at the decision date is **removed from the deciding set** (out of `m`, out of BH, out of `E_min`) **but is still built and printed descriptively wherever it reaches ≥ 20 contributing nights**, flagged "did not decide"; and the question-level CONFIRMED sentence **names every benchmark that did not decide**, so "beats every simple alternative" can never be said over an arm that was quietly not tested. `E_min`'s constituent set below three arms → the whole question to DEFERRED | DP-43 (demotion-to-descriptive clause) + DP-21; not taken: the draft's silent arm-level drop with no descriptive print |
| 8 | Basis of the ETF arms' bars | DECIDED | B1/B2/B7 ETF arms are graded on **split-adjusted price bars only, no dividends**, the same basis as every stock arm. Because ETF distributions (~1.3–1.6%/yr, i.e. ~0.10–0.13% over 20 sessions, a few percent of a typical index ATR-distance) are excluded, the index arms are graded slightly **low** — a bias that runs **in the hypothesis's favour** — and §10 carries that sentence beside the arm table, not in a footnote | rule 5 / DP-45 (a bias favouring CONFIRMED is stated, never left to the reader); one basis on both sides |
| 9 | Window end, decision date, DP-13 extension, lock-or-DEFER gate | **SETTLED at `record`** (DEFAULTED) | R1 delivered: `r_min` = **0.5652** contributing nights per elapsed session (26/46), tied across SPY, SECRAND, MOM20, MOM60, TECH — **≥ 0.36, so Q024 LOCKS**. Window = pick nights **2026-09-14..2027-04-07**, **142 elapsed sessions** (`ceil(80 / 0.5652)`); decision date **Monday 2027-05-17**; DP-13's single extension to **2027-05-19** (172 sessions), decision **Monday 2027-06-28**; still short there → DEFERRED. Full arithmetic in `## Schedule` | DP-43 (window sized on the measured rate to the earliest date every floor is projected met, + 20-session maturity + one-week margin, first Monday on or after) + DP-45 (every date moved **out** from the drafted 2027-03-29; the 0.9783 pre-maturity upper bound was **not** used); not taken: the drafted 2027-02-16 / 2027-03-29 on the assumed 0.75 rate |
| 10 | Successor freezes `manifest_v002` / `manifest_prices_v002` (+ RSP) | ROUTED → data-steward (**reissued with final dates**) | R2 covers pick nights **2026-09-14..2027-04-07**, built after **2027-05-05** (the last night's 20-session maturity) and delivered by **Monday 2027-05-10**; extension pair only if DP-13 fires (..2027-05-19, delivered by Monday 2027-06-21). The **RSP pinnability check is its first action** (item 3). Due before the decision pass, **not** a blocker for lock (DP-23) | — |
| 11 | Sub-cell suppression list, fixed at lock | DECIDED | **SUPPRESSED at lock, counts only, permanent:** score band **90+** (projected 9.3 nights at 142 sessions), **`k_t` = 4** (3.1), **`k_t` = 3** (3.1). **Not** pre-suppressed — governed by §4's standing ≥ 20 **measured** contributing-night rule: `k_t = 7` (21.6) and regime `bullish` (21.6), both marginal, and every tape stratum and every regime label other than `strongly_bullish`/`bullish` (zero sealed observations, so no rate to project — a pre-suppression there would be an invented number). All other listed sub-cells project ≥ 27.8 | DP-43 (a cell projected below 20 at the decision date is suppressed at lock) + §4's SUPPRESSED definition; R1 "Sub-cell suppression flags" |
| 12 | Any arm demoted or dropped at lock | DECIDED | **None.** All five measured arms project **80.3** contributing nights at 142 sessions against the 80 floor, so no arm leaves the deciding set at lock. Item 7's rule governs at the pass on `eval.py`'s **measured** counts, and DP-13's single extension is the registered remedy for the thin margin — the margin is not widened and the floor is not lowered | DP-43 ("an arm projected below 20 … demoted at lock" — none is; arms are not demotable here, each *is* a primary), item 7, DP-21 |
| 13 | Half A / Half B after the window moved out | DECIDED | The split **date stays 2026-12-01**, as §6 registered it. Half A = **2026-09-14..2026-11-30** (55 elapsed sessions, ≈ 31 contributing nights projected); Half B = **2026-12-01..2027-04-07** (87 sessions, ≈ 49); extension nights join Half B | §6's own clause ("the split date is fixed here and does not move with the count"), fixed pre-outcome; DP-45 — the smaller, noisier Half A is the harder §8 clause-5 sign test |
| 14 | Scope of the sealed `POST_HOC` panel | DECIDED | Computed on `manifest_v001` + `manifest_prices_v001` alone, over pick nights **2026-07-08..2026-08-12** — R1's 26 matured nights. 2026-08-13..2026-09-11 are printed as **counts only** (immature on the v001 horizon; §2.3 step 4 forbids grading them as non-touches). No sealed-night symbol coverage is added to `manifest_prices_v002` for it | ground 3, one defensible answer: the panel enters no estimate, CI, q or verdict, so it never justifies extra freeze scope; §2.3 step 4 |
| 15 | Sector artifact pin | DECIDED | Confirmed and recorded: `volatilx:data/sp500_sectors.json`, sha256 **`c4d12610ac95a8a83a0fc2365d02b4963d6a4169a9352acf2578164111390201`**, 2,405 entries, LF-normalized, **byte-identical to the Q022 pin**; `git log --follow` shows exactly one commit ever (`4171b1a`, 2026-05-17). B3 is buildable: published-slate coverage **99.47%** (BRK.B the only unmapped symbol), B3 computable on **45/45** eligible nights, 2.3% of slots drawing unconstrained. Recorded as a **static artifact** in `manifest_v002`, never re-synced, never hand-supplemented; `eval.py` fails loudly on a mismatch | header pin verified by R1 §(c); §3 B3; DP-23 |
| 16 | Measured cost of the bearish exclusion | DECIDED | Fixed for §5 R1(e) and §10 threat 7: the sealed-panel share is **3.7%** of the published slate (14 of 374 rows; Jul 4.9%, Aug 1.2%, Sep 8.2%, zero `mixed`). The in-window share is measured again at the pass and printed per month; the §8 claim-scope sentence carries it either way | R1 §(e); §2.2 |
| 17 | DP-50(a)/(b) sweep at lock | DECIDED | **Clean.** Two substantive commits since `fa70688` — `2d5776c` (PI-001, floors the legacy outcome backfill window; touches `uoa_symbol_daily.fwd_return_*`, already a **banned** input here) and `22a2e1b` (EN-002, read-only internal card, flag-off) — plus two merges. None touches the publication predicate, the publication floor, the slate size, the weights, the timeframe multipliers, the lane-plan writer or the ladder, and none repairs historical `sas_candidates` or price rows. **No `DATA_NOTES.md` entry and no window split is required at lock**; R2 repeats the sweep for the in-window period | DP-50(a)/(b); R1 §(f) |

## Corrections to silent choices

- **§7 "Across the family"** *(updated at `record` — the collision is resolved)* — the draft says *"Registered F1 questions at this registration: Q006 (2 primaries) and Q024 (6) = 8 primary endpoints"* and *"H-068 … is unregistered today and is not in F1's correction set"* → must say **10 primary endpoints — Q006 (2) + Q024 (6) + Q025 (2)**, where **Q025 is `research/questions/Q025_exposure_attribution/`** (H-068, `PREREG_DRAFT`, `m = 2`, F1), a draft registered in this same autonomous cycle that locks in it — and the never-shrink floor is **10**, held even if the EW arm defers to `m = 5` (rule 8, DP-29; item 6). The §7 bullet keeps its "not the same test, neither substitutes for the other" wording and simply names Q025 instead of "H-068, unregistered".
- **Header / `state.json` / §7 cross-references — RESOLVED at `record`.** The H-068 draft has been renumbered to **Q025** (`research/questions/Q025_exposure_attribution/state.json`, `"id": "Q025"`) by the parallel session; this question keeps **Q024** and its `state.json` needs no change. §7's cross-reference and §9/§10's mentions of the attribution successor now name **Q025** by path. Neither PREREG is edited by me. *Cross-note for the Registrar working Q025 (not an edit to this question):* that draft's own §7 still calls this question "Q024" while describing itself as Q024 and still says "H-067 (F1, unregistered)" — it needs the symmetric correction (this question is **registered**, 6 primaries, in F1's set of 10) before it locks.
- **§8 clause 4 and the question-level CONFIRMED clause** — clause 4 states BH only, the question-level clause states max-T only ("step-down max-T p giving q ≤ 0.10" also conflates an FWER-adjusted p with a BH q) → both statistics are named separately, both printed for every primary alongside raw p, and **both thresholds must be met** at their respective levels (rule 8; item 5).
- **§5 "Window, decision date, extension and DEFERRED fallback (DP-43; DP-13) — final, fixed at lock"** *(settled at `record`; the heading is **final**, not provisional)* — the drafted dates rest on an assumed 0.75 contributing nights per elapsed session; R1 measured **0.5652**, which is slower, so every date moves **out** and is now fixed: window **2026-09-14..2027-04-07** (142 elapsed sessions), decision **Monday 2027-05-17**, single DP-13 extension to **2027-05-19** (172 sessions) with decision **Monday 2027-06-28**, then DEFERRED. **Every occurrence of `2027-02-16`, `2027-03-29`, `2027-03-31` and `2027-05-10` is replaced** — header (manifest coverage), §2.2 (pick-night window and the extension date), §5 (all five bullets), §6 (Half B end, below), §7 ("if it locks before 2027-03-29" → 2027-05-17), §9 (the DP-50(b) constraint date) and §10 threat 2's "107 nights" → **142 elapsed sessions**. DP-43's ceiling is cleared: 8.1 months to the decision date, 9.5 to the extended one, against 12 (latest admissible Monday 2027-09-13).
- **§5 "never demoted to descriptive after the fact"** — replace with item 7's rule: not demoted *after the fact*, but an arm projected short **at lock** is removed from the deciding set and still printed descriptively at ≥ 20 contributing nights, with the CONFIRMED sentence naming it (DP-43).
- **§4 / §8** — add, explicitly: the SAS arm's **absolute** L3-touch rate is descriptive on its own and is never printed without B3 (the same-night, distance-matched, sector-adjusted unpublished draw) beside it (rule 5's distance-matched-control clause). Only the `E_a` differences carry a verdict.
- **§10** — add threat 13: the dividend-basis bias of item 8, in the terms written there.

### Added at `record` (2026-09-13), from R1's measured counts

- **§5 "Expected n — the planning rate"** — the 0.75 planning haircut and the sentence *"at the anchor rate the same window returns ≈ 100 contributing nights, a 25% margin over the floor"* are replaced by the **measured** rate: **0.5652 contributing nights per elapsed session (26/46)**, tied across SPY, SECRAND, MOM20, MOM60 and TECH on the sealed panel 2026-07-08..2026-09-10, cited to `research/reports/STEWARD_Q024_sas_vs_simple_benchmarks_exposure.md`. The paragraph must also say **why the margin is thin by construction** — 80.3 projected against a floor of 80 — in `## Schedule`'s terms: 0.5652 is bound to `manifest_prices_v001`'s own forward-bar horizon (only 26 of 46 nights can mature), the pre-maturity eligible-night rate is 45/46 = 0.9783 with 100% (26/26) conversion, and the desk sizes on the measured floor and not on the upper bound (DP-45, DP-50(c)).
- **§5 R1** — marked **DELIVERED 2026-09-13** with the report path, so the DEFERRED gate reads as **passed** (`r_min` 0.5652 ≥ 0.36, a 1.57× margin) rather than as an open test; R1(d) is marked **not executed** (no Alpaca credentials) and carried into R2 as its first action (item 3).
- **Header, §2.1, §5 freeze discipline** — `manifest_v002` / `manifest_prices_v002` cover pick nights **2026-09-14..2027-04-07**. The forward-bar clause *"carrying 60 sessions beyond the last included pick night"* becomes **"as far as available at the build date, with 20 sessions beyond the last included pick night (through 2027-05-05) as the binding, non-negotiable requirement"** — a literal 60-session requirement cannot be met before 2027-05-17 and would push the pass past its own decision date; the 30- and 60-session descriptive panels already print **only where matured** (§4), so nothing is lost but the descriptive tail. The daily symbol list must also carry **60 prior sessions of history for every in-window candidate symbol** (bars from ≈ 2026-06-18), which B4–B6's ranks and SMA50/RSI14 need.
- **§6 "Split for 'holds in both halves'"** — Half A is unchanged (**2026-09-14..2026-11-30**, 55 elapsed sessions); **Half B becomes 2026-12-01..2027-04-07 (87 elapsed sessions)**. The **split date does not move** (item 13).
- **§6 "The sealed stretch is printed once"** — the panel's scope is fixed to pick nights **2026-07-08..2026-08-12** (R1's 26 matured nights on `manifest_prices_v001`); 2026-08-13..2026-09-11 print as counts only (item 14). The `POST_HOC — NOT A TEST` label and the "enters no estimate, CI, q or verdict" clause are unchanged.
- **§6 regime disclosure and §10 threat 2** — replace "may be near-degenerate" with the measurement: **all 26 matured sealed nights carry a legal same-evening `v1.2` label — 19 `strongly_bullish`, 7 `bullish`, zero of any other value.** Register the consequence at lock: if the prospective window repeats this, "holds across regimes" is **structurally untestable, not merely under-measured**, the report says so in those words beside the stratified table, and the **absence of a contradicting cell is never read as robustness**. §8 clause 6 continues to bind on any cell that does reach 20 contributing nights.
- **§4 sub-cell list and §5 "Demotion and suppression at lock"** — the pre-suppressed set is no longer the 90+ band alone: **90+, `k_t` = 4 and `k_t` = 3** are SUPPRESSED at lock (counts only, permanently); `k_t = 7` and regime `bullish` are named as **marginal** (projected 21.6) and left to §4's standing ≥ 20-measured-night rule; the sentence *"R1 does not break them out and pre-suppressing a cell the desk has not counted would be an invented number"* is replaced by the R1 projection table's result (item 11).
- **§10 threat 7** — carries the measured sealed bearish share, **3.7% of the published slate (14 of 374; Jul 4.9 / Aug 1.2 / Sep 8.2)**, instead of an unquantified caveat (item 16).
- **§5 DP-50 discipline / §10 threat 8** — the sweep at lock is **clean** and says so by name: `2d5776c` (PI-001, `uoa_symbol_daily`, a banned input here) and `22a2e1b` (EN-002, flag-off internal tool), plus two merges; no `DATA_NOTES.md` entry and no window split is required at lock (item 17). R2 repeats the sweep for the in-window period.
- **§2.2, §3 B2, §4, §7, §11.3** — EW is described as a **conditional arm** in item 3's settled terms, and §7's "m = 6, falling to 5 if the EW arm is deferred" names **the settling artefact and its moment** (the Steward's dated pinnability line in the R2 report, before `manifest_prices_v002` is pinned), not "§11.3".

## Defaulted on Haci's behalf

- #1 Window start — chose prospective-only from 2026-09-14; not taken: sealed start 2026-07-08, ~2 months sooner — DP-45 (the weeklies have already printed SAS's absolute sealed-period returns, so the SPY arm is not blind on those nights; Q018/Q023 precedent). Overturn = successor question.
- #7 Arm below the 80-night floor — chose removed from the deciding set but still printed descriptively at ≥ 20 nights, with the CONFIRMED sentence naming it; not taken: drop the arm silently and let `m` fall — DP-43. Overturn = successor question.
- #3 (at `record`) EW / RSP arm — chose **conditional registration**, settled by the Steward's freeze-time pinnability check before `eval.py` exists (`m = 6` if RSP pins, 5 if not); not taken: **defer EW now and lock at `m = 5`** — DP-45: six arms is the harder gate, and dropping an arm before anyone knows whether it can be built removes a hurdle for free. Overturn = successor question.
- #9 (at `record`) Window end, decision date, extension — chose **2026-09-14..2027-04-07 (142 sessions), Monday 2027-05-17**, extension Monday 2027-06-28, sized on the measured 0.5652; not taken: **the drafted 2027-03-29, ten weeks sooner**, or the 0.9783 pre-maturity rate that would have been sooner still — DP-43/DP-45. Overturn = successor question.

## Routed requests

### data-steward — R1 (counts only) — **DELIVERED 2026-09-13**, `research/reports/STEWARD_Q024_sas_vs_simple_benchmarks_exposure.md`

Settled items 9, 11, 12, 15, 16 and 17 and the lock-or-DEFER call (`## Schedule`). **One limb was not executed: R1(d)**, the live RSP bar-availability check — `ALPACA_API_KEY` / `ALPACA_SECRET_KEY` were absent from the Steward's environment and the check was reported as unresolved rather than guessed, which is the right call. It is carried into R2 as that request's first action and is the artefact that settles item 3. Request text kept verbatim below for the record.

Q024 (SAS versus six simple benchmarks, PREREG §5 request R1) needs a **counts-only** exposure measurement on the **frozen** data — `research/data/manifest_v001.json` + `research/data/manifest_prices_v001.json` against `research/data/exclusions_v003.json`, plus read-only `git log` / `git show` in the platform repo — and **never a live query** (DP-50(c)); it is **blocking for lock**, because no desk report has ever counted this funnel (six arms, bullish-only, each arm required to be computable on the same night) and the §5 DEFERRED gate and the whole schedule turn on these numbers. **No outcome of any kind:** no touch, no first-touch date, no return, no excursion, no plan result, no arm difference, no overlap-versus-outcome cross-tab; forward bars may be read **only** to establish that a bar exists (gradeability accounting), never for a value. Population: published picks (`qualified IS TRUE AND selected_rank IS NOT NULL`, DP-28; dark-lane rows counted in the funnel and available to the benchmark pools but in no arm) with `dominant_direction = 'bullish'`, on pick nights **2026-07-08..2026-09-10**, with the nights in `exclusions_v003.json` (`manual_runs` ∪ `non_session_runs` ∪ `uncorroborated_publication_runs`) and DP-04 late-`finished_at` nights removed; **denominator for every rate = elapsed sessions** (calendar sessions minus holidays, exclusions *not* pre-removed — the Q019 Correction 3 / Q022 / Q023 convention, so the rate is comparable across questions). Please return, by month and for the period as a whole: **(a) the §2.3/§2.4 funnel step by step** — bullish published rows, swing lane plan present in `public_payload_json.lane_plans`, `outcome_target_invalid` null and swing t1 on the correct side of `C_t`, the price-scale screen (`target / C_t` outside [0.5, 2.0] or an implied distance above 12 ATR — report the count and list the symbols, the APH/KLAC/CRWD/MNST signature measured at 19 of 542 sealed picks in `STEWARD_Q022_exposure.md`, treated as a rule and not as that list), ≥ 60 prior daily bars and a session t+1 open present — with eligible picks, the `k_t` distribution and eligible **nights** (≥ 1 gradeable pick) surviving each step, and **then the same night count re-computed per arm** after §2.3 step 3's computability requirement, so the **contributing-night rate per elapsed session is reported separately for each of SPY, EW(RSP), SECRAND, MOM20, MOM60 and TECH**; the `r_min` across those six arms is the number the schedule is built on. **(b) The rank pool:** candidate-pool size per night with ≥ 60 prior daily bars in `manifest_prices_v001`, the count and dates of nights where that pool is smaller than `k_t`, and the truncated-history scan for the CTRA signature (a symbol whose daily bars stop mid-freeze while it keeps appearing as a candidate). **(c) Sector coverage for B3:** mapping coverage of the published slate and of the unpublished pool against the sha256-pinned `volatilx:data/sp500_sectors.json` (`c4d12610…0201`, 2,405 entries, Q022's pin, **never hand-supplemented**), the per-night count of slate slots whose same-sector unpublished pool is empty (these draw unconstrained and are counted), and the count of picks whose own symbol is unmapped. **(d) RSP feasibility — a bar-availability check, not a pinned artifact:** whether `RSP` daily bars for 2026-02-01 onward are retrievable from the same `data.alpaca.markets/v2/stocks/bars` endpoint, feed and adjustment that already serve `SPY, QQQ, IWM, SMH, DIA` (`research/lib/freeze_prices.py:46`), with any subscription or feed limitation named explicitly; this decides item 3 — a "no" defers the EW arm as an arm and drops `m` to 5, and **no proxy is ever substituted**. **(e)** The bearish share of the published slate per month (the §2.2 exclusion's measured cost). **(f) The DP-50(a)/(b) sweep since manifest SHA `fa70688`,** dated in ET against `sas_runs.finished_at`, for anything touching the publication predicate, the publication floor, the slate size, the weights or timeframe multipliers, the lane-plan writer, the ladder or `_enforce_ladder_monotonic`, plus any repair that rewrites historical `sas_candidates` or price rows — with a dated `DATA_NOTES.md` entry for each, naming column, date range and ship SHA. **The calls this request decides:** (i) **lock or DEFER** — Q024 locks only if **every** registered arm measures **≥ 0.36 contributing nights per elapsed session**; below that line the 80-night floor cannot be reached before 2027-09-13 and that arm — or, if `E_min`'s set falls below three arms, the whole question — goes to `research/questions/DEFERRED.md` with the measured rate and the projected date named (the H-062 form) instead of being locked; (ii) **the schedule** — the window end is `ceil(80 / r_min)` elapsed sessions from 2026-09-14, the decision date is the first Monday on or after (window end + 20 sessions of maturity + a 7-day freeze margin), the single DP-13 extension is +30 elapsed sessions with its own re-derived Monday, and **every date moves out only, never in** (DP-43, DP-45) — a rate faster than the draft's 0.75 planning haircut changes nothing. Please also flag any sub-cell (score band 80–85 / 85–90 / 90+, `best_timeframe`, `confidence_level`, `k_t`, top-3 by `selected_rank`, tape stratum, regime label) projecting below 20 contributing nights at that decision date, for the §4 suppression list — the 90+ band is already SUPPRESSED at lock. Report to `research/reports/STEWARD_Q024_exposure.md`.

### data-steward — R2 (successor freezes) — **REISSUED at `record` with final dates**; due before the decision pass, **not** a blocker for lock

**Read this paragraph first — it supersedes the dates in the text below.** Q024's schedule is now final: window **pick nights 2026-09-14..2027-04-07** (142 elapsed sessions), decision **Monday 2027-05-17**, single DP-13 extension window **..2027-05-19** with decision **Monday 2027-06-28**. So: (1) **The RSP pinnability check is R2's first action, before any bar is fetched for the freeze and long before `eval.py` exists**, and its result is reported as a **dated, standalone line in the R2 report**. The predicate is fixed and is not yours to soften: *RSP daily bars are **pinnable** iff they are retrievable from the same `data.alpaca.markets/v2/stocks/bars` endpoint, feed and adjustment basis that already serve `SPY, QQQ, IWM, SMH, DIA`, and the retrieved series covers **every session from 2026-06-18 through `manifest_prices_v002`'s last forward-bar date with no gap*** — a subscription or feed restriction, a partial series, a different adjustment basis or any missing session all read as **not pinnable**. Pinnable → RSP joins the benchmark list and the EW arm is a registered primary (`m = 6`). Not pinnable → say so plainly; the EW arm is deferred **as an arm** (the Registrar writes the `DEFERRED.md` entry in the arm-level form of the H-067 analyst-revisions entry), `m` falls to 5 and **no proxy is ever substituted** — not an equal-weight basket, not `sp500_sectors.json` constituents, not QQQ. Report the answer either way; do not fetch RSP "just in case" and decide later. (2) **Build the primary pair after 2027-05-05** — the 20-session maturity of the last pick night, 2027-04-07 — and **deliver by Monday 2027-05-10**, a week before the pass. (3) **Forward bars:** carry them as far as available at the build date; the binding, non-negotiable requirement is **20 sessions beyond the last included pick night (through 2027-05-05)**, not 60 — the 60-session figure in the text below cannot be met before the decision date and is relaxed to "where available" (the 30- and 60-session panels are descriptive and print only where matured). (4) **Daily symbol history:** every in-window candidate symbol needs **60 prior sessions** (bars from ≈ 2026-06-18) for the B4–B6 ranks, SMA50 and RSI14. (5) **The extension pair is built only if DP-13 fires** — window **..2027-05-19**, built after its 20-session maturity (2027-06-17) and delivered by **Monday 2027-06-21**. (6) Re-confirm every date **session by session** from the trading calendar; a correction may move a date **out, never in** (2027 holidays in range: 2027-01-01, 2027-01-18, 2027-02-15, 2027-03-26 Good Friday, 2027-05-31 Memorial Day, 2027-06-18 Juneteenth observed). (7) The DP-50(a)/(b) sweep at lock came back clean (`2d5776c`, `22a2e1b`, both irrelevant to this population); repeat it for the in-window period as the text below asks.

Q024 (PREREG §5 request R2, DP-23) needs successor freezes before its decision pass — **Monday 2027-05-17, fixed at `record` 2026-09-13 from your R1 report, not provisional**. Please build and pin `manifest_v002` (selections: the same SQL and the same exclusion criteria as v001, plus `market_regime_daily` v1.2 rows with `created_at`, plus the sha256-pinned `volatilx:data/sp500_sectors.json` recorded as a **static artifact**, byte-identical to `c4d12610ac95a8a83a0fc2365d02b4963d6a4169a9352acf2578164111390201` and **never re-synced**) and `manifest_prices_v002` (the same Alpaca queries), covering pick nights **2026-09-14 .. the window end fixed at `record`**, with forward daily bars carrying **60 sessions beyond the last included pick night** (20 sessions is the binding maturity; the extra bars serve the descriptive 30-session panel and the 60-session plan, printed only where matured), and — only if the single DP-13 extension fires — a second pair, built then and not before. Five scope requirements, none optional: **(i)** the daily symbol list covers **every candidate symbol on every in-window night, published and unpublished** (the rank and sector arms need their pick-night closes, 60 prior bars and their own forward bars), never assumed from v001's symbol list; **(ii)** the benchmark list is extended to **`SPY, QQQ, IWM, SMH, DIA, RSP`** — `RSP` is the equal-weight arm and without it that arm cannot be built (`research/lib/freeze_prices.py:46` carries no equal-weight instrument today; the file is not rule-15 enforcement code); **(iii)** hourly bars scoped to at least the published-pick symbols, descriptive ordering only; **(iv)** an **add-only successor exclusions file** issued with the selection freeze, applying the identical three criteria to the post-lock nights — it may add nights and may never remove one from a v003 list; **(v)** rows whose bars are missing are **excluded and counted, never back-filled or imputed**. Please repeat R1(f)'s commit sweep for the period between the freezes and report explicitly — even when the answer is "none" — whether any change to the publication predicate, the publication floor, the slate size, the weights, the lane-plan writer or the ladder shipped **inside** the window: such a ship changes the SAS arm itself, and §5 then either splits the window at the ship date as two features (DP-50(a), the DP-06 pattern) or moves the window start and recomputes the schedule. Where a successor freeze overlaps an earlier one on any table, compare the rows and treat any disagreement as a **loud failure** with a dated `DATA_NOTES.md` entry (DP-50(a)). Please re-confirm every §5 date **session by session from the trading calendar** when the freeze is built (2027 holidays in range: 2027-01-01, 2027-01-18, 2027-02-15, 2027-03-26 Good Friday, 2027-05-31); a correction to any date may move it **out, never in**.

### registrar — id collision — **RESOLVED 2026-09-13**; one standing instruction remains

The renumber is done: the H-068 draft is `research/questions/Q025_exposure_attribution/` (`state.json` id `Q025`, `PREREG_DRAFT`, `m = 2`). This question keeps **Q024** and its `state.json` is untouched. At `apply Q024`, §7 and §8 clause 4 name **Q025** and the **10-primary** F1 correction set (item 6), and the Corrections list above is applied with the rest.

**Standing instruction, for the moment R2 answers the RSP check (item 3), not for now:** if the Steward reports RSP **not pinnable**, write the arm-level entry in `research/questions/DEFERRED.md` — the same form as the H-067 analyst-revisions arm entry, naming the instrument, the endpoint and feed that refused it, the date of the check and the fact that `m` fell to 5 with F1's denominator held at 10 — and record the drop in `schedule.json`. **The locked PREREG is not edited** (rule 3): the conditional clause is already in it, and this is that clause firing. If RSP **is** pinnable, no entry is written and `m = 6` stands.

*(original request, kept for the record)* Two `PREREG_DRAFT` directories created in the 2026-09-13 autonomous cycle both carry `"id": "Q024"` in `state.json`: `research/questions/Q024_sas_vs_simple_benchmarks/` (H-067, `IDEA` at 2026-09-14T03:11:10Z) and `research/questions/Q024_exposure_attribution/` (H-068, 03:12:28Z). This question keeps **Q024** on the earlier timestamp; please renumber the H-068 draft to the next free id, update its `state.json`, its header, its `research/BACKLOG.md` line and its §7 cross-references, and update this question's §7 to name it — and, per item 6 above, set F1's correction set to **10 primary endpoints (Q006 2 + Q024 6 + the H-068 question 2), never falling below 10**. The H-068 draft's own §7 says "H-067 (F1, unregistered)" and needs the same symmetric correction at its own decision pass.

## Schedule

**FINAL — settled at `record`, 2026-09-13, from `research/reports/STEWARD_Q024_sas_vs_simple_benchmarks_exposure.md`.** Nothing below is provisional and no item in this file is pending. R2's session-by-session calendar re-confirmation may move any date **out, never in** (DP-43, DP-45).

decision_date: **2027-05-17** (Monday) · extension_date: **2027-06-28** (Monday; DP-13's single automatic extension, then DEFERRED — there is no second pass and no reduced gate) · hard_stop: **2027-06-28** (an arm still short of 80 contributing nights there goes to `research/questions/DEFERRED.md` with its measured counts; if `E_min`'s constituent set falls below three arms, the whole question does) · rule: **exposure-driven** (the window end is set by accrual at the measured per-arm rate, not by a fixed calendar end; the decision date follows from the 20-session maturity plus a one-week freeze margin, first Monday on or after) · window: pick nights **2026-09-14 .. 2027-04-07** = **142 elapsed sessions** (extended: .. **2027-05-19**, 172 sessions) · extended: **false** · maturity: **20 sessions** (the primary L3 clock and the clause-7 money gate share it) · gates: **≥ 80 contributing nights per registered arm** and **≥ 30 of them dated after the lock commit** (DP-21, DP-24; the second is met by construction — every night is post-lock), on `eval.py`'s measured counts · ceiling: **cleared — 2027-05-17 is 8.1 months after the 2026-09-13 lock**, against DP-43's 12 months (latest admissible Monday 2027-09-13); the extended date, 2027-06-28, is 9.5 months.

### The lock-or-DEFER gate (§5), tested on R1's measured counts

| arm | measured rate (sealed panel 2026-07-08..2026-09-10, 46 elapsed sessions) | threshold | margin |
|---|---|---|---|
| SPY | **0.5652** (26/46), computable 45/45 eligible nights | ≥ 0.36 | **1.57×** |
| SECRAND | **0.5652**, computable 45/45 (8 of 344 slots, 2.3%, drew unconstrained) | ≥ 0.36 | **1.57×** |
| MOM20 | **0.5652**, computable 45/45 (pool ≥ `k_t` every night) | ≥ 0.36 | **1.57×** |
| MOM60 | **0.5652**, computable 45/45 | ≥ 0.36 | **1.57×** |
| TECH | **0.5652**, computable 45/45 | ≥ 0.36 | **1.57×** |
| EW (RSP) | **not measurable on `manifest_prices_v001`** (RSP absent) — **BLOCKED-PENDING-CHECK, not below floor**; conditional on R2's pinnability check. If it pins, EW is a single-instrument arm with SPY's computability profile (45/45), so its rate **is** 0.5652 and `r_min` is unchanged | ≥ 0.36 | settled by the check |

`r_min` = **0.5652**. **Q024 locks. It does not go to DEFERRED**, and neither does any arm.

### The window: 142 sessions, and why not the drafted 107

- `80 / 0.5652 = 141.5` → **142 elapsed sessions**; 2026-09-14 + 142 sessions = **2027-04-07** on the Steward's SPY-bar calendar count.
- Session **t+20** after 2027-04-07 = **2027-05-05** (no holiday falls in that span; Good Friday 2027-03-26 precedes it). Plus the one-week freeze margin → 2027-05-12; **first Monday on or after → 2027-05-17**.
- DP-13's single extension, +30 elapsed sessions → 172 sessions → window end **2027-05-19**; t+20 = **2027-06-17** (Memorial Day 2027-05-31 removed; Juneteenth observed 2027-06-18 falls after); plus the margin → 2027-06-24; **first Monday → 2027-06-28**.
- Every date moved **out** from the draft (2027-02-16 / 2027-03-29 / 2027-03-31 / 2027-05-10), by roughly ten weeks, because the measured 0.5652 is slower than the drafted 0.75 planning haircut. A faster rate would have changed nothing (DP-43, DP-45): dates move out only.

**Floor projections at 142 sessions**

| floor | rate | sessions needed | projected at 142 |
|---|---|---:|---:|
| A — ≥ 80 contributing nights, per registered arm (DP-21) | 0.5652/session | **142 — binds** | **80.3** |
| B — ≥ 30 contributing nights dated after the lock commit (DP-24) | 0.5652/session | 54 (≈ 2026-12-01) | **80.3** (every night is post-lock by construction, §6) |
| C — ≥ 20 contributing nights per reported sub-cell (§4) | per cell | — | item 11's list; three cells pre-suppressed |

**The 80.3-against-80 margin is thin, and that is the conservative construction rather than an oversight.** R1's 0.5652 is bound to `manifest_prices_v001`'s own forward-bar horizon — only 26 of the panel's 46 nights can mature at all — while the pre-maturity eligible-night rate is **45/46 = 0.9783** with **100% (26/26)** of the testable nights converting to contributing. Sizing on the measured floor instead of that upper bound moves the decision date out by about ten weeks and prices the schedule as though the freeze artefact were the real world; DP-45 requires exactly that, and DP-50(c) forbids extrapolating past frozen data. Two consequences, both accepted at lock: if the artefact is what it looks like, the floors arrive well before 2027-04-07 and **the window still runs to its registered end — it is never cut short and there is no interim look** (rule 3, rule 9); if the rate really is 0.5652, DP-13's single pre-agreed extension is the registered remedy and fires on `eval.py`'s **measured** counts, never on this projection. No floor is lowered in either branch.

### The EW (RSP) arm — conditional registration, settled before any outcome exists

R1(d) could not be executed (no Alpaca credentials in the Steward's environment), so the arm's feasibility is genuinely unknown at lock. The two admissible answers were (a) register EW conditionally and let a fixed, outcome-independent check settle it at freeze-build time, or (b) defer EW now and lock at `m = 5`. **(a) is taken, under DP-45**: with EW registered, question-level CONFIRMED must clear **six** arms rather than five, `E_min` is a minimum over a larger set, and the step-down max-T runs over six correlated arms — every limb is strictly harder, and (b) would remove a hurdle before anyone knows whether it could have been built. Mechanics, all fixed now:

1. **The check.** The Steward's first action in R2, before any freeze bar is fetched, reported as a dated standalone line in the R2 report. **Predicate:** RSP daily bars are *pinnable* iff retrievable from the same `data.alpaca.markets/v2/stocks/bars` endpoint, feed and adjustment basis that already serve `SPY, QQQ, IWM, SMH, DIA`, **and** the series covers every session from **2026-06-18** through `manifest_prices_v002`'s last forward-bar date **with no gap**. A subscription or feed restriction, a partial series, a different adjustment basis or any missing session → **not pinnable**. Nothing about SAS, any pick or any outcome enters the predicate, so the deciding set is fixed by a fact about one ETF's bar history, recorded before `eval.py` exists.
2. **Pinnable** → RSP joins the benchmark list of `manifest_prices_v002` (and of the extension pair); EW is a registered primary; **`m = 6`**; its rate equals SPY's, so the schedule above is unchanged.
3. **Not pinnable** → EW is deferred **as an arm** (`DEFERRED.md`, the arm-level form of the H-067 analyst-revisions entry, written by the Registrar at that moment); **`m = 5`**; **no proxy is ever substituted**; F1's correction set stays at **10** (item 6); `E_min`'s set is five arms, still above the three-arm floor, so the question does not defer; and the report's CONFIRMED sentence **names EW among the benchmarks that did not decide** (item 7). The locked PREREG is not edited — this is its own clause firing.
4. **One-way.** An arm that pinned at `manifest_prices_v002` is never dropped later, and the extension freeze is held to the same predicate. If RSP pins but its bars later prove incomplete inside the window, the arm is handled by item 7's below-floor rule on measured counts, not by a second look at this question.

## Standing rules added
_none yet_ — no DP entry is added from a DEFAULTED item (it is not Haci's word).

## Standing rules proposed (for Haci to confirm later)
- **Both multiplicity corrections, stricter governs.** When a question uses a family-wise statistic (step-down max-T, a min/max roll-up) *and* belongs to a rule-8 family, both the FWER-adjusted p and the BH q across the family are computed and printed for every primary, and a CONFIRMED verdict requires both thresholds. (Item 5; generalises to every multi-arm question.)
- **An arm that cannot reach its floor is printed, not erased.** An arm projected below the 80-contributing-night floor leaves the deciding set but is still built and reported descriptively wherever it reaches 20 contributing nights, and any "beats every alternative" sentence names the arms that did not decide. (Item 7; generalises to every question whose arms are its primaries.)
- **A conditional arm, settled by a fixed availability predicate at freeze time.** When an arm needs an instrument or artefact the desk does not yet hold and availability cannot be established before lock, the arm is **registered conditionally** with an outcome-independent predicate written into the PREREG, settled by the Steward at freeze-build time and recorded with its date before `eval.py` exists — rather than dropped at lock on the strength of not knowing. Dropping it early is the weaker discipline: it shrinks the deciding set for free. (Item 3; generalises to every question whose arms need a symbol, a feed or a static artefact.)
- **Size on the measured floor, even when the measurement is bound by the freeze's own horizon.** Where the exposure rate is depressed by the freeze's forward-bar cutoff rather than by the population, the schedule is built on the measured (understated) rate and the faster pre-maturity rate is reported as context only; the resulting thin projected margin is carried by DP-13's single extension, never by a shorter window. (Item 9; generalises to every question sized from a freeze whose horizon truncates maturity.)
