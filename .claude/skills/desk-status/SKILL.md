---
name: desk-status
description: One-screen status of the research desk — every question's state and verdict, backlog counts, latest daily-check RED flags, recent ledger rows, and recent guard blocks. Use when Haci asks "where are we", "what's the status", "what did the agents do".
---
Run `python research/lib/status.py --full` and show the output verbatim, then add at most
three lines: what needs Haci's decision right now (states waiting on --by haci, MPE unset,
PREREG drafts awaiting review), and nothing else.
